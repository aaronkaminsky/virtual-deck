---
phase: 24-spread-pile-multi-select-and-sort
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - party/index.ts
  - tests/gridMove.test.ts
autonomous: true
requirements:
  - GRID-01

must_haves:
  truths:
    - "MOVE_GRID_CARD action updates gridPositions[cardId] to {row, col} on the target spread pile"
    - "MOVE_GRID_CARD is rejected when toRow or toCol is out of range [0,1] / [0,6]"
    - "MOVE_GRID_CARD is rejected when cardId is not present in the pile"
    - "MOVE_CARD to 'play' pile assigns gridPositions[cardId] when toRow/toCol are present in the action"
    - "MOVE_CARD out of 'play' pile deletes gridPositions[cardId] from the source pile"
    - "RESET_TABLE clears gridPositions on all non-draw piles"
    - "viewFor includes gridPositions field in ClientPile output for every pile"
    - "TypeScript compiles with no errors after all type changes"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "Pile.gridPositions, ClientPile.gridPositions, MOVE_GRID_CARD action, toRow/toCol on MOVE_CARD and PLAY_CARD_SET"
      contains: "gridPositions"
    - path: "party/index.ts"
      provides: "MOVE_GRID_CARD handler, gridPositions assignment/cleanup in MOVE_CARD + PLAY_CARD_SET + RESET_TABLE, viewFor mapping, onStart migration"
      contains: "MOVE_GRID_CARD"
    - path: "tests/gridMove.test.ts"
      provides: "6 passing unit tests covering all server GRID-01 behaviors"
      contains: "MOVE_GRID_CARD"
  key_links:
    - from: "party/index.ts MOVE_CARD handler"
      to: "pile.gridPositions"
      via: "assignment after dest.push(card) when toId === 'play' and toRow defined"
      pattern: "gridPositions\\[cardId\\]"
    - from: "party/index.ts viewFor"
      to: "ClientPile"
      via: "explicit gridPositions: pile.gridPositions in the pile map"
      pattern: "gridPositions: pile\\.gridPositions"
---

<objective>
Add `gridPositions` to server-side game state so the Play Area grid knows where each card sits.

Purpose: The frontend `GridZone` component (Plan 02) derives cell positions from `ClientPile.gridPositions`. That field must exist on the server, be maintained through card moves, and be threaded through `viewFor` before any UI work begins.

Output:
- Updated `src/shared/types.ts` — `Pile.gridPositions`, `ClientPile.gridPositions`, `MOVE_GRID_CARD` action, optional `toRow`/`toCol` on `MOVE_CARD` and `PLAY_CARD_SET`
- Updated `party/index.ts` — `MOVE_GRID_CARD` handler with input validation + undo; `MOVE_CARD`/`PLAY_CARD_SET` cleanup; `RESET_TABLE` clear; `viewFor` threading; `onStart` migration
- New `tests/gridMove.test.ts` — 6 passing tests covering all GRID-01 server behaviors (Wave 0 stub + implementation in one pass)
</objective>

<execution_context>
@/Users/aaronkaminsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronkaminsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-spread-pile-multi-select-and-sort/24-CONTEXT.md
@.planning/phases/24-spread-pile-multi-select-and-sort/24-RESEARCH.md
@.planning/phases/24-spread-pile-multi-select-and-sort/24-PATTERNS.md
@.planning/phases/24-spread-pile-multi-select-and-sort/24-VALIDATION.md

<interfaces>
<!-- Current types.ts — the file being extended. Key interfaces: -->

From src/shared/types.ts (lines 18–74):
```typescript
export interface Pile {
  id: string;
  name: string;
  cards: Card[];
  faceUp?: boolean;
  region?: "pile" | "spread";
  ownerId?: string | null;
  // gridPositions MISSING — add here per D-04
}

export interface ClientPile {
  id: string;
  name: string;
  cards: (Card | MaskedCard)[];
  faceUp?: boolean;
  region?: "pile" | "spread";
  ownerId?: string | null;
  // gridPositions MISSING — add here per D-04
}

export type ClientAction =
  | { type: "MOVE_CARD"; cardId: string; fromZone: "hand" | "pile"; fromId: string; toZone: "hand" | "pile"; toId: string; insertPosition?: 'top' | 'bottom' | 'random' }
  // toRow? toCol? MISSING on MOVE_CARD — add per D-06
  | { type: "REORDER_HAND"; orderedCardIds: string[]; skipSnapshot?: boolean }
  | { type: "REORDER_PILE_SPREAD"; pileId: string; orderedCardIds: string[] }
  | { type: "SET_PILE_FACE"; pileId: string; faceUp: boolean }
  | { type: "FLIP_CARD"; pileId: string; cardId: string }
  | { type: "PASS_CARD"; cardId: string; targetPlayerId: string; fromZone?: "hand" | "pile"; fromId?: string }
  | { type: "DEAL_CARDS"; cardsPerPlayer: number }
  | { type: "SHUFFLE_PILE"; pileId: string }
  | { type: "PLAY_CARD_SET"; cardIds: string[]; fromZone?: "hand" | "pile"; fromId: string; toZone: "pile" | "hand"; toId: string }
  // toRow? toCol? MISSING on PLAY_CARD_SET — add per D-06/OQ3
  | { type: "SET_HAND_REVEALED"; revealed: boolean }
  | { type: "RESET_TABLE" }
  | { type: "UNDO_MOVE" }
  | { type: "PING" }
  // MOVE_GRID_CARD MISSING — add per D-05
```

Key party/index.ts locations:
- `viewFor` pile map: lines 86–97 — add `gridPositions: pile.gridPositions` here
- `MOVE_CARD` handler: gridPositions assignment at line 316 (after `dest.push(card)`); cleanup when fromZone === 'pile'
- `PLAY_CARD_SET` handler: after `dest.push(...cardsToPlay)` at line 675; same cleanup pattern
- `RESET_TABLE` handler: lines 527–530, add `if (pile.gridPositions) pile.gridPositions = {}` inside the loop
- `REORDER_PILE_SPREAD` case at line 341: insert `MOVE_GRID_CARD` case after this block
- `onStart` migration: after line 170 (Phase 22 migration block)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend types.ts with gridPositions and MOVE_GRID_CARD</name>
  <files>src/shared/types.ts</files>
  <read_first>
    - src/shared/types.ts — read the complete file to understand the current type definitions before editing
  </read_first>
  <behavior>
    - Pile interface gains `gridPositions?: Record&lt;string, { row: number; col: number }&gt;` (optional, after ownerId)
    - ClientPile interface gains identical `gridPositions?` field (same position)
    - MOVE_CARD in ClientAction union gains `toRow?: number; toCol?: number` (optional fields appended)
    - PLAY_CARD_SET in ClientAction union gains `toRow?: number; toCol?: number` (optional fields appended)
    - New union member added: `{ type: "MOVE_GRID_CARD"; cardId: string; pileId: string; toRow: number; toCol: number }`
    - All other types are unchanged
  </behavior>
  <action>
    Edit src/shared/types.ts with three targeted changes:

    1. Add `gridPositions?: Record&lt;string, { row: number; col: number }&gt;` as the last optional field in both `Pile` and `ClientPile` interfaces (after `ownerId?: string | null`). Add a comment "// Phase 24: play grid" on the same line.

    2. Extend the MOVE_CARD union member to add `toRow?: number; toCol?: number` at the end of its field list (after `insertPosition`).

    3. Extend the PLAY_CARD_SET union member to add `toRow?: number; toCol?: number` at the end of its field list (after `toId`).

    4. Add a new union member after the PLAY_CARD_SET line:
       `| { type: "MOVE_GRID_CARD"; cardId: string; pileId: string; toRow: number; toCol: number }`

    Do not change any other types or formatting.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    - `src/shared/types.ts` contains `gridPositions` in both Pile and ClientPile
    - `src/shared/types.ts` contains `MOVE_GRID_CARD` in ClientAction
    - `npm run typecheck` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MOVE_GRID_CARD handler and gridPositions maintenance in party/index.ts</name>
  <files>party/index.ts, tests/gridMove.test.ts</files>
  <read_first>
    - party/index.ts — read the complete file to understand handler structure, takeSnapshot, broadcastState, error pattern, RESET_TABLE loop, viewFor pile map, onStart migration block, and REORDER_PILE_SPREAD case
    - tests/moveCard.test.ts — read lines 1–51 for the exact makeMockRoom, makeMockConnection, makeCard helper signatures to replicate in gridMove.test.ts
    - .planning/phases/24-spread-pile-multi-select-and-sort/24-PATTERNS.md — sections for party/index.ts and tests/gridMove.test.ts for exact code snippets to follow
  </read_first>
  <behavior>
    Server tests (tests/gridMove.test.ts):
    - Test 1: MOVE_GRID_CARD updates gridPositions[cardId] to { row: 1, col: 3 } when card is in the play pile
    - Test 2: MOVE_GRID_CARD sends ERROR code INVALID_POSITION when toRow is out of range [0,1]
    - Test 3: MOVE_GRID_CARD sends ERROR code INVALID_POSITION when toCol is out of range [0,6]
    - Test 4: MOVE_CARD with toRow/toCol to 'play' pile assigns gridPositions[cardId] = { row, col }
    - Test 5: MOVE_CARD out of 'play' pile deletes gridPositions[cardId] from the pile
    - Test 6: RESET_TABLE clears gridPositions on the play pile (gridPositions becomes {})
    - Test 7: viewFor includes gridPositions in ClientPile output; gridPositions["A-s"] === { row: 1, col: 3 }

    Server implementation (party/index.ts):
    - viewFor pile map includes `gridPositions: pile.gridPositions`
    - MOVE_GRID_CARD case: validate toRow in [0,1], toCol in [0,6]; find spread pile by pileId; verify cardId in pile.cards; takeSnapshot; set pile.gridPositions[cardId] = { row: toRow, col: toCol }
    - MOVE_CARD handler: after dest.push(card), assign gridPositions when toZone==='pile' and toId==='play' and action.toRow !== undefined; delete from source pile gridPositions when fromZone==='pile'
    - PLAY_CARD_SET handler: after dest.push(...cardsToPlay), assign gridPositions for each card to same { toRow, toCol } when toZone==='pile' and toId==='play' and action.toRow !== undefined; cleanup source pile gridPositions for each moved cardId
    - RESET_TABLE loop: after `resetDrawPile.cards.push(...pile.cards.splice(0))`, add `if (pile.gridPositions) pile.gridPositions = {}`
    - onStart migration: after the Phase 22 handRevealed migration block, add migration that initializes gridPositions for orphaned play pile cards (cards with no gridPositions entry get assigned sequential { row: Math.floor(i/7) % 2, col: i % 7 } positions)
  </behavior>
  <action>
    Two-file task. Write tests first (TDD red), then implement (TDD green).

    In tests/gridMove.test.ts: Copy the makeMockRoom, makeMockConnection, makeCard helper functions verbatim from tests/moveCard.test.ts (lines 6–33). Import from `"../party/index"` and `"../src/shared/types"`. Write 7 describe/it blocks covering the behaviors listed above. Set play pile region to "spread" and seed gridPositions as needed per test. Use `await room.onMessage(JSON.stringify({...}), sender)` to dispatch actions. Run `npm test -- --reporter=verbose tests/gridMove.test.ts` — all tests must be RED before implementation.

    In party/index.ts, make 5 targeted edits (order matters):

    1. viewFor pile map (lines 86–97): add `gridPositions: pile.gridPositions,` after `ownerId: pile.ownerId,` in the pile map object.

    2. After REORDER_PILE_SPREAD case closing brace, add MOVE_GRID_CARD case:
       - Bounds-check toRow (0–1) and toCol (0–6) with Number.isInteger, send ERROR code INVALID_POSITION if out of range
       - Find pile by pileId where region === "spread"; send ERROR PILE_NOT_FOUND if absent
       - Check pile.cards.some(c => c.id === cardId); send ERROR CARD_NOT_IN_SOURCE if absent
       - Call takeSnapshot(this.gameState)
       - Initialize pile.gridPositions if undefined; set pile.gridPositions[cardId] = { row: toRow, col: toCol }
       - break (broadcastState fires unconditionally at end of onMessage)

    3. MOVE_CARD handler — after `dest.push(card)` (currently line 315 area, inside insertPosition logic):
       Add two blocks:
       a. gridPositions assignment: `if (toZone === 'pile') { const destPile = this.gameState.piles.find(p => p.id === toId); if (destPile?.id === 'play' && action.toRow !== undefined && action.toCol !== undefined) { if (!destPile.gridPositions) destPile.gridPositions = {}; destPile.gridPositions[cardId] = { row: action.toRow, col: action.toCol }; } }`
       b. gridPositions cleanup: `if (fromZone === 'pile') { const srcPile = this.gameState.piles.find(p => p.id === fromId); if (srcPile?.gridPositions) delete srcPile.gridPositions[cardId]; }`
       Place both blocks before `break`.

    4. PLAY_CARD_SET handler — after `dest.push(...cardsToPlay)` (line 675 area):
       a. Assign gridPositions for each played card when destination is the play pile and toRow defined: iterate cardIds, `destPile.gridPositions[cardId] = { row: action.toRow, col: action.toCol }`
       b. Cleanup source pile gridPositions for each moved cardId (same delete pattern as MOVE_CARD)

    5. RESET_TABLE handler — inside `for (const pile of this.gameState.piles)` loop, after `resetDrawPile.cards.push(...pile.cards.splice(0))`: add `if (pile.gridPositions) pile.gridPositions = {};`

    6. onStart — after `// Migrate state: Phase 22 adds handRevealed` block (after line 170): add Phase 24 migration block that finds the 'play' pile, and if it has cards but no gridPositions, initializes gridPositions assigning each card sequential row/col positions (row = Math.floor(i / 7) % 2, col = i % 7).

    Run npm test after implementation — all 7 tests must be GREEN.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose tests/gridMove.test.ts</automated>
  </verify>
  <done>
    - `tests/gridMove.test.ts` exists with 7 passing tests (all green)
    - `npm test` full suite passes
    - `npm run typecheck` exits 0
    - `party/index.ts` contains `case "MOVE_GRID_CARD"`
    - `party/index.ts` contains `gridPositions: pile.gridPositions` in the viewFor pile map
    - `party/index.ts` RESET_TABLE loop contains `pile.gridPositions = {}`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→PartyKit server | All `ClientAction` messages arrive over WebSocket from an untrusted client; `cardId`, `pileId`, `toRow`, `toCol` are user-controlled values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | `MOVE_GRID_CARD` handler — `toRow`/`toCol` fields | mitigate | Validate `Number.isInteger(toRow) && toRow >= 0 && toRow < 2` and `Number.isInteger(toCol) && toCol >= 0 && toCol < 7` before any mutation; send `ERROR INVALID_POSITION` and break on violation |
| T-24-02 | Tampering | `MOVE_GRID_CARD` handler — `cardId` field | mitigate | Check `pile.cards.some(c => c.id === cardId)` before `takeSnapshot` and mutation; send `ERROR CARD_NOT_IN_SOURCE` on violation |
| T-24-03 | Tampering | `MOVE_GRID_CARD` handler — `pileId` field | mitigate | Find pile by pileId where `region === "spread"`; send `ERROR PILE_NOT_FOUND` if not found; prevents writing gridPositions onto non-spread piles |
| T-24-04 | Tampering | `gridPositions` key injection via cardId | accept | Card IDs are server-generated strings of the form `${rank}-${suit[0]}` (e.g. `A-s`); validated to exist in `pile.cards` before use as a key; no arbitrary user string reaches the map key |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; all dependencies are pre-existing and verified in production |
</threat_model>

<verification>
After both tasks complete:

1. `npm test` — full Vitest suite green (all existing tests + 7 new gridMove tests)
2. `npm run typecheck` — exits 0 with no errors
3. Grep checks:
   - `grep -c 'gridPositions' src/shared/types.ts` outputs >= 3 (Pile, ClientPile, MOVE_GRID_CARD)
   - `grep -c 'MOVE_GRID_CARD' party/index.ts` outputs >= 1
   - `grep -c 'gridPositions: pile.gridPositions' party/index.ts` outputs 1
   - `grep -c 'pile.gridPositions = {}' party/index.ts` outputs >= 1 (RESET_TABLE)
</verification>

<success_criteria>
- `tests/gridMove.test.ts` exists with 7 passing tests covering all GRID-01 server behaviors
- `MOVE_GRID_CARD` is a valid member of `ClientAction` in types.ts
- `Pile` and `ClientPile` both have `gridPositions?: Record&lt;string, { row: number; col: number }&gt;`
- `MOVE_CARD` and `PLAY_CARD_SET` union members include optional `toRow?` and `toCol?`
- Server correctly assigns, cleans up, and resets gridPositions on all relevant actions
- viewFor threads gridPositions to ClientPile
- Full test suite and typecheck pass
</success_criteria>

<output>
Create `.planning/phases/24-spread-pile-multi-select-and-sort/24-01-SUMMARY.md` when done.
</output>
