---
phase: "22"
plan: "02"
type: execute
wave: 2
depends_on:
  - "22-01"
files_modified:
  - src/components/HandZone.tsx
  - src/components/OpponentHand.tsx
  - src/components/BoardView.tsx
autonomous: true
requirements:
  - HAND-01
  - HAND-02
  - HAND-03
  - HAND-04

must_haves:
  truths:
    - "Local player's HandZone header contains an Eye/EyeOff icon button matching PileZone style (h-7 w-7 p-0 ghost variant)"
    - "Eye icon shows when hand is hidden; EyeOff icon shows when hand is revealed (per D-02)"
    - "Icon button has correct title and aria-label for each state; aria-pressed reflects isRevealed"
    - "HandZone container gains ring-1 ring-primary/50 ring-inset class when isRevealed=true (per D-05)"
    - "When opponent's hand is revealed, OpponentHand renders a CardFace row with no count cap (per D-03)"
    - "When opponent's hand is hidden, OpponentHand renders CardBack row as before (no regression)"
    - "BoardView passes isRevealed and onToggleReveal to HandZone; passes revealedCards to OpponentHand"
    - "Revealing player sees no change to their own hand view — myHand is unaffected"
    - "D-06: revealed-state indicator (ring/icon) only appears on local player's HandZone, not on OpponentHand"
  artifacts:
    - path: "src/components/HandZone.tsx"
      provides: "Toggle button in header; conditional ring on container; isRevealed and onToggleReveal props"
      contains: "onToggleReveal"
    - path: "src/components/OpponentHand.tsx"
      provides: "Revealed rendering path using CardFace; revealedCards prop"
      contains: "revealedCards"
    - path: "src/components/BoardView.tsx"
      provides: "Prop threading for myHandRevealed → HandZone; opponentRevealedHands → OpponentHand"
      contains: "myHandRevealed"
  key_links:
    - from: "BoardView.tsx"
      to: "HandZone.tsx"
      via: "isRevealed={gameState.myHandRevealed} and onToggleReveal callback"
      pattern: "myHandRevealed"
    - from: "BoardView.tsx"
      to: "OpponentHand.tsx"
      via: "revealedCards={gameState.opponentRevealedHands[id]}"
      pattern: "opponentRevealedHands"
    - from: "HandZone.tsx onToggleReveal"
      to: "sendAction SET_HAND_REVEALED"
      via: "sendAction({ type: 'SET_HAND_REVEALED', revealed: !isRevealed })"
      pattern: "SET_HAND_REVEALED"
---

<objective>
Implement the Hand Reveal UI: add the Eye/EyeOff toggle button to HandZone's header row, apply the conditional ring-primary ring to the HandZone container when revealed, implement the revealed rendering path in OpponentHand using CardFace, and thread the new ClientGameState fields through BoardView to both components.

Purpose: The UI is the user-visible surface of the feature. This plan assumes 22-01's type changes are in place — ClientGameState.myHandRevealed and opponentRevealedHands are available.
Output: Updated HandZone, OpponentHand, and BoardView. Full npm test suite passes. Visual feature is functional.
</objective>

<execution_context>
@/Users/aaronkaminsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronkaminsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-restrict-play-card-set-to-spread-region/22-CONTEXT.md
@.planning/phases/22-restrict-play-card-set-to-spread-region/22-RESEARCH.md
@.planning/phases/22-restrict-play-card-set-to-spread-region/22-UI-SPEC.md
@.planning/phases/22-restrict-play-card-set-to-spread-region/22-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor must respect — read each source file before editing. -->

From src/shared/types.ts (after Plan 22-01):
  ClientGameState now includes:
    myHandRevealed: boolean
    opponentRevealedHands: Record<string, Card[]>
  ClientAction union includes:
    | { type: "SET_HAND_REVEALED"; revealed: boolean }

Current HandZoneProps (HandZone.tsx lines 67-77):
  interface HandZoneProps {
    cards: Card[];
    playerId: string;
    displayName: string;
    connected: boolean;
    sendAction: (action: ClientAction) => void;
    draggingCardId: string | null;
    selectedIds: Set<string>;
    onToggleSelect: (id: string, zone: 'hand' | 'pile', zoneId: string) => void;
    selectionSource: { zone: 'hand' | 'pile'; zoneId: string } | null;
  }
  // ADD: isRevealed: boolean; onToggleReveal: () => void

Current HandZone container className (HandZone.tsx lines 161-164):
  cn(
    'h-[100px] sm:h-[128px] flex items-center px-4 overflow-x-auto bg-card',
    isOver ? 'border-t-2 border-primary' : ''
  )
  // ADD third clause: isRevealed ? 'ring-1 ring-primary/50 ring-inset' : ''

Current HandZone header row (HandZone.tsx lines 148-156):
  <div className="flex items-center gap-2 px-4 mb-1">
    <span ... />   {/* connection dot */}
    <span ...>{displayName}</span>
    {selectedIds.size >= 2 && ... && (<span ...>{selectedIds.size} selected</span>)}
  </div>
  // ADD toggle button AFTER the selection badge span

Current OpponentHandProps (OpponentHand.tsx lines 8-14):
  interface OpponentHandProps {
    playerId: string;
    cardCount: number;
    displayName: string;
    connected: boolean;
    sendAction: (action: ClientAction) => void;
  }
  // ADD: revealedCards?: Card[]  — when non-empty, render CardFace row instead of CardBack row

Current OpponentHand card row (OpponentHand.tsx lines 45-57):
  <div className="flex items-center">
    {Array.from({ length: Math.min(cardCount, MAX_VISIBLE_OPPONENT_CARDS) }).map((_, i) => (
      <CardBack key={i} className={cn('w-[42px] h-[59px]', i > 0 ? '-ml-3' : undefined)} />
    ))}
  </div>
  // REPLACE with conditional: if revealedCards?.length, render CardFace row; else render CardBack row

PileZone toggle button pattern (PileZone.tsx lines 77-84):
  <Button variant="ghost" className="h-7 w-7 p-0" onClick={handler} title="..." aria-label="...">
    <Eye className="w-4 h-4" /> or <EyeOff className="w-4 h-4" />
  </Button>

From src/components/BoardView.tsx (current HandZone usage, line 102-113):
  <HandZone
    cards={gameState.myHand}
    playerId={gameState.myPlayerId}
    displayName={myPlayer?.displayName ?? ''}
    connected={myPlayer?.connected ?? true}
    sendAction={sendAction}
    draggingCardId={draggingCardId}
    selectedIds={selectedIds}
    onToggleSelect={onToggleSelect}
    selectionSource={selectionSource}
  />
  // ADD: isRevealed={gameState.myHandRevealed}
  // ADD: onToggleReveal={() => sendAction({ type: 'SET_HAND_REVEALED', revealed: !gameState.myHandRevealed })}

From BoardView.tsx (current OpponentHand usage, lines 40-46):
  <OpponentHand
    playerId={id}
    cardCount={count}
    displayName={player?.displayName ?? ''}
    connected={player?.connected ?? false}
    sendAction={sendAction}
  />
  // ADD: revealedCards={gameState.opponentRevealedHands[id]}

Note: BoardView.tsx iterates Object.entries(gameState.opponentHandCounts) — after Plan 22-01, this only
contains hidden players. Revealed players are in opponentRevealedHands and absent from opponentHandCounts.
BoardView must be updated to also render OpponentHand for revealed players.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update HandZone — toggle button and revealed ring</name>
  <files>src/components/HandZone.tsx</files>
  <read_first>
    - src/components/HandZone.tsx — read entire file; understand the header row DOM structure (lines 148-156) and container className (lines 161-164) before making any edits
    - src/components/PileZone.tsx — read lines 77-95 for the exact icon-only Button pattern to replicate (h-7 w-7 p-0, ghost variant, Eye/EyeOff, title, aria-label)
    - src/shared/types.ts — confirm SET_HAND_REVEALED is in ClientAction union
  </read_first>
  <behavior>
    - HandZoneProps has isRevealed:boolean and onToggleReveal:() => void
    - When isRevealed=false: Eye icon is shown, title="Show hand to opponents", aria-label="Show hand", aria-pressed=false
    - When isRevealed=true: EyeOff icon is shown, title="Hide hand from opponents", aria-label="Hide hand", aria-pressed=true
    - Toggle button position: after the selection badge span in the header row
    - Button specs: variant="ghost" className="h-7 w-7 p-0" — matches PileZone exactly
    - HandZone container: when isRevealed=true, adds 'ring-1 ring-primary/50 ring-inset' to cn() call
    - HandZone container: when isRevealed=false, no ring class (no regression to drag-over border-t-2 behavior)
  </behavior>
  <action>
    In src/components/HandZone.tsx:

    1. Add Eye and EyeOff to the lucide-react import line. Also add Button from '@/components/ui/button' import if not already present — check existing imports first.

    2. Extend HandZoneProps interface: add `isRevealed: boolean` and `onToggleReveal: () => void` after the selectionSource field.

    3. Destructure isRevealed and onToggleReveal from the component function parameters.

    4. In the header row div (currently contains connection dot, displayName span, and selection badge), add the toggle Button AFTER the selection badge conditional. Use Eye when !isRevealed, EyeOff when isRevealed. Attributes: variant="ghost", className="h-7 w-7 p-0", onClick={onToggleReveal}, title and aria-label matching the copywriting contract, aria-pressed={isRevealed}.

    5. In the container div's cn() call, add a third clause: isRevealed ? 'ring-1 ring-primary/50 ring-inset' : ''
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep "HandZone" | head -10; npm test && npm run typecheck</automated>
  </verify>
  <done>
    - HandZoneProps has isRevealed and onToggleReveal
    - Eye/EyeOff toggle button is in the header row after the selection badge
    - aria-pressed reflects isRevealed; title and aria-label match the UI spec copywriting contract
    - Container cn() expression includes the ring-primary/50 ring-inset clause
    - npm run typecheck exits 0; npm test exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update OpponentHand and BoardView — revealed card rendering and prop threading</name>
  <files>src/components/OpponentHand.tsx, src/components/BoardView.tsx</files>
  <read_first>
    - src/components/OpponentHand.tsx — read entire file; understand the current card-back row at lines 45-57 and the MAX_VISIBLE_OPPONENT_CARDS cap
    - src/components/SpreadZone.tsx — find the CardFace usage pattern to replicate card size and margin classes
    - src/components/BoardView.tsx — read entire file; understand the opponentHandCounts iteration at lines 35-57 before modifying it
    - src/shared/types.ts — confirm Card type shape (id, rank, suit, faceUp) for CardFace props
    - .planning/phases/22-restrict-play-card-set-to-spread-region/22-UI-SPEC.md — OpponentHand Revealed Path specs (w-[42px] h-[59px], -ml-3, no cap, overflow-x-auto container)
  </read_first>
  <behavior>
    - OpponentHand: when revealedCards has length > 0, renders a CardFace row instead of the CardBack row
    - CardFace sizing: w-[42px] h-[59px] per card; -ml-3 on index > 0 — identical to current CardBack row
    - No count cap on revealed cards (D-03 explicitly: "no card count cap")
    - CardFace takes card={card} — the card object from revealedCards
    - When revealedCards is undefined or empty, CardBack row renders as before (no regression)
    - BoardView: iterates both hidden (opponentHandCounts) and revealed (opponentRevealedHands) players to render OpponentHand for all opponents
    - BoardView: HandZone receives isRevealed={gameState.myHandRevealed} and onToggleReveal callback
    - Opponents who are revealed: cardCount can be derived from revealedCards.length or passed as 0 (display name without count since faces are self-explanatory per D-04 — no count badge change needed, the name display is unchanged)
  </behavior>
  <action>
    In src/components/OpponentHand.tsx:

    1. Add Card to the types import from '@/shared/types'.

    2. Add CardFace import from './CardFace'.

    3. Extend OpponentHandProps: add `revealedCards?: Card[]`.

    4. Destructure revealedCards from function parameters.

    5. Replace the inner flex div that contains the CardBack map with a conditional:
       - If revealedCards && revealedCards.length > 0: render a flex div containing revealedCards.map((card, i) => <CardFace key={card.id} card={card} className={cn('w-[42px] h-[59px]', i > 0 ? '-ml-3' : undefined)} />)
       - Else: render the existing CardBack map (using Math.min(cardCount, MAX_VISIBLE_OPPONENT_CARDS))
       The outer flex container and "Drop to pass" hint remain unchanged.

    In src/components/BoardView.tsx:

    6. Update the opponentHandCounts iteration block to also render revealed opponents. Because after Plan 22-01, revealed players are ABSENT from opponentHandCounts and ONLY in opponentRevealedHands, BoardView must render OpponentHand for all opponents. Replace the single Object.entries(gameState.opponentHandCounts) map with logic that covers both hidden and revealed opponents:
       - Collect all opponent IDs: union of Object.keys(gameState.opponentHandCounts) and Object.keys(gameState.opponentRevealedHands)
       - For each opponent ID, render OpponentHand with:
         cardCount={gameState.opponentHandCounts[id] ?? (gameState.opponentRevealedHands[id]?.length ?? 0)}
         revealedCards={gameState.opponentRevealedHands[id]}
       - The opponentCount variable used for layout classes must also count all opponents (hidden + revealed)

    7. On the HandZone component usage (lines 102-113), add:
       isRevealed={gameState.myHandRevealed}
       onToggleReveal={() => sendAction({ type: 'SET_HAND_REVEALED', revealed: !gameState.myHandRevealed })}
  </action>
  <verify>
    <automated>npm test && npm run typecheck</automated>
  </verify>
  <done>
    - OpponentHand renders CardFace row when revealedCards is non-empty; CardBack row otherwise (no regression)
    - CardFace row has no count cap; same dimensions and overlap as CardBack row
    - BoardView renders OpponentHand for all opponents — both hidden (from opponentHandCounts) and revealed (from opponentRevealedHands)
    - HandZone usage in BoardView has isRevealed and onToggleReveal props
    - npm test exits 0; npm run typecheck exits 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Hand Reveal UI is fully wired. Toggle button added to HandZone header. Eye/EyeOff icon reflects reveal state. HandZone container glows with ring-1 ring-primary/50 ring-inset when revealed. OpponentHand shows CardFace row for revealed players. BoardView threads all new props.
  </what-built>
  <how-to-verify>
    Start the local stack: run `npm run dev` in one terminal and `npm run dev:client` in a second terminal.

    1. Open two browser tabs on localhost (two separate player sessions).
    2. In Tab A (your hand): confirm the Eye icon appears in the HandZone header row after the connection dot and display name.
    3. Click the Eye icon in Tab A. Verify:
       - The icon changes to EyeOff in Tab A.
       - A gold/amber ring appears around the HandZone container in Tab A.
       - In Tab B, the opponent's hand zone shows card faces (not backs) — all cards visible.
    4. Click the EyeOff icon in Tab A (hide hand). Verify:
       - The icon returns to Eye in Tab A.
       - The ring on the HandZone container disappears in Tab A.
       - In Tab B, the opponent's hand returns to card backs.
    5. With Tab A hand revealed: disconnect Tab B and reconnect (refresh). Verify Tab B immediately shows Tab A's hand as revealed (faces visible) without Tab A doing anything.
    6. With Tab A hand revealed: click Reset Table in controls. Verify Tab A's ring disappears and Tab B sees card backs again.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass, or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
After Tasks 1 and 2 complete (before checkpoint):
- `npm test` exits 0 — full suite green including handReveal.test.ts
- `npm run typecheck` exits 0 — all three component files and types.ts clean
- `grep "isRevealed" src/components/HandZone.tsx` shows prop in interface and cn() expression
- `grep "revealedCards" src/components/OpponentHand.tsx` shows prop and conditional rendering branch
- `grep "myHandRevealed" src/components/BoardView.tsx` shows prop passed to HandZone
- `grep "opponentRevealedHands" src/components/BoardView.tsx` shows usage in opponent iteration
</verification>

<success_criteria>
UI phase complete when:
- HandZone header displays Eye/EyeOff toggle button (h-7 w-7 p-0 ghost) matching PileZone style
- HandZone container applies ring-1 ring-primary/50 ring-inset when revealed; no ring when hidden
- Eye icon shown when hidden (click to reveal); EyeOff shown when revealed (click to hide) — per D-02
- aria-pressed reflects state; title/aria-label match UI spec copywriting contract
- Revealed indicator is only on local player's HandZone (D-06) — OpponentHand has no ring
- OpponentHand renders full CardFace row with no cap when revealedCards is non-empty (D-03)
- OpponentHand renders CardBack row normally when not revealed (no regression)
- No "showing" label added to opponent display (D-04)
- BoardView renders OpponentHand for ALL opponents — hidden and revealed
- Human checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/22-restrict-play-card-set-to-spread-region/22-02-SUMMARY.md` when done.
</output>
