---
phase: "22"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - party/index.ts
  - tests/handReveal.test.ts
  - tests/resetTable.test.ts
autonomous: true
requirements:
  - HAND-01
  - HAND-02
  - HAND-03
  - HAND-04

must_haves:
  truths:
    - "SET_HAND_REVEALED with revealed:true sets player.handRevealed=true in GameState"
    - "SET_HAND_REVEALED with revealed:false sets player.handRevealed=false in GameState"
    - "After reveal, viewFor() for a remote connection carries the revealed player's full Card[] in opponentRevealedHands; that player is absent from opponentHandCounts"
    - "After hide, viewFor() for a remote connection has revealed player back in opponentHandCounts; opponentRevealedHands entry is gone"
    - "myHandRevealed is true in viewFor() for the revealing player when their hand is revealed"
    - "RESET_TABLE sets handRevealed=false for every player"
    - "onStart() migration guard adds handRevealed:false to pre-phase-22 Player records"
    - "A player cannot reveal another player's hand — server uses senderToken, not message body"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "Updated Player (handRevealed), ClientGameState (myHandRevealed, opponentRevealedHands), ClientAction (SET_HAND_REVEALED)"
      contains: "handRevealed"
    - path: "party/index.ts"
      provides: "SET_HAND_REVEALED handler, viewFor() masking extension, RESET_TABLE extension, onStart() migration"
      contains: "SET_HAND_REVEALED"
    - path: "tests/handReveal.test.ts"
      provides: "Unit coverage for HAND-01 through HAND-04"
      exports: []
    - path: "tests/resetTable.test.ts"
      provides: "Extended with handRevealed reset case"
      contains: "handRevealed"
  key_links:
    - from: "party/index.ts viewFor()"
      to: "ClientGameState.opponentRevealedHands"
      via: "filter on player.handRevealed"
      pattern: "opponentRevealedHands"
    - from: "party/index.ts onMessage SET_HAND_REVEALED"
      to: "player.handRevealed"
      via: "senderToken lookup in players array"
      pattern: "SET_HAND_REVEALED"
---

<objective>
Wire the server-side Hand Reveal feature: extend the type system, implement the SET_HAND_REVEALED action handler with access control, update viewFor() to conditionally send full card arrays for revealed players, add the onStart() migration guard, extend RESET_TABLE to clear reveal state, and create the test suite that drives all four phase requirements.

Purpose: This is the data and logic foundation. The UI plan (22-02) depends on ClientGameState having myHandRevealed and opponentRevealedHands before any component work begins.
Output: Updated types.ts, party/index.ts, tests/handReveal.test.ts, extended tests/resetTable.test.ts. All tests green.
</objective>

<execution_context>
@/Users/aaronkaminsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronkaminsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-restrict-play-card-set-to-spread-region/22-CONTEXT.md
@.planning/phases/22-restrict-play-card-set-to-spread-region/22-RESEARCH.md
@.planning/phases/22-restrict-play-card-set-to-spread-region/22-VALIDATION.md

<interfaces>
<!-- Current type shapes the executor MUST NOT deviate from — read types.ts before touching it. -->

From src/shared/types.ts (current):

interface Player {
  id: string;
  connected: boolean;
  displayName: string;
  // handRevealed: boolean  — ADD THIS FIELD
}

interface ClientGameState {
  roomId: string;
  phase: "lobby" | "setup" | "playing";
  players: Player[];
  myPlayerId: string;
  myHand: Card[];
  opponentHandCounts: Record<string, number>;
  // myHandRevealed: boolean           — ADD (required, non-optional)
  // opponentRevealedHands: Record<string, Card[]>  — ADD (required, non-optional)
  piles: ClientPile[];
  canUndo: boolean;
  myPlayZoneId: string;
}

type ClientAction =
  | { type: "MOVE_CARD"; ... }
  | { type: "REORDER_HAND"; orderedCardIds: string[] }
  // ... existing variants ...
  | { type: "PING" };
  // ADD: | { type: "SET_HAND_REVEALED"; revealed: boolean }

From party/index.ts:

export function viewFor(state: GameState, playerToken: string | null): ClientGameState {
  // line 74-78: current opponentHandCounts mapping
  opponentHandCounts: Object.fromEntries(
    Object.entries(state.hands)
      .filter(([token]) => token !== playerToken)
      .map(([token, cards]) => [token, cards.length])
  ),
  // ADD: mutually exclusive opponentRevealedHands mapping
  // ADD: myHandRevealed field
}

// line 111: onStart() — existing migration pattern
// line 204+: onMessage() — senderToken = getPlayerToken(sender); use for auth
// line 489: RESET_TABLE case — add handRevealed clear loop after existing reset logic

Test infrastructure (tests/helpers.ts):
  makeMockRoom(connections, overrides)
  makeMockConnection(id)  — state.playerToken = id
  makeCard(id, faceUp = false)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend types and create failing test suite</name>
  <files>src/shared/types.ts, tests/handReveal.test.ts</files>
  <read_first>
    - src/shared/types.ts — read entire file; understand all current type shapes before adding fields
    - tests/broadcastMasking.test.ts — reference pattern: multi-connection setup, onMessage invocation, STATE_UPDATE extraction
    - tests/helpers.ts — makeMockRoom, makeMockConnection, makeCard signatures
    - tests/resetTable.test.ts — read the beforeEach setup; new case must use same pattern
  </read_first>
  <behavior>
    - Test 1 (HAND-01): send SET_HAND_REVEALED {revealed:true} as player-1; assert gameState.players.find(p => p.id === "player-1").handRevealed === true
    - Test 2 (HAND-02): send SET_HAND_REVEALED {revealed:false} after setting handRevealed=true; assert handRevealed === false
    - Test 3 (HAND-03 — masking): after player-1 reveals, player-2's STATE_UPDATE must have opponentRevealedHands["player-1"] equal to player-1's full card array AND player-1 must be absent from opponentHandCounts; player-1's own STATE_UPDATE must have myHandRevealed=true and opponentRevealedHands not containing player-1's own token
    - Test 4 (HAND-04 — reconnect persistence): set player.handRevealed=true in gameState directly; call viewFor(); assert result.myHandRevealed is true for that player's token and opponentRevealedHands is populated for other connections
    - Test 5 (V4 access control): send SET_HAND_REVEALED as player-2 with a message body that does not include a playerId field; assert only player-2's handRevealed changes — player-1 is unaffected (server derives identity from senderToken, not body)
    - Test 6 (V5 input validation): send SET_HAND_REVEALED with revealed: "true" (string); server must treat it as falsy (false); no crash; player.handRevealed must be false
  </behavior>
  <action>
    In src/shared/types.ts:

    1. Add `handRevealed: boolean` to the Player interface after the displayName field.

    2. Add two required fields to ClientGameState after myHand:
       - `myHandRevealed: boolean`
       - `opponentRevealedHands: Record<string, Card[]>`

    3. Add the SET_HAND_REVEALED variant to the ClientAction union:
       `| { type: "SET_HAND_REVEALED"; revealed: boolean }`
       Place it before the RESET_TABLE variant for logical grouping.

    Then in tests/handReveal.test.ts, write the six failing tests described in the behavior block. Import GameRoom from "../party/index", makeMockRoom / makeMockConnection / makeCard from "./helpers", and ClientGameState / ServerEvent from "../src/shared/types". Tests will fail (RED) until Task 2 implements the server logic.

    TypeScript will also error on existing code that constructs ClientGameState without the two new required fields — those errors are expected and will be fixed in Task 2 (viewFor) and Task 3 of Plan 22-02 (BoardView).
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -v "party/index.ts\|src/components" | head -20; npm test -- --reporter=verbose tests/handReveal.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/shared/types.ts: Player has handRevealed:boolean; ClientGameState has myHandRevealed:boolean and opponentRevealedHands:Record&lt;string,Card[]&gt;; ClientAction union includes SET_HAND_REVEALED
    - tests/handReveal.test.ts exists with 6 tests; all 6 fail (RED state — server not yet implemented)
    - TypeScript errors are only in party/index.ts and components (expected; will be resolved in Tasks 2 and Plan 22-02)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement server logic — handler, viewFor, migration, reset</name>
  <files>party/index.ts, tests/resetTable.test.ts</files>
  <read_first>
    - party/index.ts — read the entire file; pay particular attention to:
      - viewFor() at line 64 (current opponentHandCounts mapping)
      - onStart() at line 111 (existing migration guard pattern)
      - onMessage() at line 204 (senderToken derivation, existing action cases)
      - RESET_TABLE case at line 489 (where to add the handRevealed clear)
      - getPlayerToken() at line 98 (must use this for auth, not message body)
      - broadcastState() at line 679 (confirm it calls viewFor per connection — no changes needed)
    - src/shared/types.ts — must see the new type shapes from Task 1 before writing viewFor()
    - tests/resetTable.test.ts — read existing beforeEach setup to write a compatible new test case
  </read_first>
  <behavior>
    After this task all 6 handReveal tests must be GREEN. The resetTable new case must also be GREEN.
    Existing npm test suite must remain fully green (no regressions in broadcastMasking, viewFor, resetTable, etc.).
  </behavior>
  <action>
    In party/index.ts, make these four targeted changes:

    1. MIGRATION GUARD — in onStart(), after the existing pile migrations (around line 158), add:
       Iterate this.gameState.players; for each player where !('handRevealed' in player), set (player as any).handRevealed = false.

    2. viewFor() EXTENSION — replace the current single opponentHandCounts mapping with two mutually exclusive mappings.
       opponentHandCounts: entries of state.hands filtered to tokens !== playerToken AND where the player's handRevealed is falsy, mapped to card count (same as current).
       opponentRevealedHands: entries of state.hands filtered to tokens !== playerToken AND where the player's handRevealed is true, returning the full Card[] value (not masked — these are intentionally revealed).
       Also add myHandRevealed: state.players.find(p => p.id === playerToken)?.handRevealed ?? false.
       The two opponent collections must be mutually exclusive (a revealed player appears only in opponentRevealedHands; a hidden player appears only in opponentHandCounts).

    3. SET_HAND_REVEALED HANDLER — in onMessage(), add a new case before RESET_TABLE:
       case "SET_HAND_REVEALED": {
         V4 Access Control: find the player where player.id === senderToken. Do not use any playerId from the message body — senderToken from getPlayerToken(sender) is the authoritative identity.
         V5 Input Validation: treat action.revealed as boolean; use Boolean(action.revealed) === true (so string "true" becomes true, but explicitly use === true comparison: const isRevealed = action.revealed === true).
         Set player.handRevealed = isRevealed.
         Do NOT call takeSnapshot() — reveal state is not undoable (consistent with RESET_TABLE).
         Fall through to await persist() + broadcastState() at the end of the switch.
       }

    4. RESET_TABLE EXTENSION — inside the RESET_TABLE case at line 489, after the existing reset logic (setting phase to "setup" and clearing undoSnapshots), add:
       for (const player of this.gameState.players) { player.handRevealed = false; }

    In tests/resetTable.test.ts, add one new test case to the existing describe block:
    "clears handRevealed for all players on RESET_TABLE" — set player-1.handRevealed=true and player-2.handRevealed=true before sending RESET_TABLE; assert both are false afterward.
  </action>
  <verify>
    <automated>npm test && npm run typecheck</automated>
  </verify>
  <done>
    - All 6 tests in tests/handReveal.test.ts pass (GREEN)
    - tests/resetTable.test.ts new case passes
    - Full npm test suite exits 0 (no regressions)
    - npm run typecheck exits 0 (no type errors in party/index.ts)
    - party/index.ts viewFor() returns ClientGameState with myHandRevealed, opponentHandCounts, and opponentRevealedHands; revealed players are in opponentRevealedHands only, hidden in opponentHandCounts only
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket client → PartyKit server | Any connected player can send any ClientAction message; the server cannot trust the message body for identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | SET_HAND_REVEALED handler | mitigate | Server derives player identity from senderToken = getPlayerToken(sender) (connection state, set at onConnect); never reads a playerId field from the message body. A player cannot reveal another player's hand. |
| T-22-02 | Tampering | SET_HAND_REVEALED handler — revealed field | mitigate | Validate with `action.revealed === true` strict equality; string "true", 1, or any non-boolean truthy value resolves to false. No crash path. |
| T-22-03 | Information Disclosure | viewFor() opponentRevealedHands | mitigate | opponentRevealedHands only populated for players where handRevealed===true; hidden players are absent from this field; both collections are mutually exclusive |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed in this phase — no supply-chain surface |
</threat_model>

<verification>
After both tasks complete:
- `npm test` exits 0 — full suite green including handReveal.test.ts (6 cases) and resetTable.test.ts new case
- `npm run typecheck` exits 0 — no type errors in party/index.ts or src/shared/types.ts
- `grep -c "opponentRevealedHands" party/index.ts` returns >= 2 (field in viewFor + type reference)
- `grep -c "SET_HAND_REVEALED" party/index.ts` returns >= 1 (action handler)
- `grep -c "handRevealed" party/index.ts` returns >= 3 (migration + handler + RESET_TABLE + viewFor)
- `grep "handRevealed" src/shared/types.ts` shows the field on Player interface
</verification>

<success_criteria>
Server-side Hand Reveal is complete when:
- Player types carry handRevealed:boolean; ClientGameState carries myHandRevealed and opponentRevealedHands as required fields
- SET_HAND_REVEALED action is handled; server uses senderToken for auth (cannot reveal another player's hand); revealed:non-boolean is rejected as false
- viewFor() sends full Card[] in opponentRevealedHands for revealed players; those players are absent from opponentHandCounts
- RESET_TABLE clears handRevealed for all players (D-07)
- onStart() migration guard protects rooms with pre-phase-22 persisted state
- All 6 handReveal unit tests pass; resetTable reset-handRevealed case passes; full suite green
</success_criteria>

<output>
Create `.planning/phases/22-restrict-play-card-set-to-spread-region/22-01-SUMMARY.md` when done.
</output>
