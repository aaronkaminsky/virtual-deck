---
phase: 25
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SpreadZone.tsx
autonomous: true
requirements:
  - POLISH-01
  - POLISH-04
  - ZONE-01

must_haves:
  truths:
    - "Empty spread zones show no body text inside the droppable area"
    - "Personal spread zone droppable div uses h-[64px] sm:h-[88px] when non-empty"
    - "Empty personal spread zone collapses to h-px opacity-0 by default"
    - "Empty personal spread zone reveals a h-[40px] sm:h-[56px] dashed strip when isOver is true"
    - "The SpreadZone droppable node remains in the DOM at all times (no conditional unmounting)"
    - "SpreadZone controls (Eye, SelectAll) are hidden when isEmpty"
  artifacts:
    - path: "src/components/SpreadZone.tsx"
      provides: "Updated SpreadZone with polish changes"
      contains: "h-[64px] sm:h-[88px]"
  key_links:
    - from: "SpreadZone useDroppable"
      to: "isOver boolean"
      via: "useDroppable({ id: 'pile-${pile.id}' })"
      pattern: "isOver.*h-\\[40px\\]|h-\\[40px\\].*isOver"
---

<objective>
Three targeted polish changes to SpreadZone.tsx: remove empty-zone body text (POLISH-01), update card zone height to compact dimensions (POLISH-04), and add collapse/reveal logic for empty personal spread zones (ZONE-01).

Purpose: Empty spread zones become visually quiet; zone heights are more compact across the board; the personal spread zone is invisible when empty and only reveals a slim drop-target strip when a card is dragged over it.
Output: Updated SpreadZone.tsx with all three changes applied.
</objective>

<execution_context>
@/Users/aaronkaminsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronkaminsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-play-area-layout-center-canvas/25-CONTEXT.md
@.planning/phases/25-play-area-layout-center-canvas/25-UI-SPEC.md

<interfaces>
From src/components/SpreadZone.tsx (read before modifying):

SpreadZoneProps interface:
```typescript
interface SpreadZoneProps {
  pile: ClientPile;
  sendAction: (action: ClientAction) => void;
  draggingCardId: string | null;
  className?: string;
  interactive?: boolean;
  selectedIds?: Set<string>;
  onToggleSelect?: (id: string, zone: 'hand' | 'pile', zoneId: string) => void;
  onSelectAll?: (cardIds: string[], zone: 'hand' | 'pile', zoneId: string) => void;
  selectionSource?: { zone: 'hand' | 'pile'; zoneId: string } | null;
}
```

Key state already in component:
- `const { setNodeRef, isOver } = useDroppable({ id: 'pile-${pile.id}', data: { toZone: 'pile', toId: pile.id } });`
- `const isEmpty = pile.cards.length === 0;`
- `interactive` prop distinguishes personal zone (interactive !== false) from opponent zone (interactive={false})

Current droppable div (line 165-173):
```
className={cn(
  'min-w-[56px] h-[79px] sm:min-w-[80px] sm:h-[112px] rounded-lg border flex items-center px-2 overflow-x-auto bg-secondary',
  isEmpty ? 'border-dashed' : '',
  isOver ? 'border-primary' : 'border-border',
  className
)}
```

Current empty state (line 175-176):
```
{isEmpty ? (
  <span className="text-xs text-muted-foreground">{pile.name}</span>
```

Current controls below droppable (lines 213-235):
```
<div className="flex gap-1">
  <Button variant="ghost" className="h-7 w-7 p-0" onClick={handleToggleFace} ...>
    {pile.faceUp !== false ? <Eye .../> : <EyeOff .../>}
  </Button>
  {interactive !== false && (
    <Button variant="ghost" className="h-7 w-7 p-0" onClick={handleSelectAll} disabled={faceUpCards.length === 0} ...>
      <SquareCheck .../>
    </Button>
  )}
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: POLISH-01 — Remove empty zone body text from SpreadZone</name>
  <files>src/components/SpreadZone.tsx</files>
  <read_first>
    - src/components/SpreadZone.tsx — read the full file before touching anything; focus on lines 155-237 (the return block). Note the isEmpty conditional at line 175 that renders the span with pile.name inside the droppable.
  </read_first>
  <action>
    In SpreadZone.tsx, in the droppable div's children block (currently lines 175-211), remove the isEmpty branch that renders `span className="text-xs text-muted-foreground">{pile.name}`. After removal, when isEmpty is true the droppable renders nothing inside itself — just an empty container with its dashed border. The interactive and non-interactive non-empty branches remain unchanged.

    The resulting children structure should be: when isEmpty, render nothing (or a React fragment with no children); when !isEmpty and interactive !== false, render the SortableContext block; when !isEmpty and interactive === false, render the read-only cards div.

    Per D-08: the label above the droppable (the `span` at the top of the outer flex-col, lines 157-163) already shows the pile name — no duplication needed inside the zone.
  </action>
  <verify>
    <automated>grep -n "pile.name" /Users/aaronkaminsky/code/virtual-deck/src/components/SpreadZone.tsx</automated>
  </verify>
  <done>The file has `pile.name` only in the label span at the top of the component (inside the flex items-center header row). There is no `pile.name` reference inside the droppable div children. npm run typecheck exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: POLISH-04 + ZONE-01 — Compact height and empty-zone collapse/reveal</name>
  <files>src/components/SpreadZone.tsx</files>
  <read_first>
    - src/components/SpreadZone.tsx — already read in Task 1; the droppable div is at lines 165-212 (after Task 1 edits may shift lines slightly). Focus on the className prop of the droppable div and the controls div below it.
    - .planning/phases/25-play-area-layout-center-canvas/25-UI-SPEC.md §POLISH-04 and §ZONE-01 — confirm height tokens and strip dimensions.
  </read_first>
  <action>
    Two changes to the droppable div's className and the surrounding structure:

    POLISH-04: Replace the fixed height classes on the droppable div:
    - Replace `h-[79px]` with `h-[64px]`
    - Replace `sm:h-[112px]` with `sm:h-[88px]`
    The min-width, border, flex, px-2, overflow-x-auto, bg-secondary classes are unchanged.

    ZONE-01: Add conditional collapse/reveal behavior to the droppable div, gated on `isEmpty && interactive !== false` (personal zones only — opponent zones with interactive={false} are never collapsed).

    The droppable div's className must now apply height conditionally:
    - When `isEmpty && interactive !== false && !isOver`: apply `h-px opacity-0` (1-pixel transparent strip keeps the node in DOM for dnd-kit collision detection; visually invisible)
    - When `isEmpty && interactive !== false && isOver`: apply `h-[40px] sm:h-[56px] border border-dashed border-primary rounded-lg` — slim drop strip at approximately half card height; remove `bg-secondary` and `border-border` classes in this state since border-primary dashed replaces them
    - When `!isEmpty` or `interactive === false`: apply the standard compact height `h-[64px] sm:h-[88px]` with the existing isEmpty/isOver border logic

    Use `cn()` with conditional class strings to express this. The droppable node ref (`ref={setNodeRef}`) must always be attached — do not wrap it in a conditional render.

    ZONE-01 controls: The controls div below the droppable (`div className="flex gap-1"` with Eye/SelectAll buttons) should only render when `!isEmpty || interactive === false`. When `isEmpty && interactive !== false`, suppress the controls div entirely (no buttons when there are no cards to act on in a collapsed personal zone). Use `{(!isEmpty || interactive === false) && (<div className="flex gap-1">...)}`.

    Note: the `overflow-x-auto` class may be removed from the collapsed states to avoid unexpected scroll artifacts when height is 1px, but it should remain in the standard non-empty rendering path.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `h-[64px]` and `sm:h-[88px]` appear in SpreadZone.tsx replacing the old `h-[79px]` and `sm:h-[112px]` in the standard-height path
    - `h-px opacity-0` appears in SpreadZone.tsx for the collapsed empty personal zone state
    - `h-[40px] sm:h-[56px]` appears in SpreadZone.tsx for the isOver reveal state
    - `border-primary` appears in the ZONE-01 isOver branch
    - `setNodeRef` is attached to the droppable div unconditionally (node always in DOM)
    - npm run typecheck exits 0
    - npm test exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→dnd-kit | isOver is a boolean derived from dnd-kit pointer intersection — no user-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | SpreadZone collapse logic | accept | isOver is read-only from dnd-kit; no user input influences collapse state beyond pointer position |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
- `grep -n "h-\[79px\]\|h-\[112px\]" src/components/SpreadZone.tsx` returns no matches (old heights fully replaced)
- `grep -c "h-px opacity-0" src/components/SpreadZone.tsx` returns 1
- `grep -c "h-\[40px\]" src/components/SpreadZone.tsx` returns 1
- `grep -v "^//" src/components/SpreadZone.tsx | grep -c "pile\.name"` returns 1 (header label only, not inside droppable)
- `npm run typecheck` exits 0
- `npm test` exits 0
</verification>

<success_criteria>
1. Empty spread zones display no text inside the droppable area — dashed border only (POLISH-01 / D-08)
2. SpreadZone droppable uses h-[64px] sm:h-[88px] when non-empty or opponent zone (POLISH-04)
3. Personal empty spread zone is visually invisible (h-px opacity-0) when not hovered (ZONE-01 / D-05, D-06)
4. Personal empty spread zone reveals a h-[40px] sm:h-[56px] border-dashed border-primary strip when isOver (ZONE-01 / D-07)
5. Droppable node remains mounted at all times for dnd-kit collision detection (ZONE-01 note)
6. Controls are suppressed for empty personal zones (no buttons when no cards)
7. npm run typecheck exits 0; npm test exits 0
</success_criteria>

<output>
Create `.planning/phases/25-play-area-layout-center-canvas/25-01-SUMMARY.md` when done.
</output>
