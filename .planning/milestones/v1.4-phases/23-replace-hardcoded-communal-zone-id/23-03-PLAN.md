---
phase: 23-replace-hardcoded-communal-zone-id
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/BoardDragLayer.tsx
  - src/components/BoardView.tsx
  - src/components/PileZone.tsx
  - src/components/SpreadZone.tsx
  - tests/selectAll.test.ts
autonomous: true
requirements:
  - SELECT-01
  - SELECT-02
  - SELECT-03
tags:
  - ui
  - select-all
  - selection
  - pile-zone
  - spread-zone
  - board-drag-layer
must_haves:
  truths:
    - "A player can click a Select All icon button on any pile and the top card of that pile becomes the only selected card; selectionSource points to that pile (SELECT-01, D-09)"
    - "A player can click a Select All icon button on the communal or their own personal spread zone and ALL face-up cards in that zone become selected; selectionSource points to that zone (SELECT-02, D-10)"
    - "A Select All click on a non-empty zone REPLACES any existing selection (clears `selectedIds` and updates `selectionSource`) — no cross-zone merging (D-07)"
    - "A Select All click on a face-down pile still produces a valid single-card selection (top card id) and that card can be dragged while face-down (D-08, SELECT-03)"
    - "After Select All, dragging any selected card fires the existing `PLAY_CARD_SET` server action with all selected ids, and the destination receives every card atomically (SELECT-03)"
    - "Select All button is disabled on empty piles (no top card) and empty spread zones (no face-up cards)"
    - "Select All button is NOT rendered on opponent SpreadZone instances (`interactive={false}` path receives no `onSelectAll` prop)"
    - "`tests/selectAll.test.ts` it.todo stubs are converted into passing `it(...)` tests covering SELECT-01, SELECT-02, SELECT-03 via the PLAY_CARD_SET handler"
  artifacts:
    - path: src/components/BoardDragLayer.tsx
      provides: "handleSelectAll callback wired into state and threaded to BoardView"
      contains: "handleSelectAll"
    - path: src/components/BoardView.tsx
      provides: "onSelectAll prop on BoardViewProps; threaded to PileZone and interactive SpreadZone instances only"
      contains: "onSelectAll"
    - path: src/components/PileZone.tsx
      provides: "Select All ghost icon button in controls row; top-card-only selection"
      contains: "handleSelectAll"
    - path: src/components/SpreadZone.tsx
      provides: "Select All ghost icon button next to Eye toggle; selects all face-up cards"
      contains: "handleSelectAll"
    - path: tests/selectAll.test.ts
      provides: "Passing unit/integration tests for SELECT-01, SELECT-02, SELECT-03"
      contains: "describe"
  key_links:
    - from: src/components/PileZone.tsx Select All button
      to: src/components/BoardDragLayer.tsx handleSelectAll
      via: "onSelectAll prop chain: BoardDragLayer → BoardView → PileZone"
      pattern: "onSelectAll"
    - from: src/components/SpreadZone.tsx Select All button
      to: src/components/BoardDragLayer.tsx handleSelectAll
      via: "onSelectAll prop chain: BoardDragLayer → BoardView → interactive SpreadZone only"
      pattern: "onSelectAll"
    - from: tests/selectAll.test.ts
      to: party/index.ts PLAY_CARD_SET handler
      via: "Server-side simulation: pre-populate selection on the client (skipped in the test) and send PLAY_CARD_SET with the would-be selectedIds"
      pattern: "PLAY_CARD_SET"
---

<objective>
Add Select All affordances on every PileZone and every interactive SpreadZone per CONTEXT.md D-07..D-10 and SELECT-01/02/03. Selection state, drag, and drop-handling all already exist — this plan only adds:
1. A new `handleSelectAll` callback in `BoardDragLayer.tsx` that atomically sets `selectedIds` and `selectionSource`.
2. Prop threading through `BoardView.tsx` to `PileZone.tsx` and to `SpreadZone.tsx` (only when `interactive={true}` — opponent zones are excluded).
3. Ghost icon Select All buttons in `PileZone.tsx` and `SpreadZone.tsx`.
4. Conversion of Wave-0 `it.todo` stubs in `tests/selectAll.test.ts` into passing server-side tests that prove the multi-card move (the existing `PLAY_CARD_SET` handler) accepts the populated selections.

Purpose:
- Satisfy SELECT-01, SELECT-02, SELECT-03 (all three requirements assigned to this plan).
- Resolve RESEARCH.md Open Question 3: Select All on a face-down pile means "top card only" — this is the only mechanically valid interpretation since interior pile cards are masked (no client-visible id) and `PLAY_CARD_SET` rejects unknown ids with `CARD_NOT_IN_SOURCE`.

Output:
- 4 source files updated, each preserving existing behavior for selection, drag, and other controls.
- 1 test file (`tests/selectAll.test.ts`) with three real `it(...)` tests replacing the Wave-0 todos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-replace-hardcoded-communal-zone-id/23-CONTEXT.md
@.planning/phases/23-replace-hardcoded-communal-zone-id/23-RESEARCH.md
@.planning/phases/23-replace-hardcoded-communal-zone-id/23-PATTERNS.md
@.planning/phases/23-replace-hardcoded-communal-zone-id/23-UI-SPEC.md
@.planning/phases/23-replace-hardcoded-communal-zone-id/23-VALIDATION.md
@src/components/BoardDragLayer.tsx
@src/components/BoardView.tsx
@src/components/PileZone.tsx
@src/components/SpreadZone.tsx
@src/shared/types.ts
@src/components/ui/button.tsx
@src/lib/utils.ts
@tests/playCardSet.test.ts
@tests/helpers.ts

<interfaces>
<!-- Contracts the executor needs -->

From src/shared/types.ts:
```
PLAY_CARD_SET: { type: "PLAY_CARD_SET"; cardIds: string[]; fromZone?: "hand" | "pile"; fromId: string; toZone: "pile" | "hand"; toId: string }
```

From src/components/BoardDragLayer.tsx (existing state and callbacks):
- `const [selectedIds, setSelectedIds] = useState<Set<string>>(new Set());`
- `const [selectionSource, setSelectionSource] = useState<SelectionSource>(null);`
- `const handleToggleSelect = (id, zone, zoneId) => { ... };` (lines 87–109)
- `<BoardView ... onToggleSelect={handleToggleSelect} ... />` on line 332

From src/components/BoardView.tsx (existing prop threading):
- `BoardViewProps` includes `onToggleSelect`, `selectedIds`, `selectionSource` (lines 10–21)
- Communal SpreadZone (lines 78–89): receives `selectedIds`, `onToggleSelect`, `selectionSource`, `interactive={true}`
- My spread SpreadZone (lines 94–103): same — receives all three plus `interactive={true}`
- Opponent SpreadZone (lines 55–61): receives only `pile`, `sendAction`, `draggingCardId`, `interactive={false}` — DO NOT thread onSelectAll here
- PileZone instances (line 74): currently receive NO selection props

From src/components/PileZone.tsx (existing structure):
- `topCard = isEmpty ? null : pile.cards[pile.cards.length - 1]` (line 24)
- Controls row at lines 76–95: existing `<div className="flex gap-1 mt-1">` with Eye + Shuffle buttons
- The Select All button is the THIRD button added to this div

From src/components/SpreadZone.tsx (existing structure):
- `const faceUpCards = pile.cards.filter((c): c is Card => 'id' in c);` (line 87) — already computed
- Existing standalone Eye button at lines 207–215 (rendered AFTER the zone body, NOT in the header)
- Plan: replace that standalone `<Button>` with a `<div className="flex gap-1">` wrapping BOTH the Eye button and a new Select All button. Match the PileZone controls row pattern.

Locked decision values:
- D-07: Select All replaces existing selection (clears + sets atomically)
- D-08: Works on face-down piles (top card only)
- D-09: PileZone — ghost icon button in `flex gap-1 mt-1` controls row
- D-10: SpreadZone — ghost icon button alongside existing eye icon (same h-7 w-7 p-0 style)
- UI-SPEC: icon = `SquareCheck` from lucide-react; always `text-muted-foreground` (never active); tooltip "Select all cards in pile" / "Select all cards in zone"; disabled on empty

Existing PLAY_CARD_SET test helpers (from tests/playCardSet.test.ts):
- `makeStateWithPlayerAndCards(playerId, cards)` — builds GameState with player + their hand
- `makeStateWithPileCards(playerId, pileId, cards)` — builds GameState with player + cards already in named pile; ALSO creates the personal spread zone `spread-${playerId}`
- The test sets `room.gameState = makeStateWithPileCards(...)` directly, then calls `await room.onMessage(JSON.stringify({ type: "PLAY_CARD_SET", ... }), sender)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add handleSelectAll in BoardDragLayer and thread onSelectAll through BoardView</name>
  <files>src/components/BoardDragLayer.tsx, src/components/BoardView.tsx</files>
  <read_first>
    - src/components/BoardDragLayer.tsx (full file — state declarations at 78–82, handleToggleSelect at 87–109, BoardView render at line 332)
    - src/components/BoardView.tsx (full file — BoardViewProps interface, PileZone render line 74, SpreadZone renders at 55–61 opponent / 78–89 communal / 94–103 mySpreadZone)
    - .planning/phases/23-replace-hardcoded-communal-zone-id/23-PATTERNS.md (§BoardDragLayer.tsx and §BoardView.tsx pattern sections)
    - .planning/phases/23-replace-hardcoded-communal-zone-id/23-RESEARCH.md (Pitfall 3 — guard for empty zones)
  </read_first>
  <behavior>
    - `handleSelectAll(cardIds: string[], zone: 'hand' | 'pile', zoneId: string)` in BoardDragLayer:
      - Early-return when `cardIds.length === 0` (guard against empty-zone clicks; the UI also disables the button, this is defense in depth).
      - Sets `selectedIds = new Set(cardIds)` and `selectionSource = { zone, zoneId }` atomically (both React state setters fire in the same handler call so they batch under React 18 automatic batching).
    - `BoardView`:
      - `BoardViewProps` gains `onSelectAll: (cardIds: string[], zone: 'hand' | 'pile', zoneId: string) => void;` (non-optional — BoardDragLayer always provides it).
      - Every `<PileZone>` render gets `onSelectAll={onSelectAll}`.
      - The communal `<SpreadZone>` and the `mySpreadZone` `<SpreadZone>` (both `interactive={true}`) get `onSelectAll={onSelectAll}`.
      - The opponent `<SpreadZone>` (`interactive={false}`) gets NO `onSelectAll` prop. This is enforced by simply omitting it in that JSX.
  </behavior>
  <action>
    In `src/components/BoardDragLayer.tsx`, immediately after the `handleToggleSelect` function (after line 109), add `const handleSelectAll = (cardIds: string[], zone: 'hand' | 'pile', zoneId: string) => { ... };` with the body described in `<behavior>`. The function only sets state — do NOT add console logging, do NOT add a `useCallback` (consistent with `handleToggleSelect` which is a plain arrow). Then update the `<BoardView ... />` JSX at line 332 to include `onSelectAll={handleSelectAll}` (place it after `onToggleSelect={handleToggleSelect}` for readability).

    In `src/components/BoardView.tsx`, extend `BoardViewProps` (lines 10–21) to add `onSelectAll: (cardIds: string[], zone: 'hand' | 'pile', zoneId: string) => void;` as a NON-optional prop. Add `onSelectAll` to the destructured arguments list in the `BoardView({ ... })` signature (line 23). In each of these JSX call sites, pass `onSelectAll={onSelectAll}`:
    - `<PileZone ... />` on line 74
    - communal `<SpreadZone ... />` block (lines 78–89)
    - `mySpreadZone` `<SpreadZone ... />` block (lines 94–103)

    Do NOT add `onSelectAll` to the opponent `<SpreadZone>` (lines 55–61) — opponent zones must not receive the callback.

    Do NOT touch the `HandZone` JSX (the hand has no Select All requirement). Do NOT alter selection clearing logic in the existing `useEffect` at lines 129–144 (it already correctly handles stale selections after server-driven state changes).
  </action>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/BoardDragLayer.tsx` contains the literal string `handleSelectAll` and the line `setSelectionSource({ zone, zoneId })` within that function body.
    - `src/components/BoardView.tsx` `BoardViewProps` includes `onSelectAll:` (non-optional, no `?`).
    - The opponent SpreadZone JSX block (lines 55–61 region) does NOT contain the string `onSelectAll`.
    - The communal and mySpreadZone SpreadZone blocks DO contain `onSelectAll={onSelectAll}`.
    - The PileZone JSX line contains `onSelectAll={onSelectAll}`.
    - `npm test` exits 0 (existing test suite still green — no behavior was changed yet; PileZone/SpreadZone don't accept the prop yet but TypeScript allows ignored extra props as long as the receiving component has no excess-property check… IMPORTANT: This task will FAIL typecheck unless Task 2 ships PileZone/SpreadZone prop additions in the same wave. To avoid the typecheck failure, do BOTH Task 1 and Task 2 together in the same agent session; alternatively split into a single commit. Recommendation: commit after Task 2 is also done, but Task 1 and Task 2 can be implemented serially in one execution pass before running typecheck.)
    - `npm run typecheck` exits 0 only AFTER Task 2 has also been applied.
  </acceptance_criteria>
  <done>BoardDragLayer creates and threads `handleSelectAll`; BoardView prop interface and call sites are updated; opponent zones excluded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Select All buttons to PileZone and SpreadZone</name>
  <files>src/components/PileZone.tsx, src/components/SpreadZone.tsx</files>
  <read_first>
    - src/components/PileZone.tsx (full file — controls row at lines 76–95, topCard derivation at line 24, isEmpty at line 23)
    - src/components/SpreadZone.tsx (full file — SpreadZoneProps at 69–78, faceUpCards at 87, standalone Eye button at lines 207–215, isEmpty at 147)
    - .planning/phases/23-replace-hardcoded-communal-zone-id/23-UI-SPEC.md (Select All button placement, icon SquareCheck, tooltips, disabled state)
    - .planning/phases/23-replace-hardcoded-communal-zone-id/23-PATTERNS.md (§PileZone.tsx and §SpreadZone.tsx pattern sections)
    - .planning/phases/23-replace-hardcoded-communal-zone-id/23-RESEARCH.md (Pitfall 4 — top-card-only; Pitfall 5 — SpreadZone Eye button placement; Pattern 4)
  </read_first>
  <behavior>
    - PileZone:
      - `PileZoneProps` gains `onSelectAll?: (cardIds: string[], zone: 'hand' | 'pile', zoneId: string) => void;` (optional — keeps backward compat).
      - `handleSelectAll()` early-returns when `!onSelectAll`, `!topCard`, or `!('id' in topCard)`. Otherwise calls `onSelectAll([(topCard as Card).id], 'pile', pile.id)`.
      - A third `<Button>` is appended to the existing `<div className="flex gap-1 mt-1">` controls row (after Eye and Shuffle). Uses `variant="ghost" className="h-7 w-7 p-0"`. Child: `<SquareCheck className="w-4 h-4" />`. `title="Select all cards in pile"`, `aria-label="Select all"`. `disabled={isEmpty || !topCard || !('id' in topCard)}`.
    - SpreadZone:
      - `SpreadZoneProps` gains `onSelectAll?: (cardIds: string[], zone: 'hand' | 'pile', zoneId: string) => void;` (optional — preserves opponent-zone backward compat).
      - `handleSelectAll()` early-returns when `!onSelectAll`, when `interactive === false`, or when `faceUpCards.length === 0`. Otherwise calls `onSelectAll(faceUpCards.map(c => c.id), 'pile', pile.id)`.
      - The standalone `<Button>` Eye button at lines 207–215 is now wrapped together with the new Select All button in a `<div className="flex gap-1">`. Select All button: `variant="ghost" className="h-7 w-7 p-0"`, child `<SquareCheck className="w-4 h-4" />`, `title="Select all cards in zone"`, `aria-label="Select all"`, `disabled={faceUpCards.length === 0}`. Place the Select All button AFTER the Eye button inside the new flex container.
      - The Select All button must NOT render or function when `interactive === false`. Easiest approach: only render the new flex container's Select All `<Button>` when `interactive !== false && onSelectAll` is provided (the existing Eye button continues to render in all cases as today — wrap it conditionally if needed, but keep its rendering identical for opponent zones).
  </behavior>
  <action>
    In `src/components/PileZone.tsx`:
    - Extend the lucide-react import on line 2 from `{ Eye, EyeOff, Shuffle }` to `{ Eye, EyeOff, Shuffle, SquareCheck }`.
    - Extend `PileZoneProps` (lines 10–15) with `onSelectAll?: (cardIds: string[], zone: 'hand' | 'pile', zoneId: string) => void;`.
    - Destructure `onSelectAll` in the component signature (line 17): `export function PileZone({ pile, sendAction, draggingCardId, shufflingPileIds = new Set(), onSelectAll }: PileZoneProps)`.
    - Add a `handleSelectAll` function near the other handlers (after `handleFlipCard`, around line 41): early-return when `onSelectAll` is undefined OR `topCard` is null OR `!('id' in topCard)`; else call `onSelectAll([(topCard as Card).id], 'pile', pile.id)`.
    - Append a third `<Button>` to the existing `<div className="flex gap-1 mt-1">` at lines 76–95 (after the Shuffle Button). Use the exact attribute pattern described in `<behavior>`. The button is always rendered (consistent with Eye and Shuffle which always render). It is `disabled` on empty piles.

    In `src/components/SpreadZone.tsx`:
    - Extend the lucide-react import on line 6 from `{ Eye, EyeOff }` to `{ Eye, EyeOff, SquareCheck }`.
    - Extend `SpreadZoneProps` (lines 69–78) with `onSelectAll?: (cardIds: string[], zone: 'hand' | 'pile', zoneId: string) => void;`.
    - Destructure `onSelectAll` in the component signature (line 80).
    - Add a `handleSelectAll` function alongside `handleToggleFace` (around line 143): early-return when `onSelectAll` is undefined OR `interactive === false` OR `faceUpCards.length === 0`; else call `onSelectAll(faceUpCards.map(c => c.id), 'pile', pile.id)`.
    - Wrap the existing standalone Eye `<Button>` at lines 207–215 inside a new `<div className="flex gap-1">`. Inside that div, after the existing Eye Button, add the new Select All Button (only when `interactive !== false`; this can be expressed as `{interactive !== false && (<Button ... />)}`). Use the exact attribute pattern described in `<behavior>`. Keep the existing Eye Button rendering UNCHANGED — same props, same handler, same children.

    Do NOT alter the existing SortableSpreadCard rendering, the `useDndMonitor` block, or the spread-zone header/body. Do NOT change PileZone's `topCard` derivation, FLIP_CARD handler, or shuffling animation logic.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/PileZone.tsx` imports `SquareCheck` from `lucide-react`.
    - `src/components/PileZone.tsx` contains the function `handleSelectAll` and a `<Button>` whose child is `<SquareCheck`.
    - `src/components/PileZone.tsx` Select All Button JSX contains `disabled={` (verifies a disabled prop exists).
    - `src/components/SpreadZone.tsx` imports `SquareCheck` from `lucide-react`.
    - `src/components/SpreadZone.tsx` contains the function `handleSelectAll` and conditional rendering keyed on `interactive` and `onSelectAll` (assert via `grep -E 'interactive !== false' src/components/SpreadZone.tsx`).
    - `npm run typecheck` exits 0 (now that PileZone and SpreadZone accept `onSelectAll`, BoardView's threading also typechecks).
    - `npm test` exits 0 (existing tests unaffected — no behavior changes apply to the test fixtures).
    - Manual: clicking Select All on a non-empty pile in a live session selects the top card (ring + lift visible); clicking on a non-empty spread zone selects all face-up cards; clicking on an empty zone does nothing (button disabled); button does not appear on opponent spread zones.
  </acceptance_criteria>
  <done>Select All buttons present on PileZone (all) and SpreadZone (interactive only). State updates flow through to `BoardDragLayer.handleSelectAll`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert selectAll.test.ts todos into passing PLAY_CARD_SET coverage</name>
  <files>tests/selectAll.test.ts</files>
  <read_first>
    - tests/selectAll.test.ts (current Wave-0 scaffold from Plan 1 with three it.todo entries and copied helper functions)
    - tests/playCardSet.test.ts (analog of the test patterns — makeStateWithPlayerAndCards / makeStateWithPileCards, PLAY_CARD_SET dispatch, expectations)
    - party/index.ts §PLAY_CARD_SET handler (search for `case "PLAY_CARD_SET"` — confirms server validates all-ids-present and moves atomically)
    - src/shared/types.ts (PLAY_CARD_SET shape on line 70)
    - tests/helpers.ts (makeMockRoom, makeMockConnection, makeCard)
  </read_first>
  <behavior>
    Test 1 — "SELECT-01: PLAY_CARD_SET with a single pile top-card id removes that card from the pile":
      - Setup: build a state where pile `draw` (or `play`) has 3 cards; player has empty hand and a spread-${playerId} zone (use makeStateWithPileCards).
      - Action: send `PLAY_CARD_SET` with cardIds=[topCardId], fromZone="pile", fromId="draw", toZone="pile", toId=`spread-${playerId}`.
      - Assertion: source pile no longer contains the top card; destination zone now contains it.

    Test 2 — "SELECT-02: PLAY_CARD_SET with all face-up card ids from a spread zone moves the whole group atomically":
      - Setup: state where `play` (communal spread) has 3 face-up cards; player has empty hand and a spread-${playerId} zone.
      - Action: PLAY_CARD_SET with cardIds=all three ids, fromZone="pile", fromId="play", toZone="pile", toId=`spread-${playerId}`.
      - Assertion: `play` pile is empty; destination spread zone contains exactly the 3 cards in the order they were sent.

    Test 3 — "SELECT-03: PLAY_CARD_SET with multi-card source produces an atomic move that all-present validation accepts":
      - Setup: state where `play` has 3 cards.
      - Action: PLAY_CARD_SET with cardIds=[id1, id3] (skip middle card to prove partial multi-select works), fromZone="pile", fromId="play", toZone="pile", toId=`spread-${playerId}`.
      - Assertion: source retains exactly the middle card; destination contains both selected cards; `sender.send` was NOT called with an ERROR event whose code is `CARD_NOT_IN_SOURCE`.
  </behavior>
  <action>
    Open `tests/selectAll.test.ts`. The scaffold from Plan 1 already imports `vitest`, `GameRoom`, `defaultGameState`, `makeMockRoom`, `makeMockConnection`, `makeCard`, and includes the helper functions `makeStateWithPlayerAndCards` / `makeStateWithPileCards`.

    Replace each `it.todo("...")` call with a real `it("..." async () => { ... })` matching the test names already in the scaffold. Inside each test:
    1. `room.gameState = makeStateWithPileCards("player-1", "<pileId>", [makeCard("A-s"), makeCard("K-h"), makeCard("Q-d")]);` — use pile `"draw"` for Test 1 and `"play"` for Tests 2/3.
    2. `await room.onMessage(JSON.stringify({ type: "PLAY_CARD_SET", cardIds: [...], fromZone: "pile", fromId: "<pileId>", toZone: "pile", toId: "spread-player-1" }), sender);`
    3. Assertions per `<behavior>` above. Use `room.gameState.piles.find(p => p.id === ...)!.cards.map(c => c.id)` to inspect state. Use `sender.send` mock inspection to assert no `CARD_NOT_IN_SOURCE` error was emitted in Test 3.

    Do NOT add a React-rendering test — the tests target server-observable behavior, which is the proof that the UI's post-Select-All drag succeeds. The UI click-to-select itself is covered by the manual verifications in 23-VALIDATION.md.

    If the existing scaffold contains a `describe(...)` wrapper, keep it. If it does not, wrap all three `it(...)` calls in `describe("Select All → PLAY_CARD_SET", () => { ... })`. Use a `beforeEach` block to fresh-construct `room` and `sender` (mirror the pattern from `tests/playCardSet.test.ts` lines 29–33). Make sure the helper functions either remain at module scope (as Plan 1 wrote them) OR are moved inside the describe — either is acceptable; do NOT duplicate them.
  </action>
  <verify>
    <automated>npm test -- tests/selectAll.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/selectAll.test.ts` contains 0 `it.todo` and 3 `it(` calls with `async` bodies.
    - `npm test -- tests/selectAll.test.ts` exits 0 with 3 passing tests.
    - Each test contains `await room.onMessage(JSON.stringify({ type: "PLAY_CARD_SET"`.
    - Test 3 asserts on the absence of a `CARD_NOT_IN_SOURCE` send (e.g. via `expect(sender.send).not.toHaveBeenCalledWith(expect.stringContaining("CARD_NOT_IN_SOURCE"))`).
    - `npm test` full suite exits 0.
  </acceptance_criteria>
  <done>Select All flow is proven end-to-end via server-side coverage: the multi-card sources Select All produces are accepted by the existing `PLAY_CARD_SET` handler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PartyKit server | Select All is a pure client-side selection — no new server action is introduced. Drag dispatches existing `PLAY_CARD_SET`, which already validates `fromZone`/`fromId`/all-cards-present. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Tampering | Client sends `PLAY_CARD_SET` with masked interior pile card ids it should not know | mitigate | Existing server validation in `PLAY_CARD_SET` rejects with `CARD_NOT_IN_SOURCE` when any id is not in the resolved source. Select All in PileZone surfaces only the top card id (Pitfall 4) to prevent the client even attempting to use masked interior ids — defense in depth. |
| T-23-07 | Information disclosure | Pile Select All accidentally exposes interior pile order | accept | Pile Select All by design only operates on the top card (D-08 confirms face-down piles are supported; only top card is mechanically draggable). No interior order is communicated client-side. |
| T-23-08 | Repudiation | Select All button is rendered on opponent SpreadZone where `interactive={false}` | mitigate | `BoardView.tsx` does NOT pass `onSelectAll` to opponent SpreadZones, AND `SpreadZone.handleSelectAll` early-returns on `interactive === false`. Two-layer defense — visible button is also conditionally rendered (`interactive !== false && (<Button .../>)`). |
| T-23-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this plan. |

Security posture: leverages existing `PLAY_CARD_SET` validation; no new attack surface.
</threat_model>

<verification>
- `npm test` full suite green.
- `npm run typecheck` clean.
- All four source files updated with the additions described; opponent SpreadZone exclusion verified.
- `tests/selectAll.test.ts` has 3 passing assertions covering SELECT-01, SELECT-02, SELECT-03.
- Manual checks per 23-VALIDATION.md: Select All visually selects cards (ring + lift), drag fires PLAY_CARD_SET, opponent zones show no button, empty zones disable the button.
</verification>

<success_criteria>
- SELECT-01: Select All button on every pile selects the top card.
- SELECT-02: Select All button on every interactive spread zone selects all face-up cards.
- SELECT-03: A group selection from either source can be dragged via the existing multi-card drag and is accepted by `PLAY_CARD_SET`.
- D-07 honored: Select All replaces existing selection atomically.
- D-08 honored: works on face-down piles (top card).
- Opponent SpreadZone receives no Select All button and no `onSelectAll` prop.
</success_criteria>

<output>
Create `.planning/phases/23-replace-hardcoded-communal-zone-id/23-03-SUMMARY.md` when done. Record:
- Diff applied to each of the four source files
- Confirmation that opponent SpreadZone JSX has NO `onSelectAll` and that `SpreadZone.handleSelectAll` early-returns on `interactive === false`
- Conversion of all three `it.todo` in `tests/selectAll.test.ts` into passing tests
- The chosen interpretation of SELECT-01 on face-down piles (top card only) and the rationale (Pitfall 4 — interior masked card ids unavailable client-side)
- Any deviations (e.g. additional disabled tooltip text, button order tweaks)
</output>
