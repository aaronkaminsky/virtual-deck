---
phase: 19
plan: 03
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/components/HandZone.tsx
  - src/components/SpreadZone.tsx
  - src/components/PileZone.tsx
  - src/components/BoardView.tsx
autonomous: true
requirements:
  - LAYOUT-04
tags:
  - responsive-layout
  - tailwind
  - zone-containers
  - overflow

must_haves:
  truths:
    - "PileZone card slot scales: w-[56px] h-[79px] default, sm:w-[80px] sm:h-[112px]"
    - "SpreadZone card slot scales: min-w-[56px] h-[79px] default, sm:min-w-[80px] sm:h-[112px]"
    - "Both SpreadZone overlap sites (SortableSpreadCard + masked CardBack wrapper) use -ml-3 default and sm:-ml-5 at >= 640px"
    - "HandZone SortableHandCard wrapper scales w/h with -ml-3 sm:-ml-5 overlap"
    - "HandZone drop container is h-[100px] default, sm:h-[128px]"
    - "BoardView root div is overflow-x-hidden overflow-y-auto sm:overflow-hidden (h-screen w-screen retained)"
    - "After this plan + Plan 02 ship, the Plan 01 e2e responsive test (LAYOUT-04: no horizontal scroll at 375x667) passes (GREEN)"
  artifacts:
    - path: "src/components/PileZone.tsx"
      provides: "Responsive pile card slot container"
      contains: "w-[56px] h-[79px] sm:w-[80px] sm:h-[112px]"
    - path: "src/components/SpreadZone.tsx"
      provides: "Responsive spread zone container + overlap on both card render paths"
      contains: "min-w-[56px] h-[79px] sm:min-w-[80px] sm:h-[112px]"
      contains_also: "-ml-3 sm:-ml-5"
    - path: "src/components/HandZone.tsx"
      provides: "Responsive sortable card wrapper, overlap, and drop container height"
      contains: "w-[42px] h-[59px] sm:w-[63px] sm:h-[88px]"
      contains_also: "h-[100px] sm:h-[128px]"
    - path: "src/components/BoardView.tsx"
      provides: "Phone-mode vertical scroll, desktop-mode locked viewport"
      contains: "overflow-x-hidden overflow-y-auto sm:overflow-hidden"
  key_links:
    - from: "src/components/BoardView.tsx (root div)"
      to: "Tailwind sm: breakpoint"
      via: "responsive overflow utilities"
      pattern: "sm:overflow-hidden"
    - from: "src/components/SpreadZone.tsx (both overlap sites)"
      to: "Tailwind responsive negative margin"
      via: "conditional class"
      pattern: "-ml-3 sm:-ml-5"
    - from: "src/components/HandZone.tsx (drop container)"
      to: "Tailwind responsive height"
      via: "h-[100px] sm:h-[128px]"
      pattern: "h-\\[100px\\] sm:h-\\[128px\\]"
---

<objective>
Apply responsive sizing and overflow classes to the four zone-level components that wrap card primitives, plus the BoardView root. After this plan ships alongside Plan 02, the entire board renders at phone-friendly dimensions at <640px and snaps back to desktop at >=640px — closing LAYOUT-04 and turning the Plan 01 e2e gate GREEN.

Purpose: Card primitives shrinking in isolation (Plan 02) is necessary but insufficient — zone containers (PileZone slot, SpreadZone slot, HandZone strip, BoardView root) are fixed-pixel boxes whose sizes still force horizontal overflow at 375px. Per CONTEXT.md D-02 (zones scale proportionally), D-05 (root vertical scroll on phone), D-06 (desktop overflow-hidden preserved), D-07 (no column stacking — flex layout absorbs the slack).

Output: Four modified files with exactly seven class-string edits across them; typecheck green; Vitest green; Plan 01 e2e responsive test now PASSES (GREEN flip from RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-npm-audit/19-CONTEXT.md
@.planning/phases/19-npm-audit/19-RESEARCH.md
@.planning/phases/19-npm-audit/19-PATTERNS.md
@.planning/phases/19-npm-audit/19-UI-SPEC.md

@src/components/HandZone.tsx
@src/components/SpreadZone.tsx
@src/components/PileZone.tsx
@src/components/BoardView.tsx

<interfaces>
<!-- All four files retain their existing public component surfaces — only inner className strings change. -->

HandZone — public component contract (unchanged):
```tsx
export function HandZone({ cards, playerId, sendAction, draggingCardId, selectedIds, onToggleSelect }: HandZoneProps): JSX.Element
```

PileZone — public component contract (unchanged):
```tsx
export function PileZone({ pile, sendAction, draggingCardId, isShuffling }: PileZoneProps): JSX.Element
```

SpreadZone — public component contract (unchanged):
```tsx
export function SpreadZone({ pile, sendAction, draggingCardId, ownerId, viewerId }: SpreadZoneProps): JSX.Element
```

BoardView — public component contract (unchanged): see file head, no prop changes in this plan.

The shared `cn()` utility from `@/lib/utils` composes class strings — every edit below stays inside an existing `cn()` call.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PileZone slot + BoardView root overflow</name>
  <files>src/components/PileZone.tsx, src/components/BoardView.tsx</files>
  <read_first>
    - src/components/PileZone.tsx (current state — single edit at line ~49)
    - src/components/BoardView.tsx (current state — root div around line 29)
    - .planning/phases/19-npm-audit/19-RESEARCH.md (Code Examples — exact target strings for both)
    - .planning/phases/19-npm-audit/19-PATTERNS.md (lines 113-178 — exact before/after for PileZone and BoardView)
    - .planning/phases/19-npm-audit/19-UI-SPEC.md (Zone Container Sizes table)
  </read_first>
  <behavior>
    - PileZone card slot scales 80x112 -> 56x79 below sm:.
    - BoardView root scrolls vertically at <640px (overflow-x-hidden overflow-y-auto) and is locked at >=640px (sm:overflow-hidden).
    - h-screen / w-screen on the BoardView root are preserved at both breakpoints.
    - Pitfall guard (Pitfall 3 in RESEARCH.md): the `sm:overflow-hidden` shorthand is appended AFTER the axis-specific classes so the cascade order resolves correctly — the shorthand wins at >= 640px.
  </behavior>
  <action>
**Edit A — `src/components/PileZone.tsx` (one site):**

Find the card-slot container class (the inner box that wraps each card slot — currently around line 49):
```tsx
'w-[80px] h-[112px] rounded-lg border flex flex-col items-center justify-center relative bg-secondary'
```

Replace with:
```tsx
'w-[56px] h-[79px] sm:w-[80px] sm:h-[112px] rounded-lg border flex flex-col items-center justify-center relative bg-secondary'
```

Per CONTEXT.md D-02: 80 * 0.667 ≈ 53.3 → 56 (4px grid); 112 * 0.667 ≈ 74.7 → 79 (4px grid). Do NOT improvise different values.

Do NOT alter:
- Surrounding `cn()` call structure
- Any other class string in PileZone.tsx
- The `bg-secondary` token (color is fixed per UI-SPEC.md)
- Component imports or props

**Edit B — `src/components/BoardView.tsx` (one site, line ~29):**

Find the root div className:
```tsx
className="h-screen w-screen overflow-hidden flex flex-col bg-background"
```

Replace with:
```tsx
className="h-screen w-screen overflow-x-hidden overflow-y-auto sm:overflow-hidden flex flex-col bg-background"
```

Per CONTEXT.md D-05 (phone vertical scroll = safety valve) and D-06 (desktop locked).

Do NOT alter:
- Any other div in BoardView.tsx
- The h-screen / w-screen tokens (UI-SPEC explicitly preserves them at both breakpoints)
- The flex flex-col bg-background suffix
- Children of the root (header bar, opponents strip, center row, player spread row, HandZone — all stay at their current nesting; only the root's overflow strategy changes)
  </action>
  <verify>
    <automated>npm run typecheck && grep -c "w-\\[56px\\] h-\\[79px\\] sm:w-\\[80px\\] sm:h-\\[112px\\]" src/components/PileZone.tsx | grep -E "^1$" && grep -c "overflow-x-hidden overflow-y-auto sm:overflow-hidden" src/components/BoardView.tsx | grep -E "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - PileZone has exactly 1 occurrence of the responsive slot fragment: `grep -c "w-\\[56px\\] h-\\[79px\\] sm:w-\\[80px\\] sm:h-\\[112px\\]" src/components/PileZone.tsx` == 1
    - PileZone no longer has a bare `w-[80px] h-[112px]` (without `sm:` companion): `grep -E "(^|[^]])w-\\[80px\\] h-\\[112px\\]([^a-zA-Z0-9]|$)" src/components/PileZone.tsx | grep -v "sm:w-\\[80px\\]"` produces no output
    - BoardView root has the new overflow trio in one place: `grep -c "overflow-x-hidden overflow-y-auto sm:overflow-hidden" src/components/BoardView.tsx` == 1
    - BoardView no longer contains a bare `overflow-hidden flex flex-col bg-background` (the desktop-only form): `grep -c "overflow-hidden flex flex-col bg-background" src/components/BoardView.tsx` == 0
    - h-screen w-screen retained on the root: `grep -c "h-screen w-screen overflow-x-hidden" src/components/BoardView.tsx` == 1
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    PileZone slot scales proportionally; BoardView root unlocks vertical scroll on phone and stays locked on desktop. Typecheck green. Plan 01 e2e gate may still fail until Tasks 2 + 3 land — expected.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SpreadZone container + both overlap sites</name>
  <files>src/components/SpreadZone.tsx</files>
  <read_first>
    - src/components/SpreadZone.tsx (current state — three edit sites: container around line 95, SortableSpreadCard overlap around line 36, masked CardBack wrapper around line 118)
    - .planning/phases/19-npm-audit/19-RESEARCH.md (Pitfall 2 — both overlap sites must be edited)
    - .planning/phases/19-npm-audit/19-PATTERNS.md (lines 129-163 — exact before/after for all three sites)
  </read_first>
  <behavior>
    - SpreadZone container is min-w-[56px] h-[79px] at default and sm:min-w-[80px] sm:h-[112px] at >=640px.
    - Both overlap sites (SortableSpreadCard wrapper + masked CardBack wrapper div) use the conditional `-ml-3 sm:-ml-5` pattern when index/i > 0.
    - Mixed face-up + masked spreads render with consistent overlap because both sites are updated together.
  </behavior>
  <action>
Open `src/components/SpreadZone.tsx` and make exactly THREE class-string edits. All three are required — Pitfall 2 (RESEARCH.md) calls out that updating only one of the two overlap sites visually breaks any spread that mixes face-up and masked cards.

**Edit Site 1 — zone container (line ~95):**

Find:
```tsx
'min-w-[80px] h-[112px] rounded-lg border flex items-center px-2 overflow-x-auto bg-secondary'
```

Replace with:
```tsx
'min-w-[56px] h-[79px] sm:min-w-[80px] sm:h-[112px] rounded-lg border flex items-center px-2 overflow-x-auto bg-secondary'
```

**Edit Site 2 — SortableSpreadCard overlap (line ~36):**

Find:
```tsx
className={cn('flex-shrink-0', index > 0 ? '-ml-5' : '')}
```

Replace with:
```tsx
className={cn('flex-shrink-0', index > 0 ? '-ml-3 sm:-ml-5' : '')}
```

**Edit Site 3 — masked CardBack wrapper (line ~118):**

Find:
```tsx
<div className={cn('flex-shrink-0', i > 0 ? '-ml-5' : '')}>
```

Replace with:
```tsx
<div className={cn('flex-shrink-0', i > 0 ? '-ml-3 sm:-ml-5' : '')}>
```

Per CONTEXT.md D-02 + D-04 specifics: `-ml-5` (20px on 63px = 31.7%) -> `-ml-3` (12px on 42px = 28.6%). Do NOT improvise other margin values.

Do NOT alter:
- Other class strings in SpreadZone.tsx (zone label, empty state, drop highlight border)
- The `index > 0` / `i > 0` conditional logic — only the truthy branch's class string changes
- Import statements or component exports
  </action>
  <verify>
    <automated>npm run typecheck && grep -c "min-w-\\[56px\\] h-\\[79px\\] sm:min-w-\\[80px\\] sm:h-\\[112px\\]" src/components/SpreadZone.tsx | grep -E "^1$" && grep -c "'-ml-3 sm:-ml-5'" src/components/SpreadZone.tsx | grep -E "^2$"</automated>
  </verify>
  <acceptance_criteria>
    - Container fragment present once: `grep -c "min-w-\\[56px\\] h-\\[79px\\] sm:min-w-\\[80px\\] sm:h-\\[112px\\]" src/components/SpreadZone.tsx` == 1
    - Both overlap sites updated: `grep -c "'-ml-3 sm:-ml-5'" src/components/SpreadZone.tsx` == 2
    - No legacy bare `'-ml-5'` literal survives in conditional positions: `grep -c "? '-ml-5' : ''" src/components/SpreadZone.tsx` == 0
    - No bare `min-w-[80px] h-[112px]` (without `sm:` prefix) remains: `grep -E "(^|[^]])min-w-\\[80px\\] h-\\[112px\\]([^a-zA-Z0-9]|$)" src/components/SpreadZone.tsx | grep -v "sm:min-w-\\[80px\\]"` produces no output
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    SpreadZone container scales proportionally and both overlap sites switch from -ml-5 to -ml-3 below sm:. Typecheck green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HandZone wrapper + overlap + container; turn Plan 01 e2e gate GREEN</name>
  <files>src/components/HandZone.tsx</files>
  <read_first>
    - src/components/HandZone.tsx (current state — two edit sites: SortableHandCard wrapper around line 39, drop container around line 124)
    - .planning/phases/19-npm-audit/19-RESEARCH.md (Pitfall 4 — HandZone container is h-[100px] not derived mathematically)
    - .planning/phases/19-npm-audit/19-PATTERNS.md (lines 83-107 — exact before/after)
    - .planning/phases/19-npm-audit/19-UI-SPEC.md (HandZone container height row in Zone Container Sizes)
  </read_first>
  <behavior>
    - SortableHandCard wrapper applies `w-[42px] h-[59px] sm:w-[63px] sm:h-[88px]` and the conditional overlap toggles `-ml-3 sm:-ml-5` for index > 0.
    - HandZone drop container is `h-[100px]` at phone and `sm:h-[128px]` at desktop.
    - After this task ships, the Plan 01 e2e responsive test (LAYOUT-04: no horizontal scroll at 375x667) PASSES.
  </behavior>
  <action>
Open `src/components/HandZone.tsx` and make exactly TWO class-string edits.

**Edit Site 1 — SortableHandCard wrapper (line ~39):**

Find:
```tsx
className={cn('relative w-[63px] h-[88px] flex-shrink-0', index > 0 ? '-ml-5' : '')}
```

Replace with:
```tsx
className={cn('relative w-[42px] h-[59px] sm:w-[63px] sm:h-[88px] flex-shrink-0', index > 0 ? '-ml-3 sm:-ml-5' : '')}
```

**Edit Site 2 — HandZone drop container (line ~124):**

Find the className string (passed as the second arg to `cn()` on the drop container):
```tsx
'h-[128px] flex items-center px-4 overflow-x-auto bg-card'
```

Replace with:
```tsx
'h-[100px] sm:h-[128px] flex items-center px-4 overflow-x-auto bg-card'
```

Per RESEARCH.md Pitfall 4: the 100px value is the spec value from UI-SPEC.md (59px card + ~41px non-card overhead). Do NOT compute a different value mathematically — use 100 verbatim.

Do NOT alter:
- The `useDroppable`, `SortableContext`, or any dnd-kit config
- The `data-testid="hand-zone"` attribute (Plan 01's e2e spec depends on it)
- The drop highlight class (`border-primary` or any conditional ring)
- The "N selected" badge classes
- Imports or component exports

After your edits, run the Plan 01 e2e gate to prove GREEN flip.
  </action>
  <verify>
    <automated>npm run typecheck && grep -c "w-\\[42px\\] h-\\[59px\\] sm:w-\\[63px\\] sm:h-\\[88px\\]" src/components/HandZone.tsx | grep -E "^1$" && grep -c "h-\\[100px\\] sm:h-\\[128px\\]" src/components/HandZone.tsx | grep -E "^1$" && npm test && npm run test:e2e -- --grep "LAYOUT-04: no horizontal scroll"</automated>
  </verify>
  <acceptance_criteria>
    - SortableHandCard wrapper has the responsive size fragment: `grep -c "relative w-\\[42px\\] h-\\[59px\\] sm:w-\\[63px\\] sm:h-\\[88px\\] flex-shrink-0" src/components/HandZone.tsx` == 1
    - HandZone wrapper overlap conditional updated: `grep -c "'-ml-3 sm:-ml-5'" src/components/HandZone.tsx` == 1
    - Drop container responsive height present: `grep -c "h-\\[100px\\] sm:h-\\[128px\\]" src/components/HandZone.tsx` == 1
    - No legacy bare `w-[63px] h-[88px]` (without `sm:` companion) survives: `grep -E "(^|[^]])w-\\[63px\\] h-\\[88px\\]([^a-zA-Z0-9]|$)" src/components/HandZone.tsx | grep -v "sm:w-\\[63px\\]"` produces no output
    - No legacy bare `h-[128px]` (without `sm:` prefix) survives in the drop container: `grep -E "(^|[^]])h-\\[128px\\]([^a-zA-Z0-9]|$)" src/components/HandZone.tsx | grep -v "sm:h-\\[128px\\]"` produces no output
    - `data-testid="hand-zone"` retained: `grep -c "data-testid=\"hand-zone\"" src/components/HandZone.tsx` == 1
    - `npm run typecheck` exits 0
    - `npm test` (Vitest) exits 0
    - `npm run test:e2e -- --grep "LAYOUT-04: no horizontal scroll"` exits 0 (GREEN — was RED before this task)
  </acceptance_criteria>
  <done>
    HandZone is fully responsive. Combined with Tasks 1, 2 and Plan 02, the entire board renders within a 375px viewport without horizontal overflow, and the Plan 01 RED gate flips GREEN — proving LAYOUT-04 holds end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies only client-side className strings and a single root container's overflow strategy. No data flow, no auth, no validation, no input handling, no network surface change.

| Boundary | Description |
|----------|-------------|
| (none in scope) | Tailwind class strings are static literals; the only behavioral change is which CSS rules render at which viewport width |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering | Zone-container className strings | accept | Static string literals in source — modification requires source access (covered by repo controls). |
| T-19-06 | Information disclosure | BoardView vertical scroll on phone (D-05) | accept | Vertical scroll exposes existing UI further down the page; no new data is rendered. The HandZone, opponents, and spread zones already render their authorized state per the existing PartyKit hand-masking contract — scroll merely allows the user to see the same content their session is already authorized to see. |
| T-19-07 | Denial of service | Tailwind JIT additional arbitrary values | accept | New arbitrary values added: w-[56px], h-[79px], sm:w-[80px], sm:h-[112px], sm:min-w-[80px], sm:h-[112px], h-[100px], sm:h-[128px], -ml-3, sm:-ml-5, sm:overflow-hidden — all standard JIT inputs, no measurable build-time impact. |
</threat_model>

<verification>
1. `npm run typecheck` exits 0 after each task
2. `npm test` (Vitest) exits 0
3. `npm run test:e2e -- --grep "LAYOUT-04: no horizontal scroll"` exits 0 (the Plan 01 RED gate flips to GREEN — definitive proof of LAYOUT-04)
4. All grep gates in each task's acceptance_criteria pass
</verification>

<success_criteria>
- Seven class-string edits across four files: PileZone (1), BoardView (1), SpreadZone (3), HandZone (3 — wrapper-size + wrapper-overlap counted as one site, plus drop-container).
- No bare desktop-size class fragment survives in any of the four files (every occurrence is sm:-prefixed or paired with the phone default).
- Plan 01 e2e gate goes from RED to GREEN as the final task of this plan ships.
- Vitest unit suite + typecheck both green.
</success_criteria>

<output>
After completion, create `.planning/phases/19-npm-audit/19-03-SUMMARY.md` documenting:
- Each of the seven edit sites (file:line:before -> after)
- Output of the Plan 01 e2e run (the GREEN flip is the headline result)
- Confirmation of typecheck + Vitest green
</output>
