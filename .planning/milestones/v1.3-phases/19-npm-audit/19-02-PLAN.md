---
phase: 19
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/components/CardFace.tsx
  - src/components/CardBack.tsx
autonomous: true
requirements:
  - LAYOUT-04
tags:
  - responsive-layout
  - tailwind
  - card-primitives

must_haves:
  truths:
    - "Every CardFace render path applies w-[42px] h-[59px] at default and sm:w-[63px] sm:h-[88px] at >= 640px"
    - "Every CardBack render path applies w-[42px] h-[59px] at default and sm:w-[63px] sm:h-[88px] at >= 640px"
    - "OpponentHand sizing is unchanged (already at the phone target size — D-03)"
    - "No legacy bare 'w-[63px]' or 'h-[88px]' class fragment remains in CardFace.tsx or CardBack.tsx (every occurrence is now prefixed by sm: or paired with the phone-size default)"
  artifacts:
    - path: "src/components/CardFace.tsx"
      provides: "Responsive card front sizing (image + fallback paths)"
      contains: "w-[42px] h-[59px] sm:w-[63px] sm:h-[88px]"
    - path: "src/components/CardBack.tsx"
      provides: "Responsive card back sizing (image + fallback paths)"
      contains: "w-[42px] h-[59px] sm:w-[63px] sm:h-[88px]"
  key_links:
    - from: "src/components/CardFace.tsx"
      to: "Tailwind sm: breakpoint (640px)"
      via: "responsive class prefix"
      pattern: "sm:w-\\[63px\\] sm:h-\\[88px\\]"
    - from: "src/components/CardBack.tsx"
      to: "Tailwind sm: breakpoint (640px)"
      via: "responsive class prefix"
      pattern: "sm:w-\\[63px\\] sm:h-\\[88px\\]"
---

<objective>
Apply mobile-first responsive size classes to both CardFace and CardBack so card primitives render at 42x59px on phone (default) and 63x88px on >= 640px (sm:). Both files have two render paths each (image + fallback div), and ALL FOUR sites must be updated — missing any one leaves part of the production render flow at the old desktop size.

Purpose: Per CONTEXT.md D-02 / D-03 / D-04, card primitives are the foundation of every zone's visible layout. Until both render paths in both files are updated, zones that cascade their size from card width (HandZone, SpreadZone, PileZone) will visually disagree with their containers.

Output: Two modified files with responsive classes in all four sites; typecheck green; the Wave 1 RED test from Plan 01 may still fail (zones not yet shrunk — Plan 03 closes that), but card primitives in isolation are at the correct phone size.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-npm-audit/19-CONTEXT.md
@.planning/phases/19-npm-audit/19-RESEARCH.md
@.planning/phases/19-npm-audit/19-PATTERNS.md
@.planning/phases/19-npm-audit/19-UI-SPEC.md

@src/components/CardFace.tsx
@src/components/CardBack.tsx
@src/components/OpponentHand.tsx

<interfaces>
<!-- Card primitives are leaf components called by HandZone, SpreadZone, OpponentHand. -->
<!-- Their props (`card`, `className`) do not change — only the className strings rendered inside. -->

CardFace public surface (unchanged by this plan):
```tsx
export function CardFace({ card, className }: { card: Card; className?: string }): JSX.Element
```

CardBack public surface (unchanged by this plan):
```tsx
export function CardBack({ className }: { className?: string }): JSX.Element
```

The `className` prop is composed via `cn()` from `@/lib/utils` — callers can still pass extra classes that compose on top of the responsive base. No caller updates required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply responsive size classes to CardFace.tsx (both render paths)</name>
  <files>src/components/CardFace.tsx</files>
  <read_first>
    - src/components/CardFace.tsx (current state — both render paths must be edited)
    - src/components/CardBack.tsx (mirror structure — confirms the two-path pattern)
    - src/components/OpponentHand.tsx (already uses w-[42px] h-[59px] — your edits MUST match this exact pixel pair so opponents and the player's own cards visually agree on phone)
    - .planning/phases/19-npm-audit/19-RESEARCH.md (Code Examples section — exact target strings)
    - .planning/phases/19-npm-audit/19-PATTERNS.md (lines 23-54 — exact before/after pairs)
  </read_first>
  <behavior>
    - After this edit, both render paths in CardFace.tsx contain the literal class fragment `w-[42px] h-[59px] sm:w-[63px] sm:h-[88px]`.
    - The bare `w-[63px] h-[88px]` form (without the `sm:` companion) does not appear anywhere in CardFace.tsx.
    - The `cn()` call shape, the surrounding JSX, and the `className` prop composition are otherwise unchanged.
  </behavior>
  <action>
Open `src/components/CardFace.tsx` and make exactly two class-string replacements.

**Edit Site 1 — image render path:**

Find:
```tsx
className={cn('w-[63px] h-[88px] rounded-md select-none object-cover', className)}
```

Replace with:
```tsx
className={cn('w-[42px] h-[59px] sm:w-[63px] sm:h-[88px] rounded-md select-none object-cover', className)}
```

**Edit Site 2 — fallback div render path:**

Find:
```tsx
className={cn(
  'w-[63px] h-[88px] relative bg-white rounded-md border border-gray-300 select-none',
  className
)}
```

Replace with:
```tsx
className={cn(
  'w-[42px] h-[59px] sm:w-[63px] sm:h-[88px] relative bg-white rounded-md border border-gray-300 select-none',
  className
)}
```

Do NOT change:
- The inner `<span>` widths (any rank/suit text widths inside the fallback div remain at their existing values)
- The `cn()` import or any other class strings
- The order of arguments to `cn()`
- Component props or exports

Per CONTEXT.md D-02 / D-04 — exact pixel values 42/59/63/88 are mandated; do not improvise.
Per CONTEXT.md D-03 — these match the existing `OpponentHand.tsx` `w-[42px] h-[59px]` deliberately.
  </action>
  <verify>
    <automated>npm run typecheck && grep -c "w-\\[42px\\] h-\\[59px\\] sm:w-\\[63px\\] sm:h-\\[88px\\]" src/components/CardFace.tsx | grep -E "^2$"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 2 occurrences of the responsive size fragment in CardFace.tsx: `grep -c "w-\\[42px\\] h-\\[59px\\] sm:w-\\[63px\\] sm:h-\\[88px\\]" src/components/CardFace.tsx` == 2
    - Zero occurrences of the bare desktop-only fragment (without phone companion): `grep -E "(^|[^]])w-\\[63px\\] h-\\[88px\\]([^a-zA-Z0-9]|$)" src/components/CardFace.tsx | grep -v "sm:w-\\[63px\\]"` produces no output
    - `cn(` import preserved: `grep -c "from '@/lib/utils'" src/components/CardFace.tsx` >= 1
    - Both render paths present (image + fallback): `grep -c "object-cover" src/components/CardFace.tsx` >= 1 AND `grep -c "bg-white rounded-md border border-gray-300" src/components/CardFace.tsx` >= 1
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    Both render paths in CardFace.tsx use the responsive size string. No bare `w-[63px] h-[88px]` remains. Typecheck is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply responsive size classes to CardBack.tsx (both render paths) and run e2e gate</name>
  <files>src/components/CardBack.tsx</files>
  <read_first>
    - src/components/CardBack.tsx (current state — both render paths must be edited)
    - src/components/CardFace.tsx (mirror structure — already updated in Task 1; confirm pattern symmetry)
    - .planning/phases/19-npm-audit/19-RESEARCH.md (Code Examples section)
    - .planning/phases/19-npm-audit/19-PATTERNS.md (lines 58-79 — exact before/after pairs)
  </read_first>
  <behavior>
    - After this edit, both render paths in CardBack.tsx contain the literal class fragment `w-[42px] h-[59px] sm:w-[63px] sm:h-[88px]`.
    - The bare `w-[63px] h-[88px]` form does not appear anywhere in CardBack.tsx.
    - With both Task 1 (CardFace) and this task (CardBack) shipped, the Plan 01 e2e responsive test may still be RED until Plan 03 ships zone-container shrinks. That is expected.
  </behavior>
  <action>
Open `src/components/CardBack.tsx` and make exactly two class-string replacements.

**Edit Site 1 — image render path:**

Find:
```tsx
className={cn('w-[63px] h-[88px] rounded-md select-none object-cover', className)}
```

Replace with:
```tsx
className={cn('w-[42px] h-[59px] sm:w-[63px] sm:h-[88px] rounded-md select-none object-cover', className)}
```

**Edit Site 2 — fallback div render path:**

Find:
```tsx
className={cn('w-[63px] h-[88px] rounded-md border border-gray-600 select-none', className)}
```

Replace with:
```tsx
className={cn('w-[42px] h-[59px] sm:w-[63px] sm:h-[88px] rounded-md border border-gray-600 select-none', className)}
```

Do NOT change:
- Any other class strings in the file
- Component props, exports, or the `cn()` import
- The conditional / ternary that selects between image and fallback paths

Per CONTEXT.md D-02 — exact pixel pair `42/59` (phone) and `63/88` (sm:) are mandated.
  </action>
  <verify>
    <automated>npm run typecheck && grep -c "w-\\[42px\\] h-\\[59px\\] sm:w-\\[63px\\] sm:h-\\[88px\\]" src/components/CardBack.tsx | grep -E "^2$"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 2 occurrences of the responsive size fragment in CardBack.tsx: `grep -c "w-\\[42px\\] h-\\[59px\\] sm:w-\\[63px\\] sm:h-\\[88px\\]" src/components/CardBack.tsx` == 2
    - Zero occurrences of the bare desktop-only fragment: `grep -E "(^|[^]])w-\\[63px\\] h-\\[88px\\]([^a-zA-Z0-9]|$)" src/components/CardBack.tsx | grep -v "sm:w-\\[63px\\]"` produces no output
    - Both render paths present (image + fallback border): `grep -c "object-cover" src/components/CardBack.tsx` >= 1 AND `grep -c "border-gray-600" src/components/CardBack.tsx` >= 1
    - `cn(` import preserved: `grep -c "from '@/lib/utils'" src/components/CardBack.tsx` >= 1
    - `npm run typecheck` exits 0
    - `npm test` (Vitest) exits 0 — proves no unit-test regression from the class change
  </acceptance_criteria>
  <done>
    Both render paths in CardBack.tsx use the responsive size string. Combined with Task 1, every card primitive in the app now scales 42->63 across the sm: breakpoint. Typecheck and Vitest are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies only client-side CSS class strings inside two leaf React components. No data flow changes, no auth surface, no validation logic, no external input handling.

| Boundary | Description |
|----------|-------------|
| (none in scope) | CSS class strings are static literals; no untrusted input crosses any boundary as a result of these edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | CardFace.tsx / CardBack.tsx | accept | Class strings are static literals embedded in source; tampering would require source modification (covered by repo access controls). |
| T-19-04 | Denial of service | Tailwind JIT compilation | accept | Adding 4 new arbitrary-value classes (w-[42px], h-[59px], sm:w-[63px], sm:h-[88px]) to JIT-compiled CSS is well within Tailwind's design envelope; no measurable build-time impact. |
</threat_model>

<verification>
1. `npm run typecheck` exits 0
2. `npm test` exits 0 (Vitest unit suite)
3. `grep -c "w-\\[42px\\] h-\\[59px\\] sm:w-\\[63px\\] sm:h-\\[88px\\]" src/components/CardFace.tsx` returns 2
4. `grep -c "w-\\[42px\\] h-\\[59px\\] sm:w-\\[63px\\] sm:h-\\[88px\\]" src/components/CardBack.tsx` returns 2
5. The Plan 01 e2e gate may still fail until Plan 03 ships — that is expected; this plan covers card primitives only.
</verification>

<success_criteria>
- All 4 class-edit sites (2 in CardFace, 2 in CardBack) use the responsive 42->63 / 59->88 pair
- No bare `w-[63px] h-[88px]` survives in either file
- Typecheck and unit tests both green
- OpponentHand.tsx is NOT modified (per CONTEXT.md D-03 — it already uses the phone target size)
</success_criteria>

<output>
After completion, create `.planning/phases/19-npm-audit/19-02-SUMMARY.md` documenting:
- The 4 class-string edits made (file:line for each)
- Confirmation of typecheck + Vitest green
- Status of the Plan 01 e2e gate (likely still RED — note that Plan 03 closes it)
</output>
