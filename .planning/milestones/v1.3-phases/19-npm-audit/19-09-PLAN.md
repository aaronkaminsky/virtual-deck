---
phase: 19
plan: "09"
type: execute
wave: 7
gap_closure: true
depends_on:
  - 19-07
files_modified:
  - src/components/OpponentHand.tsx
autonomous: true
requirements:
  - LAYOUT-04
tags:
  - responsive-layout
  - gap-closure
  - opponent-hand

must_haves:
  truths:
    - "The card count is displayed inline next to the player's display name (above the card back stack), not as an absolutely-positioned badge over the card stack"
    - "The old absolute-positioned Badge over the card stack is removed"
    - "The count is always visible at 375px regardless of column width — it is in the name row which has full column width"
    - "No other file is modified"
  artifacts:
    - path: "src/components/OpponentHand.tsx"
      provides: "Inline count display next to player name"
  key_links:
    - from: "Player name row"
      to: "Card count"
      via: "Inline text or small badge in the name row"
      pattern: "displayName ({cardCount})"
---

<objective>
Close Gap 9: the opponent card count badge is absolutely positioned over the card back stack and gets clipped at 375px viewport when the per-opponent column is narrow.

Root cause: the Badge component uses `absolute bottom-0 left-0` (or similar) positioning relative to the card stack container. At 375px with max-w-[200px] columns (or flex-1 split columns after Gap 10 fix), the badge overflows or is hidden by overflow-x-hidden on the column wrapper.

Fix: remove the absolute-positioned badge. Display the card count inline in the player name row, immediately after the display name. Format: `{displayName} ({cardCount})` as plain text, or use a small inline Badge variant. This count is always visible because the name row spans the full column width and is not subject to card-stack overflow clipping.

Only `src/components/OpponentHand.tsx` is modified.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Move card count from absolute badge to inline name row in OpponentHand</name>
  <files>src/components/OpponentHand.tsx</files>
  <read_first>
    - src/components/OpponentHand.tsx (FULL FILE — understand current badge positioning and name row structure)
  </read_first>
  <behavior>
    - The card count is rendered inline after the display name in the name/header row. Format the name row as: display name text followed by a count indicator. Use a small inline Badge (shadcn Badge component with `variant="secondary"` or plain text in a `text-xs text-muted-foreground` span) — whichever is already imported and available in the file.
    - The absolute-positioned Badge that overlays the card back stack is removed entirely.
    - The `MAX_VISIBLE_OPPONENT_CARDS` constant and the `Math.min(cardCount, MAX_VISIBLE_OPPONENT_CARDS)` render loop are preserved — only the badge positioning changes.
    - The count Badge `{cardCount}` still shows the total cardCount (not the capped display count) — same value as before, different position.
    - No new imports needed if Badge is already imported; if not, use a plain `<span>` with Tailwind classes.
    - No other file is modified.
  </behavior>
  <action>
Read the full OpponentHand.tsx file first to understand:
1. Where the Badge is currently rendered (absolute positioned over the card stack)
2. Where the player name / displayName is rendered
3. What imports are at the top (check if Badge is already imported)

Then:
1. Remove the absolute-positioned `<Badge>{cardCount}</Badge>` (or equivalent) from the card stack container
2. In the name/header row where `displayName` is shown, add the count inline. If Badge is imported, add `<Badge variant="secondary" className="ml-1 text-xs">{cardCount}</Badge>` after the name. If Badge is not imported, add `<span className="ml-1 text-xs text-muted-foreground">({cardCount})</span>` after the name text.
3. Preserve all other structure: the presence dot, the displayName text, the card back stack loop, the connected prop usage.
  </action>
  <verify>
    <automated>npm run typecheck && npm test -- --run 2>&1 | tail -5 && grep -c "absolute" src/components/OpponentHand.tsx || echo "0 absolute — good"</automated>
  </verify>
  <acceptance_criteria>
    - The absolute-positioned badge over the card stack is gone: `grep -c "absolute" src/components/OpponentHand.tsx` should be 0 (or the only `absolute` reference is unrelated to the badge)
    - The card count appears in the name row: `grep -c "cardCount" src/components/OpponentHand.tsx` >= 1 (still used, now in name row)
    - `MAX_VISIBLE_OPPONENT_CARDS` constant still present: `grep -c "MAX_VISIBLE_OPPONENT_CARDS" src/components/OpponentHand.tsx` >= 1
    - `npm run typecheck` exits 0
    - `npm test -- --run` exits 0
    - No other file modified: `git diff --name-only src/ | grep -v "^src/components/OpponentHand.tsx$" | wc -l | tr -d ' '` == 0
  </acceptance_criteria>
  <done>
    Card count is inline next to the player name. Always visible at any column width. Absolute badge over card stack is removed.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` exits 0
2. `npm test -- --run` exits 0
3. Absolute badge over card stack removed
4. Count visible inline in name row
5. Only OpponentHand.tsx modified
</verification>

<success_criteria>
- Card count is always visible at 375px because it is in the name row, not overlaid on the card stack
- No absolute-positioned badge over the card back stack
- Only `src/components/OpponentHand.tsx` modified
- Typecheck and Vitest pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-npm-audit/19-09-SUMMARY.md`.
</output>
