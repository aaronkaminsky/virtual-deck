---
phase: 17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SpreadZone.tsx
  - playwright/game.spec.ts
autonomous: true
requirements:
  - SPREAD-04
tags:
  - dnd-kit
  - sortable
  - id-collision
  - spread-zone
must_haves:
  truths:
    - "Each card in a spread zone registers exactly one dnd-kit ID (via useSortable, not useDraggable)"
    - "Dragging a card from a spread zone to the player's hand zone produces no console warnings about duplicate IDs"
    - "Dragging a card from a spread zone to the player's hand zone moves the card without ghost events or drag misfires"
    - "Drag-reorder of cards within a spread zone still works (cards visibly change order) — useSortable data shape preserved"
    - "Face up vs face down rendering inside a spread zone still works — face-up cards show CardFace, face-down show CardBack"
  artifacts:
    - path: "src/components/SpreadZone.tsx"
      provides: "SortableSpreadCard renders CardFace/CardBack directly with useSortable; no nested DraggableCard"
      contains: "useSortable"
      must_not_contain: "DraggableCard"
    - path: "playwright/game.spec.ts"
      provides: "e2e test asserting drag from spread zone to hand zone succeeds (SPREAD-04 regression coverage)"
      contains: "spread zone drag: drag card from communal spread zone to hand"
  key_links:
    - from: "src/components/SpreadZone.tsx — SortableSpreadCard"
      to: "@dnd-kit/sortable useSortable hook"
      via: "single registration of card.id"
      pattern: "useSortable\\(\\{[^}]*id: card\\.id"
    - from: "src/components/SpreadZone.tsx — useSortable data"
      to: "src/components/BoardDragLayer.tsx — dragDataRef"
      via: "active.data.current with shape { card, fromZone: 'pile', fromId: pileId, toZone: 'pile', toId: pileId }"
      pattern: "fromZone: 'pile' as const, fromId: pileId, toZone: 'pile' as const, toId: pileId"
    - from: "src/components/SpreadZone.tsx — useSortable data"
      to: "src/components/SpreadZone.tsx — useDndMonitor.onDragEnd"
      via: "intra-spread reorder check reads activeData.fromZone === 'pile' && activeData.fromId === pile.id"
      pattern: "activeData\\?\\.fromZone === 'pile'"
---

<objective>
Resolve the dnd-kit ID collision in `SortableSpreadCard` by removing the nested `DraggableCard`
and rendering `CardFace`/`CardBack` directly — mirroring the verified `SortableHandCard` pattern.
This is the prerequisite for Phase 20 multi-select (a second `useDraggable` registration on the
same `card.id` produces duplicate IDs in the dnd-kit DndContext and causes drag misfires once
selection state interacts with the listeners).

Purpose: One dnd-kit ID per card. Drag stability for selection-aware interactions in Phase 20.
Output: Fixed `SortableSpreadCard` + new e2e test case proving spread→hand drag works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-board-layout-restructure/17-CONTEXT.md
@.planning/phases/17-board-layout-restructure/17-RESEARCH.md
@.planning/phases/17-board-layout-restructure/17-PATTERNS.md
@.planning/phases/17-board-layout-restructure/17-UI-SPEC.md

@src/components/SpreadZone.tsx
@src/components/HandZone.tsx
@src/components/BoardDragLayer.tsx
@playwright/game.spec.ts
@playwright/fixtures.ts

<interfaces>
<!-- Verified contracts the executor must preserve. Extracted from current code 2026-05-02. -->

From src/components/SpreadZone.tsx (current — to fix):
```typescript
// Line 17–41 — SortableSpreadCard
function SortableSpreadCard({ card, pileId, index, draggingCardId }: SortableSpreadCardProps) {
  const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({
    id: card.id,
    data: { card, fromZone: 'pile' as const, fromId: pileId, toZone: 'pile' as const, toId: pileId },
  });

  const style: React.CSSProperties = {
    transform: isDragging ? undefined : CSS.Transform.toString(transform),
    transition,
    touchAction: 'none',
    opacity: draggingCardId === card.id ? 0 : 1,
  };

  return (
    <div
      ref={setNodeRef}
      style={style}
      {...listeners}
      {...attributes}
      className={cn('flex-shrink-0', index > 0 ? '-ml-5' : '')}
    >
      <DraggableCard card={card} fromZone="pile" fromId={pileId} />  {/* ← THE BUG: registers useDraggable on same card.id */}
    </div>
  );
}
```

From src/components/HandZone.tsx (line 18–60 — reference pattern to mirror):
```typescript
function SortableHandCard({ card, playerId, isDraggingThis, index, isSelected, onToggleSelect }: SortableHandCardProps) {
  const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({
    id: card.id,
    data: { card, fromZone: 'hand' as const, fromId: playerId, toZone: 'hand' as const, toId: playerId },
  });
  // ...
  return (
    <div ... >
      {/* ... */}
      <div ref={setNodeRef} style={style} {...listeners} {...attributes} ...>
        {card.faceUp ? <CardFace card={card} /> : <CardBack />}
      </div>
    </div>
  );
}
```

Drag data consumers (must NOT change shape — verified 2026-05-02):
- src/components/BoardDragLayer.tsx line 111: reads `event.active.data.current as { card?, fromZone?, fromId? }`
- src/components/BoardDragLayer.tsx line 191: `const isIntraSpreadReorder = fromZone === 'pile' && fromId === toId;`
- src/components/SpreadZone.tsx line 65: `const fromThisPile = activeData?.fromZone === 'pile' && activeData?.fromId === pile.id;`

Imports to remove from src/components/SpreadZone.tsx line 6:
```typescript
import { DraggableCard } from './DraggableCard';   // REMOVE
```

Imports to add to src/components/SpreadZone.tsx (CardBack already imported on line 7):
```typescript
import { CardFace } from './CardFace';             // ADD
```

Note on didDragRef: 17-UI-SPEC.md "Click vs. Drag Disambiguation" describes the didDragRef pattern as Phase 20 pre-wiring. RESEARCH.md Anti-Patterns is explicit that the click handler body must stay empty in Phase 17. We therefore do NOT add didDragRef in this plan — it has no consumer in Phase 17, and adding an unused ref violates the project rule "only make changes that are directly requested or clearly necessary." Phase 20 will add didDragRef + click handler together. (Discretion choice; documented here.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Add e2e regression test for spread-zone drag-out (Wave 0 gap)</name>
  <files>playwright/game.spec.ts</files>
  <read_first>
    - playwright/game.spec.ts (whole file — preserve existing test style and helpers)
    - playwright/fixtures.ts (twoPlayerRoom fixture and any helpers it exposes)
    - src/components/SpreadZone.tsx (confirm data-testid="spread-zone-${pile.id}" — line 92)
    - src/components/HandZone.tsx (confirm data-testid="hand-zone" — line 122)
    - .planning/phases/17-board-layout-restructure/17-VALIDATION.md (Wave 0 requirement: spread zone drag test)
  </read_first>
  <behavior>
    - Test 1 (NEW): "spread zone drag: drag card from communal spread zone to hand"
      - Setup: Use twoPlayerRoom fixture. Deal cards. Have P1 select 2 hand cards and play them as a set into the communal spread zone (reuse the pattern from the existing "multi-card set play" test at line 166).
      - Action: Drag the first card from the communal spread zone (locator: `p1.getByTestId('spread-zone-play').locator('img, [class*="rounded"]').first()` or equivalent — match how the existing "multi-card set play" test locates dragged cards) back to P1's hand zone (`p1.getByTestId('hand-zone')`).
      - Assertion: P1's hand zone gains one card (count check), and the communal spread zone loses one card. P2 sees the same state change (broadcast verified). No console warnings about duplicate dnd-kit IDs (use `page.on('console', ...)` to capture; assert no message matches `/duplicate.*id/i` or `/multiple elements with the same id/i`).
    - Test 2 (NEW): "spread zone reorder: drag-reorder within communal spread zone preserves useSortable data routing"
      - Setup: Same as above but play 3 cards into communal zone.
      - Action: Drag card at index 0 over card at index 2 inside `spread-zone-play`.
      - Assertion: Card order changes for both P1 and P2 (verify via card image alt text or rank attribute order — match how existing tests assert card order). No console warnings about duplicate IDs.
    - These tests MUST FAIL before the SortableSpreadCard fix in Task 1.2 if they exercise the duplicate-ID path; if they pass before the fix because Playwright doesn't surface the warning at the level we capture it, that is acceptable — the assertion's purpose is regression protection going forward.
  </behavior>
  <action>
    Open `playwright/game.spec.ts`. After the existing `'multi-card set play: select 2 cards, drag to communal zone, both players see them'` test (which ends near line 207-ish based on grep), add two new test cases inside the same `test.describe('virtual-deck e2e', () => { ... })` block:

    1. `test('spread zone drag: drag card from communal spread zone to hand', async ({ twoPlayerRoom }) => { ... })`
       - Reuse the multi-card-set-play setup verbatim to get 2 cards into the communal zone (P1 deals, selects 2 cards via aria-pressed click, drags first selected card to `spread-zone-play`).
       - After the cards are in the communal zone, perform a Playwright drag from the first card inside `p1.getByTestId('spread-zone-play')` to `p1.getByTestId('hand-zone')`. Use the same `dragTo()` API used elsewhere in the file (mirror the multi-card-set-play test's drag pattern).
       - Assert P1's hand card count increased by 1 from the post-play state; assert `spread-zone-play` card count decreased by 1; assert P2 (`p2.getByTestId('spread-zone-play')`) sees the same decreased count.
       - Console capture: register `p1.on('console', msg => { ... })` BEFORE the drag, push messages into an array; after the drag, assert no message text matches `/duplicate id|multiple elements with the same id/i`.

    2. `test('spread zone reorder: drag-reorder within communal spread zone preserves useSortable data routing', async ({ twoPlayerRoom }) => { ... })`
       - Reuse the multi-card-set-play setup but play 3 cards (select 3, drag selected first card to `spread-zone-play`).
       - Capture the order of cards in `spread-zone-play` (extract a stable identifier per card — e.g. `data-rank` or alt-text — match how existing tests read card identity).
       - Drag the first card to a position over the third card within `spread-zone-play`.
       - Assert the order changed for P1 AND for P2.

    If the existing "multi-card set play" test does NOT include a 3-card variant or a re-grab pattern, mirror its locator strategy and adapt — DO NOT invent new test helpers; reuse `twoPlayerRoom`, `getByTestId`, and the existing card locator approach.

    Style rules:
    - Match the existing file's import style (`import { test, expect } from './fixtures'` — line 2).
    - Match the existing `await expect(...).toHaveCount(...)` assertion style.
    - Do NOT add new fixtures, do NOT add new top-level helper functions unless an existing identical helper is missing — if a helper would be needed, inline the logic in the test.

    Per SPREAD-04 the test exists primarily as regression coverage; if it currently passes against the broken code (because the duplicate-ID path may not always surface visibly in this specific drag scenario), it still must remain green AFTER Task 1.2 — the test's job is to lock in correct behavior going forward.
  </action>
  <verify>
    <automated>npx playwright test playwright/game.spec.ts -g "spread zone drag|spread zone reorder" --reporter=list</automated>
    <!-- Note: this test may pass or fail before Task 1.2; either way it must pass AFTER Task 1.2. -->
  </verify>
  <acceptance_criteria>
    - `grep -c "spread zone drag: drag card from communal spread zone to hand" playwright/game.spec.ts` returns >= 1
    - `grep -c "spread zone reorder: drag-reorder within communal spread zone" playwright/game.spec.ts` returns >= 1
    - `grep -E "duplicate id|multiple elements with the same id" playwright/game.spec.ts` returns at least one match (the console-warning negative assertion)
    - `grep -c "spread-zone-play" playwright/game.spec.ts` returns >= 4 (2 existing references + at least 2 new ones)
    - `npx playwright test playwright/game.spec.ts --list 2>&1 | grep -c "spread zone"` returns >= 3 (existing "spread zone visibility" + 2 new tests)
    - `npm run typecheck` exits 0 (no new TS errors introduced by the additions)
  </acceptance_criteria>
  <done>
    Two new test cases exist in `playwright/game.spec.ts`, the test file still typechecks, and `npx playwright test playwright/game.spec.ts --list` shows the new test names. Whether the new tests pass before Task 1.2 is acceptable — they MUST pass after Task 1.2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Remove DraggableCard from SortableSpreadCard; render CardFace/CardBack directly</name>
  <files>src/components/SpreadZone.tsx</files>
  <read_first>
    - src/components/SpreadZone.tsx (current state — lines 1–41 are the change scope; lines 42–137 must NOT change)
    - src/components/HandZone.tsx (lines 1–60 — reference pattern; SortableHandCard is the model for the fix)
    - src/components/CardFace.tsx (confirm export name `CardFace` and props shape `{ card }`)
    - src/components/CardBack.tsx (confirm export name `CardBack` and zero-prop usage — already imported in SpreadZone.tsx line 7)
    - src/components/BoardDragLayer.tsx lines 105–215 (verify `active.data.current` shape and `isIntraSpreadReorder` check at line 191 — DO NOT modify)
    - .planning/phases/17-board-layout-restructure/17-CONTEXT.md (D-07, D-08, D-09 — locked decisions)
    - .planning/phases/17-board-layout-restructure/17-RESEARCH.md (Pattern 1, Pitfalls 1 and 4 — atomic edit warning)
  </read_first>
  <action>
    Per locked decisions D-07, D-08, D-09, edit `src/components/SpreadZone.tsx` to eliminate the dnd-kit ID collision. The fix is atomic — do all three edits in a single Edit call (per RESEARCH.md Pitfall 1: "Do not test with DraggableCard still present"):

    EDIT 1 — imports (lines 1–8 currently):
    - REMOVE line 6: `import { DraggableCard } from './DraggableCard';`
    - ADD a new import alongside the existing `CardBack` import (line 7): `import { CardFace } from './CardFace';`
    - Keep all other imports unchanged. Final import block must contain (in any order matching project style): `useDroppable, useDndMonitor` from `'@dnd-kit/core'`; `SortableContext, useSortable, horizontalListSortingStrategy, arrayMove` from `'@dnd-kit/sortable'`; `CSS` from `'@dnd-kit/utilities'`; `Card, ClientPile, ClientAction` (type) from `'@/shared/types'`; `Button` from `'@/components/ui/button'`; `CardFace` from `'./CardFace'`; `CardBack` from `'./CardBack'`; `cn` from `'@/lib/utils'`. NO `DraggableCard` import.

    EDIT 2 — SortableSpreadCard render body (line 38 currently):
    - REPLACE line 38: `<DraggableCard card={card} fromZone="pile" fromId={pileId} />`
    - WITH: `{card.faceUp ? <CardFace card={card} /> : <CardBack />}`
    - This mirrors HandZone.tsx line 56 exactly.

    EDIT 3 — preserve everything else verbatim:
    - The `useSortable` call (lines 18–21) — DO NOT change. The data shape `{ card, fromZone: 'pile' as const, fromId: pileId, toZone: 'pile' as const, toId: pileId }` is read by BoardDragLayer.tsx line 111 and SpreadZone.tsx line 65 (the useDndMonitor reorder check). Changing it breaks intra-spread reorder.
    - The `style` object (lines 23–28) — DO NOT change.
    - The wrapping `<div ref={setNodeRef} style={style} {...listeners} {...attributes} className={cn(...)}>` (lines 31–36) — DO NOT change. Listeners/attributes stay on this outer div (matching the SortableHandCard inner ref div pattern).
    - The `SpreadZone` export function (lines 43–135) — DO NOT change. No edits to `useDroppable`, `useDndMonitor`, `handleToggleFace`, or any JSX below line 41.

    EXPLICIT NON-CHANGES (per CONTEXT.md and RESEARCH.md Anti-Patterns):
    - DO NOT add `didDragRef` — Phase 17 has no click handler to disambiguate. Phase 20 will add ref + click handler together.
    - DO NOT add `onClick`, `onPointerDown`, `onToggleSelect`, `selectedIds`, `aria-pressed`, or `ring-primary` styling to `SortableSpreadCard`. Selection in spread zones is Phase 20.
    - DO NOT modify `DraggableCard.tsx` — it is still used by `PileZone.tsx` (per RESEARCH.md "Component Responsibilities" table).
    - DO NOT modify `BoardDragLayer.tsx` — D-08 confirmed it already handles `fromZone: 'pile'` from sortable data.
    - DO NOT change `SpreadZone`'s inner spread container CSS (`min-w-[80px] h-[112px]` on line 94). Communal zone width is Plan 02's concern; Plan 01 must NOT touch the SpreadZone container CSS or this plan and Plan 02 will conflict.

    After all edits, the diff for SpreadZone.tsx should be:
    - Net 0 lines: line 6 removed, one new import line added.
    - One line changed: line 38 swapped from `<DraggableCard ... />` to `{card.faceUp ? <CardFace card={card} /> : <CardBack />}`.

    Per project skill rule (CLAUDE.md): "No comments/docstrings on code you didn't change" — do not add explanatory comments around the edited lines.
  </action>
  <verify>
    <automated>npm run typecheck && npm test && npx playwright test playwright/game.spec.ts -g "spread zone drag|spread zone reorder|spread zone visibility|multi-card set play" --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DraggableCard" src/components/SpreadZone.tsx` returns 0 (import removed AND usage removed)
    - `grep -c "import { CardFace } from './CardFace'" src/components/SpreadZone.tsx` returns 1
    - `grep -c "import { CardBack } from './CardBack'" src/components/SpreadZone.tsx` returns 1 (already present, must remain)
    - `grep -c "card.faceUp ? <CardFace card={card} /> : <CardBack />" src/components/SpreadZone.tsx` returns 1
    - `grep -c "useSortable({" src/components/SpreadZone.tsx` returns 1 (still exactly one useSortable registration)
    - `grep "fromZone: 'pile' as const, fromId: pileId, toZone: 'pile' as const, toId: pileId" src/components/SpreadZone.tsx` returns exactly 1 match (data shape unchanged — verifies D-08 preservation)
    - `grep -c "didDragRef\|onToggleSelect\|isSelected\|aria-pressed" src/components/SpreadZone.tsx` returns 0 (no Phase 20 wiring leaked in)
    - `grep -c "min-w-\[80px\] h-\[112px\]" src/components/SpreadZone.tsx` returns 1 (inner spread container CSS untouched)
    - `npm run typecheck` exits 0
    - `npm test` exits 0 (existing unit suite green; spreadZoneCreation, broadcastMasking, viewFor, moveCard tests must remain passing)
    - `npx playwright test playwright/game.spec.ts -g "spread zone"` exits 0 (existing visibility test + 2 new tests from Task 1.1 all pass)
    - `npx playwright test playwright/game.spec.ts -g "multi-card set play"` exits 0 (existing select-then-drag flow not regressed)
    - Diff to `src/components/DraggableCard.tsx`: zero changes (`git diff --stat src/components/DraggableCard.tsx` shows no entry)
    - Diff to `src/components/BoardDragLayer.tsx`: zero changes
    - Diff to `src/components/HandZone.tsx`: zero changes
  </acceptance_criteria>
  <done>
    `SortableSpreadCard` registers exactly one dnd-kit ID per card via `useSortable` alone. The full e2e suite passes including the two new SPREAD-04 regression tests. No nested `DraggableCard` remains anywhere in `SpreadZone.tsx`. Drag-out (spread → hand) and drag-reorder (within spread) both work for both players in real-time.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PartyKit server | Drag-end events become `MOVE_CARD` / `REORDER_PILE_SPREAD` `ClientAction` messages over WebSocket; server is authoritative |

Note: Phase 17 introduces no new trust boundary. The above already exists; Phase 17 only changes which React component fires the action message.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `SortableSpreadCard` useSortable data shape | mitigate | The `data` object on `useSortable` (`{ card, fromZone: 'pile', fromId: pileId, toZone: 'pile', toId: pileId }`) is consumed by `BoardDragLayer.handleDragStart` (line 111) and `SpreadZone.useDndMonitor` (line 65). Plan 01 acceptance criterion explicitly preserves the exact string match — no shape mutation possible without grep gate failure. |
| T-17-02 | Repudiation / DoS | dnd-kit duplicate-ID warning leading to inconsistent drag events | mitigate | The phase exists to remove this. Acceptance criterion `grep -c DraggableCard ... = 0` enforces removal; the new e2e test asserts no duplicate-ID console warning. |
| T-17-03 | Spoofing | A client claims to drag from a spread zone that isn't theirs to bypass ownership | accept | Out of scope per REQUIREMENTS.md "Future Requirements" (personal spread zone drag-out ownership guard deferred to a later milestone). Phase 17 is a no-behavior-change refactor — does not alter the existing authorization surface, so risk is unchanged from baseline. |
| T-17-04 | Information Disclosure | Direct CardFace render exposes card data the server already broadcasts to this client | accept | The card data being rendered is already in `pile.cards` for cards the server allows this client to see (server-side hand masking is unchanged — VERIFIED by viewFor.test.ts and broadcastMasking.test.ts coverage in Plan 01 verify command). No new data exposure: `DraggableCard` rendered the same `CardFace`/`CardBack` internally. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm test` exits 0 (full Vitest suite — server logic unchanged, all tests must remain green)
- `npx playwright test playwright/game.spec.ts -g "spread zone"` exits 0 (3 tests: existing visibility + 2 new regression tests)
- `npx playwright test playwright/game.spec.ts -g "multi-card set play"` exits 0 (regression check on the select-then-drag flow that depends on the spread zone drop target)
- Visual sanity (no checkpoint task — Plan 02 owns visual checkpoint): start `npm run dev` + `npm run dev:client`, open the app, deal cards, play a card into the communal zone, then drag it back to hand. Drag should work. (Optional dev confirmation; not required for plan completion.)
</verification>

<success_criteria>
- All "must_haves.truths" hold (verified via the e2e tests in Task 1.1)
- All "must_haves.artifacts" present (verified via grep gates in Task 1.2 acceptance_criteria)
- All "must_haves.key_links" wired (verified by `grep "fromZone: 'pile' as const, fromId: pileId, toZone: 'pile' as const, toId: pileId" src/components/SpreadZone.tsx` returning 1)
- SPREAD-04 requirement satisfied: drag interactions on spread zones produce no event misfires; one dnd-kit ID per card
- No regression in the unit test suite or in existing e2e tests
- Plan 02 (BoardView restructure) is unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/17-board-layout-restructure/17-01-SUMMARY.md` capturing:
- The exact diff applied to `src/components/SpreadZone.tsx`
- The two new e2e test names added to `playwright/game.spec.ts`
- Confirmation that `DraggableCard` is no longer imported or referenced in `SpreadZone.tsx`
- Confirmation that `useSortable` data shape was preserved verbatim
- Test results (unit + targeted e2e)
</output>
