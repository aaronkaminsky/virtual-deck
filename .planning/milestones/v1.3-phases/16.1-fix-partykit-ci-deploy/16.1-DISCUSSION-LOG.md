# Phase 16.1: fix-partykit-ci-deploy - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-02
**Phase:** 16.1-fix-partykit-ci-deploy
**Areas discussed:** Workflow structure, Deploy ordering, Failure coupling

---

## Workflow Structure

| Option | Description | Selected |
|--------|-------------|----------|
| Same job | Add partykit deploy as a step in the existing `build-and-deploy` job | ✓ |
| Separate job | New `deploy-partykit` job that runs after the build job; cleaner separation, different permission scope | |

**User's choice:** Same job
**Notes:** Simpler YAML, single place to maintain. Sequential by default.

---

## Deploy Ordering

| Option | Description | Selected |
|--------|-------------|----------|
| Before Pages | `npm run deploy` after `npm run build`, before `configure-pages` | ✓ |
| After Pages | `npm run deploy` after `deploy-pages` | |

**User's choice:** Before Pages
**Notes:** Server goes live first so new client always finds new server ready.

---

## Failure Coupling

| Option | Description | Selected |
|--------|-------------|----------|
| Block Pages (no continue-on-error) | Default behavior — partykit failure halts job, Pages never deploys | ✓ |
| Continue on error | `continue-on-error: true` on partykit step; Pages deploys regardless | |

**User's choice:** Block Pages
**Notes:** Both deployments succeed together or neither does.

---

## Claude's Discretion

None — all decisions were made by the user.

## Deferred Ideas

None.
