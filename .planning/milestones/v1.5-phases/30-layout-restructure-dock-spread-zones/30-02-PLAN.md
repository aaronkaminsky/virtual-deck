---
phase: 30-layout-restructure-dock-spread-zones
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - playwright/game.spec.ts
autonomous: false
requirements:
  - LAYOUT-05

must_haves:
  truths:
    - "A new Playwright e2e test exercises drag-from-hand to the opponent spread zone (now located in the board area) and verifies the card lands correctly"
    - "The test asserts the opponent spread zone's bounding box is below the bg-card header band (structural assertion that the spread is in the board area, not the header)"
    - "The test captures console messages during the drag and asserts no duplicate-id dnd-kit warnings appear (regression guard for useDndMonitor subscription loss per STATE.md and D-08)"
    - "When both dev servers are running, `npm run test:e2e` (or scoped grep) executes the new test and exits 0"
  artifacts:
    - path: "playwright/game.spec.ts"
      provides: "e2e drag test against the post-restructure layout"
      contains: "spread zone dock"
  key_links:
    - from: "playwright/game.spec.ts"
      to: "src/components/SpreadZone.tsx"
      via: "data-testid={`spread-zone-${pile.id}`} selector"
      pattern: "spread-zone-spread-"
    - from: "playwright/game.spec.ts"
      to: "src/components/BoardView.tsx"
      via: "bg-card header band selector for structural assertion"
      pattern: "\\.bg-card"
---

<objective>
Add a dedicated Playwright e2e test that drives a real pointer-driven drag against the post-restructure board layout (Plan 30-01) and asserts:
1. The drag from hand into the opponent's now-docked spread zone routes correctly (card lands, hand loses one card, opponent spread gains one card).
2. The opponent spread zone bounding box is below the `bg-card` header band — proves the DOM was actually restructured, not just visually adjusted.
3. No "duplicate id" or "multiple elements with the same id" warnings fire during the drag — guards against `useDndMonitor` subscription loss (the second failure mode flagged in STATE.md and D-08).

Purpose: STATE.md and CONTEXT.md identify LAYOUT-05 as the highest-risk change in v1.5 with two specific failure modes — stale droppable rects (mitigated by `MeasuringStrategy.Always` in Plan 30-01) and `useDndMonitor` subscription loss (mitigated by this e2e test). The unit test suite cannot exercise dnd-kit's pointer pipeline, so a Playwright drag is the only way to verify drag-and-drop still works after the DOM move.

Output: One new `test(...)` block appended to the existing `test.describe('virtual-deck e2e', ...)` scope in `playwright/game.spec.ts`. No changes to other tests; no new fixtures.

This plan is non-autonomous because Playwright requires both dev servers running (`npm run dev` and `npm run dev:client`); the executor must start them if not already up, then run the e2e suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-layout-restructure-dock-spread-zones/30-CONTEXT.md
@.planning/phases/30-layout-restructure-dock-spread-zones/30-PATTERNS.md
@.planning/phases/30-01-SUMMARY.md
@playwright/game.spec.ts
@playwright/fixtures.ts

<interfaces>
<!-- The selectors and helpers the new test will use. Extracted from src/ and playwright/. -->

Test fixture (already imported at playwright/game.spec.ts line 2):
```typescript
import { test, expect } from './fixtures';
// twoPlayerRoom yields { p1: Page, p2: Page } already joined into a room and on the board.
```

Local helper (already defined at playwright/game.spec.ts lines 4-8):
```typescript
async function dealCards(page: Page, count = 5) {
  await page.getByRole('button', { name: /open controls/i }).click();
  await page.locator('input[type="number"]').fill(String(count));
  await page.getByRole('button', { name: 'Deal' }).click();
}
```

SpreadZone test ID pattern (src/components/SpreadZone.tsx line 164):
```tsx
data-testid={`spread-zone-${pile.id}`}
// pile.id is `spread-${playerId}` for personal/opponent spreads or 'play' for communal
// → matching selector: '[data-testid^="spread-zone-spread-"]'
```

HandZone test ID (src/components/HandZone.tsx):
```tsx
data-testid="hand-zone"
// card selector inside hand: '[aria-pressed]' (5 after dealCards(5))
```

Header band selector (src/components/BoardView.tsx line 39):
```tsx
className="flex items-start justify-between px-4 py-2 gap-4 bg-card"
// → matching selector: '.bg-card' (first occurrence is the header)
```

Existing pointer-drag pattern used throughout game.spec.ts (e.g. lines 234-238):
```typescript
const src = await sourceLocator.boundingBox();
const tgt = await targetLocator.boundingBox();
if (!src || !tgt) throw new Error('bounding boxes unavailable');
await page.mouse.move(src.x + src.width / 2, src.y + src.height / 2);
await page.mouse.down();
await page.mouse.move(tgt.x + tgt.width / 2, tgt.y + tgt.height / 2, { steps: 15 });
await page.mouse.up();
```

Console-warning scrub pattern (game.spec.ts lines 246-247, 272-274):
```typescript
const consoleMessages: string[] = [];
page.on('console', msg => { consoleMessages.push(msg.text()); });
// ... drag ...
const duplicateIdWarnings = consoleMessages.filter(msg =>
  /duplicate id|multiple elements with the same id/i.test(msg)
);
expect(duplicateIdWarnings).toHaveLength(0);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add post-restructure spread-dock drag e2e test</name>
  <files>playwright/game.spec.ts</files>
  <read_first>
    - playwright/game.spec.ts (read the full file — the new test is appended to the existing test.describe block; existing drag tests at lines 163-275 and 346-413 are the reference patterns)
    - playwright/fixtures.ts (confirm `twoPlayerRoom` fixture shape — returns `{ p1, p2 }` Pages already on the board)
    - src/components/SpreadZone.tsx (confirm `data-testid={`spread-zone-${pile.id}`}` at line 164 — selector `[data-testid^="spread-zone-spread-"]` matches both personal and opponent spreads, NOT the communal `play` zone)
    - src/components/BoardView.tsx (confirm the new spread row is inside the board area below the `<div ... bg-card>` header — used for the structural Y-coordinate assertion)
    - .planning/phases/30-layout-restructure-dock-spread-zones/30-CONTEXT.md (D-08 — "dedicated Playwright e2e test that drags a card via useDndMonitor in the post-restructure layout")
    - .planning/phases/30-layout-restructure-dock-spread-zones/30-PATTERNS.md (playwright/game.spec.ts section — full test scaffold, selectors, structural assertion pattern)
  </read_first>
  <action>
    Append exactly one new `test(...)` block inside the existing `test.describe('virtual-deck e2e', ...)` scope in `playwright/game.spec.ts`. Place the new block after the existing `'spread zone drag: drag card from communal spread zone to hand'` test and before the next test. Do not modify any other test, the import block, or the `dealCards` helper.

    The new test must use the `twoPlayerRoom` fixture, the local `dealCards(p1, 5)` helper, and the established pointer-drag pattern. Concrete requirements:

    1. **Test name:** `'spread zone dock: drag from hand to docked spread lands and stays in board area'`.

    2. **Setup:** Destructure `{ p1 }` from `twoPlayerRoom`. Call `await dealCards(p1, 5)`. Assert P1's `hand-zone` has 5 `[aria-pressed]` cards (mirror line 218).

    3. **Console listener:** Attach `p1.on('console', msg => consoleMessages.push(msg.text()))` BEFORE the drag — same pattern as line 247.

    4. **Locate P1's personal spread zone (the docked target):** Use `p1.locator('[data-testid^="spread-zone-spread-"]')`. In a 2-player room there are two such elements visible on P1's page (P1's own personal spread, P2's opponent spread); pick the one whose bounding box has the GREATEST `y` value (the personal spread sits below the opponent spread row in the board area). Use a `p1.evaluate(...)` block that queries `document.querySelectorAll('[data-testid^="spread-zone-spread-"]')`, returns `[{ testId, y, height }]` for each, then in test code reduces over the array to find the maximum-y entry. Re-locate via `p1.getByTestId(ownSpread.testId)` for subsequent operations. This mirrors the lookup logic in PATTERNS.md lines 227-235.

    5. **Drag card 0 from hand to that spread zone:** Grab `handZone.locator('[aria-pressed]').nth(0)`. Compute `boundingBox()` for source and target; throw if either is unavailable. Execute the four-step mouse drag (`move → down → move with { steps: 15 } → up`) at the center of each bounding box. Use exact pattern from game.spec.ts lines 234-238.

    6. **Behavioral assertions:**
       - After the drag, `handZone.locator('[aria-pressed]')` has count 4.
       - After the drag, the target spread zone contains at least one card (`ownSpreadEl.locator('[role="button"]')` count ≥ 1; spread zones nest a draggable card per item).

    7. **Structural assertion (proves DOM was restructured, not just CSS):**
       - Take `headerBox = await p1.locator('.bg-card').first().boundingBox()`.
       - Take `spreadBox = await ownSpreadEl.boundingBox()` after the drag.
       - Throw if either is unavailable.
       - `expect(spreadBox.y).toBeGreaterThan(headerBox.y + headerBox.height)`.

    8. **Console-warning scrub:**
       - `const duplicateIdWarnings = consoleMessages.filter(msg => /duplicate id|multiple elements with the same id/i.test(msg))`.
       - `expect(duplicateIdWarnings).toHaveLength(0)` — this is the explicit `useDndMonitor` subscription-loss regression guard from D-08.

    Do NOT add any `npx playwright install` step, do NOT modify other tests, do NOT add new helpers. The test uses only `test`, `expect`, `Page`, the existing `dealCards` helper, and the `twoPlayerRoom` fixture already imported.

    After writing the test, run the e2e suite (see verify). The pre-push hook documented in CLAUDE.md will run all e2e tests; this is OK — only the new test needs to pass, but no existing test should regress.
  </action>
  <verify>
    <automated>
      # First confirm typecheck + unit tests still green
      npm run typecheck &amp;&amp; npm test -- --run
      # Then run only the new test (requires `npm run dev` and `npm run dev:client` to be running)
      npx playwright test --grep "spread zone dock: drag from hand to docked spread"
    </automated>
    <human-check>
      Before running the playwright command, confirm both dev servers are up:
      - PartyKit (port 1999): `lsof -i :1999`
      - Vite (port 5173): `lsof -i :5173`
      If either is not running, start it (`npm run dev` and `npm run dev:client` in separate terminals) and rerun.
    </human-check>
  </verify>
  <acceptance_criteria>
    - playwright/game.spec.ts contains the literal substring `'spread zone dock: drag from hand to docked spread lands and stays in board area'` (verify: `rg -c "spread zone dock: drag from hand to docked spread lands and stays in board area" playwright/game.spec.ts` returns 1)
    - playwright/game.spec.ts contains the structural assertion `toBeGreaterThan(headerBox.y + headerBox.height)` (verify: `rg -c "toBeGreaterThan\(headerBox\.y \+ headerBox\.height\)" playwright/game.spec.ts` returns at least 1)
    - playwright/game.spec.ts contains the duplicate-id regex `/duplicate id|multiple elements with the same id/i` at least 3 times (verify: `rg -c "duplicate id\|multiple elements with the same id" playwright/game.spec.ts` returns at least 3 — two pre-existing tests + this new one)
    - The new test sits inside `test.describe('virtual-deck e2e', ...)` (verify: `rg -n "test\(" playwright/game.spec.ts | wc -l` is exactly one greater than before this plan ran)
    - `npm run typecheck` exits 0
    - `npm test -- --run` exits 0
    - `npx playwright test --grep "spread zone dock: drag from hand to docked spread"` exits 0 with `1 passed`
    - No previously-passing playwright test in `playwright/game.spec.ts` regresses (verify: a full `npm run test:e2e` exits 0)
  </acceptance_criteria>
  <done>
    The new e2e test passes against the post-restructure layout; structural Y-coordinate assertion confirms the spread is in the board area; the console-warning scrub confirms no dnd-kit duplicate-id warnings fire during the drag.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright pointer events → dnd-kit DndContext | Real browser drag pipeline; this is the only test layer that exercises pointer sensors, droppable measurement, and `useDndMonitor` subscriptions end-to-end |
| Playwright client → PartyKit dev server | Drag triggers `sendAction(MOVE_CARD)` over WebSocket; broken wiring would surface as the assertion `handZone count == 4` failing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | Tampering | DOM duplicate IDs from re-rendered SpreadZone (useDndMonitor subscription loss) | mitigate | Console-warning scrub filters for `/duplicate id\|multiple elements with the same id/i` and asserts length 0. This is the explicit guard called out in STATE.md for the second known LAYOUT-05 failure mode. |
| T-30-08 | Repudiation | Drag does not produce a server MOVE_CARD because the docked spread's droppable rect is stale or the column re-mounted under a new key | mitigate | The behavioral assertion `handZone count after drag == 4` would fail; combined with `MeasuringStrategy.Always` (Plan 30-01) and stable `key={id}` props in the new spread row, the test acts as a regression guard. |
| T-30-09 | Tampering | Structural regression — someone "fixes" by moving the spread back into the header band with CSS only | mitigate | The structural assertion `spreadBox.y > headerBox.y + headerBox.height` would fail. The test enforces the actual DOM topology required by LAYOUT-05. |
| T-30-10 | Denial of Service | Test flakiness from dev-server not running blocks PR pre-push | accept | The pre-push hook (per CLAUDE.md) already handles "servers down" by printing a reminder rather than failing; `<human-check>` in this task documents the same expectation. |
| T-30-11 | Information Disclosure | n/a | accept | Test reads only DOM data already visible on P1's page; no cross-player data inspection. |
| T-30-12 | Elevation of Privilege | n/a | accept | Test exercises existing user-facing drag, no privilege changes. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm test -- --run` exits 0
- With both dev servers running: `npx playwright test --grep "spread zone dock: drag from hand to docked spread"` reports `1 passed`
- With both dev servers running: `npm run test:e2e` reports all tests passing (no regressions to existing drag tests)
- The new test fails deterministically if Plan 30-01's changes are reverted (manual sanity check — useful to confirm the test is meaningful, not just permissive)
</verification>

<success_criteria>
- A new `test('spread zone dock: drag from hand to docked spread lands and stays in board area', ...)` block exists in `playwright/game.spec.ts`
- The test asserts the spread zone's Y coordinate is below the header band's bottom edge (DOM-topology assertion, not CSS)
- The test asserts zero duplicate-id console warnings fire during the drag (`useDndMonitor` subscription-loss guard)
- The test asserts the drag actually moved a card (hand count 5 → 4; spread count gained ≥1)
- LAYOUT-05 success criterion #4 ("Drag-and-drop to and from spread zones works correctly after the DOM restructure (e2e verified)") is satisfied
</success_criteria>

<output>
Create `.planning/phases/30-layout-restructure-dock-spread-zones/30-02-SUMMARY.md` when done.
</output>
