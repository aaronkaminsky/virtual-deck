---
phase: 26-zero-risk-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SpreadZone.tsx
  - src/components/GridZone.tsx
autonomous: true
requirements:
  - CTRL-05
  - CTRL-07
  - LAYOUT-07

must_haves:
  truths:
    - "Opponent spread zones show no face-toggle button"
    - "Own spread zone still shows the face-toggle button"
    - "The communal grid face-toggle button is in the 'Play Area' label row, not below the grid"
    - "Spread zones show no name label"
    - "The spread zone header div renders only when a multi-card selection is active for that zone"
  artifacts:
    - path: "src/components/SpreadZone.tsx"
      provides: "Guarded face-toggle, removed name label, conditional header div"
      contains: "interactive !== false"
    - path: "src/components/GridZone.tsx"
      provides: "Face-toggle in label row, standalone block removed"
      contains: "justify-between"
  key_links:
    - from: "SpreadZone.tsx:217"
      to: "face-toggle Button"
      via: "interactive !== false guard (mirrors select-all guard at line 228)"
      pattern: "interactive !== false"
    - from: "SpreadZone.tsx:155"
      to: "header div"
      via: "conditional render on selectedIds.size >= 2 && selectionSource?.zoneId === pile.id"
      pattern: "selectionSource\\?\\."
    - from: "GridZone.tsx:134"
      to: "label row div"
      via: "flex items-center justify-between with Eye/EyeOff button on right"
      pattern: "justify-between"
---

<objective>
Fix three control/label issues across SpreadZone and GridZone with no behavior changes.

Purpose:
- CTRL-05: Opponents should never see a face-toggle on someone else's spread zone.
- CTRL-07: The grid face-toggle should live beside the "Play Area" label, not below the card grid in a separate row.
- LAYOUT-07: Spread zones should not show a redundant name label — the adjacent hand header already identifies the zone.

Output: SpreadZone.tsx and GridZone.tsx with surgical guard additions, a label removal, a conditional header, and a button relocation.
</objective>

<execution_context>
@/Users/aaronkaminsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronkaminsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-zero-risk-visual-polish/26-CONTEXT.md
@.planning/phases/26-zero-risk-visual-polish/26-UI-SPEC.md
</context>

<interfaces>
<!-- Key code facts extracted from src/components/SpreadZone.tsx -->
<!--
  SpreadZoneProps.interactive?: boolean
  Line 153: const isEmpty = pile.cards.length === 0;
  Line 155-163: header div containing name span + conditional selection badge
    <div className="flex items-center">
      <span className="text-xs text-muted-foreground">{pile.name}</span>
      {selectedIds !== undefined && selectedIds.size >= 2 && selectionSource?.zoneId === pile.id && (
        <span className="ml-2 text-xs bg-primary text-primary-foreground rounded-full px-1.5">
          {selectedIds.size} selected
        </span>
      )}
    </div>
  Line 217-241: controls section
    {(!isEmpty || interactive === false) && (
      <div className="flex gap-1">
        <Button ...face-toggle... />           ← needs interactive !== false guard (CTRL-05)
        {interactive !== false && (
          <Button ...select-all... />          ← already guarded
        )}
      </div>
    )}
-->

<!-- Key code facts extracted from src/components/GridZone.tsx -->
<!--
  GridZoneProps.interactive?: boolean
  Line 128: function handleToggleFace() { ... }
  Line 133-136: label row
    <div className="flex items-center">
      <span className="text-xs text-muted-foreground">Play Area</span>
    </div>
  Line 153-165: standalone face-toggle block (to be removed)
    {interactive !== false && (
      <div className="flex gap-1">
        <Button ...face-toggle... />
      </div>
    )}
-->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard SpreadZone face-toggle and remove name label (CTRL-05, LAYOUT-07)</name>
  <files>src/components/SpreadZone.tsx</files>
  <read_first>
    - src/components/SpreadZone.tsx — read the full file before editing; key locations are lines 155-163 (header div + name span) and lines 217-241 (controls section with face-toggle and select-all)
    - .planning/phases/26-zero-risk-visual-polish/26-CONTEXT.md — D-04 specifies the face-toggle guard; D-06 and D-07 specify the label removal and header div condition; note the ml-2 warning in D-07
    - .planning/phases/26-zero-risk-visual-polish/26-UI-SPEC.md — Component Inventory rows for SpreadZone:218-227 (CTRL-05) and SpreadZone:155-163 (LAYOUT-07)
  </read_first>
  <behavior>
    - CTRL-05: when interactive === false (opponent zone), the face-toggle Button does not render; the controls section outer condition `(!isEmpty || interactive === false)` may stay, but the face-toggle Button inside must be wrapped with `{interactive !== false && ...}`
    - CTRL-05: when interactive is true or undefined (own zone), face-toggle renders as before
    - LAYOUT-07 (D-06): the `<span className="text-xs text-muted-foreground">{pile.name}</span>` is removed entirely
    - LAYOUT-07 (D-07): the outer header `<div className="flex items-center">` renders only when the selection badge condition is true — `selectedIds !== undefined && selectedIds.size >= 2 && selectionSource?.zoneId === pile.id`. When no selection is active, no header div renders.
    - LAYOUT-07 (D-07 note): after removing the name span, the `ml-2` class on the selection badge span may be redundant (nothing to the left); remove `ml-2` from the badge span since there is no longer a name span preceding it.
  </behavior>
  <action>
    Two edits to src/components/SpreadZone.tsx:

    1. Per D-06 and D-07 (LAYOUT-07): Replace the header div block (lines 155-163) — which currently always renders containing the name span and the conditional badge span — with a conditional block that renders ONLY when the selection badge condition is true. The new structure is:
       - No outer div rendered when no selection is active
       - When `selectedIds !== undefined && selectedIds.size >= 2 && selectionSource?.zoneId === pile.id` is true, render the selection badge span alone (no name span, no wrapping div with always-on render). The `ml-2` on the badge span should be removed since no name span precedes it anymore. The badge span itself (`text-xs bg-primary text-primary-foreground rounded-full px-1.5`) is kept.

    2. Per D-04 (CTRL-05): Inside the controls section `<div className="flex gap-1">` (line 218), wrap the face-toggle Button with `{interactive !== false && ( ... )}` — mirroring the existing pattern already applied to the select-all Button at line 228. Do not change the outer `{(!isEmpty || interactive === false) && ...}` condition; only the face-toggle Button itself gets the guard.
  </action>
  <verify>
    <automated>npm run typecheck && npm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SpreadZone.tsx does NOT contain `<span className="text-xs text-muted-foreground">{pile.name}</span>`
    - src/components/SpreadZone.tsx contains a conditional render for the selection badge that only shows when `selectedIds !== undefined && selectedIds.size >= 2 && selectionSource?.zoneId === pile.id`
    - src/components/SpreadZone.tsx does NOT contain `ml-2` on the selection count badge span (after the name span is removed)
    - src/components/SpreadZone.tsx wraps the face-toggle Button with `interactive !== false` guard (matching the select-all guard pattern)
    - `npm run typecheck` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Opponent spread zones show no face-toggle; own spread zones still show face-toggle; spread zone name labels are gone; selection badge renders only when cards are actively selected in that zone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move GridZone face-toggle into label row (CTRL-07)</name>
  <files>src/components/GridZone.tsx</files>
  <read_first>
    - src/components/GridZone.tsx — read the full file before editing; target locations are lines 133-136 (label row div) and lines 153-165 (standalone face-toggle block to be removed)
    - .planning/phases/26-zero-risk-visual-polish/26-CONTEXT.md — D-05 specifies the exact relocation: label row becomes `flex items-center justify-between` with "Play Area" text left and Eye/EyeOff button right; the standalone block below the grid is removed
    - .planning/phases/26-zero-risk-visual-polish/26-UI-SPEC.md — Component Inventory rows for GridZone:134-136 and GridZone:153-165; Copywriting Contract confirms aria-label and title values are unchanged when the button moves
  </read_first>
  <behavior>
    - CTRL-07: the face-toggle Button renders inline-right of the "Play Area" label text in the same row
    - CTRL-07: the standalone `{interactive !== false && <div className="flex gap-1">...</div>}` block after the grid is removed entirely
    - CTRL-07: the `interactive !== false` guard still applies to the face-toggle — moved from the outer div into the button position in the label row
    - CTRL-07: aria-label values ("Cards land face-up" / "Cards land face-down") and title values are unchanged
    - CTRL-07: `handleToggleFace` function is unchanged; only the render location of the Button changes
  </behavior>
  <action>
    Two edits to src/components/GridZone.tsx:

    1. Per D-05 (CTRL-07): Replace the label row div (lines 133-136) from:
       `<div className="flex items-center"><span className="text-xs text-muted-foreground">Play Area</span></div>`
       with a `flex items-center justify-between` div containing the label span on the left and the face-toggle Button on the right (guarded by `{interactive !== false && ...}`). The Button uses the existing `handleToggleFace`, existing title and aria-label strings, and the same `h-7 w-7 p-0` sizing from the existing implementation.

    2. Per D-05 (CTRL-07): Remove the entire standalone block at lines 153-165:
       `{interactive !== false && (<div className="flex gap-1"><Button ...face-toggle.../></div>)}`
       This block is now redundant because the button has been promoted into the label row.
  </action>
  <verify>
    <automated>npm run typecheck && npm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/GridZone.tsx label row div has className `flex items-center justify-between`
    - src/components/GridZone.tsx Eye/EyeOff Button exists inside the label row div (after "Play Area" span)
    - src/components/GridZone.tsx does NOT contain a standalone `flex gap-1` div with a face-toggle Button after the grid (the old block is gone)
    - src/components/GridZone.tsx contains only ONE face-toggle Button render site (in the label row)
    - `npm run typecheck` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>GridZone face-toggle button is positioned beside the "Play Area" label text, not below the card grid. The redundant standalone block is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client | pile state (interactive prop, card contents) flows from server through BoardView; SpreadZone and GridZone only read these values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
| T-26-02 | Spoofing | SpreadZone interactive prop | accept | `interactive` is set by BoardView from server-derived player identity; no client can forge it to gain face-toggle access |
| T-26-03 | Information Disclosure | Opponent spread zone | mitigate | CTRL-05 guard ensures opponent cannot trigger SET_PILE_FACE for another player's spread zone via the UI button |
</threat_model>

<verification>
After execution:
1. `npm run typecheck` — exits 0
2. `npm test` — exits 0
3. Manual: view an opponent's spread zone — no face-toggle button visible
4. Manual: view your own spread zone — face-toggle button is present and functional
5. Manual: view the communal grid ("Play Area") — face-toggle button is inline-right of the "Play Area" label, not below the card grid
6. Manual: view any spread zone with no cards selected — no name label, no header strip
7. Manual: select 2+ cards in a spread zone — selection count badge appears (no name label beside it)
</verification>

<success_criteria>
- SpreadZone face-toggle is guarded by `interactive !== false` (CTRL-05)
- SpreadZone name label span is removed (LAYOUT-07)
- SpreadZone header div renders conditionally only with active multi-selection (LAYOUT-07)
- GridZone face-toggle is in the label row with `justify-between` layout (CTRL-07)
- GridZone standalone face-toggle block after the grid is removed (CTRL-07)
- TypeScript compilation passes
- All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/26-zero-risk-visual-polish/26-02-SUMMARY.md` when done
</output>
