---
phase: 28-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/GridZone.tsx
  - playwright/responsive.spec.ts
autonomous: true
requirements:
  - BUG-02

must_haves:
  truths:
    - "D-05: grid-cols-4 sm:grid-cols-7 replaces grid-cols-7 in GridZone — communal grid renders 4 columns below 640px, 7 columns at 640px+"
    - "D-06: 14 cells wrapping to 4 rows on mobile is accepted — card coordinates remain in 7-col server space"
    - "D-07: The outer overflow-x-auto wrapper on the grid is unchanged"
    - "D-08: Playwright test at 375px viewport asserts grid-cols-4 class on the grid element"
  artifacts:
    - path: "src/components/GridZone.tsx"
      provides: "Responsive grid column count"
      contains: "grid-cols-4 sm:grid-cols-7"
    - path: "playwright/responsive.spec.ts"
      provides: "Mobile grid column test"
      contains: "grid-cols-4"
  key_links:
    - from: "GridZone.tsx:148"
      to: "grid div className"
      via: "grid-cols-4 sm:grid-cols-7 replaces grid-cols-7"
      pattern: "grid-cols-4 sm:grid-cols-7"
    - from: "responsive.spec.ts:4"
      to: "new describe block"
      via: "Phase 28 BUG-02 test at 375x667 viewport"
      pattern: "grid-cols-4"
---

<objective>
Fix BUG-02: The communal grid zone renders 7 columns at all viewport widths, including mobile. At viewports below 640px the grid must collapse to 4 columns.

Purpose: A CSS-only fix — change `grid-cols-7` to `grid-cols-4 sm:grid-cols-7` in GridZone.tsx so Tailwind's `sm:` breakpoint (640px) controls the transition. No server-side remapping or coordinate changes required.
Output: `GridZone.tsx` with the responsive grid class, and a new Playwright test in `responsive.spec.ts` asserting 4 columns at 375px viewport.
</objective>

<execution_context>
@/Users/aaronkaminsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronkaminsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-bug-fixes/28-CONTEXT.md
@.planning/phases/28-bug-fixes/28-UI-SPEC.md
</context>

<interfaces>
<!-- GridZone.tsx line 148 (current):
  <div data-testid="grid-zone-play" className="grid grid-cols-7 gap-px bg-border rounded-md overflow-hidden w-fit">
  Target: change grid-cols-7 → grid-cols-4 sm:grid-cols-7
-->
<!-- responsive.spec.ts (current):
  One describe block: 'Phase 19 responsive layout'
  One test: LAYOUT-04 no horizontal scroll at 375x667
  Pattern: test.use({ viewport: { width: 375, height: 667 } })
           page.goto(`/?room=${roomCode}`)
           page.getByPlaceholder('Your name').fill(...)
           page.getByRole('button', { name: 'Join Game' }).click()
           await expect(page.getByTestId('hand-zone')).toBeVisible()
-->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Change grid-cols-7 to grid-cols-4 sm:grid-cols-7 in GridZone</name>
  <files>src/components/GridZone.tsx</files>
  <read_first>
    - src/components/GridZone.tsx — read the full file; the target is line 148: `data-testid="grid-zone-play" className="grid grid-cols-7 gap-px bg-border rounded-md overflow-hidden w-fit"`
    - .planning/phases/28-bug-fixes/28-CONTEXT.md — D-05: change grid-cols-7 to grid-cols-4 sm:grid-cols-7; D-06: 14 cells wrapping to 4 rows on mobile is acceptable (out-of-scope decision); D-07: do NOT change the outer overflow-x-auto wrapper
    - .planning/REQUIREMENTS.md — BUG-02 acceptance criteria and Out of Scope row ("Server-side grid position remapping on mobile — CSS-only column collapse accepted")
  </read_first>
  <behavior>
    - At viewport < 640px: grid renders 4 columns (Tailwind default, no prefix)
    - At viewport >= 640px: grid renders 7 columns (Tailwind sm: prefix)
    - The `data-testid="grid-zone-play"` attribute is unchanged
    - All other className tokens (`gap-px bg-border rounded-md overflow-hidden w-fit`) are unchanged
    - The outer `overflow-x-auto` wrapper div is NOT modified
    - Card positions in the 7-column grid coordinate space are unchanged (server-side remapping is out of scope)
  </behavior>
  <action>
    One edit to src/components/GridZone.tsx line 148:

    Per D-05: Replace `grid-cols-7` with `grid-cols-4 sm:grid-cols-7` in the grid div className. The full className becomes:
    `"grid grid-cols-4 sm:grid-cols-7 gap-px bg-border rounded-md overflow-hidden w-fit"`

    Do not change any other part of the file.
  </action>
  <verify>
    <automated>npm run typecheck && npm test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/GridZone.tsx contains `grid-cols-4 sm:grid-cols-7` (not bare `grid-cols-7`)
    - src/components/GridZone.tsx does NOT contain bare `grid-cols-7` (the old class is fully replaced)
    - src/components/GridZone.tsx line 148 data-testid="grid-zone-play" is unchanged
    - `npm run typecheck` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>GridZone grid collapses to 4 columns on mobile (< 640px) and expands to 7 columns on desktop (>= 640px).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Playwright test for BUG-02 mobile grid columns</name>
  <files>playwright/responsive.spec.ts</files>
  <read_first>
    - playwright/responsive.spec.ts — read the full file; the existing test structure uses `test.use({ viewport })`, `page.goto`, `page.getByPlaceholder`, `page.getByRole`, `page.getByTestId`; add a new `test.describe` block after the existing one
    - src/components/GridZone.tsx — confirms the data-testid is `grid-zone-play` (line 148)
    - .planning/phases/28-bug-fixes/28-CONTEXT.md — D-08: load game at 375px viewport; assert grid element has 4 columns computed style (or CSS class)
  </read_first>
  <behavior>
    - A new `test.describe` block named `'Phase 28 BUG-02 mobile grid columns'` is added to responsive.spec.ts
    - The describe uses `test.use({ viewport: { width: 375, height: 667 } })`
    - The test navigates to a room, joins, waits for the board to be visible, then locates `[data-testid="grid-zone-play"]`
    - The test asserts the grid element has the CSS class `grid-cols-4` (not `grid-cols-7`) at this viewport
    - The existing LAYOUT-04 describe block is NOT modified
  </behavior>
  <action>
    Append a new describe block to playwright/responsive.spec.ts after the existing LAYOUT-04 block:

    Use `test.describe('Phase 28 BUG-02 mobile grid columns', ...)` with `test.use({ viewport: { width: 375, height: 667 } })`.
    Test name: `'BUG-02: grid renders 4 columns at 375px viewport'`.
    Steps: navigate to `/?room=${nanoid(8)}`, fill name, click Join Game, wait for `getByTestId('hand-zone')` to be visible (matches existing LAYOUT-04 pattern), then locate `[data-testid="grid-zone-play"]`.
    Assertion: use `page.evaluate` or `toHaveClass` to assert the grid element has class `grid-cols-4`. The simplest approach is:
    `await expect(page.locator('[data-testid="grid-zone-play"]')).toHaveClass(/grid-cols-4/);`
    Do NOT assert absence of `grid-cols-7` — at 375px Tailwind does not apply `sm:grid-cols-7`, but the class string still contains the token; asserting presence of `grid-cols-4` is sufficient.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - playwright/responsive.spec.ts contains a describe block `'Phase 28 BUG-02 mobile grid columns'`
    - playwright/responsive.spec.ts contains `toHaveClass(/grid-cols-4/)` assertion on the grid-zone-play element
    - The existing LAYOUT-04 describe block is present and unmodified
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Playwright test verifies the grid renders with grid-cols-4 class at 375px viewport width (BUG-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client | Grid card positions originate from server; this plan does not change position coordinates |
| Client viewport | CSS-only change; no server state or user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-SC | Tampering | npm installs | accept | No new packages installed in this plan |
| T-28-03 | Information Disclosure | Grid layout on mobile | accept | Column count change is visual only; no card data is exposed or hidden |
</threat_model>

<verification>
After execution:
1. `npm run typecheck` — exits 0
2. `npm test` — exits 0
3. Playwright e2e (if servers are running): `npm run test:e2e` — BUG-02 test passes
4. Manual: open the game in a browser at 375px viewport (DevTools device simulation) — communal grid shows 4 columns
5. Manual: open at desktop width (>= 640px) — communal grid shows 7 columns as before
</verification>

<success_criteria>
- GridZone.tsx contains `grid-cols-4 sm:grid-cols-7` (BUG-02)
- responsive.spec.ts contains BUG-02 mobile grid columns test (BUG-02)
- TypeScript compilation passes
- All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/28-bug-fixes/28-02-SUMMARY.md` when done
</output>
