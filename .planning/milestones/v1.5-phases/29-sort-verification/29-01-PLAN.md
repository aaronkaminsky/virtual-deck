---
phase: 29-sort-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HandZone.tsx
  - tests/handSort.test.ts
autonomous: true
requirements:
  - SORT-02

must_haves:
  truths:
    - "D-01: handleSort only calls setSortMode(nextMode) — no sendAction, no dispatch construction"
    - "D-02: buildSortDispatch function and export are removed from HandZone.tsx"
    - "D-03: sortCards function body is unchanged (still uses [...cards].sort)"
    - "D-04: 'Original order' is the current server/manual order — set by drag-reorder, never by sort click"
    - "D-06: Drag in non-original sort mode still calls setSortMode('original') and dispatches REORDER_HAND with no skipSnapshot"
    - "D-07: handSort.test.ts has buildSortDispatch import removed and the buildSortDispatch describe-block 'it' removed"
    - "D-07: handSort.test.ts adds a sortCards non-mutation test asserting input.map(c => c.id) is unchanged after sortCards calls"
    - "D-08: No new test files created"
    - "displayedCards = sortMode === 'original' ? cards : sortCards(cards, sortMode) is preserved verbatim"
    - "vitest run passes; tsc --noEmit passes"
  artifacts:
    - path: "src/components/HandZone.tsx"
      provides: "Render-time-only hand sort with simplified handleSort"
      contains: "function handleSort"
    - path: "tests/handSort.test.ts"
      provides: "sortCards correctness tests + non-mutation invariant test"
      contains: "does not mutate"
  key_links:
    - from: "src/components/HandZone.tsx handleSort"
      to: "setSortMode only (no sendAction)"
      via: "render-time sort"
      pattern: "setSortMode\\(nextMode\\)"
    - from: "src/components/HandZone.tsx displayedCards line"
      to: "sortCards pure function"
      via: "render-time visual overlay"
      pattern: "sortMode === 'original' \\? cards : sortCards"
    - from: "src/components/HandZone.tsx useDndMonitor onDragEnd"
      to: "REORDER_HAND dispatch"
      via: "drag-reorder is the only writer of 'original order'"
      pattern: "type: 'REORDER_HAND'"
    - from: "tests/handSort.test.ts import line"
      to: "src/components/HandZone exports"
      via: "named import of sortCards only"
      pattern: "import \\{ sortCards \\} from"
---

<objective>
Implement SORT-02: convert hand sort to a render-time-only visual overlay. Delete `buildSortDispatch` and the server dispatch in `handleSort`, leaving sort as a pure client-side display preference. Update `tests/handSort.test.ts` to drop the dispatch test and add a non-mutation invariant test that proves the source `cards` array (the "original order") is preserved across sort calls.

Purpose: "Original order" is now defined as the current server/manual order (D-04). Sort clicks no longer mutate server state, so cycling sort → original always returns the hand to the last drag-reordered (or initial) server order. The non-mutation test locks this invariant in place.

Output: Simplified `handleSort` and removed `buildSortDispatch` in `HandZone.tsx`; updated `tests/handSort.test.ts` with the dispatch test removed and the non-mutation test added; vitest + tsc green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-sort-verification/29-CONTEXT.md
@.planning/phases/29-sort-verification/29-PATTERNS.md
</context>

<interfaces>
<!-- Key exports from src/components/HandZone.tsx that other modules depend on:
  - export type SortMode = 'original' | 'bySuit' | 'byRank';
  - export function sortCards(cards: Card[], mode: 'bySuit' | 'byRank'): Card[]
  - export function HandZone(props: HandZoneProps): JSX.Element
  - export function buildSortDispatch(...)  <-- TO BE DELETED in Task 1
-->
<!-- After this plan, the public export surface of HandZone.tsx is:
  - SortMode (type)
  - sortCards (function)
  - HandZone (component)
  buildSortDispatch is gone.
-->
<!-- Only consumer of buildSortDispatch is tests/handSort.test.ts line 6 (named import).
  No other file imports buildSortDispatch — verified via the canonical refs in 29-CONTEXT.md.
-->
<!-- ClientAction type (src/shared/types) is no longer referenced by buildSortDispatch
  after deletion. The `ClientAction` import in HandZone.tsx is still used by the
  sendAction prop type (HandZoneProps.sendAction) and by the REORDER_HAND dispatch
  inside useDndMonitor.onDragEnd — DO NOT remove the ClientAction import.
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Convert handleSort to render-time-only and delete buildSortDispatch</name>
  <files>src/components/HandZone.tsx</files>
  <read_first>
    - src/components/HandZone.tsx (full file — see current state of lines 24–46 sortCards/buildSortDispatch, lines 143–164 sortMode state + handleSort, lines 154 displayedCards line, lines 218–221 drag-reorder sortMode reset)
    - .planning/phases/29-sort-verification/29-CONTEXT.md (D-01 through D-06)
    - .planning/phases/29-sort-verification/29-PATTERNS.md (HandZone.tsx pattern assignment — shows exact current vs target shape of handleSort)
  </read_first>
  <action>
    Make three changes in `src/components/HandZone.tsx`, in this order:

    1. Delete the entire `buildSortDispatch` function declaration and its surrounding blank line — the block currently spanning lines 39–46 (the `export function buildSortDispatch(cards: Card[], nextMode: SortMode): ClientAction | null { ... }` and the trailing blank line). The preceding `sortCards` function (lines 24–37) and the `// --- Tooltip copy per D-06 ---` section (line 48 onward) remain untouched. Per D-02 the export is removed with the function.

    2. Simplify `handleSort` (currently lines 156–164). Keep the `nextMode` computation and `setSortMode(nextMode)`. Remove the `const dispatch = buildSortDispatch(cards, nextMode);` line, the `if (dispatch !== null) { sendAction(dispatch); }` block, and the inline comment `// Always derive dispatch from canonical server order (cards), not the visual order.`. Replace the body so the function only computes `nextMode` and calls `setSortMode(nextMode)`. Add a single short comment inside the function body explaining the render-time-only invariant (Claude's discretion per D-01: e.g., `// Sort is render-time only (D-01) — displayedCards recomputes on next render.`). The closing brace stays.

    3. Verify (do not modify) that these three lines remain exactly as written and operative:
       - Line 154: `const displayedCards = sortMode === 'original' ? cards : sortCards(cards, sortMode);`
       - Line 218–220: the `if (sortMode !== 'original') { setSortMode('original'); }` block inside `onDragEnd`
       - Line 221: `sendAction({ type: 'REORDER_HAND', orderedCardIds: reordered.map(c => c.id) });` (no `skipSnapshot` field — drag is the canonical source of original order per D-06)

    Do NOT touch the `ClientAction` import on line 6 — it is still used by `HandZoneProps.sendAction` and by the REORDER_HAND dispatch in `onDragEnd`. Do NOT touch the `sendAction` prop in `HandZoneProps` — drag-reorder still uses it. Do NOT change `sortCards` per D-03.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -5 && grep -n 'buildSortDispatch' src/components/HandZone.tsx | grep -v '^[[:space:]]*//' | wc -l | xargs -I {} test {} -eq 0 && echo OK_NO_BUILDSORTDISPATCH && grep -c "type: 'REORDER_HAND'" src/components/HandZone.tsx | xargs -I {} test {} -eq 1 && echo OK_ONE_REORDER_DISPATCH && grep -c "sortMode === 'original' ? cards : sortCards(cards, sortMode)" src/components/HandZone.tsx | xargs -I {} test {} -eq 1 && echo OK_RENDER_TIME_SORT_PRESERVED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'buildSortDispatch' src/components/HandZone.tsx` returns no non-comment matches (function and any references gone)
    - `grep -c "function handleSort" src/components/HandZone.tsx` returns 1
    - `grep -c "sendAction(dispatch)" src/components/HandZone.tsx` returns 0 (no dispatch sendAction)
    - `grep -c "type: 'REORDER_HAND'" src/components/HandZone.tsx` returns 1 (only the drag-reorder dispatch survives)
    - `grep -c "skipSnapshot" src/components/HandZone.tsx` returns 0 (skipSnapshot pattern fully removed with buildSortDispatch)
    - `grep -c "sortMode === 'original' ? cards : sortCards(cards, sortMode)" src/components/HandZone.tsx` returns 1 (D-03 / render-time line preserved verbatim)
    - `grep -c "setSortMode('original')" src/components/HandZone.tsx` returns 1 (the drag-end reset on line ~219 preserved per D-06)
    - `grep -c "export function sortCards" src/components/HandZone.tsx` returns 1 (sortCards still exported for tests)
    - `grep -c "import type { Card, ClientAction" src/components/HandZone.tsx` returns 1 (ClientAction import preserved — still used by HandZoneProps.sendAction and REORDER_HAND dispatch)
    - `npm run typecheck` exits 0 (no orphaned references)
  </acceptance_criteria>
  <done>
    `buildSortDispatch` is deleted from `HandZone.tsx`. `handleSort` contains only `nextMode` computation, `setSortMode(nextMode)`, and a short explanatory comment. The render-time `displayedCards` line and the drag-reorder `REORDER_HAND` dispatch are untouched. `tsc --noEmit` is green. Note: at this checkpoint, `npm test` will fail because `tests/handSort.test.ts` still imports `buildSortDispatch` — Task 2 fixes that.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update handSort.test.ts — drop dispatch test, add non-mutation invariant</name>
  <files>tests/handSort.test.ts</files>
  <read_first>
    - tests/handSort.test.ts (full file — current state has 3 `it` blocks; the third tests `buildSortDispatch` and must go)
    - src/components/HandZone.tsx (post-Task 1 — confirms `buildSortDispatch` no longer exists; only `sortCards` and `SortMode` remain exported alongside `HandZone`)
    - .planning/phases/29-sort-verification/29-CONTEXT.md (D-07, D-08 — exact scope of test changes)
    - .planning/phases/29-sort-verification/29-PATTERNS.md (tests/handSort.test.ts pattern assignment — shows import line update, exact block to delete, and shape of the new non-mutation `it` block)
  </read_first>
  <action>
    Make three changes in `tests/handSort.test.ts`:

    1. Fix the import on line 6: drop `buildSortDispatch` from the named import. The line currently reads `import { sortCards, buildSortDispatch } from "../src/components/HandZone";` and must become `import { sortCards } from "../src/components/HandZone";`.

    2. Delete the third `it` block (currently lines 48–67) — the `"Sort click dispatches REORDER_HAND with skipSnapshot: true and sorted ids"` test. Remove the entire block including its closing `});` and any single trailing blank line before the describe-block's closing `});`. The two preceding `it` blocks (`"By Suit (D-01, D-03): ..."` and `"By Rank (D-02, D-03): ..."`) stay verbatim.

    3. Add one new `it` block inside the same `describe("sortCards pure function", () => { ... })`, placed after the existing `byRank` test. The new test verifies that `sortCards` does not mutate its input array — proving the "original order" (the input `cards` array passed in by HandZone) is preserved across visual sorts. Use the existing `mkCard` local factory. Concrete test shape (per D-07):

       - Test description: `"does not mutate the input array (original order is preserved as server/manual order per D-04)"` (final wording is Claude's discretion per CONTEXT, but it MUST reference non-mutation and the "original order" invariant).
       - Build an `input: Card[]` of at least 3 cards spanning multiple suits and ranks using `mkCard`.
       - Capture `const originalIds = input.map(c => c.id);` BEFORE any sort calls.
       - Call `sortCards(input, "bySuit");` and `sortCards(input, "byRank");` back-to-back (return values not stored — the point is to check the input).
       - Assert `expect(input.map(c => c.id)).toEqual(originalIds);`.

    Do not add any new top-level imports beyond what's already there. `vitest` and the `Card`/`Suit`/`Rank` type imports already in the file are sufficient. Do not create new test files (D-08).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20 && grep -c "buildSortDispatch" tests/handSort.test.ts | xargs -I {} test {} -eq 0 && echo OK_NO_BUILDSORTDISPATCH_IN_TEST && grep -cE "it\(" tests/handSort.test.ts | xargs -I {} test {} -eq 3 && echo OK_THREE_IT_BLOCKS && grep -c "does not mutate" tests/handSort.test.ts | xargs -I {} test {} -ge 1 && echo OK_NON_MUTATION_TEST_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "buildSortDispatch" tests/handSort.test.ts` returns 0 (import and test block both gone)
    - `grep -cE "^\s*it\(" tests/handSort.test.ts` returns 3 (two existing sortCards correctness tests + one new non-mutation test)
    - `grep -c "import { sortCards }" tests/handSort.test.ts` returns 1 (named import is exactly `sortCards`)
    - `grep -c "does not mutate" tests/handSort.test.ts` returns at least 1 (new test description mentions non-mutation)
    - `grep -c "toEqual(originalIds)" tests/handSort.test.ts` returns 1 (the non-mutation assertion)
    - `grep -c "REORDER_HAND" tests/handSort.test.ts` returns 0 (dispatch test fully removed)
    - `grep -c "skipSnapshot" tests/handSort.test.ts` returns 0 (dispatch test fully removed)
    - `npm test` exits 0 (vitest passes — including the two existing sortCards correctness tests and the new non-mutation test)
    - `npm run typecheck` exits 0 (test file type-checks against post-Task-1 HandZone exports)
  </acceptance_criteria>
  <done>
    `tests/handSort.test.ts` imports only `sortCards` from `HandZone`, contains exactly three `it` blocks (bySuit correctness, byRank correctness, non-mutation invariant), and `npm test` passes. The full phase invariant — "original order = server/manual order, preserved across sort cycles" — is now locked in by the non-mutation test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → PartyKit server | `sendAction` calls cross this boundary; this plan REMOVES sort-click dispatches across it (sort becomes purely local) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | `handleSort` — accidentally emitting REORDER_HAND on sort click | mitigate | Acceptance criterion `grep -c "type: 'REORDER_HAND'" src/components/HandZone.tsx` MUST be exactly 1 (the drag-reorder dispatch). Plus `grep -c "sendAction(dispatch)"` MUST be 0. Verifies sort cannot reach the server. |
| T-29-02 | Tampering | `sortCards` — mutating the source `cards` prop, polluting React state and server-sync expectations | mitigate | New non-mutation test in `handSort.test.ts` asserts `input.map(c => c.id)` is unchanged after two consecutive `sortCards` calls. Locks the `[...cards].sort(...)` invariant from D-03. |
| T-29-03 | Information Disclosure | Hand sort revealing card data to opponents | accept | `viewFor` server-side masking is unchanged. Sort is purely client-side render-time visual; opponents never receive the local sort state. Drag-reorder is the only writer, and `REORDER_HAND` already goes through existing authenticated server handlers (unchanged by this phase). |
| T-29-04 | Repudiation | Sort actions appearing in undo history and being "undone" unexpectedly | mitigate | By removing the `skipSnapshot: true` dispatch entirely, sort can no longer enter any snapshot/undo path. `grep -c "skipSnapshot" src/components/HandZone.tsx` MUST be 0. The drag-reorder path remains the only contributor to undo history (a deliberate change in semantics, captured in D-04/D-06). |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — only edits to two existing TypeScript files. No legitimacy audit required. |
</threat_model>

<verification>
1. `npm run typecheck` exits 0 (both files type-check)
2. `npm test` exits 0 (vitest — all sortCards tests + non-mutation test pass)
3. `grep -n buildSortDispatch src/components/HandZone.tsx tests/handSort.test.ts` returns no matches across both files
4. `grep -n skipSnapshot src/components/HandZone.tsx tests/handSort.test.ts` returns no matches across both files
5. `grep -n "type: 'REORDER_HAND'" src/components/HandZone.tsx` returns exactly one line — the drag-reorder dispatch inside `useDndMonitor.onDragEnd`
6. Manual confirm (Claude reads file): `displayedCards` line 154 unchanged; `setSortMode('original')` drag-end reset unchanged
</verification>

<success_criteria>
- SORT-02 is implemented: sort is render-time only; "original order" = current server/manual order (D-04)
- `buildSortDispatch` is removed from `HandZone.tsx`; `handleSort` is reduced to `nextMode` + `setSortMode(nextMode)` plus a short explanatory comment
- `tests/handSort.test.ts` imports only `sortCards`, has exactly 3 `it` blocks, and includes a non-mutation invariant test
- `npm test` and `npm run typecheck` both exit 0
- The drag-reorder REORDER_HAND dispatch and the `setSortMode('original')` drag-end reset are preserved exactly (D-06)
- The `sortCards` function body is unchanged (D-03)
</success_criteria>

<output>
Create `.planning/phases/29-sort-verification/29-01-SUMMARY.md` when done.
</output>
