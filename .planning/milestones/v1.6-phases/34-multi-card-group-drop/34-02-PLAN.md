---
phase: 34-multi-card-group-drop
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/components/BoardDragLayer.tsx
  - src/components/BoardView.tsx
  - src/components/CanvasZone.tsx
  - src/components/CanvasDraggableCard.tsx
  - src/components/HandZone.tsx
  - src/components/SpreadZone.tsx
  - src/components/DraggableCard.tsx
autonomous: true
requirements: [MULTI-01]
must_haves:
  truths:
    - "Clicking a canvas card adds it to selectedIds (or removes if already selected); selectionSource becomes { zone:'canvas', zoneId:'canvas' }"
    - "Clicking a canvas card while a hand/pile selection is active clears that selection and switches to canvas (zone-exclusive invariant preserved)"
    - "Clicking empty canvas area deselects all (selectedIds=∅, selectionSource=null)"
    - "Selected canvas cards render the selection ring (boxShadow blue ring) — same visual contract as hand/spread zones"
    - "Selection count badge `{N} selected` is visible in canvas top-left when ≥2 canvas cards selected"
    - "Every draggable card element (hand DraggableCard, SpreadZone card, CanvasDraggableCard) carries a `data-card-id` attribute equal to its card.id"
  artifacts:
    - path: "src/components/CanvasDraggableCard.tsx"
      provides: "Click toggle selection, isSelected/isPassenger props, ring styling, data-card-id"
      contains: "data-card-id"
    - path: "src/components/CanvasZone.tsx"
      provides: "Background onClick deselect-all, selection count badge, props forwarding"
      contains: "onDeselectAll"
    - path: "src/components/BoardDragLayer.tsx"
      provides: "handleToggleSelectCanvas, handleDeselectAll, extended SelectionSource type usage, groupIds computation"
      contains: "handleToggleSelectCanvas"
  key_links:
    - from: "src/components/CanvasDraggableCard.tsx onClick"
      to: "src/components/BoardDragLayer.tsx handleToggleSelectCanvas"
      via: "onToggleSelect prop threaded via CanvasZone → BoardView → BoardDragLayer"
      pattern: "onToggleSelect"
    - from: "src/components/CanvasZone.tsx container div onClick"
      to: "src/components/BoardDragLayer.tsx handleDeselectAll"
      via: "onDeselectAll prop"
      pattern: "onDeselectAll"
    - from: "src/components/BoardDragLayer.tsx selectionSource state"
      to: "src/shared/types.ts SelectionSource"
      via: "import SelectionSource from '@/shared/types'"
      pattern: "SelectionSource"
---

<objective>
Wire client-side canvas selection state: extend `SelectionSource` consumers, add click-to-select on canvas cards, render selection ring + selection count badge, deselect-all on canvas background click, and add `data-card-id` attributes to all draggable card components (hand, spread, canvas) so Plan 03 can read positions via `getBoundingClientRect()`.

Purpose: Selection is the foundation for multi-card group drag. The visual contract must match hand/spread zones (zone-exclusive selection, count badge, ring/lift) so canvas multi-select feels native. `data-card-id` is a prerequisite for Plan 03's DOM offset capture.

Output: Selection works end-to-end (click → ring; click empty canvas → deselect; badge appears at ≥2). Drag mechanics for groups land in Plan 03.
</objective>

<execution_context>
@$HOME/code/dev-setup/claude/get-shit-done/workflows/execute-plan.md
@$HOME/code/dev-setup/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-multi-card-group-drop/34-CONTEXT.md
@.planning/phases/34-multi-card-group-drop/34-RESEARCH.md
@.planning/phases/34-multi-card-group-drop/34-PATTERNS.md
@.planning/phases/34-multi-card-group-drop/34-UI-SPEC.md
@.planning/phases/34-multi-card-group-drop/34-01-SUMMARY.md
@src/components/BoardDragLayer.tsx
@src/components/BoardView.tsx
@src/components/CanvasZone.tsx
@src/components/CanvasDraggableCard.tsx
@src/components/HandZone.tsx
@src/components/SpreadZone.tsx
@src/components/DraggableCard.tsx
@src/spikes/Spike004MultiCardDrop.tsx
@src/lib/canvas-utils.ts

<interfaces>
<!-- Plan 01 already exported SelectionSource from src/shared/types.ts. -->
<!-- Existing inline forms must be replaced with imports from '@/shared/types'. -->

From src/shared/types.ts (created in Plan 01):
```typescript
export type SelectionSource =
  | { zone: 'hand' | 'pile'; zoneId: string; hasMaskedCards?: boolean }
  | { zone: 'canvas'; zoneId: 'canvas' }
  | null;
```

Existing consumers of inline SelectionSource form (must be updated):
- src/components/BoardDragLayer.tsx — local `type SelectionSource = ...` declaration around line 64
- src/components/BoardView.tsx line 23 — `selectionSource: { zone: 'hand' | 'pile'; ... } | null` prop type
- src/components/HandZone.tsx line 122 — same form on prop type
- src/components/SpreadZone.tsx line 78 — same form on prop type

Spike 004 selection ring pattern (src/spikes/Spike004MultiCardDrop.tsx):
```typescript
boxShadow: isSelected
  ? '0 0 0 2px #60a5fa, 0 0 0 4px rgba(96,165,250,0.3)' + (coversAnother ? `, ${STACK_SHADOW}` : '')
  : coversAnother ? STACK_SHADOW : undefined
```

HandZone count badge pattern (HandZone.tsx line 217):
```typescript
{selectedIds.size >= 2 && selectionSource?.zone === 'hand' && selectionSource.zoneId === playerId && (
  <span className="...">{selectedIds.size} selected</span>
)}
```

Existing handleToggleSelect signature in BoardDragLayer.tsx (line ~98-120):
```typescript
const handleToggleSelect = (id: string, zone: 'hand' | 'pile', zoneId: string) => { ... }
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user pointer events → React handlers | DOM events; pointer events on draggable cards must not accidentally toggle global selection state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Tampering | CanvasDraggableCard click handler | accept | Click toggle is local UI state; no server impact. Mitigation: `e.stopPropagation()` per D-05 so card click doesn't reach canvas background deselect handler |
| T-34-06 | Information Disclosure | data-card-id attribute | accept | card.id is already in the DOM via React keys and aria-labels; data-card-id surfaces no new information |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Switch all SelectionSource consumers to shared import; add canvas variant handlers in BoardDragLayer</name>
  <files>src/components/BoardDragLayer.tsx, src/components/BoardView.tsx, src/components/HandZone.tsx, src/components/SpreadZone.tsx</files>
  <read_first>
    - src/shared/types.ts (verify `export type SelectionSource` from Plan 01)
    - src/components/BoardDragLayer.tsx (full file — local SelectionSource type, handleToggleSelect, selectionSource state, handleDragStart, handleDragEnd)
    - src/components/BoardView.tsx (prop chain: lines 20-30 props interface, line 93 CanvasZone render call, lines 100-130 HandZone/SpreadZone render)
    - src/components/HandZone.tsx lines 118-128 + line 217 (count badge pattern)
    - src/components/SpreadZone.tsx lines 75-85 + line 157 (count badge pattern)
  </read_first>
  <action>
    In `src/components/BoardDragLayer.tsx`:
    1. Delete the local `type SelectionSource = ...` declaration; add `import type { SelectionSource } from '@/shared/types';` to the imports.
    2. The existing `useState<SelectionSource>(null)` declaration is unchanged in shape but now uses the imported type.
    3. Add a new handler `handleToggleSelectCanvas(id: string)` next to the existing `handleToggleSelect`. Logic per D-01/D-03/D-04:
       - If `selectionSource !== null && selectionSource.zone !== 'canvas'`: set `selectionSource` to `{ zone: 'canvas', zoneId: 'canvas' }` and replace `selectedIds` with `new Set([id])` (zone-exclusive switch).
       - Otherwise (already canvas or null): if `selectedIds` contains `id`, remove it; else add it. If `selectionSource` was null, set it to `{ zone: 'canvas', zoneId: 'canvas' }`.
       - When toggling reduces `selectedIds` to size 0, set `selectionSource` back to `null`.
    4. Add a new handler `handleDeselectAll()` per D-05: `setSelectedIds(new Set()); setSelectionSource(null);`.
    5. Compute a `groupIds: Set<string>` value (useMemo) following Spike 004 line 118-123 pattern: if `activeCard` is null, return empty set; else return `new Set([...selectedIds, activeCard.id])`. This is consumed by Plan 03 for passenger rendering; expose it as state-derived value reachable by BoardView.
    6. Add a `dragDelta` state next to existing `dragDeltaRef` (per RESEARCH.md Pitfall 1): `const [dragDelta, setDragDelta] = useState<{ x: number; y: number } | null>(null);`. Initialize to null. Do NOT yet update this state in handleDragMove (Plan 03 does that). Reset to null at end of all existing successful drop branches in handleDragEnd.
    7. Pass new values to `<BoardView>`: `onToggleSelectCanvas={handleToggleSelectCanvas}`, `onDeselectAll={handleDeselectAll}`, `groupIds={groupIds}`, `activeCardId={activeCard?.id ?? null}`, `dragDelta={dragDelta}`.

    In `src/components/BoardView.tsx`:
    1. Replace the inline `selectionSource: { zone: 'hand' | 'pile'; zoneId: string } | null;` prop type with `selectionSource: SelectionSource;` and add `import type { SelectionSource } from '@/shared/types';`.
    2. Add new props to the `BoardViewProps` interface: `onToggleSelectCanvas: (id: string) => void`, `onDeselectAll: () => void`, `groupIds: Set<string>`, `activeCardId: string | null`, `dragDelta: { x: number; y: number } | null`.
    3. Destructure them in the function signature.
    4. Forward to `<CanvasZone>`: `selectedIds={selectedIds}`, `groupIds={groupIds}`, `activeCardId={activeCardId}`, `dragDelta={dragDelta}`, `onToggleSelectCanvas={onToggleSelectCanvas}`, `onDeselectAll={onDeselectAll}`.

    In `src/components/HandZone.tsx`:
    1. Replace the inline `selectionSource: { zone: 'hand' | 'pile'; zoneId: string } | null;` prop type with `selectionSource: SelectionSource;` and `import type { SelectionSource } from '@/shared/types';`.
    2. The existing equality check `selectionSource?.zone === 'hand'` (line ~217) remains valid because `SelectionSource` still contains the `'hand'` variant — no logic change.

    In `src/components/SpreadZone.tsx`:
    1. Same replacement as HandZone: inline type → imported `SelectionSource` from `@/shared/types`.
    2. Existing `selectionSource?.zoneId === pile.id` check on line 157 remains valid; the `.zone === 'pile'` discriminant still works.

    Do NOT yet edit `CanvasZone.tsx`, `CanvasDraggableCard.tsx`, `DraggableCard.tsx`, or `BoardDragLayer.handleDragStart/Move/End` group-drop dispatch logic. Those are later tasks.

    Note on signature collision: the existing `handleToggleSelect(id, zone, zoneId)` for hand/pile must stay untouched. Canvas gets its own dedicated handler `handleToggleSelectCanvas(id)` to avoid widening the existing 3-arg signature. Both handlers manipulate the same `selectedIds`/`selectionSource` state.
  </action>
  <acceptance_criteria>
    - `grep -c "import type { SelectionSource }" src/components/BoardDragLayer.tsx src/components/BoardView.tsx src/components/HandZone.tsx src/components/SpreadZone.tsx` returns 4 (one per file)
    - `grep -c "type SelectionSource = " src/components/BoardDragLayer.tsx` returns 0 (local type removed)
    - `grep -c "{ zone: 'hand' | 'pile'; zoneId: string }" src/components/BoardView.tsx src/components/HandZone.tsx src/components/SpreadZone.tsx` returns 0 (all inline forms replaced)
    - `grep -n "handleToggleSelectCanvas" src/components/BoardDragLayer.tsx` returns at least 2 matches (declaration + prop pass)
    - `grep -n "handleDeselectAll" src/components/BoardDragLayer.tsx` returns at least 2 matches
    - `grep -n "groupIds" src/components/BoardDragLayer.tsx` returns at least 1 match (useMemo declaration)
    - `npm run typecheck` exits 0
    - `npm test` exits 0 (no behavior change to existing flows; selection canvas not yet wired into card click — that's Task 2)
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <done>
    All four consumers import `SelectionSource` from `@/shared/types`. BoardDragLayer has `handleToggleSelectCanvas`, `handleDeselectAll`, `groupIds` useMemo, and parallel `dragDelta` state declared (not yet updated). BoardView forwards all five new props to CanvasZone (which doesn't yet use them — Task 2 wires that). HandZone and SpreadZone unchanged behaviorally. TypeScript clean. Full suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire CanvasDraggableCard click toggle, ring style, isPassenger opacity, data-card-id</name>
  <files>src/components/CanvasDraggableCard.tsx</files>
  <read_first>
    - src/components/CanvasDraggableCard.tsx (full file — current handleClick is no-op, current style block lines 44-54)
    - src/spikes/Spike004MultiCardDrop.tsx lines 55-102 (validated DraggableCard inner component with selection ring)
    - .planning/phases/34-multi-card-group-drop/34-UI-SPEC.md (Interaction States section — exact boxShadow values for selected and passenger states)
    - src/lib/canvas-utils.ts (STACK_SHADOW constant)
  </read_first>
  <action>
    Update `src/components/CanvasDraggableCard.tsx`:

    1. Extend `CanvasDraggableCardProps`:
       - `isSelected?: boolean` (default false)
       - `isPassenger?: boolean` (default false; D-17: true when card is in active drag group but not the handle)
       - `onToggleSelect?: (id: string) => void`

    2. Destructure new props in the function signature.

    3. Update `handleClick` to accept `e: React.MouseEvent`, call `e.stopPropagation()` first (D-05: prevents canvas background deselect from firing), then preserve the existing `didDragRef` guard, then call `onToggleSelect?.(canvasCard.card.id)` instead of being a no-op.

    4. Update the `style` object:
       - `opacity`: `(isDragging || isPassenger) ? 0 : 1` (passengers also invisible — D-17)
       - `boxShadow`: if `isSelected`, use `'0 0 0 2px #60a5fa, 0 0 0 4px rgba(96,165,250,0.3)'` and append `, ${STACK_SHADOW}` when `coversAnother` is true. If not selected, fall back to existing `coversAnother ? STACK_SHADOW : undefined`.
       - `borderRadius`: `6` when `isSelected || coversAnother`, else `undefined`.

    5. Add `data-card-id={canvasCard.card.id}` attribute to the root `<div>` (alongside existing `aria-roledescription`, `aria-label`). This is the DOM hook Plan 03 uses to query positions via `document.querySelector('[data-card-id="..."]')`.

    6. Update `aria-pressed`: set to `isSelected` (boolean) instead of `undefined`, placed AFTER the `{...attributes}` spread per CLAUDE.md convention.

    Do NOT change the existing `didDragRef` useEffect block (lines 21-33). Do NOT change `useDraggable` config. Do NOT change the `CardFace`/`CardBack` ternary at line 68.
  </action>
  <acceptance_criteria>
    - `grep -n "isSelected" src/components/CanvasDraggableCard.tsx` returns matches in props interface + destructure + style
    - `grep -n "isPassenger" src/components/CanvasDraggableCard.tsx` returns matches in props interface + destructure + opacity expression
    - `grep -n "onToggleSelect" src/components/CanvasDraggableCard.tsx` returns matches in props + destructure + handleClick body
    - `grep -n "data-card-id" src/components/CanvasDraggableCard.tsx` returns exactly 1 match on the root div
    - `grep -n "e.stopPropagation" src/components/CanvasDraggableCard.tsx` returns at least 1 match in handleClick
    - `grep -n "60a5fa" src/components/CanvasDraggableCard.tsx` returns exactly 1 match (selection ring color)
    - `grep -n "aria-pressed={isSelected}" src/components/CanvasDraggableCard.tsx` returns 1 match, ordered AFTER `{...attributes}`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    `CanvasDraggableCard` accepts `isSelected`, `isPassenger`, `onToggleSelect` props. Click handler toggles selection via callback with `stopPropagation` guard. Selection ring renders via boxShadow matching UI-SPEC. Passenger source cards set opacity:0. `data-card-id` attribute present. `aria-pressed` mirrors `isSelected`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire CanvasZone props, deselect-all background click, selection count badge</name>
  <files>src/components/CanvasZone.tsx</files>
  <read_first>
    - src/components/CanvasZone.tsx (full file — current container div, CanvasDraggableCard render call)
    - src/components/CanvasDraggableCard.tsx (props interface from Task 2)
    - src/spikes/Spike004MultiCardDrop.tsx lines 229-269 (validated canvas container with deselect onClick)
    - src/components/HandZone.tsx line 217 (count badge pattern reference)
    - .planning/phases/34-multi-card-group-drop/34-UI-SPEC.md (Selection count badge section — exact className and conditions)
  </read_first>
  <action>
    Update `src/components/CanvasZone.tsx`:

    1. Extend `CanvasZoneProps`:
       - `selectedIds: Set<string>`
       - `groupIds: Set<string>`
       - `activeCardId: string | null`
       - `dragDelta: { x: number; y: number } | null` (declared now; passenger ghost rendering is added in Plan 03)
       - `onToggleSelectCanvas: (id: string) => void`
       - `onDeselectAll: () => void`

    2. Destructure all six new props.

    3. Add `onClick={onDeselectAll}` to the root container `<div>`. D-05: when a card is clicked, the card's own `stopPropagation` (added in Task 2) prevents this from firing.

    4. In the `<CanvasDraggableCard>` render call, pass:
       - `isSelected={selectedIds.has(cc.card.id)}`
       - `isPassenger={groupIds.has(cc.card.id) && cc.card.id !== activeCardId}`
       - `onToggleSelect={onToggleSelectCanvas}`

    5. Add the selection count badge overlay inside the container div (before the `canvasCards.map(...)` block, or as a sibling at the top). Visibility condition per UI-SPEC: render only when `selectedIds.size >= 2`. The badge JSX:
       ```
       <span
         data-testid="canvas-selection-count"
         className="absolute top-2 left-2 z-10 text-xs bg-primary text-primary-foreground rounded-full px-1.5 py-0.5"
       >
         {selectedIds.size} selected
       </span>
       ```
       Positioned absolutely top-left of canvas at zIndex 10.

    6. The `coveringIds` useMemo (lines 24-32) is unchanged.

    Do NOT yet render passenger ghost divs — `dragDelta` is plumbed through as a prop but consumed in Plan 03. Plan 03 will add the `passengerGhosts` useMemo and JSX block.
  </action>
  <acceptance_criteria>
    - `grep -n "onDeselectAll" src/components/CanvasZone.tsx` returns matches in props + destructure + onClick handler
    - `grep -n "onToggleSelectCanvas" src/components/CanvasZone.tsx` returns matches in props + destructure + CanvasDraggableCard prop
    - `grep -n "selectedIds" src/components/CanvasZone.tsx` returns matches in props + isSelected expression + badge condition
    - `grep -n "groupIds" src/components/CanvasZone.tsx` returns matches in props + isPassenger expression
    - `grep -n "canvas-selection-count" src/components/CanvasZone.tsx` returns exactly 1 match (the badge data-testid)
    - `grep -n "selectedIds.size >= 2" src/components/CanvasZone.tsx` returns 1 match (badge visibility condition)
    - `npm run typecheck` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <done>
    `CanvasZone` accepts new selection props, forwards them to `CanvasDraggableCard`, registers `onClick={onDeselectAll}` on the container, renders the count badge when ≥2 cards selected. Build green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add data-card-id to hand DraggableCard and SpreadZone card elements</name>
  <files>src/components/DraggableCard.tsx, src/components/SpreadZone.tsx</files>
  <read_first>
    - src/components/DraggableCard.tsx (full file — locate the root draggable div, identify where `card.id` is in scope)
    - src/components/SpreadZone.tsx (full file — locate the card element rendered inside the SortableContext mapping)
    - .planning/phases/34-multi-card-group-drop/34-RESEARCH.md Pitfall 3 (data-card-id requirement)
  </read_first>
  <action>
    In `src/components/DraggableCard.tsx`: Add `data-card-id={card.id}` to the root `<div>` of the draggable card (the same div that carries `{...listeners}` / `{...attributes}` / `ref={setNodeRef}`). If multiple wrappers exist, place it on the outermost interactive div the user actually grabs — the one Plan 03 will read via `document.querySelector('[data-card-id="..."]').getBoundingClientRect()`.

    In `src/components/SpreadZone.tsx`: Locate the rendering of each card inside the SortableContext (look for the sortable item mapping — usually `pile.cards.map((card) => <SortableSpreadCard ... />)` or similar). Add `data-card-id={card.id}` to the root draggable div of that card element (the one with `useSortable`'s `setNodeRef`/`listeners`). If the card is rendered as a nested component (e.g. `<SortableSpreadCard>`), add the attribute inside that component's root div.

    Do NOT add `data-card-id` to pile-back placeholder divs, empty-state UI, or zone container divs. Only the actual draggable card element per card in the rendered list.

    Do NOT change any other behavior. No imports needed (DOM attribute is intrinsic).
  </action>
  <acceptance_criteria>
    - `grep -c "data-card-id" src/components/DraggableCard.tsx` returns exactly 1
    - `grep -c "data-card-id" src/components/SpreadZone.tsx` returns exactly 1 (on the per-card draggable element)
    - In both files, the attribute value references `card.id` (the per-iteration card identifier)
    - `npm run typecheck` exits 0
    - `npm test` exits 0
    - `npm run test:e2e` (if local dev servers running) exits 0 — adding a DOM attribute should not regress any e2e selector
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <done>
    `data-card-id` attribute present on the root draggable div of hand DraggableCard and SpreadZone per-card element. Same attribute already present on CanvasDraggableCard (Task 2). Plan 03 can now `document.querySelector('[data-card-id="..."]')` for any card on the board.
  </done>
</task>

</tasks>

<verification>
- `npm run typecheck` clean
- `npm test` full Vitest suite green (no behavior regression to existing flows)
- Manual: in dev mode, click a canvas card → ring appears; click again → ring disappears; click second canvas card → both rings; badge "2 selected" visible top-left; click empty canvas → all deselect
- Manual: click a hand card while a canvas selection is active → canvas deselects, hand selection appears (zone-exclusive invariant)
- `grep -c "data-card-id" src/components/{CanvasDraggableCard,DraggableCard,SpreadZone}.tsx` returns 3 total
</verification>

<success_criteria>
- MULTI-01 selection mechanics fully functional: click-to-toggle on canvas, zone-exclusive against hand/spread, deselect-all on empty canvas, count badge at ≥2
- Plan 03 can immediately consume `groupIds`, `activeCardId`, `dragDelta` from BoardDragLayer → BoardView → CanvasZone prop chain
- `data-card-id` present on all three draggable card components for Plan 03 DOM offset capture
- Zero changes to server, zero changes to test files
- TypeScript clean; full unit test suite green
</success_criteria>

<output>
Create `.planning/phases/34-multi-card-group-drop/34-02-SUMMARY.md` when done.
</output>
