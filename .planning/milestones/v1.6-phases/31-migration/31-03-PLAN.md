---
phase: 31-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 31-02
files_modified: []
autonomous: false
requirements:
  - MIGRATE-02
  - MIGRATE-03
tags:
  - migration
  - visual-verify
  - human-checkpoint

must_haves:
  truths:
    - "At 1280x720 desktop, draw and discard piles render in a left sidebar, vertically stacked (draw above discard), aligned to the top of the middle band"
    - "At 1280x720 desktop, the canvas shell occupies the remaining horizontal space to the right of the sidebar with no decoration, no placeholder text, no dashed border"
    - "At 1280x720 desktop, a 1px muted vertical line separates the sidebar from the canvas"
    - "At 375x667 (mobile), both pile zones remain visible at their natural width; the sidebar is not collapsed or hidden; the canvas occupies the remaining width"
    - "Reset table action moves all draw/discard/hand cards back into the draw pile and shuffles (existing MIGRATE-03 behavior; no canvas cards exist yet so MIGRATE-03 is vacuously satisfied at this phase)"
    - "Existing opponent hand row, opponent spread row, personal spread zone, and hand zone are unchanged from Phase 30"
  artifacts: []
  key_links: []
---

<objective>
Verify the Phase 31 sidebar+canvas migration visually against the locked UI-SPEC contract.
This is the human-verification gate that closes MIGRATE-02 (no automated DOM check can
prove that the sidebar "looks right" at the contracted spacing, border tokens, and
felt color) and reconfirms MIGRATE-03 (reset table behavior with the new pile set).

Purpose: Catch any visual regression from the layout restructure before phase close, and
explicitly approve the empty canvas as the foundation for Phase 32.

Output: Operator approval (or a list of issues to gap-close).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-migration/31-CONTEXT.md
@.planning/phases/31-migration/31-UI-SPEC.md
@.planning/phases/31-migration/31-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human visual verification of sidebar + canvas at 1280x720 and 375x667</name>
  <files>(none — verification task)</files>
  <read_first>
    - .planning/phases/31-migration/31-UI-SPEC.md (full file — the binding visual contract being verified)
    - .planning/phases/31-migration/31-CONTEXT.md §"Decisions" (D-01 through D-12 — each must be observable)
    - .planning/phases/31-migration/31-VALIDATION.md §"Manual-Only Verifications"
  </read_first>
  <what-built>
    Plans 01 and 02 removed the communal grid from server, types, and client. The middle
    band of `BoardView.tsx` is now a fixed left sidebar containing the draw and discard
    PileZones (vertically stacked) and a free canvas shell occupying the remaining
    horizontal space. No GridZone renders anywhere. No grid-related code paths exist in
    server or client. The Playwright suite is realigned.
  </what-built>
  <action>
    Pause and present the verification checklist below to the operator. Do not proceed
    until the operator types "approved" or itemizes blocking issues. If the operator
    reports issues, capture each numbered step that failed in the SUMMARY and recommend
    `/gsd:plan-phase 31 --gaps` to close them. If approved, recommend `/gsd:verify-work 31`.
  </action>
  <how-to-verify>
    Start the local stack:
    1. In terminal A: `npm run dev` (PartyKit server on :1999).
    2. In terminal B: `npm run dev:client` (Vite frontend on :5173).
    3. Open http://localhost:5173 in a browser. Create or join a room.

    Desktop verification (1280x720 viewport, two-player room):
    4. Confirm the middle band of the board has a vertical sidebar on the LEFT containing
       exactly two pile zones: "Draw" on top, "Discard" below.
    5. Confirm the sidebar piles are vertically stacked with a small gap between them
       (the spec is 8px, gap-2; visually a clear, consistent space — not flush, not large).
    6. Confirm the sidebar has a thin (1px) right-edge divider in a muted color — visible
       but not visually loud, matching other border-color usage on the board.
    7. Confirm the sidebar does NOT have a different background color than the rest of
       the board (D-04: 1px border only, no background contrast).
    8. Confirm the area to the right of the sidebar is EMPTY felt — same green/dark color
       as the rest of the board background, NO placeholder text, NO dashed border,
       NO grid lines, NO decorations of any kind (D-05).
    9. Confirm the canvas area fills the full vertical height of the middle band (the
       canvas extends from top to bottom of the band, same height as the sidebar).
    10. Confirm the canvas area fills the full horizontal width remaining to the right
        of the sidebar (no gap between sidebar's right edge and canvas's left edge other
        than the 1px divider).
    11. Confirm the sidebar piles align to the TOP of the middle band (D-11), not the
        center — there should be visible empty canvas space below the discard pile.
    12. Confirm opponent hands (top), opponent spreads (below opponent hands), personal
        spread (below the middle band), and player hand (bottom) are all rendered
        exactly as in Phase 30 — unchanged (D-10).

    Mobile verification (375x667 viewport):
    13. Resize the browser to 375x667 (Chrome DevTools device toolbar or window resize).
    14. Confirm both pile zones in the sidebar are STILL VISIBLE at their natural width
        (the sidebar does NOT collapse, hide, or shrink) (D-03).
    15. Confirm the canvas area still occupies the remaining horizontal space to the
        right of the sidebar, even if narrow (no horizontal scrollbar on the board).
    16. Confirm the canvas is still empty felt with no decoration.

    Functional verification (reset table — MIGRATE-03):
    17. From the desktop viewport, click the controls hamburger and deal cards.
    18. Drag a card from your hand to the discard pile.
    19. Click reset table. Confirm all cards return to the draw pile and the draw pile
        appears face-down (no card faces visible) — this is the existing MIGRATE-03
        behavior preserved through the migration.

    Server clean state verification:
    20. Open the browser devtools network tab, filter on WS, and confirm the STATE_UPDATE
        payloads after reset contain only `draw` and `discard` piles plus `spread-{playerId}`
        personal spread piles. There should be NO pile with `id: "play"` in any payload.

    Report any visual or functional issues. If everything matches the spec, approve.
  </how-to-verify>
  <verify>
    <human-check>Operator types "approved" after walking through all 20 checks above. If issues are reported, they are captured by numbered step in the SUMMARY for `/gsd:plan-phase 31 --gaps` to address.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator confirms desktop visual matches all 12 desktop checks above
    - Operator confirms mobile visual matches all 3 mobile checks above (D-03)
    - Operator confirms reset table sweeps all cards back to the draw pile (MIGRATE-03)
    - Operator confirms STATE_UPDATE payloads do not contain a 'play' pile (cross-check on Plan 01's server change)
    - Operator types "approved" or itemizes specific issues
  </acceptance_criteria>
  <resume-signal>
    Type "approved" if all checks pass. Otherwise, describe the issue(s) by referencing
    the numbered step(s) above. If issues exist, `/gsd:plan-phase --gaps` to close gaps;
    otherwise `/gsd:verify-work 31` to close out the phase.
  </resume-signal>
  <done>
    Operator approval is recorded in `31-03-SUMMARY.md`. Either the phase is ready for
    `/gsd:verify-work 31`, or specific numbered checks are flagged for gap closure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator pointer → live board | Visual verification is the trust boundary; automated DOM checks cannot prove visual correctness of color tokens, spacing, or empty-state appearance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-08 | Tampering | Visual contract drift | mitigate | UI-SPEC.md numbered checks pin every D-decision to an observable visual; operator approval is recorded in SUMMARY |
| T-31-09 | Repudiation | Operator approval audit trail | mitigate | Resume signal stored in plan SUMMARY; gsd-transition records the checkpoint outcome |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- All 20 numbered checks pass under operator inspection
- Operator types "approved"
</verification>

<success_criteria>
- MIGRATE-02 visually verified at both 1280x720 and 375x667
- MIGRATE-03 functionally verified via reset-table behavior
- Phase 31 is ready for `/gsd:verify-work 31`
</success_criteria>

<output>
Create `.planning/phases/31-migration/31-03-SUMMARY.md` when done, recording:
- Operator approval status (approved | issues-list)
- Which numbered checks passed
- Any issues itemized for gap closure
- Browser + viewport sizes actually used
</output>
