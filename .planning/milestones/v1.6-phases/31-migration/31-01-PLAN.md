---
phase: 31-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/gridRemoval.test.ts
  - tests/gridMove.test.ts
  - tests/gridZoneFaceToggle.test.ts
  - tests/resetTable.test.ts
  - tests/moveCard.test.ts
  - tests/playCardSet.test.ts
  - tests/selectAll.test.ts
  - tests/reorderUndo.test.ts
  - tests/boardDragLayerDialog.test.ts
  - tests/spreadZoneCreation.test.ts
  - src/shared/types.ts
  - party/index.ts
autonomous: true
requirements:
  - MIGRATE-01
  - MIGRATE-03
tags:
  - migration
  - server
  - types
  - tests

must_haves:
  truths:
    - "Server defaultGameState contains only draw + discard piles (no 'play' pile)"
    - "MOVE_GRID_CARD case is absent from server onMessage switch"
    - "ClientAction type union has no MOVE_GRID_CARD member"
    - "Pile and ClientPile types have no gridPositions field"
    - "MOVE_CARD and PLAY_CARD_SET ClientAction members have no toRow/toCol fields"
    - "Full vitest suite passes (npm test exits 0) after all changes"
    - "TypeScript compiles clean (npm run typecheck exits 0) after all changes"
    - "RESET_TABLE still sweeps non-draw piles into draw (existing behavior preserved with new pile set)"
  artifacts:
    - path: "tests/gridRemoval.test.ts"
      provides: "Migration regression guard: assertions on absence of 'play' pile and graceful handling of unknown MOVE_GRID_CARD action"
      min_lines: 30
    - path: "party/index.ts"
      provides: "Server with all grid-specific code removed: defaultGameState, MOVE_GRID_CARD handler, MOVE_CARD/PLAY_CARD_SET grid blocks, onStart play-pile migration guards, viewFor gridPositions field"
      contains: "id: \"draw\""
    - path: "src/shared/types.ts"
      provides: "Shared types with grid surface removed from Pile, ClientPile, ClientAction"
      contains: "export type ClientAction"
  key_links:
    - from: "tests/gridRemoval.test.ts"
      to: "party/index.ts defaultGameState"
      via: "imported defaultGameState seeded piles checked for absence of 'play'"
      pattern: "piles.find\\(p => p.id === \"play\"\\)"
    - from: "party/index.ts viewFor"
      to: "ClientPile shape in src/shared/types.ts"
      via: "the gridPositions field must be removed from both sites in lockstep to keep TypeScript clean"
      pattern: "gridPositions"
---

<objective>
Remove all server-side and shared-type surface area for the communal grid in a single atomic
change. The pre-commit hook runs `npm test` and `npm run typecheck`; any partial removal
(server-only or types-only) leaves the test suite broken because numerous existing tests
reference `id: "play"` from the seeded defaultGameState. This plan therefore bundles:
(a) a new Wave 0 RED test pinning the contract, (b) deletion of obsolete tests that exercise
the removed behavior, (c) repair of lateral tests that incidentally seed `'play'`, (d) the
server cleanup, and (e) the type cleanup — all so the post-Task commit is green.

Purpose: Land MIGRATE-01 server/type surface in one atomic, GREEN commit. MIGRATE-03 is
vacuously satisfied because the existing RESET_TABLE handler already sweeps every non-draw
pile into draw — removing the 'play' pile means there is no canvas pile to sweep at this
phase (Phase 32 introduces the canvas card x/y/z model). The existing RESET_TABLE test
continues to pass after the 'play' fixture line is removed.

Output: Server + shared-types in their post-migration shape; the existing test suite
realigned to the new pile set; one new regression test (`gridRemoval.test.ts`) that fails
loudly if a future change reintroduces the `'play'` pile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-migration/31-CONTEXT.md
@.planning/phases/31-migration/31-RESEARCH.md
@.planning/phases/31-migration/31-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from src/shared/types.ts. -->

Current Pile shape (src/shared/types.ts:18–26):

```typescript
export interface Pile {
  id: string;
  name: string;
  cards: Card[];
  faceUp?: boolean;
  region?: "pile" | "spread";
  ownerId?: string | null;
  gridPositions?: Record<string, { row: number; col: number }>; // Phase 24: play grid  ← REMOVE
}
```

Current ClientPile shape (src/shared/types.ts:30–38):

```typescript
export interface ClientPile {
  id: string;
  name: string;
  cards: (Card | MaskedCard)[];
  faceUp?: boolean;
  region?: "pile" | "spread";
  ownerId?: string | null;
  gridPositions?: Record<string, { row: number; col: number }>; // Phase 24  ← REMOVE
}
```

Current ClientAction union (src/shared/types.ts:63–78). To remove:
- `MOVE_GRID_CARD` member entirely (line 74)
- `toRow?: number; toCol?: number` from `MOVE_CARD` (line 64)
- `toRow?: number; toCol?: number` from `PLAY_CARD_SET` (line 72)

Target post-cleanup MOVE_CARD signature:

```typescript
| { type: "MOVE_CARD"; cardId: string; fromZone: "hand" | "pile"; fromId: string;
    toZone: "hand" | "pile"; toId: string; insertPosition?: 'top' | 'bottom' | 'random' }
```

Target post-cleanup PLAY_CARD_SET signature:

```typescript
| { type: "PLAY_CARD_SET"; cardIds: string[]; fromZone?: "hand" | "pile"; fromId: string;
    toZone: "pile" | "hand"; toId: string }
```

Current defaultGameState seeded piles (party/index.ts:46–50). Target post-cleanup:

```typescript
piles: [
  { id: "draw", name: "Draw", cards: buildDeck(), faceUp: false, region: "pile", ownerId: null },
  { id: "discard", name: "Discard", cards: [], faceUp: true, region: "pile", ownerId: null },
],
```

Existing RESET_TABLE sweep logic (party/index.ts:579–601) is preserved unchanged. The
`if (pile.gridPositions) pile.gridPositions = {};` line at 589 is removed in lockstep with
the type field removal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Create gridRemoval RED regression test</name>
  <files>tests/gridRemoval.test.ts</files>
  <read_first>
    - tests/resetTable.test.ts (use as scaffolding template — same imports, same makeMockRoom/makeMockConnection helpers; this is the closest existing analog so the new test matches established conventions)
    - party/index.ts lines 40–53 (defaultGameState — the unit under test)
    - src/shared/types.ts lines 63–78 (ClientAction union — for the unknown-action behavior test)
    - .planning/phases/31-migration/31-RESEARCH.md §"Wave 0 Gaps" (test specification source)
    - .planning/phases/31-migration/31-VALIDATION.md (sampling/runtime expectations)
  </read_first>
  <behavior>
    - Test A: `defaultGameState(roomId)` returns a state whose `piles` array contains exactly two entries, both `region: "pile"`, with ids `"draw"` and `"discard"` — and no entry with id `"play"`.
    - Test B: Sending an action with `type: "MOVE_GRID_CARD"` to `room.onMessage` does not throw; the server gracefully ignores it (the switch statement has no default case, and after this migration there is no MOVE_GRID_CARD case either — the switch falls through silently). No state mutation occurs; no `gameState.piles` entry is added or modified.
    - Test C: After the migration, `Pile` and `ClientPile` types have no `gridPositions` field — encoded as a type-level assertion using a `// @ts-expect-error` comment on `const p: Pile = { ..., gridPositions: {} }` so a future re-add of the field fails the type check loudly.
  </behavior>
  <action>
    Create `tests/gridRemoval.test.ts` modeled on the structure of `tests/resetTable.test.ts`:
    - Import `GameRoom`, `defaultGameState` from `../party/index`; import `Pile`, `ClientAction` from `../src/shared/types`; reuse the `makeMockRoom` and `makeMockConnection` helpers verbatim (copy them from resetTable.test.ts — there is no shared helper export for them).
    - `describe("Phase 31 — grid removal regression", ...)` with three `it` blocks matching the three behaviors above.
    - Test A asserts `state.piles.map(p => p.id).sort()` equals `["discard", "draw"]` and `state.piles.find(p => p.id === "play")` is `undefined`.
    - Test B builds a room, sends `JSON.stringify({ type: "MOVE_GRID_CARD", cardId: "A-s", pileId: "play", toRow: 0, toCol: 0 })`, awaits onMessage, asserts no throw and that `room.gameState.piles` is unchanged (same ids, same lengths).
    - Test C uses `// @ts-expect-error gridPositions removed in Phase 31` immediately above `const p: Pile = { id: "x", name: "x", cards: [], gridPositions: {} };` so the compile-time check is enforced.
    - This file is created in this task BEFORE the type/server changes are committed. Test A and C will FAIL on commit because `defaultGameState` still seeds 'play' and `Pile.gridPositions` still exists. The pre-commit hook will block the commit until Task 2 lands the GREEN flip.
    - Therefore: do NOT commit Task 1 in isolation. Run it locally with `npx vitest run tests/gridRemoval.test.ts` to confirm Test A fails RED and Test C produces a TS error, then proceed immediately to Task 2 in the same session and commit both as one atomic change.
  </action>
  <verify>
    <automated>npx vitest run tests/gridRemoval.test.ts 2>&amp;1 | grep -E "(FAIL|RED|expected|received|gridRemoval)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/gridRemoval.test.ts` exists
    - File contains literal string `describe("Phase 31 — grid removal regression"`
    - File contains literal string `id === "play"` (the assertion proving the 'play' pile is gone)
    - File contains literal string `// @ts-expect-error`
    - File contains literal string `type: "MOVE_GRID_CARD"` (the unknown-action test)
    - Running `npx vitest run tests/gridRemoval.test.ts` reports a failing test referencing the 'play' pile assertion (proving the RED state — this is expected at end of Task 1 and is fixed by Task 2)
  </acceptance_criteria>
  <done>
    `tests/gridRemoval.test.ts` exists with the three specified `it` blocks. Test A is observably RED against the current `defaultGameState`. Do not commit until Task 2 completes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atomic server + types + test-suite GREEN flip</name>
  <files>
    party/index.ts,
    src/shared/types.ts,
    tests/gridMove.test.ts,
    tests/gridZoneFaceToggle.test.ts,
    tests/resetTable.test.ts,
    tests/moveCard.test.ts,
    tests/playCardSet.test.ts,
    tests/selectAll.test.ts,
    tests/reorderUndo.test.ts,
    tests/boardDragLayerDialog.test.ts,
    tests/spreadZoneCreation.test.ts
  </files>
  <read_first>
    - party/index.ts lines 40–53 (defaultGameState)
    - party/index.ts lines 117–181 (onStart and migration guards — lines 140–179 to be removed per D-08, D-09)
    - party/index.ts lines 84–100 (viewFor — remove `gridPositions: pile.gridPositions` line)
    - party/index.ts lines 326–348 (MOVE_CARD grid block to remove — D-09)
    - party/index.ts lines 400–426 (MOVE_GRID_CARD case to delete entirely — D-09)
    - party/index.ts lines 575–601 (RESET_TABLE handler — leave structure; remove only the `if (pile.gridPositions) pile.gridPositions = {};` line at 589)
    - party/index.ts lines 613–766 (PLAY_CARD_SET — remove grid block at 736–755 and the `srcPile.gridPositions` cleanup at 757–763)
    - src/shared/types.ts (full file — only ~84 lines)
    - tests/gridMove.test.ts (entire file — DELETED)
    - tests/gridZoneFaceToggle.test.ts (entire file — DELETED)
    - tests/resetTable.test.ts (lines 101–117 — the `'play'` pile test block to be removed; remaining tests stay)
    - tests/moveCard.test.ts lines 95–155 and 270–295 (tests that drop into `toId: "play"` — these test the deleted behavior and must be deleted)
    - tests/playCardSet.test.ts (multiple `toId: "play"` cases — each test asserting cards land in the play pile must be removed because the destination no longer exists)
    - tests/selectAll.test.ts lines 60–105 (uses `"play"` as a pile id via `makeStateWithPileCards("player-1", "play", ...)` — change pile id to `"spread-player-1"` so the test exercises the personal spread zone instead)
    - tests/reorderUndo.test.ts lines 14–45 (uses `'play'` for REORDER_PILE_SPREAD — change to `spread-player-1`)
    - tests/boardDragLayerDialog.test.ts lines 395–445 (logic helper tests reference `id: "play"` literals — change to `id: "spread-player-1"`)
    - tests/spreadZoneCreation.test.ts lines 25–155 (onStart migration tests reference 'play' pile behavior — these tests assert the OLD migration guards; since D-08 removes the guards, delete the test cases that exercise the play-pile-specific migration code paths; keep the spread-zone-creation tests for `spread-{playerId}` which are still valid)
    - tests/helpers.ts (makeStateWithPileCards signature)
    - .planning/phases/31-migration/31-RESEARCH.md §"Pattern 2: Server-Side Grid Code Removal" and §"Code Examples"
  </read_first>
  <action>
    Perform all of the following as one atomic change. Commit only when the final
    `npm test` and `npm run typecheck` both pass.

    Server (party/index.ts) — per D-07, D-08, D-09:
    - In `defaultGameState`, delete the third pile entry `{ id: "play", name: "Play Area", ... }`. Final piles array: `[draw, discard]` only.
    - In `onStart`, delete the entire block of migration guards from the `// Migrate state: Phase 14-GAP04` comment through the `// Migrate state: Phase 24` block (currently lines 140–180 inclusive). Keep the earlier migrations for `undoSnapshots`, `displayName`, `region`/`ownerId`, and `handRevealed`.
    - In `viewFor`, delete the line `gridPositions: pile.gridPositions, // Phase 24: Pitfall 4 — must be explicit` (currently line 90). The ClientPile mapping must contain only fields that exist on the new ClientPile type.
    - In the `MOVE_CARD` case, delete the entire `// Assign grid position when card enters the play grid (D-06)` block (currently lines 326–343) AND the `// Clean up gridPositions when card leaves a pile (Pitfall 2)` block (currently lines 344–348).
    - Delete the entire `case "MOVE_GRID_CARD":` block (currently lines 400–426).
    - In `RESET_TABLE`, delete only the inline `if (pile.gridPositions) pile.gridPositions = {};` clause inside the for-loop (currently line 589). The rest of the sweep loop is unchanged.
    - In `PLAY_CARD_SET`, delete the `// Assign grid positions when cards land in the play grid` block (currently lines 736–755) AND the `// Clean up gridPositions when cards leave a pile (Pitfall 2)` block (currently lines 756–764). Note: the `MOVE_ALL_PILE_CARDS` case at line 783 also has `if (srcPile.gridPositions) srcPile.gridPositions = {};` — delete that line too.

    Shared types (src/shared/types.ts) — per D-09 + Pitfall 1, 2:
    - In `Pile` interface, delete the `gridPositions?: Record<string, { row: number; col: number }>;` field (currently line 25).
    - In `ClientPile` interface, delete the same field (currently line 37).
    - In `ClientAction` union: from `MOVE_CARD` (line 64) remove the trailing `; toRow?: number; toCol?: number`. From `PLAY_CARD_SET` (line 72) remove the trailing `; toRow?: number; toCol?: number`. Delete the entire `MOVE_GRID_CARD` member (line 74).

    Test cleanup — delete files that test deleted behavior:
    - `git rm tests/gridMove.test.ts` — all 7 tests exercise MOVE_GRID_CARD handler behavior.
    - `git rm tests/gridZoneFaceToggle.test.ts` — all 6 tests exercise GridZone component face-up state which goes away with the component (component itself deleted in Plan 02).

    Test cleanup — repair tests that incidentally reference `'play'` pile:
    - `tests/resetTable.test.ts`: Delete the `it("clears cards from spread zones without removing the zone itself ...")` block at lines 101–117. The comment in the test header (`GAP-04: 'play' pile is now the communal spread zone`) is stale and the test asserts the 'play' pile persists post-reset — both untrue after this migration. RESET_TABLE behavior for the remaining piles (draw, discard, personal spread zones) is exercised by the other tests in the file.
    - `tests/moveCard.test.ts`: Delete every `it` block whose action sets `toId: "play"`. Use grep `toId: "play"` to find all of them (approximately lines 95–155 and 270–295). These tests exercise MOVE_CARD's grid-position assignment, which is removed. Surrounding tests that do not reference 'play' stay untouched.
    - `tests/playCardSet.test.ts`: Delete every `it` block whose action sets `toId: "play"` (approximately lines 38–215 across multiple blocks). PLAY_CARD_SET into a personal spread zone (`spread-{playerId}`) is still covered by other tests in this file.
    - `tests/selectAll.test.ts`: In each `it` block that calls `makeStateWithPileCards("player-1", "play", ...)`, change the second argument from `"play"` to `"spread-player-1"` and update every subsequent assertion (`fromId: "play"`, `find(p => p.id === "play")`) to match `"spread-player-1"`. The test then exercises the personal spread zone, which is functionally equivalent for the select-all behavior under test.
    - `tests/reorderUndo.test.ts`: Same pattern — replace `'play'` with `'spread-player-1'` everywhere it appears as a pile id. Update the `beforeEach` to push cards into the personal spread pile (which is created by `onConnect` for `player-1`); seed `room.gameState` so a personal spread pile with id `spread-player-1` exists with `region: "spread"`.
    - `tests/boardDragLayerDialog.test.ts`: In each test fixture that hardcodes `[{ id: "play", cards: [...], region: "spread" }, ...]`, change `"play"` to `"spread-player-1"`. The assertions on `handlePileDrop` behavior are pile-id-agnostic except for the literal id string passed in.
    - `tests/spreadZoneCreation.test.ts`: This file currently has multiple `it` blocks that assert the `'play'` pile is seeded by `defaultGameState` or that the onStart migration guards convert old 'play' piles to `region: "spread"`. Delete every `it` block that asserts the existence or migration of the 'play' pile (approximately lines 25–80 and 140–155 — verify with grep `id === "play"` in this file). Keep all `it` blocks that test creation of `spread-{playerId}` personal spread zones in `onConnect`.

    Constraint discharge:
    - Run `npm run typecheck` after the type changes — it will catch any stale reference to `gridPositions` or `toRow/toCol` anywhere in the repo. Fix every error reported. Common offenders: imports in test files, mock fixtures, and the existing client-side `BoardDragLayer.tsx` and `GridZone.tsx` (those are out of scope here — Plan 02 — but typecheck errors against them are expected; if pre-commit blocks, scope test runs with `npm test` not `npm run typecheck` for this commit, then fix client in Plan 02). Reconcile: the pre-commit hook runs BOTH `npm test` and `npm run typecheck`. So Plan 02 client cleanup must be in the same wave… No — re-reading the hook script, if the client cleanup is required for typecheck to pass, then we have a circular dependency.

    Mitigation for the typecheck circular dependency:
    - The client files `BoardView.tsx`, `BoardDragLayer.tsx`, and `GridZone.tsx` reference the types being removed. Removing the types here breaks the client compile. Therefore: in THIS task, also stub-fix the client surface just enough to compile — but do NOT do the layout restructure (that is Plan 02). Specifically:
      1. In `src/components/GridZone.tsx`: comment out the file body and replace with `export function GridZone(): null { return null; }`. Leave the file present so existing imports in BoardView still resolve until Plan 02 deletes the file. Remove all internal references to `gridPositions`, `MOVE_GRID_CARD`, `toRow`, `toCol` from this stub.
      2. In `src/components/BoardDragLayer.tsx`: delete the `gridCellContainers` declaration (lines 18–20), remove `!String(c.id).startsWith('grid-cell-')` from the `cardContainers` filter (line 22), delete the `gridCollisions` block (lines 41–43), remove the `gridOverDataMulti` destructuring and the `...(gridOverDataMulti?.toRow !== undefined ? ... : {})` spread (lines 234, 244), remove the `gridOverData` destructuring and the `...(gridOverData?.toRow !== undefined ? ... : {})` spread (lines 300, 309), and remove the `else if (selectedIds.size > 1 && selectionSource?.zoneId === 'play')` branch and its body (lines 311–321). This eliminates all type references to `toRow/toCol` and `MOVE_GRID_CARD`.
      3. In `src/components/BoardView.tsx`: leave the file untouched in this task except for one minimal change — guard the `communalZone` derivation so it does not crash when the 'play' pile is gone: change `const communalZone = spreadPiles.find(p => p.id === 'play');` to `const communalZone = undefined;` (single-line replacement). The `{communalZone && (...)}` block becomes dead code, which is fine — Plan 02 restructures the band and deletes both `communalZone` and the GridZone import.

    Final verification (still in Task 2):
    - Run `npm run typecheck` — must exit 0.
    - Run `npm test` — must exit 0. All deleted-test files are gone (`git rm` confirmed via `git status`); all repaired tests pass; `tests/gridRemoval.test.ts` now flips GREEN because Test A's `find(p.id === "play")` returns `undefined`.
    - Commit as one atomic change. Commit message: `refactor(31): remove communal grid from server, types, and tests (MIGRATE-01)`
  </action>
  <verify>
    <automated>npm test 2>&amp;1 | tail -40 &amp;&amp; npm run typecheck 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files tests/gridMove.test.ts` returns empty (file deleted)
    - `git ls-files tests/gridZoneFaceToggle.test.ts` returns empty (file deleted)
    - `grep -c "gridPositions" src/shared/types.ts` returns 0
    - `grep -c "MOVE_GRID_CARD" src/shared/types.ts` returns 0
    - `grep -c "toRow" src/shared/types.ts` returns 0
    - `grep -c "MOVE_GRID_CARD" party/index.ts` returns 0
    - `grep -c "gridPositions" party/index.ts` returns 0
    - `grep -n "id: \"play\"" party/index.ts` returns no matches
    - `grep -nE "id === [\"']play[\"']" tests/*.ts | grep -v gridRemoval.test.ts` returns no matches
    - `npm run typecheck` exits 0
    - `npm test` exits 0
    - `tests/gridRemoval.test.ts` Test A (no 'play' pile in defaultGameState) passes
    - `tests/gridRemoval.test.ts` Test C (`@ts-expect-error` on `gridPositions`) passes (the directive does not error, meaning the type field truly is gone)
    - `git diff --stat HEAD~1` (after the atomic commit) shows the files listed in the `<files>` block above and no others outside the scope of Plan 01 (BoardView/BoardDragLayer/GridZone touched only with the minimal stub fixes described in `<action>`)
  </acceptance_criteria>
  <done>
    The atomic commit lands with `npm test` and `npm run typecheck` both green.
    `defaultGameState` returns only draw + discard piles. `MOVE_GRID_CARD` is removed
    from both the type union and the server switch. `gridPositions` does not appear
    in any non-test source file. The existing test suite is realigned and the
    new regression test passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PartyKit room | Untrusted ClientAction JSON arrives via `onMessage`; existing validation surface is reduced (one fewer action type) — net attack surface decreases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | `defaultGameState` initial piles | mitigate | `tests/gridRemoval.test.ts` Test A asserts exactly `["draw","discard"]` after migration; future re-add of the 'play' pile breaks the test loudly |
| T-31-02 | Denial of Service | `onMessage` unknown action handling | mitigate | `tests/gridRemoval.test.ts` Test B confirms an unknown `MOVE_GRID_CARD` action falls through the switch without throwing; server remains responsive |
| T-31-03 | Information Disclosure | `viewFor` ClientPile mapping | accept | Removing `gridPositions` from the mapping cannot leak data — the field is being deleted, not exposed. No new exposure surface. |
| T-31-04 | Repudiation | Atomic commit boundary | accept | Single commit captures all server + type + test changes; git log preserves authorship |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan (RESEARCH.md §"Package Legitimacy Audit": none); no slopcheck gate required |
</threat_model>

<verification>
- `npm test` exits 0
- `npm run typecheck` exits 0
- `tests/gridRemoval.test.ts` exists and is green
- `tests/gridMove.test.ts` and `tests/gridZoneFaceToggle.test.ts` are deleted
- No `'play'` pile id appears in any non-gridRemoval test file (proven by grep)
- No `gridPositions`, `toRow`, `toCol`, or `MOVE_GRID_CARD` appears in `party/index.ts` or `src/shared/types.ts`
</verification>

<success_criteria>
- MIGRATE-01 server/type surface: COMPLETE (all grid code paths deleted from server and types)
- MIGRATE-03 RESET_TABLE behavior: PRESERVED (sweep loop still drains every non-draw pile into draw; no canvas pile exists yet, so MIGRATE-03 is vacuously satisfied at this phase — Phase 32 introduces canvas cards with x/y/z)
- Atomic commit lands green (pre-commit hook does not block)
- Test suite realignment is complete; future Plan 02 client work can proceed without re-touching the test layer
</success_criteria>

<output>
Create `.planning/phases/31-migration/31-01-SUMMARY.md` when done, summarizing:
- Exact lines deleted from `party/index.ts` (cite line ranges)
- Exact fields deleted from `src/shared/types.ts`
- Tests deleted and tests repaired (list each)
- Confirmation that `tests/gridRemoval.test.ts` is green
- Any unexpected typecheck or test failure encountered and how it was resolved
</output>
