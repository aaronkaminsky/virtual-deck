---
phase: 31-migration
plan: 06
type: execute
wave: 5
depends_on:
  - 31-04
  - 31-05
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - MIGRATE-02
tags:
  - migration
  - visual-verify
  - human-checkpoint
  - gap-closure

must_haves:
  truths:
    - "Dragging a card from the discard pile downward does NOT activate the hand drop zone (no border-t-2 border-primary highlight) until the pointer is inside the visible hand strip"
    - "The sidebar spans the full height of the middle band with the two piles vertically centered (equal empty space above draw pile and below discard pile)"
    - "SpreadZone populated slot has symmetric vertical padding matching horizontal padding"
    - "All previously-approved checks from Plan 03 (checks 1–3, 5–10, 12–19) remain unbroken"
  artifacts: []
  key_links: []
---

<objective>
Re-verify the three gap-closure items fixed in Plans 04 and 05, and confirm no regression
in the 19 checks previously approved by the operator in Plan 03.

This is the final human-verification gate before phase close. On approval, the phase
is ready for `/gsd:verify-work 31`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-migration/31-CONTEXT.md
@.planning/phases/31-migration/31-UI-SPEC.md
@.planning/phases/31-migration/31-03-SUMMARY.md
@.planning/phases/31-migration/31-04-PLAN.md
@.planning/phases/31-migration/31-05-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-verify three gap-closure items and confirm no regression</name>
  <files>(none — verification task)</files>
  <read_first>
    - .planning/phases/31-migration/31-03-SUMMARY.md §"Issues Captured for Gap Closure" (the 3 gaps being re-checked)
    - .planning/phases/31-migration/31-04-PLAN.md §"must_haves" (GAP-02, GAP-03 success criteria)
    - .planning/phases/31-migration/31-05-PLAN.md §"must_haves" (GAP-01 success criteria)
    - .planning/phases/31-migration/31-UI-SPEC.md §"Layout Contract" (binding visual contract)
  </read_first>
  <what-built>
    Plan 04 fixed GAP-02 (symmetric py-2 vertical padding on SpreadZone populated slots)
    and GAP-03 (sidebar self-stretch + justify-center so piles span and center within the
    full middle-band height). Plan 05 fixed GAP-01 (hand zone isOver tightened to
    over.id === 'hand' only, eliminating early activation when dragging from discard).
  </what-built>
  <action>
    Pause and present the verification checklist below to the operator. Do not proceed
    until the operator types "approved" or itemizes any remaining issues.
  </action>
  <how-to-verify>
    Start the local stack:
    1. In terminal A: `npm run dev` (PartyKit server on :1999).
    2. In terminal B: `npm run dev:client` (Vite frontend on :5173).
    3. Open http://localhost:5173 in a browser. Create or join a room and deal cards.

    GAP-01 re-check (collision detection — 1280x720 viewport, two-player room):
    G1. With a card in the discard pile, start dragging it downward slowly. Confirm the
        player hand strip does NOT show the amber top border highlight until the pointer
        VISUALLY ENTERS the hand strip (the bg-card strip at the bottom of the board).
        A tiny downward move from the discard should NOT light up the hand zone.
    G2. Continue the drag into the hand strip. Confirm the highlight DOES appear once
        the pointer is inside the visible strip, and dropping releases the card into the hand.
    G3. Drag a card within the hand sideways (intra-hand reorder). Confirm it still works
        and the hand highlight does not appear spuriously.

    GAP-02 re-check (SpreadZone padding — 1280x720 viewport):
    G4. With cards in a personal spread zone, inspect the lighter-background slot.
        Confirm there is visible vertical breathing room above and below the card inside
        the slot, matching the horizontal breathing room on the left and right.
        The padding should appear roughly equal on all four sides.

    GAP-03 re-check (sidebar centering — 1280x720 viewport):
    G5. With no player action (fresh game state), observe the sidebar. Confirm the sidebar
        column extends the full height of the middle band — from the bottom of the
        opponent-spread row to the top of the personal-spread row.
    G6. Confirm the two pile zones (Draw on top, Discard below) appear vertically CENTERED
        within the sidebar — there should be equal empty space above the Draw pile and
        below the Discard pile inside the sidebar column.

    Regression re-checks (sample from the previously-approved Plan 03 checks):
    R1. Sidebar has a thin (1px) right-edge muted divider; no background color contrast (D-04).
    R2. Canvas area is empty felt with no placeholder text, no dashed border, no grid (D-05).
    R3. Canvas fills full vertical height and full horizontal width to the right of the sidebar.
    R4. At 375x667 (mobile), sidebar piles remain visible; canvas gets remaining width (D-03).
    R5. Reset table sweeps all cards back to the draw pile (MIGRATE-03).

    Report any visual or functional issues. If everything matches, approve.
  </how-to-verify>
  <verify>
    <human-check>Operator types "approved" after walking through G1–G6 and R1–R5. If any item fails, capture it in the SUMMARY with the gap number and description.</human-check>
  </verify>
  <acceptance_criteria>
    - GAP-01: Hand zone does not highlight until pointer enters the visible hand strip (G1 passes, G2 passes, G3 passes)
    - GAP-02: SpreadZone populated slot has symmetric four-side padding (G4 passes)
    - GAP-03: Sidebar extends full middle-band height with piles vertically centered (G5, G6 pass)
    - Regression: R1–R5 all pass (no regression from Plan 04/05 changes)
    - Operator types "approved" or itemizes remaining issues
  </acceptance_criteria>
  <resume-signal>
    Type "approved" if all checks pass. Otherwise describe which check(s) failed.
    If issues remain, `/gsd:plan-phase 31 --gaps` to add another closure round;
    if approved, `/gsd:verify-work 31` to close the phase.
  </resume-signal>
  <done>
    Operator approval is recorded in `31-06-SUMMARY.md`. Phase 31 is ready for
    `/gsd:verify-work 31`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator pointer → live board | Visual verification is the trust boundary; GAP-01/02/03 fixes cannot be fully confirmed by automated assertion alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-15 | Tampering | Visual contract drift | mitigate | Plan 06 numbered checks pin the exact observable behavior; operator approval recorded in SUMMARY |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- GAP-01, GAP-02, GAP-03 all pass human inspection
- Regression checks R1–R5 all pass
- Operator types "approved"
</verification>

<success_criteria>
- All three gap-closure items verified closed
- No regression from Plans 04 and 05
- Phase 31 ready for `/gsd:verify-work 31`
</success_criteria>

<output>
Create `.planning/phases/31-migration/31-06-SUMMARY.md` when done, recording:
- Result of each G-check and R-check
- Operator approval status
- Any remaining issues and what to do about them
</output>
