---
phase: 35-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright/mobile.spec.ts
  - src/components/CanvasZone.tsx
  - src/components/BoardDragLayer.tsx
  - src/components/BoardView.tsx
autonomous: true
requirements:
  - MOBILE-01
  - MOBILE-02
  - MOBILE-03
tags:
  - mobile
  - canvas
  - edge-pan
  - dnd-kit
  - responsive

must_haves:
  truths:
    - "An edge arrow appears in a direction when canvas content overflows in that direction"
    - "No edge arrow appears when canvas content does not overflow in that direction"
    - "Holding an edge arrow continuously pans the canvas viewport (PAN_STEP=8px, PAN_INTERVAL=16ms)"
    - "Releasing the edge arrow (pointerup or pointerleave) stops panning immediately"
    - "Pressing an edge arrow does not register as a dnd-kit drag start"
    - "At viewport width <640px, canvas height is capped so spread zone and hand zone remain visible without page scroll at 375×667"
    - "Cards dropped from hand/pile onto a panned canvas land at the correct canvas-space coordinates (scroll offset applied)"
    - "Cards dropped from canvas to canvas while panned land at correct positions without double-counting scroll offset"
  artifacts:
    - path: "playwright/mobile.spec.ts"
      provides: "Wave-0 RED tests for MOBILE-01, MOBILE-02, MOBILE-03"
      contains: "Phase 35 mobile edge pan"
    - path: "src/components/CanvasZone.tsx"
      provides: "Two-div viewport+canvas structure, EdgeArrow x4, pan state, ResizeObserver, scrollOffsetRef write"
      contains: "EdgeArrow"
    - path: "src/components/BoardDragLayer.tsx"
      provides: "scrollOffsetRef declared and passed down; scroll offset added to hand/pile→canvas drop math (single + group paths)"
      contains: "scrollOffsetRef"
    - path: "src/components/BoardView.tsx"
      provides: "max-h-[Xpx] sm:max-h-none wrapper for CanvasZone; scrollOffsetRef prop forwarded"
      contains: "scrollOffsetRef"
  key_links:
    - from: "src/components/CanvasZone.tsx"
      to: "src/components/BoardDragLayer.tsx"
      via: "scrollOffsetRef.current = scroll inside useEffect; same ref instance read in handleDragEnd"
      pattern: "scrollOffsetRef\\.current = scroll"
    - from: "src/components/CanvasZone.tsx"
      to: "@dnd-kit/core useDroppable"
      via: "setRefs (setNodeRef + canvasRef) attached to INNER canvas div, not outer viewport div"
      pattern: "ref=\\{setRefs\\}"
    - from: "src/components/BoardView.tsx"
      to: "src/components/CanvasZone.tsx"
      via: "scrollOffsetRef prop pass-through"
      pattern: "scrollOffsetRef=\\{scrollOffsetRef\\}"
---

<objective>
Implement Phase 35 Mobile by transplanting the validated Spike003 edge-pan architecture into production: refactor CanvasZone to a two-div (outer viewport + inner canvas) model with four EdgeArrow components, dynamic inner canvas bounds derived from canvasCards positions, a setInterval-based pan loop, and a scrollOffsetRef shared with BoardDragLayer so hand/pile→canvas drop coordinates remain correct while the canvas is panned. Apply a Tailwind max-h cap at <640px on the canvas wrapper. Wave-0 establishes failing Playwright tests first.

Purpose: Make the free canvas play area usable on narrow viewports (375px and up) without one-finger drags conflicting with viewport panning. Satisfies MOBILE-01 (arrow visibility + hold-to-pan), MOBILE-02 (drag/pan non-conflict), MOBILE-03 (height cap).
Output: 4 modified files (3 source, 1 new test); arrows visible on overflow; hold-to-pan working; one-finger card drag works; spread+hand visible at 375×667 height.
</objective>

<execution_context>
@$HOME/code/dev-setup/claude/get-shit-done/workflows/execute-plan.md
@$HOME/code/dev-setup/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-mobile/35-CONTEXT.md
@.planning/phases/35-mobile/35-RESEARCH.md
@.planning/phases/35-mobile/35-PATTERNS.md
@.planning/phases/35-mobile/35-UI-SPEC.md
@.planning/phases/35-mobile/35-VALIDATION.md
@src/spikes/Spike003EdgePan.tsx
@src/components/CanvasZone.tsx
@src/components/BoardDragLayer.tsx
@src/components/BoardView.tsx
@src/lib/canvas-utils.ts
@playwright/responsive.spec.ts
@playwright/fixtures.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From `src/lib/canvas-utils.ts`:
- `export const CARD_W = 63;`
- `export const CARD_H = 88;`
- `export function getCardDimensions(): { w: number; h: number }` — returns `{w:42,h:59}` at `window.innerWidth < 640`, else `{w:63,h:88}`
- `export function coversMajority(top, bottom): boolean`

From `src/components/CanvasZone.tsx` (current props interface):
```
interface CanvasZoneProps {
  canvasCards: ClientCanvasCard[];
  canvasRef: React.RefObject<HTMLDivElement | null>;
  selectedIds: Set<string>;
  groupIds: Set<string>;
  activeCardId: string | null;
  dragDelta: { x: number; y: number } | null;
  onToggleSelectCanvas: (id: string) => void;
  onDeselectAll: () => void;
}
```
After this plan adds: `scrollOffsetRef: React.MutableRefObject<{ x: number; y: number }>;`

From `src/components/BoardDragLayer.tsx` (existing refs, around line 86–96):
- `isDraggingRef`, `dragDeltaRef`, `passengerOffsetsRef`, `dropSuccessRef`, `topButtonRef`, `snapBackTimerRef`, `canvasRef` — all `useRef` pattern
- New ref to add: `const scrollOffsetRef = useRef<{ x: number; y: number }>({ x: 0, y: 0 });`

From `src/components/BoardView.tsx` (current canvas usage, line 98):
```
<CanvasZone canvasCards={...} canvasRef={canvasRef} selectedIds={...} groupIds={...} activeCardId={...} dragDelta={...} onToggleSelectCanvas={...} onDeselectAll={...} />
```
After this plan: append `scrollOffsetRef={scrollOffsetRef}` and wrap the entire `CanvasZone` in a `div` with `className="max-h-[240px] sm:max-h-none flex-1 min-w-0 self-stretch flex"`.

From `src/spikes/Spike003EdgePan.tsx` (production source — extract):
- `type PanDir = 'left' | 'right' | 'up' | 'down';`
- `EdgeArrow` component (lines 90–136): props `{ dir, visible, onPanStart, onPanEnd }`
- `PAN_STEP = 8`, `PAN_INTERVAL = 16` (constants)
- ResizeObserver pattern (lines 150–158)
- `stopPan` / `startPan` callbacks (lines 167–186)
- Glyph: `‹` with rotation map `{ left: '0deg', right: '180deg', up: '90deg', down: '270deg' }`

From `playwright/responsive.spec.ts`:
- `test.use({ viewport: { width: 375, height: 667 } })` pattern
- `page.evaluate(() => document.documentElement.scrollWidth/clientWidth)` overflow probe

From `playwright/fixtures.ts`:
- `test` / `expect` from `./fixtures`; `twoPlayerRoom` fixture providing `{ p1, p2, roomCode }` with two separate `BrowserContext`s
- Single-player tests should use plain `@playwright/test` import (mirroring responsive.spec.ts) — Phase 35 tests are single-player

dnd-kit drag simulation (from CLAUDE.md):
- Use `mouse.move/down/move/up steps:15`, NOT `dragAndDrop()` — HTML5 drag events are ignored by dnd-kit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): RED Playwright tests for MOBILE-01, MOBILE-02, MOBILE-03</name>
  <files>playwright/mobile.spec.ts</files>
  <read_first>
    - playwright/mobile.spec.ts (verify file does not yet exist; create new)
    - playwright/responsive.spec.ts (analog: viewport override + overflow probe pattern)
    - playwright/fixtures.ts (single-player tests use plain `@playwright/test`; mirror responsive.spec.ts)
    - playwright/game.spec.ts (dealCards helper pattern; mouse.move/down/move/up steps:15 drag pattern)
    - src/components/CanvasZone.tsx (current `data-testid="canvas-zone"` on outer wrapper)
    - .planning/phases/35-mobile/35-PATTERNS.md (lines 359–404 — full test scaffold pattern)
    - .planning/phases/35-mobile/35-CONTEXT.md (D-06 overflow conditions, D-07 PAN_STEP/PAN_INTERVAL)
  </read_first>
  <action>
    Create `playwright/mobile.spec.ts` as a NEW file containing five Playwright tests under `test.describe('Phase 35 mobile edge pan', () => { test.use({ viewport: { width: 375, height: 667 } }); ... })`. All tests run single-player using `@playwright/test` direct import + `nanoid` for room code, mirroring the structure of `playwright/responsive.spec.ts`. Add `data-testid` attributes that the implementation tasks (Tasks 2–3) MUST honor:
    - `data-testid="edge-arrow-left"`, `data-testid="edge-arrow-right"`, `data-testid="edge-arrow-up"`, `data-testid="edge-arrow-down"` on each EdgeArrow root div
    - `data-testid="canvas-inner"` on the inner canvas (transformed) div

    Test 1 — "MOBILE-01: edge arrows hidden when canvas has no overflow": join room as "MobileTest", do NOT deal cards (canvas empty = no overflow). Assert `await expect(page.locator('[data-testid="edge-arrow-right"]')).toHaveCount(0)`. Repeat for left/up/down. Per D-06 — empty canvas → no overflow → no arrows.

    Test 2 — "MOBILE-01: edge arrows appear when canvas content overflows": join + deal 5 cards via the existing dealCards helper pattern from game.spec.ts (open controls popover, fill `input[type="number"]`, click Deal button). Drag a hand card via `mouse.move`/`down`/`move(target, { steps: 15 })`/`up` to a canvas position near the right edge (target x ~ 350 within 375px viewport). After drop, the placed card's x + cardW (42 at <640px) will exceed viewportW, causing the right arrow to appear. Assert `await expect(page.locator('[data-testid="edge-arrow-right"]')).toBeVisible()`. Then drag another card to a y position near the bottom of the canvas band (target y exceeds the capped canvas height). Assert `await expect(page.locator('[data-testid="edge-arrow-down"]')).toBeVisible()`.

    Test 3 — "MOBILE-01: holding the right arrow pans the canvas (transform changes)": set up canvas overflow as in Test 2. Read inner canvas computed transform: `const before = await page.locator('[data-testid="canvas-inner"]').evaluate(el => getComputedStyle(el).transform)`. Locate `[data-testid="edge-arrow-right"]`, call `boundingBox()`, then `page.mouse.move(box.x + box.width/2, box.y + box.height/2)`, `page.mouse.down()`, `await page.waitForTimeout(120)` (~7 pan ticks × 8px = 56px), `page.mouse.up()`. Read transform again: `const after = ...`. Assert `before !== after` AND `after.includes('matrix')` AND `after` contains a more-negative translateX than `before`. (Matrix string contains the translate values; you can parse `matrix(1, 0, 0, 1, tx, ty)` to confirm `tx` decreased.)

    Test 4 — "MOBILE-02: pressing edge arrow does not trigger card drag": deal cards, place a canvas card to create overflow (per Test 2), then press the right arrow region without releasing for 100ms, then release. Assert `await page.locator('[data-testid="canvas-selection-count"]').count()` returns 0 (the selection badge only appears when `selectedIds.size >= 2` per CanvasZone.tsx line 61; pressing the arrow MUST NOT cause card selection or drag activation).

    Test 5 — "MOBILE-03: at 375×667 viewport, hand zone remains visible without scroll": join + deal. Assert `await expect(page.getByTestId('hand-zone')).toBeVisible()`. Get hand zone bounding box: `const handBox = await page.getByTestId('hand-zone').boundingBox()`. Assert `handBox.y + handBox.height <= 667` (entire hand zone fits within viewport height). Also assert `await page.evaluate(() => document.documentElement.scrollHeight) <= await page.evaluate(() => document.documentElement.clientHeight)` (no vertical page scroll required to reach hand). Mirror the `scrollWidth <= clientWidth` probe from responsive.spec.ts but for height.

    These tests MUST fail RED before implementation. Do NOT add `test.skip()`. The arrow `data-testid` selectors and `canvas-inner` testid will not match anything until Task 2 renders them.
  </action>
  <verify>
    <automated>npx playwright test playwright/mobile.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `playwright/mobile.spec.ts` exists
    - `grep -c "test.describe.*Phase 35 mobile edge pan" playwright/mobile.spec.ts` returns ≥ 1
    - `grep -c "test.use.*viewport.*375.*667" playwright/mobile.spec.ts` returns ≥ 1
    - `grep -c "edge-arrow-right\|edge-arrow-left\|edge-arrow-up\|edge-arrow-down" playwright/mobile.spec.ts` returns ≥ 4
    - `grep -c "canvas-inner" playwright/mobile.spec.ts` returns ≥ 1
    - `grep -c "MOBILE-01\|MOBILE-02\|MOBILE-03" playwright/mobile.spec.ts` returns ≥ 3
    - Tests fail RED on first run (locators for `edge-arrow-*` and `canvas-inner` do not yet match anything in production code)
    - `grep -c "test.skip(" playwright/mobile.spec.ts` returns 0
  </acceptance_criteria>
  <done>
    playwright/mobile.spec.ts exists with 5 tests covering MOBILE-01 (arrow visibility, hold-to-pan), MOBILE-02 (no drag conflict), MOBILE-03 (height fit at 375×667). All tests fail RED on initial run because the production code does not yet render `edge-arrow-*` testids or `canvas-inner` testid.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor CanvasZone to two-div + EdgeArrow + pan state + scroll plumbing</name>
  <files>src/components/CanvasZone.tsx</files>
  <read_first>
    - src/components/CanvasZone.tsx (current single-div structure, lines 1–97; setRefs pattern lines 26–29; selection count badge line 61; passenger ghosts lines 79–93)
    - src/spikes/Spike003EdgePan.tsx (lines 81–136 EdgeArrow; lines 144–158 viewportRef+ResizeObserver; lines 167–189 stopPan/startPan; line 26 PAN_STEP=8; line 27 PAN_INTERVAL=16)
    - src/lib/canvas-utils.ts (getCardDimensions, CARD_W, CARD_H)
    - .planning/phases/35-mobile/35-PATTERNS.md (lines 28–257 — full transplant pattern with all required edits)
    - .planning/phases/35-mobile/35-CONTEXT.md (D-02 inner canvas size formula; D-03 two-div structure; D-06 hasOverflow conditions; D-07 PAN_STEP/PAN_INTERVAL values; D-08 stopPropagation requirement; D-09 styling)
    - .planning/phases/35-mobile/35-UI-SPEC.md (Interaction Contract: EdgeArrow visibility, pan speed, glyph rotation map; Aria labels per direction)
  </read_first>
  <action>
    Refactor `src/components/CanvasZone.tsx` to the two-div panning model. Concrete changes:

    1. Update imports: extend the existing `import { useMemo } from 'react'` to `import { useState, useRef, useEffect, useMemo, useCallback } from 'react'`. Add `import { getCardDimensions } from '@/lib/canvas-utils';` (alongside the existing `coversMajority` import).

    2. Add module-level constants at top of file (after imports, before component):
       - `PAN_STEP = 8`
       - `PAN_INTERVAL = 16`
       - `CANVAS_PADDING = 48`

    3. Add `type PanDir = 'left' | 'right' | 'up' | 'down';` at module level.

    4. Add `EdgeArrow` as a module-level function component (NOT a separate file) with props `{ dir, visible, onPanStart, onPanEnd }`. Transplant the full implementation from `src/spikes/Spike003EdgePan.tsx` lines 90–136 verbatim with TWO additions: (a) add `data-testid={\`edge-arrow-${dir}\`}` on the root div; (b) add `aria-label` per direction per UI-SPEC section "Aria label for EdgeArrow" — exactly `Pan canvas left` / `Pan canvas right` / `Pan canvas up` / `Pan canvas down`. Use the `‹` glyph (single base) for all four directions; rotation map exactly `{ left: '0deg', right: '180deg', up: '90deg', down: '270deg' }`. Do NOT use `›`. Styling: width 32 height 80 for left/right; width 80 height 32 for up/down; background `rgba(255,255,255,0.15)`; backdropFilter `blur(4px)`; borderRadius 6; touchAction `'none'`; zIndex 1000. `onPointerDown` calls `e.stopPropagation()` then `onPanStart(dir)`; `onPointerUp` and `onPointerLeave` both call `onPanEnd`. Return null when `!visible`.

    5. Extend `CanvasZoneProps`: add `scrollOffsetRef: React.MutableRefObject<{ x: number; y: number }>;` after `dragDelta`.

    6. Inside `CanvasZone` function body, after the existing `useDroppable` call, ADD:
       - `const viewportRef = useRef<HTMLDivElement>(null);`
       - `const panIntervalRef = useRef<ReturnType<typeof setInterval> | null>(null);`
       - `const [scroll, setScroll] = useState({ x: 0, y: 0 });`
       - `const [viewportSize, setViewportSize] = useState({ w: 0, h: 0 });`

    7. Add ResizeObserver effect (mirror spike lines 150–158): observe `viewportRef.current`, call `setViewportSize({ w: el.clientWidth, h: el.clientHeight })` on resize, disconnect on unmount.

    8. Add scroll → scrollOffsetRef sync effect: `useEffect(() => { scrollOffsetRef.current = scroll; }, [scroll, scrollOffsetRef])`.

    9. Add inner canvas size computation as a `useMemo`:
       - Call `getCardDimensions()` inside the memo to derive `cardW`, `cardH`.
       - If `canvasCards.length === 0`: return `{ innerW: viewportSize.w, innerH: viewportSize.h }`.
       - Else: compute `maxX = Math.max(...canvasCards.map(c => c.x + cardW))`, `maxY = Math.max(...canvasCards.map(c => c.y + cardH))`. Return `{ innerW: Math.max(viewportSize.w, maxX + CANVAS_PADDING), innerH: Math.max(viewportSize.h, maxY + CANVAS_PADDING) }`.
       - Dependencies: `[canvasCards, viewportSize.w, viewportSize.h]`.

    10. Add `hasOverflow` derivation (not useMemo — cheap object literal, recomputed each render):
        - `hasOverflow = { left: scroll.x > 0, right: scroll.x < innerW - viewportSize.w, up: scroll.y > 0, down: scroll.y < innerH - viewportSize.h }`

    11. Add `stopPan` and `startPan` callbacks:
        - `stopPan = useCallback(() => { if (panIntervalRef.current) { clearInterval(panIntervalRef.current); panIntervalRef.current = null; } }, []);`
        - `startPan = useCallback((dir: PanDir) => { stopPan(); panIntervalRef.current = setInterval(() => { setScroll(prev => { const dx = dir === 'left' ? -PAN_STEP : dir === 'right' ? PAN_STEP : 0; const dy = dir === 'up' ? -PAN_STEP : dir === 'down' ? PAN_STEP : 0; return { x: Math.max(0, Math.min(innerW - viewportSize.w, prev.x + dx)), y: Math.max(0, Math.min(innerH - viewportSize.h, prev.y + dy)) }; }); }, PAN_INTERVAL); }, [stopPan, viewportSize, innerW, innerH]);`
        - CRITICAL: `innerW` AND `innerH` MUST appear in `startPan`'s dep array — the spike uses constants but here the inner canvas is dynamic. Omitting them causes stale closure (Pitfall 4 in RESEARCH.md).

    12. Add cleanup effect: `useEffect(() => () => stopPan(), [stopPan]);`

    13. Restructure the JSX return: replace the existing single `<div ref={setRefs} ...>` with:
        - OUTER `<div ref={viewportRef} aria-label="Play area" data-testid="canvas-zone" onClick={onDeselectAll} className={cn('relative flex-1 min-w-0 self-stretch overflow-hidden bg-background', isOver && 'ring-1 ring-primary/30')}>`
        - INNER `<div ref={setRefs} data-testid="canvas-inner" style={{ position: 'absolute', width: innerW, height: innerH, transform: \`translate(${-scroll.x}px, ${-scroll.y}px)\` }}>`
        - Move ALL existing inner content (selection count badge, `canvasCards.map` rendering `CanvasDraggableCard`, `passengerGhosts.map`) inside the INNER div, unchanged.
        - After the inner div, BEFORE closing the outer div, render four `EdgeArrow` components: `<EdgeArrow dir="left" visible={hasOverflow.left} onPanStart={startPan} onPanEnd={stopPan} />` and the same for right, up, down. Each MUST receive `onPanEnd={stopPan}`.

    14. Confirm `setRefs` (the existing dual-ref function combining `setNodeRef` + `canvasRef`) attaches to the INNER canvas div — NOT the outer viewport div. This is critical: `BoardDragLayer.handleDragEnd` calls `canvasRef.current?.getBoundingClientRect()` and expects inner-canvas bounds for clamping (Pitfall 2 in RESEARCH.md).

    15. Do NOT introduce a separate `src/components/EdgeArrow.tsx` file — UI-SPEC permits inline. Define `EdgeArrow` inside `CanvasZone.tsx` at module scope (between imports and `CanvasZone`).
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type PanDir" src/components/CanvasZone.tsx` returns ≥ 1
    - `grep -c "PAN_STEP = 8" src/components/CanvasZone.tsx` returns ≥ 1
    - `grep -c "PAN_INTERVAL = 16" src/components/CanvasZone.tsx` returns ≥ 1
    - `grep -c "CANVAS_PADDING = 48" src/components/CanvasZone.tsx` returns ≥ 1
    - `grep -c "function EdgeArrow" src/components/CanvasZone.tsx` returns ≥ 1
    - `grep -c 'data-testid={`edge-arrow-' src/components/CanvasZone.tsx` returns ≥ 1
    - `grep -c 'aria-label="Pan canvas ' src/components/CanvasZone.tsx` returns ≥ 4
    - `grep -c "ResizeObserver" src/components/CanvasZone.tsx` returns ≥ 1
    - `grep -c "scrollOffsetRef.current = scroll" src/components/CanvasZone.tsx` returns ≥ 1
    - `grep -c "viewportRef" src/components/CanvasZone.tsx` returns ≥ 2
    - `grep -c 'data-testid="canvas-inner"' src/components/CanvasZone.tsx` returns 1
    - `grep -c 'data-testid="canvas-zone"' src/components/CanvasZone.tsx` returns 1
    - `grep -c 'transform: `translate(' src/components/CanvasZone.tsx` returns ≥ 1
    - `grep -c "\\bsetRefs\\b" src/components/CanvasZone.tsx` returns ≥ 2 (definition + attach to inner div only — not on outer viewport div)
    - The line containing `ref={viewportRef}` does NOT also contain `ref={setRefs}`
    - The line containing `ref={setRefs}` is on the INNER canvas div (the one with `data-testid="canvas-inner"` and the `transform: translate(...)` style)
    - `grep -c "<EdgeArrow " src/components/CanvasZone.tsx` returns ≥ 4
    - `grep -c "stopPropagation()" src/components/CanvasZone.tsx` returns ≥ 1
    - `grep -c "‹" src/components/CanvasZone.tsx` returns ≥ 1 (the single base glyph)
    - `grep -c "›" src/components/CanvasZone.tsx` returns 0 (NOT used — would be the double-flip bug)
    - `grep -c "scrollOffsetRef:" src/components/CanvasZone.tsx` returns ≥ 1 (prop type added to CanvasZoneProps)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    CanvasZone now renders an outer viewport div (overflow:hidden), an inner canvas div (transformed by `-scroll.x, -scroll.y`), four EdgeArrow components (each with `data-testid="edge-arrow-{dir}"` and aria-label), and exposes the live scroll position via `scrollOffsetRef.current`. `setRefs` (setNodeRef + canvasRef) is on the inner div. ResizeObserver tracks viewport size. PAN_STEP/PAN_INTERVAL constants match Spike003. `startPan` deps include `innerW` and `innerH`. TypeScript clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Plumb scrollOffsetRef + scroll-adjusted drop math through BoardDragLayer and BoardView, add mobile height cap</name>
  <files>src/components/BoardDragLayer.tsx, src/components/BoardView.tsx</files>
  <read_first>
    - src/components/BoardDragLayer.tsx (existing refs lines 84–96; handleDragEnd canvas branch lines 295–432; group drop coordinate computation lines 313–323; single-card drop coordinate computation lines 397–413; BoardView prop list line 617)
    - src/components/BoardView.tsx (current CanvasZone usage line 98; prop interface lines 12–30)
    - src/components/CanvasZone.tsx (after Task 2: `scrollOffsetRef` is now a required prop)
    - .planning/phases/35-mobile/35-PATTERNS.md (lines 263–355 — exact before/after diffs for both edits; lines 327–355 — BoardView height-cap pattern)
    - .planning/phases/35-mobile/35-CONTEXT.md (D-04 height cap; D-15 from phase 32 = clamping formula being extended)
    - .planning/phases/35-mobile/35-RESEARCH.md (Pattern 4 + Pitfall 5: scroll offset applies ONLY to hand/pile→canvas paths, NOT canvas→canvas paths)
    - src/lib/canvas-utils.ts (640px breakpoint reference for the Tailwind `sm:` cap)
  </read_first>
  <action>
    Two-file edit. All edits are surgical — no structural change to either file.

    `src/components/BoardDragLayer.tsx`:

    1. Add a new ref alongside the existing refs (e.g., immediately after `canvasRef` on line 96):
       `const scrollOffsetRef = useRef<{ x: number; y: number }>({ x: 0, y: 0 });`

    2. In `handleDragEnd`, inside the GROUP path's `else` branch (where `fromZone !== 'canvas'`) at lines 318–322, read scroll offset from the ref and ADD it to the converted pointer coordinates. The current lines compute `handleDropX = pointerFinalX - canvasBounds.left - CARD_W / 2;` and `handleDropY = pointerFinalY - canvasBounds.top - CARD_H / 2;`. After the change, both lines must include `+ scrollX` and `+ scrollY` respectively, where `const { x: scrollX, y: scrollY } = scrollOffsetRef.current;` is declared immediately above the two assignments inside the `else` block at line 317.

    3. In `handleDragEnd`, inside the SINGLE-CARD path's `else` branch (lines 404–412 — where `fromZone !== 'canvas'`), read scroll offset and ADD it to `baseX` and `baseY`. The current lines compute `const baseX = pointerFinalX - (canvasBounds?.left ?? 0) - CARD_W / 2;` and `const baseY = pointerFinalY - (canvasBounds?.top ?? 0) - CARD_H / 2;`. After the change, both lines must include `+ scrollX` and `+ scrollY` respectively, where `const { x: scrollX, y: scrollY } = scrollOffsetRef.current;` is declared above them.

    4. CRITICAL: do NOT modify the `canvas → canvas` branches in either path (group path lines 313–316, single-card path lines 397–403). Those compute coordinates as `existing.x + event.delta.x` which are already in canvas-space; adding scroll offset would double-count panning (Pitfall 5 in RESEARCH.md).

    5. In the JSX at the bottom of `BoardDragLayer`, append `scrollOffsetRef={scrollOffsetRef}` to the `<BoardView ... />` prop list (line 617). Keep all other props identical.

    `src/components/BoardView.tsx`:

    1. Extend `BoardViewProps` interface: add `scrollOffsetRef: React.MutableRefObject<{ x: number; y: number }>;` after `dragDelta`.

    2. Destructure `scrollOffsetRef` in the function signature alongside the other props.

    3. Apply the mobile height cap on the canvas wrapper. The current line 98 renders `<CanvasZone .../>` as the right column of a flex row that begins on line 92 (`<div className="flex-1 min-h-0 flex items-start mt-1 pr-2">`). The pile sidebar is the first child (line 93–97). Wrap `<CanvasZone />` in a div that applies the height cap AND retains existing flex behavior. The wrapper className must be exactly `flex-1 min-w-0 self-stretch flex max-h-[240px] sm:max-h-none`. Inside the wrapper, the CanvasZone receives the new `scrollOffsetRef={scrollOffsetRef}` prop in addition to its existing 8 props.

       The 240px starting value is per UI-SPEC ("Starting value ~220–260px — tune in live browser at 375×667"). The exact pixel value MAY be tuned during the verification plan (Plan 02); for this task, the committed starting value is exactly 240. `sm:` is Tailwind's 640px breakpoint, matching `getCardDimensions()`'s 640px threshold in `canvas-utils.ts`.

       Do not predict-tune; commit 240 and let Plan 02 confirm or adjust based on live measurement.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -10 && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scrollOffsetRef = useRef<{ x: number; y: number }>" src/components/BoardDragLayer.tsx` returns 1
    - `grep -c "scrollOffsetRef.current" src/components/BoardDragLayer.tsx` returns ≥ 2 (read once in single-card path, once in group path)
    - `grep -c "+ scrollX" src/components/BoardDragLayer.tsx` returns ≥ 2 (one in single-card path, one in group path)
    - `grep -c "+ scrollY" src/components/BoardDragLayer.tsx` returns ≥ 2
    - `grep -c "scrollOffsetRef={scrollOffsetRef}" src/components/BoardDragLayer.tsx` returns 1 (passed to BoardView)
    - In `src/components/BoardDragLayer.tsx`, the lines computing `baseX` or `handleDropX` for the canvas→canvas branch (where the existing `existing.x + event.delta.x` pattern lives, lines 313–316 and 397–403) MUST NOT contain `+ scrollX`. Spot-check by reading the lines and confirming the `if (fromZone === 'canvas')` blocks remain literally unchanged from their pre-edit form.
    - `grep -c "scrollOffsetRef:" src/components/BoardView.tsx` returns ≥ 1 (prop type)
    - `grep -c "scrollOffsetRef={scrollOffsetRef}" src/components/BoardView.tsx` returns 1 (forwarded to CanvasZone)
    - `grep -c "max-h-\[240px\] sm:max-h-none" src/components/BoardView.tsx` returns 1
    - `npm run typecheck` exits 0
    - `npm test` exits 0 (no unit tests regress — Phase 35 adds no new unit tests; existing tests continue to pass)
  </acceptance_criteria>
  <done>
    `scrollOffsetRef` flows BoardDragLayer → BoardView → CanvasZone. `handleDragEnd` adds scroll offset to `hand/pile → canvas` drop coordinates in BOTH the single-card path AND the group path. `canvas → canvas` paths are unchanged (no scroll offset applied). BoardView wraps CanvasZone in a `max-h-[240px] sm:max-h-none` div. TypeScript and existing unit tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client touch/pointer → CanvasZone | User pointer events trigger pan via `onPointerDown`; arrow events must not leak to dnd-kit |
| client → PartyKit server | No new server boundary — this phase is client-only. Existing `PLACE_ON_CANVAS` / `GROUP_PLACE_ON_CANVAS` are reused; only the client-side coordinate arithmetic changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | Drop coordinates after pan (`hand/pile → canvas`) | mitigate | `scrollOffsetRef` is added to BOTH single-card and group drop paths in BoardDragLayer; without it, dropped card x/y stored on server would be wrong (visual ≠ stored). Verified by acceptance criteria grep for `+ scrollX`/`+ scrollY` in both paths |
| T-35-02 | Tampering | Drop coordinates (`canvas → canvas`) double-counting scroll | mitigate | Explicit non-mitigation: do NOT apply scrollX/Y to `canvas → canvas` formulas. Enforced by acceptance criterion that `if (fromZone === 'canvas')` blocks remain unchanged |
| T-35-03 | Denial of Service | Pan interval leak on unmount | mitigate | `useEffect(() => () => stopPan(), [stopPan])` cleanup explicitly clears interval; `stopPan()` called at top of `startPan` to prevent double-interval if user rapid-touches different arrows |
| T-35-04 | Information Disclosure | Pan state visible to other players | accept | `scroll` is client-only state, never sent to server. No leakage possible by construction |
| T-35-05 | Spoofing | Edge arrow press registered as drag start | mitigate | `e.stopPropagation()` on arrow `onPointerDown` prevents dnd-kit from receiving the event. Verified by acceptance criterion grep for `stopPropagation()` |
| T-35-06 | Tampering | Stale closure in `startPan` returning wrong pan boundary | mitigate | `innerW` and `innerH` MUST be in `startPan`'s `useCallback` dep array (production has dynamic canvas vs. spike's constants). Pitfall 4 in RESEARCH.md; enforced by code review and visual verification |
| T-35-07 | Denial of Service | ResizeObserver leak | mitigate | `obs.disconnect()` in effect cleanup |
| T-35-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this phase — `package.json` is unchanged. Skipping package legitimacy gate (no install tasks present) |

ASVS Level 1 coverage: this phase is purely a client-side rendering/UX refactor; no new authentication, secrets, or network surfaces are introduced. Existing server-side validation in `party/index.ts` for `PLACE_ON_CANVAS` / `GROUP_PLACE_ON_CANVAS` (bounds, ownership, atomicity) is unchanged and continues to enforce the trust boundary at the server.
</threat_model>

<verification>
- `npx playwright test playwright/mobile.spec.ts` is RED at end of Task 1, GREEN at end of Task 3.
- `npm run typecheck` exits 0 after each of Tasks 2 and 3.
- `npm test` continues to pass — no unit tests regress.
- Plan 02 follows up with human visual verification of arrow rendering, hold-to-pan loop, drop accuracy under pan, and height-cap tuning at 375×667.
</verification>

<success_criteria>
- All 5 tests in `playwright/mobile.spec.ts` pass GREEN after Task 3.
- CanvasZone renders outer viewport div + inner canvas div + four EdgeArrow components.
- ResizeObserver tracks viewport size; pan loop uses PAN_STEP=8, PAN_INTERVAL=16.
- `setRefs` (setNodeRef + canvasRef) attaches ONLY to the inner canvas div.
- `scrollOffsetRef` flows BoardDragLayer → BoardView → CanvasZone; CanvasZone writes to it on every scroll change.
- BoardDragLayer adds `scrollX/scrollY` to hand/pile→canvas drop math in single-card AND group paths; does NOT add it to canvas→canvas paths.
- BoardView wraps CanvasZone in a `max-h-[240px] sm:max-h-none` div.
- Aria labels `Pan canvas {left|right|up|down}` present on all four EdgeArrow components.
- All three requirements (MOBILE-01, MOBILE-02, MOBILE-03) have automated coverage via the new Playwright spec.
</success_criteria>

<output>
Create `.planning/phases/35-mobile/35-01-SUMMARY.md` when done.
</output>
