---
phase: 33-overlap-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/canvas-utils.ts
  - tests/overlapUtils.test.ts
  - src/components/CanvasDraggableCard.tsx
  - src/components/CanvasZone.tsx
autonomous: true
requirements:
  - OVERLAP-03
tags: [canvas, overlap, shadow, react, vitest]

must_haves:
  truths:
    - "coversMajority(top, bottom) returns true iff overlap area > 50% of CARD_W*CARD_H"
    - "STACK_SHADOW constant equals the spec string from Spike002 line 47"
    - "When canvasCards contains a higher-z card that covers >50% of a lower-z card, the higher-z card's id appears in the coveringIds set rendered by CanvasZone"
    - "A static (non-dragging) canvas card whose id is in coveringIds renders with boxShadow=STACK_SHADOW and borderRadius=6"
    - "A static canvas card whose id is NOT in coveringIds renders with boxShadow=undefined and borderRadius=undefined"
  artifacts:
    - path: "src/lib/canvas-utils.ts"
      provides: "coversMajority(), STACK_SHADOW, CARD_W, CARD_H exports"
      contains: "export function coversMajority"
    - path: "tests/overlapUtils.test.ts"
      provides: "Unit coverage for coversMajority boundary cases and coveringIds set logic"
      contains: "describe(\"coversMajority\""
    - path: "src/components/CanvasDraggableCard.tsx"
      provides: "coversAnother prop + conditional boxShadow/borderRadius"
      contains: "coversAnother"
    - path: "src/components/CanvasZone.tsx"
      provides: "coveringIds useMemo + coversAnother prop threading"
      contains: "coveringIds"
  key_links:
    - from: "src/components/CanvasZone.tsx"
      to: "src/lib/canvas-utils.ts"
      via: "import { coversMajority }"
      pattern: "from '@/lib/canvas-utils'"
    - from: "src/components/CanvasDraggableCard.tsx"
      to: "src/lib/canvas-utils.ts"
      via: "import { STACK_SHADOW }"
      pattern: "from '@/lib/canvas-utils'"
    - from: "src/components/CanvasZone.tsx"
      to: "src/components/CanvasDraggableCard.tsx"
      via: "coversAnother={coveringIds.has(cc.card.id)} prop"
      pattern: "coversAnother=\\{coveringIds"
---

<objective>
Extract the validated overlap-shadow utility from Spike002 into production and wire the static (at-rest) shadow path: cards that cover >50% of a lower-z canvas card render with the stack shadow indicator.

Purpose: Deliver OVERLAP-03 at-rest behavior. Pure client-side rendering; no server changes. Sets up the shared utility that Plan 02 (drag-time shadow) also imports.

Output:
- `src/lib/canvas-utils.ts` — new utility file (coversMajority, STACK_SHADOW, CARD_W, CARD_H)
- `tests/overlapUtils.test.ts` — Vitest unit coverage for the utility and the covering-set logic
- `src/components/CanvasDraggableCard.tsx` — adds `coversAnother` prop and conditional boxShadow/borderRadius
- `src/components/CanvasZone.tsx` — adds `coveringIds` useMemo and threads `coversAnother` to each card
</objective>

<execution_context>
@$HOME/code/dev-setup/claude/get-shit-done/workflows/execute-plan.md
@$HOME/code/dev-setup/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-overlap-visibility/33-CONTEXT.md
@.planning/phases/33-overlap-visibility/33-RESEARCH.md
@.planning/phases/33-overlap-visibility/33-PATTERNS.md
@.planning/phases/33-overlap-visibility/33-UI-SPEC.md
@.planning/phases/33-overlap-visibility/33-VALIDATION.md

# Source-of-truth reference implementations
@src/spikes/Spike002OverlapHitTesting.tsx
@src/components/CanvasDraggableCard.tsx
@src/components/CanvasZone.tsx
@src/shared/types.ts
@src/lib/utils.ts
@tests/deck.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/shared/types.ts:
- `interface CanvasCard { card: Card; x: number; y: number; z: number; }`
- `type ClientCanvasCard = CanvasCard` — alias used in client code (no masking)
- `interface Card { id: string; suit: string; rank: string; faceUp: boolean }`

From src/components/CanvasDraggableCard.tsx (current state):
- `interface CanvasDraggableCardProps { canvasCard: ClientCanvasCard; isDraggingActive?: boolean; }`
- Component uses `useDraggable` and renders a div with inline `style` object at lines 39–47
- `style` currently sets: position, left, top, zIndex, opacity, transform, touchAction

From src/components/CanvasZone.tsx (current state):
- `interface CanvasZoneProps { canvasCards: ClientCanvasCard[]; draggingCardId: string | null; canvasRef: React.RefObject<HTMLDivElement | null>; }`
- Maps `canvasCards` to `CanvasDraggableCard` at lines 33–39 with key, canvasCard, isDraggingActive props
- React is imported as default namespace: `import React from 'react';`

From src/spikes/Spike002OverlapHitTesting.tsx (reference impl — VALIDATED):
- Line 45–47: `const CARD_W = 63; const CARD_H = 88; const STACK_SHADOW = '2px 2px 0 0 #fff, 2px 2px 0 1px #d1d5db';`
- Lines 49–53: `coversMajority(top, bottom)` — AABB overlap check returning `overlapW * overlapH > CARD_W * CARD_H * 0.5`
- Lines 90–98: `coveringIds` useMemo pattern — iterate cards, add id to Set if any other card has lower z AND coversMajority returns true
- Lines 66–77: FreeDraggableCard style block applying `boxShadow: coversAnother ? STACK_SHADOW : undefined` and `borderRadius: coversAnother ? 6 : undefined`

From tsconfig.json:
- Path alias `@/*` → `./src/*` (use `@/lib/canvas-utils` for imports inside src/components/)
- Tests under `tests/` use relative imports: `from "../src/..."`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create canvas-utils utility and Vitest unit suite</name>
  <files>src/lib/canvas-utils.ts, tests/overlapUtils.test.ts</files>
  <read_first>
    - src/spikes/Spike002OverlapHitTesting.tsx (extract coversMajority lines 49–53 and constants lines 45–47 verbatim, retyping `bottom` arg to be {x:number;y:number} — see action below)
    - src/lib/utils.ts (project's existing lib utility pattern — confirms src/lib/ as the home for shared utilities)
    - tests/deck.test.ts (Vitest describe/it/expect pattern used in this repo — pure utility unit test style; no mocks)
    - tests/canvasCards.test.ts (confirms tests/ path layout and `from "../src/..."` import convention)
    - .planning/phases/33-overlap-visibility/33-RESEARCH.md (Pattern 3 + Wave 0 test scope)
    - .planning/phases/33-overlap-visibility/33-CONTEXT.md (D-02 shadow CSS, D-03 threshold formula)
  </read_first>
  <behavior>
    canvas-utils.ts exports:
    - CARD_W = 63 (number)
    - CARD_H = 88 (number)
    - STACK_SHADOW = '2px 2px 0 0 #fff, 2px 2px 0 1px #d1d5db' (string)
    - coversMajority(top: {x:number;y:number}, bottom: {x:number;y:number}): boolean — returns true iff `overlapW * overlapH > CARD_W * CARD_H * 0.5`

    overlapUtils.test.ts asserts (RED-then-GREEN — write tests first, then implement utility):
    - coversMajority returns false when cards do not overlap at all (e.g. top {x:0,y:0}, bottom {x:200,y:200})
    - coversMajority returns false at the exact 50% threshold (use strict > comparison — overlap area exactly equal to CARD_W*CARD_H/2 must return false)
    - coversMajority returns true when overlap area exceeds 50% (e.g. nearly-aligned positions with small offset)
    - coversMajority returns true for full overlap (top and bottom at the same position)
    - coversMajority handles negative-offset edge case (top to the upper-left of bottom by a small amount)
    - STACK_SHADOW constant equals the literal '2px 2px 0 0 #fff, 2px 2px 0 1px #d1d5db'
    - CARD_W === 63 and CARD_H === 88
    - Inline coveringIds-set scenario: build an array of 3 CanvasCard-shaped objects (one pair where top covers >50% of bottom, one isolated card); replicate the useMemo loop inline in the test; assert the resulting Set contains exactly the covering card's id and not the bottom card's id and not the isolated card's id
  </behavior>
  <action>
    Create src/lib/canvas-utils.ts as a self-contained TypeScript module with NO imports (do not import CanvasCard from @/shared/types — `bottom` must be typed structurally as `{x:number;y:number}` so the utility works for both stored cards and drag-time draggedPos). Export the four symbols in the behavior block above. Body of coversMajority is verbatim from Spike002 lines 49–53 with the `bottom: CanvasCard` parameter retyped to `{x:number;y:number}`.

    Create tests/overlapUtils.test.ts with imports `import { describe, it, expect } from "vitest"` and `import { coversMajority, STACK_SHADOW, CARD_W, CARD_H } from "../src/lib/canvas-utils"`. Use describe blocks: `describe("coversMajority", ...)`, `describe("STACK_SHADOW", ...)`, `describe("CARD_W / CARD_H", ...)`, `describe("coveringIds inline scenario", ...)`. Inline-replicate the spike's coveringIds useMemo body (Spike002 lines 90–98) in the last describe block — no need to import the production useMemo (Task 3 wires it).

    Per D-03 the threshold is STRICTLY greater than (`>` not `>=`); the "exactly 50%" test asserts the boundary returns false.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose tests/overlapUtils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/canvas-utils.ts exists
    - `grep -E '^export (function|const) ' src/lib/canvas-utils.ts | wc -l` returns 4 (CARD_W, CARD_H, STACK_SHADOW, coversMajority)
    - `grep -c "from '@/shared/types'" src/lib/canvas-utils.ts` returns 0 (utility must be dependency-free)
    - `grep -c "from .*shared" src/lib/canvas-utils.ts` returns 0
    - File tests/overlapUtils.test.ts exists
    - `grep -c 'describe("coversMajority"' tests/overlapUtils.test.ts` returns 1
    - `grep -c 'describe("STACK_SHADOW"' tests/overlapUtils.test.ts` returns 1
    - `grep -c 'describe("coveringIds' tests/overlapUtils.test.ts` returns 1
    - `npm test -- --reporter=verbose tests/overlapUtils.test.ts` exits 0
    - Test file contains at least 8 `it(` cases (5 coversMajority + 1 STACK_SHADOW + 1 dims + 1+ coveringIds scenarios): `grep -vc '^[[:space:]]*//' tests/overlapUtils.test.ts | xargs -I{} sh -c 'grep -c "^[[:space:]]*it(" tests/overlapUtils.test.ts'` returns ≥ 8
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    Utility file exports the four symbols. Vitest suite green. No type errors. Wave 0 dependency from VALIDATION.md (tests/overlapUtils.test.ts) is resolved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire static stack shadow into CanvasDraggableCard + CanvasZone</name>
  <files>src/components/CanvasDraggableCard.tsx, src/components/CanvasZone.tsx</files>
  <read_first>
    - src/components/CanvasDraggableCard.tsx (current style object at lines 39–47; props interface at lines 8–11; existing import block)
    - src/components/CanvasZone.tsx (current render loop at lines 33–39; existing imports)
    - src/lib/canvas-utils.ts (created in Task 1 — exports being imported)
    - src/spikes/Spike002OverlapHitTesting.tsx (FreeDraggableCard style at lines 55–82; coveringIds useMemo at lines 90–98 — direct transplant target)
    - .planning/phases/33-overlap-visibility/33-CONTEXT.md (D-02 shadow on covering card; D-04 useMemo in CanvasZone returning Set<string>; D-07 isDraggingActive prop NOT used for shadow)
    - .planning/phases/33-overlap-visibility/33-PATTERNS.md (`CanvasDraggableCard.tsx` and `CanvasZone.tsx` sections — prop interface extension and import addition examples)
    - .planning/phases/33-overlap-visibility/33-UI-SPEC.md (Visual States > Stack Shadow Indicator: boxShadow and borderRadius:6 spec)
  </read_first>
  <behavior>
    CanvasDraggableCard:
    - Accepts new optional prop `coversAnother?: boolean` on `CanvasDraggableCardProps`
    - When `coversAnother === true`, the rendered div has inline `boxShadow: STACK_SHADOW` and `borderRadius: 6`
    - When `coversAnother` is false/undefined, both style properties are `undefined`
    - `isDraggingActive` prop is unchanged (still reserved/unused; do NOT couple shadow to it per D-07)

    CanvasZone:
    - Computes a `coveringIds: Set<string>` via `useMemo([canvasCards])` containing the ids of cards that have a lower-z neighbor where `coversMajority(card, other) === true`
    - Passes `coversAnother={coveringIds.has(cc.card.id)}` to each CanvasDraggableCard
    - Static behavior end-to-end: given a `canvasCards` array containing a higher-z card whose stored (x,y) covers >50% of a lower-z card, the higher-z card's rendered element has the shadow; the lower-z card does not
  </behavior>
  <action>
    Modify src/components/CanvasDraggableCard.tsx:
    - Add `import { STACK_SHADOW } from '@/lib/canvas-utils';` to the import block
    - Extend `CanvasDraggableCardProps` (lines 8–11) with `coversAnother?: boolean;` after `isDraggingActive`
    - Destructure `coversAnother` from props alongside `canvasCard` in the function signature (currently destructures only `canvasCard`)
    - Extend the `style` object (lines 39–47) with two new keys at the end: `boxShadow: coversAnother ? STACK_SHADOW : undefined,` and `borderRadius: coversAnother ? 6 : undefined,`
    - Preserve the existing D-13 comment and all other style keys unchanged

    Modify src/components/CanvasZone.tsx:
    - Add `useMemo` to the React import: change `import React from 'react';` to `import React, { useMemo } from 'react';`
    - Add `import { coversMajority } from '@/lib/canvas-utils';` to the import block (after the existing imports)
    - Add a `coveringIds` useMemo inside the CanvasZone function body BEFORE the `return` statement; body verbatim from Spike002 lines 90–98 except substitute `canvasCards` for `cards`. The memo body iterates `canvasCards`, and for each `card` checks `canvasCards.some(other => other.z < card.z && coversMajority(card, other))`; if true, add `card.card.id` to a `Set<string>`. Dependency array `[canvasCards]`.
    - Modify the render loop (lines 33–39) to pass `coversAnother={coveringIds.has(cc.card.id)}` as the fourth prop to `CanvasDraggableCard` (after `isDraggingActive`)
    - Per RESEARCH.md Pitfall 4: do NOT add deep-equality memoization — useMemo recomputing on every `canvasCards` reference change is correct (n ≤ 52)
  </action>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "coversAnother" src/components/CanvasDraggableCard.tsx` returns ≥ 3 (prop declaration, destructure, two style refs)
    - `grep -c "STACK_SHADOW" src/components/CanvasDraggableCard.tsx` returns ≥ 2 (import + style usage)
    - `grep -c "borderRadius: coversAnother" src/components/CanvasDraggableCard.tsx` returns 1
    - `grep -c "isDraggingActive" src/components/CanvasDraggableCard.tsx` returns ≥ 1 (prop still declared, NOT removed per D-07)
    - `grep -c "coveringIds" src/components/CanvasZone.tsx` returns ≥ 2 (useMemo binding + render-loop usage)
    - `grep -c "useMemo" src/components/CanvasZone.tsx` returns ≥ 2 (import + invocation)
    - `grep -c "coversMajority" src/components/CanvasZone.tsx` returns ≥ 2 (import + usage)
    - `grep -c "from '@/lib/canvas-utils'" src/components/CanvasZone.tsx` returns 1
    - `grep -c "from '@/lib/canvas-utils'" src/components/CanvasDraggableCard.tsx` returns 1
    - `grep -c "coversAnother={coveringIds" src/components/CanvasZone.tsx` returns 1
    - `npm test` exits 0 (full suite still green — no existing test regressions)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    Static stack shadow renders for any canvas card whose stored position covers >50% of a lower-z card. Existing Phase 32 canvas behavior unchanged. Full Vitest suite + TypeScript both green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 33 is pure client-side rendering. No new trust boundaries introduced. Server state remains the trust boundary at the existing WebSocket connection — `canvasCards` arrives via existing Phase 32 viewFor path; no additional input is parsed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `coveringIds` useMemo in CanvasZone | accept | Pure derivation from server-authoritative `canvasCards`. Even if a malicious client tampered with their own local state, the visible shadow on their UI affects only their view; server state is untouched. No mitigation needed. |
| T-33-02 | Information Disclosure | `boxShadow` indicator on cards | accept | Shadow visibility reveals only positional overlap — no card identity, suit, or rank. Already visible from card positions. No PII or game-secret leakage. |
| T-33-03 | Denial of Service | O(n²) useMemo in CanvasZone | accept | n ≤ 52 (single deck); worst-case 2704 comparisons per canvasCards update. Per RESEARCH.md Pitfall 4, negligible cost. No mitigation needed. |
| T-33-SC | Tampering | npm installs (supply chain) | accept | This plan installs no new packages — no supply-chain surface introduced. RESEARCH.md Package Legitimacy Audit confirms zero new deps. |

ASVS L1 assessment from RESEARCH.md: no V2–V6 categories apply (pure client-side geometry over trusted server state).
</threat_model>

<verification>
- `npm test` — full Vitest suite green
- `npm run typecheck` — zero TypeScript errors
- `npm test -- --reporter=verbose tests/overlapUtils.test.ts` — new utility suite green with ≥ 8 it() cases
- Static shadow visible in dev env when two canvas cards overlap >50% (covered by Plan 03 human verify)
</verification>

<success_criteria>
- `src/lib/canvas-utils.ts` exports `coversMajority`, `STACK_SHADOW`, `CARD_W`, `CARD_H` with no `@/shared/types` import
- `tests/overlapUtils.test.ts` covers boundary/threshold/coveringIds scenarios and passes
- `CanvasDraggableCard` accepts `coversAnother` prop and applies the shadow conditionally
- `CanvasZone` computes `coveringIds` via useMemo and threads `coversAnother` to each card
- Existing Phase 32 canvas tests + all other tests still green
- No new packages installed
</success_criteria>

<output>
Create `.planning/phases/33-overlap-visibility/33-01-SUMMARY.md` when done. Frontmatter must include:
- `requirements-completed: [OVERLAP-03]` (partial — static path; full requirement closes after Plan 02 drag-time path lands and Plan 03 human verify passes)
- `provides:` list the utility exports + the CanvasDraggableCard prop + the CanvasZone useMemo + the coveringIds Set semantics
- `affects:` 33-02 (imports from canvas-utils), 33-03 (human verify of at-rest shadow)
- `key-files`: created src/lib/canvas-utils.ts + tests/overlapUtils.test.ts; modified CanvasDraggableCard.tsx + CanvasZone.tsx
</output>
