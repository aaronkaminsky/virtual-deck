---
quick_id: 260514-r8m
slug: fix-5-failing-viewfor-tests-in-tests-vie
description: Fix 5 failing viewFor tests
date: 2026-05-14
status: planned
must_haves:
  truths:
    - All 5 failing tests pass
    - No previously passing tests regress
  artifacts:
    - tests/viewFor.test.ts (updated)
    - tests/spreadZoneCreation.test.ts (updated)
    - party/index.ts (updated)
---

# Quick Task 260514-r8m: Fix 5 Failing Tests

## Root Causes

### Group A — null playerToken (4 tests)
WR-04 added `throw new Error("viewFor requires a non-null playerToken")` to `viewFor()`.
Four tests call `viewFor(state, null)` and expect a return value; they must now expect a throw.

Files:
- `tests/viewFor.test.ts` lines 67–70, 92–96, 109–113
- `tests/spreadZoneCreation.test.ts` lines 84–88

### Group B — undo cap (1 test)
WR-01 changed `state.undoSnapshots.length > 20` to `>= 20` in `takeSnapshot`.
- `>= 20` trims when length hits 20, leaving 19 → cap is 19
- `> 20` trims when length hits 21, leaving 20 → cap is 20
The test expects 20. Revert to `> 20` in `party/index.ts:59`.

## Tasks

### T1: Fix null playerToken tests in tests/viewFor.test.ts
- Lines 67–70: change to `expect(() => viewFor(state, null)).toThrow("viewFor requires a non-null playerToken")`
- Lines 92–96: same pattern
- Lines 109–113: same pattern

### T2: Fix null playerToken test in tests/spreadZoneCreation.test.ts
- Lines 84–88: same throw assertion pattern

### T3: Revert undo cap in party/index.ts
- Line 59: change `>= 20` back to `> 20`

### T4: Verify all tests pass
- Run `npm test -- --run`
