---
phase: quick-260525-uo7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright/responsive.spec.ts
autonomous: true
requirements:
  - CLEANUP-01

must_haves:
  truths:
    - "Stale `Phase 28 BUG-02 mobile grid columns` describe block is removed from playwright/responsive.spec.ts"
    - "`Phase 19 responsive layout` (LAYOUT-04) describe block remains intact and unchanged"
    - "`npm run typecheck` passes"
    - "Playwright responsive.spec.ts contains exactly one describe block"
  artifacts:
    - path: "playwright/responsive.spec.ts"
      provides: "Only the LAYOUT-04 no-horizontal-scroll test"
      contains: "Phase 19 responsive layout"
      forbids: "Phase 28 BUG-02"
  key_links:
    - from: "playwright/responsive.spec.ts"
      to: "DOM testids that still exist (hand-zone)"
      via: "test selectors"
      pattern: "getByTestId\\('hand-zone'\\)"
---

<objective>
Delete the stale `Phase 28 BUG-02 mobile grid columns` describe block from `playwright/responsive.spec.ts`. The test references `data-testid="grid-zone-play"`, which no longer exists in the DOM after Phase 31-01 (commit 19b74f9) removed the communal `GridZone` component in favor of a free-form canvas.

Purpose: Restore green Playwright runs by removing an orphaned test that pins behavior no longer in the product. The `Phase 19 responsive layout` (LAYOUT-04) test remains valid and must not be touched.

Output: `playwright/responsive.spec.ts` containing only the `Phase 19 responsive layout` describe block.
</objective>

<execution_context>
@$HOME/code/dev-setup/claude/get-shit-done/workflows/execute-plan.md
@$HOME/code/dev-setup/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@playwright/responsive.spec.ts

<interfaces>
<!-- Current file structure — extracted from playwright/responsive.spec.ts -->
<!-- Executor should use these directly — no codebase exploration needed. -->

The file contains exactly two describe blocks today:

1. Lines 4-23: `test.describe('Phase 19 responsive layout', ...)` — KEEP UNCHANGED
   - Contains one test: `LAYOUT-04: no horizontal scroll at 375x667 viewport`
   - Uses `getByTestId('hand-zone')` which still exists in DOM

2. Lines 25-37: `test.describe('Phase 28 BUG-02 mobile grid columns', ...)` — DELETE ENTIRELY
   - Contains one test: `BUG-02: grid renders 4 columns at 375px viewport`
   - References `data-testid="grid-zone-play"` which was removed in Phase 31-01 (commit 19b74f9, communal grid → free-form canvas migration)

After deletion, file should be 23 lines (lines 1-23 of the current file), with no trailing blank-block remnants.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete stale Phase 28 BUG-02 describe block</name>
  <files>playwright/responsive.spec.ts</files>
  <action>
    Use the Edit tool to remove the entire `test.describe('Phase 28 BUG-02 mobile grid columns', ...)` block from `playwright/responsive.spec.ts` (currently lines 25-37 inclusive), including the blank line at line 24 that separates it from the preceding block. Do NOT modify the `Phase 19 responsive layout` describe block (lines 1-23). Do NOT modify the imports on lines 1-2. Resulting file must end with the closing `});` of the `Phase 19 responsive layout` block followed by a single trailing newline. Rationale per CLAUDE.md root-cause guidance: the testid `grid-zone-play` was removed in Phase 31-01 when GridZone was replaced by the free-form canvas, so the test is an orphan, not a regression to fix.
  </action>
  <verify>
    <automated>cd /Users/aaronkaminsky/code/virtual-deck && ! grep -q "grid-zone-play" playwright/responsive.spec.ts && ! grep -q "Phase 28 BUG-02" playwright/responsive.spec.ts && grep -q "Phase 19 responsive layout" playwright/responsive.spec.ts && grep -q "LAYOUT-04" playwright/responsive.spec.ts && npm run typecheck</automated>
  </verify>
  <done>
    File contains the `Phase 19 responsive layout` describe block unchanged. File contains zero references to `Phase 28 BUG-02` or `grid-zone-play`. `npm run typecheck` exits 0.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "test.describe" playwright/responsive.spec.ts | grep -v '^#'` → 1
2. `grep -c "grid-zone-play" playwright/responsive.spec.ts` → 0
3. `grep -q "LAYOUT-04" playwright/responsive.spec.ts` → exit 0
4. `npm run typecheck` → exit 0
</verification>

<success_criteria>
- `playwright/responsive.spec.ts` contains only the `Phase 19 responsive layout` describe block
- No reference to `grid-zone-play` or `Phase 28 BUG-02` remains in the file
- `npm run typecheck` passes
- File ends with the closing `});` of the LAYOUT-04 describe block plus a single trailing newline
</success_criteria>

<output>
Create `.planning/quick/260525-uo7-delete-stale-bug-02-responsive-spec-ts-t/260525-uo7-SUMMARY.md` when done
</output>
