---
phase: quick
plan: 260405-nl3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/OpponentHand.tsx
  - src/components/BoardView.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TABLE-03]

must_haves:
  truths:
    - "All players can see how many cards each opponent is holding"
    - "No opponent card face values are visible (only card backs)"
    - "Each opponent hand area shows a labeled count that updates as cards move"
  artifacts:
    - path: "src/components/OpponentHand.tsx"
      provides: "Renders overlapping card backs + count badge; labeled per player index"
    - path: "src/components/BoardView.tsx"
      provides: "Passes player label derived from players array to OpponentHand"
  key_links:
    - from: "src/components/BoardView.tsx"
      to: "src/components/OpponentHand.tsx"
      via: "playerLabel prop derived from gameState.players index"
      pattern: "gameState\\.players\\.findIndex"
---

<objective>
Surface opponent hand card counts in the UI so TABLE-03 is fully satisfied and REQUIREMENTS.md is updated to reflect it.

Purpose: TABLE-03 requires that all players can see how many cards each opponent holds, while face values remain hidden. The `OpponentHand` component already renders stacked card backs and a count badge — the data flows correctly from `opponentHandCounts` via `viewFor` on the server. The only gap is a hardcoded "Player" label (should show a player-indexed label like "P2") and the requirement checkbox being unchecked in REQUIREMENTS.md.
Output: Updated OpponentHand with a meaningful player label prop, BoardView passing the label, and REQUIREMENTS.md with TABLE-03 checked off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/shared/types.ts
@src/components/OpponentHand.tsx
@src/components/BoardView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add player label to OpponentHand and wire it from BoardView</name>
  <files>src/components/OpponentHand.tsx, src/components/BoardView.tsx</files>
  <action>
    In `OpponentHand.tsx`:
    - Add a `playerLabel: string` prop to `OpponentHandProps`
    - Replace the hardcoded `<span className="text-xs text-muted-foreground">Player</span>` with `<span className="text-xs text-muted-foreground">{playerLabel}</span>`
    - No other changes

    In `BoardView.tsx`:
    - When mapping over `Object.entries(gameState.opponentHandCounts)`, derive a `playerLabel` for each opponent:
      - Find the opponent's index in `gameState.players` using `gameState.players.findIndex(p => p.id === id)`
      - If found (index >= 0), label is `P${index + 1}`; if not found (defensive fallback), label is `P?`
    - Pass `playerLabel` to each `<OpponentHand>` instance

    Do NOT change the card rendering, badge, dnd wiring, or any other logic.
  </action>
  <verify>
    <automated>cd /Users/aaronkaminsky/code/virtual-deck && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes with no TypeScript errors. OpponentHand accepts playerLabel prop. BoardView derives label from players array index.</done>
</task>

<task type="auto">
  <name>Task 2: Mark TABLE-03 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    In `.planning/REQUIREMENTS.md`, change the TABLE-03 line from:
    `- [ ] **TABLE-03**: Opponent hand card counts are visible to all players (face values are not)`
    to:
    `- [x] **TABLE-03**: Opponent hand card counts are visible to all players (face values are not)`

    No other changes to the file.
  </action>
  <verify>grep "TABLE-03" /Users/aaronkaminsky/code/virtual-deck/.planning/REQUIREMENTS.md</verify>
  <done>TABLE-03 shows `[x]` in REQUIREMENTS.md.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npm run build` passes with no errors
- `grep "TABLE-03" .planning/REQUIREMENTS.md` shows `[x]`
- `grep "playerLabel" src/components/OpponentHand.tsx` shows the prop is used
- `grep "playerLabel" src/components/BoardView.tsx` shows it's derived and passed
</verification>

<success_criteria>
- OpponentHand renders a player-indexed label ("P1", "P2", etc.) instead of hardcoded "Player"
- No face card values are exposed (card backs only — unchanged behavior)
- Count badge continues to show the correct card count per opponent
- TABLE-03 checkbox is checked in REQUIREMENTS.md
- TypeScript build is clean
</success_criteria>

<output>
After completion, create `.planning/quick/260405-nl3-implement-table-03-opponent-hand-card-co/260405-nl3-SUMMARY.md`
</output>
