---
phase: 31-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/components/GridZone.tsx
  - src/components/BoardView.tsx
  - src/components/BoardDragLayer.tsx
  - playwright/game.spec.ts
autonomous: true
requirements:
  - MIGRATE-01
  - MIGRATE-02
tags:
  - migration
  - client
  - layout
  - sidebar
  - canvas

must_haves:
  truths:
    - "No GridZone component renders anywhere on the board"
    - "No 'grid-cell-' droppable IDs exist in the dnd-kit collision graph"
    - "The middle band is a horizontal flex row: a fixed sidebar on the left and a flexible canvas shell on the right"
    - "Draw and discard piles render in the sidebar, vertically stacked (draw above discard)"
    - "The sidebar has a 1px right border in the muted border token"
    - "The canvas shell renders as plain felt (bg-background) with no decoration, no placeholder text, no dashed border"
    - "At 1280x720 and at 375x667, the sidebar remains visible at its natural PileZone width and the canvas occupies the remaining horizontal space"
    - "playwright/game.spec.ts contains no references to 'grid-zone-play' or GridZone"
    - "npm test, npm run typecheck, and npm run test:e2e all pass"
  artifacts:
    - path: "src/components/GridZone.tsx"
      provides: "(deleted) — file is removed from the repository"
    - path: "src/components/BoardView.tsx"
      provides: "Board with sidebar+canvas middle band; no GridZone import, no communalZone derivation"
      contains: "border-r border-border"
    - path: "src/components/BoardDragLayer.tsx"
      provides: "DndContext customCollision with all grid-cell logic deleted"
      contains: "customCollision"
    - path: "playwright/game.spec.ts"
      provides: "E2E suite with grid-specific test blocks removed and remaining tests aligned to the sidebar+canvas DOM"
      contains: "twoPlayerRoom"
  key_links:
    - from: "src/components/BoardView.tsx"
      to: "src/components/PileZone.tsx"
      via: "imported and rendered inside the new sidebar div, one per pile in `pilePiles`"
      pattern: "pilePiles.map"
    - from: "src/components/BoardView.tsx middle band sidebar div"
      to: "TailwindCSS border-border token"
      via: "className includes border-r border-border (D-04)"
      pattern: "border-r border-border"
    - from: "src/components/BoardView.tsx middle band canvas div"
      to: "TailwindCSS bg-background token"
      via: "className includes flex-1 min-w-0 bg-background (D-05, D-06)"
      pattern: "flex-1 min-w-0"
---

<objective>
Complete the client side of the grid removal: delete the `GridZone.tsx` component file
that Plan 01 stubbed to a null-returning placeholder; fully clean grid-cell collision
detection from `BoardDragLayer.tsx`; restructure the middle band of `BoardView.tsx` from
the current center-aligned pile row into a fixed left sidebar + free canvas shell layout;
and remove all grid-specific assertions from the Playwright spec so `npm run test:e2e`
passes against the new DOM.

Purpose: Land MIGRATE-02 (sidebar + canvas shell layout) and finish MIGRATE-01 (full client
removal of grid surface). After this plan, the only remaining work for Phase 31 is human
visual verification (Plan 03). The empty canvas shell here is the foundation Phase 32 will
build the x/y/z canvas card model on.

Output: A board with the new sidebar+canvas middle band. GridZone is deleted entirely.
BoardDragLayer has zero grid references. The Playwright spec is realigned to the new DOM
(no grid-zone-play locators, no MOVE_GRID_CARD dispatches). All automated tests stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-migration/31-CONTEXT.md
@.planning/phases/31-migration/31-RESEARCH.md
@.planning/phases/31-migration/31-UI-SPEC.md
@.planning/phases/31-migration/31-VALIDATION.md

<interfaces>
<!-- Layout and component contracts the executor needs. -->

Current BoardView middle band (src/components/BoardView.tsx lines 86–100) — the target
of the restructure:

```tsx
<div className="flex-1 min-h-0 flex items-center px-4 gap-4">
  {pilePiles.map((pile) => (
    <PileZone key={pile.id} pile={pile} sendAction={sendAction} draggingCardId={draggingCardId} shufflingPileIds={shufflingPileIds} onSelectAll={onSelectAll} selectedIds={selectedIds} />
  ))}
  {communalZone && (
    <div className="shrink-0">
      <GridZone pile={communalZone} sendAction={sendAction} draggingCardId={draggingCardId} interactive={true} />
    </div>
  )}
</div>
```

Target middle band (from 31-UI-SPEC.md §"Middle Band Structure" and 31-RESEARCH.md
§"Code Examples"):

```tsx
<div className="flex-1 min-h-0 flex items-start">
  <div className="flex-shrink-0 flex flex-col gap-2 py-2 px-2 border-r border-border">
    {pilePiles.map((pile) => (
      <PileZone key={pile.id} pile={pile} sendAction={sendAction} draggingCardId={draggingCardId} shufflingPileIds={shufflingPileIds} onSelectAll={onSelectAll} selectedIds={selectedIds} />
    ))}
  </div>
  <div className="flex-1 min-w-0 overflow-hidden bg-background self-stretch" data-testid="canvas-shell" />
</div>
```

Tailwind tokens (resolved from src/globals.css per 31-UI-SPEC.md §"Color"):
- `border-border` → `hsl(160 20% 20%)` — sidebar right divider (D-04)
- `bg-background` → `hsl(160 38% 16%)` — canvas shell felt (D-05)

PileZone prop signature (kept unchanged from current usage at line 88):
- `pile: ClientPile`
- `sendAction: (action: ClientAction) => void`
- `draggingCardId: string | null`
- `shufflingPileIds: Set<string>`
- `onSelectAll: (cardIds, zone, zoneId) => void`
- `selectedIds: Set<string>`

BoardDragLayer customCollision (src/components/BoardDragLayer.tsx lines 11–62). After
Plan 01's stub fixes the `gridCellContainers` declaration was already deleted; this plan
verifies the file contains zero remaining grid references.

Playwright spec test locations referencing grid (from grep):
- Line 119–123: test "both players see communal + personal zones" — assertion on
  `getByTestId('grid-zone-play')`. DELETE the grid-specific assertion block; keep the
  personal spread zone assertions in the same test.
- Lines 163–210: test "select 2 cards, drag to communal zone, both players see them" —
  DELETE entirely. Phase 32+ introduces a replacement canvas drop test.
- Lines 212–270: test "drag card from communal spread zone to hand" — DELETE entirely.
- Lines 330–390: test "rendered in center row band, not bottom bar" — has both communal
  and personal assertions; DELETE only the lines referencing `grid-zone-play`/`p2Communal`;
  keep the layout band assertions for personal spread.
- Lines 400–460: test "drag card within communal grid zone to a new cell" — DELETE entirely.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete GridZone, finalize BoardDragLayer cleanup</name>
  <files>
    src/components/GridZone.tsx,
    src/components/BoardDragLayer.tsx
  </files>
  <read_first>
    - src/components/GridZone.tsx (entire file — confirms Plan 01 left a null-returning stub; this task deletes the file outright)
    - src/components/BoardDragLayer.tsx (entire file — verify Plan 01's grid-cell cleanup is complete; this task confirms zero remaining `grid-cell-` or `MOVE_GRID_CARD` references and removes anything Plan 01 missed)
    - src/components/BoardView.tsx (only to confirm Plan 01 already changed `communalZone` to `undefined` and the GridZone import is the next item to remove — Task 2 of this plan handles BoardView)
    - .planning/phases/31-migration/31-RESEARCH.md §"Pattern 3: BoardDragLayer Grid Collision Cleanup" and §"Pitfall 5"
  </read_first>
  <action>
    Two steps, both surgical:

    Step A — Delete GridZone.tsx:
    - Run `git rm src/components/GridZone.tsx`. The file was reduced to a null-returning stub in Plan 01 to keep the BoardView import compiling. Now it is removed entirely. The `import { GridZone } from './GridZone';` line in `BoardView.tsx` will be removed in Task 2 of this plan; do not touch BoardView in this task.

    Step B — Finalize BoardDragLayer cleanup:
    - Read the current `BoardDragLayer.tsx` and verify that the following are already absent (from Plan 01's stub work). If any are still present, delete them:
      1. The `gridCellContainers` variable declaration (was lines 18–20).
      2. The clause `!String(c.id).startsWith('grid-cell-')` inside the `cardContainers` filter (was line 22).
      3. The `gridCollisions` block (was lines 41–43, including the comment about T-24-06).
      4. Any `gridOverData` / `gridOverDataMulti` variable destructuring of `toRow` / `toCol` from `event.over?.data.current` (was lines 234, 244, 300, 309).
      5. The conditional spread `...(gridOverData?.toRow !== undefined ? { toRow: ..., toCol: ... } : {})` in both the `PLAY_CARD_SET` and `MOVE_CARD` dispatch sites (was lines 244, 309).
      6. The `else if (selectedIds.size > 1 && selectionSource?.zoneId === 'play') { ... }` branch and its body (was lines 311–321).
    - Run `grep -nE "(grid-cell|MOVE_GRID_CARD|gridOverData|toRow|toCol|zoneId === 'play')" src/components/BoardDragLayer.tsx`. The expected result is zero matches.
    - The customCollision function must still correctly handle the remaining drop targets: hand-zone droppables (`hand`, `opponent-hand-*`), pile droppables (`pile-*`), and card-level droppables (everything else). Confirm by reading the function top-to-bottom — there is now a single `pointerWithin` for zone-containers, an early return for opponent-hand wins, and a fallback `pointerWithin` for pile-containers with the existing intra-pile-reorder optimization. No third tier for grid cells.

    BoardDragLayer's handleDragEnd in the multi-select branch (around line 226–248): after removing the `gridOverDataMulti` destructuring and the conditional spread, the `PLAY_CARD_SET` dispatch is plain — it always omits `toRow`/`toCol`. Confirm the dispatched object's TypeScript shape now matches the post-migration `PLAY_CARD_SET` action type from Plan 01.
  </action>
  <verify>
    <automated>test ! -f src/components/GridZone.tsx &amp;&amp; (grep -nE "(grid-cell|MOVE_GRID_CARD|gridOverData|toRow|toCol|zoneId === 'play'|GridZone)" src/components/BoardDragLayer.tsx; echo "exit=$?") &amp;&amp; npm run typecheck 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files src/components/GridZone.tsx` returns empty (file deleted)
    - `grep -cE "grid-cell" src/components/BoardDragLayer.tsx` returns 0
    - `grep -cE "MOVE_GRID_CARD" src/components/BoardDragLayer.tsx` returns 0
    - `grep -cE "gridOverData" src/components/BoardDragLayer.tsx` returns 0
    - `grep -cE "toRow|toCol" src/components/BoardDragLayer.tsx` returns 0
    - `grep -cE "zoneId === 'play'" src/components/BoardDragLayer.tsx` returns 0
    - `grep -c "GridZone" src/components/BoardDragLayer.tsx` returns 0
    - `npm run typecheck` exits 0 (note: this task leaves the `import { GridZone }` line in BoardView.tsx temporarily; that line will compile because the import statement targets a deleted file — typecheck WILL fail here. To handle: do not split the commit between Task 1 and Task 2; both tasks of Plan 02 commit together. If a commit boundary is required, perform Task 1 + Task 2 in one working tree, then commit.)
  </acceptance_criteria>
  <done>
    `GridZone.tsx` is deleted. `BoardDragLayer.tsx` contains zero grid references.
    Do not commit until Task 2 also lands — the BoardView import to the deleted file
    must be removed in the same commit to keep typecheck green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure BoardView middle band + Playwright spec cleanup</name>
  <files>
    src/components/BoardView.tsx,
    playwright/game.spec.ts
  </files>
  <read_first>
    - src/components/BoardView.tsx (entire file — 138 lines; the entire middle band, the imports header, and the `communalZone` derivation all change)
    - src/components/PileZone.tsx (only to confirm the prop signature is unchanged from current usage; do not modify)
    - .planning/phases/31-migration/31-UI-SPEC.md §"Layout Contract" and §"Component Inventory" (binding visual contract)
    - .planning/phases/31-migration/31-RESEARCH.md §"Pattern 1: Sidebar + Canvas Middle Band Structure" and §"Code Examples"
    - .planning/phases/31-migration/31-CONTEXT.md decisions D-01 through D-12 (locked)
    - playwright/game.spec.ts (entire file — every grid-zone-play locator and every test block exercising the communal zone must be cleaned up)
  </read_first>
  <action>
    Two coordinated changes, committed together with the Task 1 changes as one Plan 02 commit.

    Step A — Restructure BoardView.tsx middle band:

    Imports header (currently lines 1–9):
    - Remove the line `import { GridZone } from './GridZone';` (line 6). This is the last reference to the deleted component.

    Local derivations (currently lines 25–29):
    - Remove the line `const communalZone = spreadPiles.find(p => p.id === 'play');` (line 29). It is dead after Plan 01.

    Middle band JSX (currently lines 86–100):
    - Replace the entire `<div className="flex-1 min-h-0 flex items-center px-4 gap-4">...</div>` block with the structure from the UI-SPEC and RESEARCH code examples (per D-10, D-11, D-12):
      - Outer div: `className="flex-1 min-h-0 flex items-start"`. The `items-center` and `px-4 gap-4` are dropped (D-11). Outer alignment is top per D-11.
      - First child (sidebar): `<div className="flex-shrink-0 flex flex-col gap-2 py-2 px-2 border-r border-border">` containing `{pilePiles.map((pile) => <PileZone key={pile.id} pile={pile} sendAction={sendAction} draggingCardId={draggingCardId} shufflingPileIds={shufflingPileIds} onSelectAll={onSelectAll} selectedIds={selectedIds} />)}`. The PileZone props match the current usage at line 88 verbatim — preserve every prop. Sidebar uses `flex-shrink-0` so it never yields width (D-01, D-02, D-03). The `py-2 px-2 gap-2` provides 8px internal padding and 8px between stacked piles (per 31-UI-SPEC.md §"Spacing Scale"). The `border-r border-border` is the muted 1px right divider (D-04, per 31-UI-SPEC.md §"Color").
      - Second child (canvas shell): `<div className="flex-1 min-w-0 overflow-hidden bg-background self-stretch" data-testid="canvas-shell" />`. `flex-1 min-w-0` lets the canvas fill remaining horizontal space and shrink correctly inside the flex parent (D-06, D-12). `overflow-hidden` matches the Phase 31 empty-shell choice (resolved by RESEARCH.md open question 3 and UI-SPEC.md §"Layout Contract"). `self-stretch` makes the canvas fill the band's full height even though the outer is `items-start` (UI-SPEC.md §"Layout Contract"). `data-testid="canvas-shell"` provides a Playwright hook for Task 2 Step B and for Plan 03 visual verification.
    - Remove the entire `{communalZone && (...)}` block — it is dead code after `communalZone` is deleted.

    Do NOT touch any other part of the file:
    - `ConnectionBanner`, the opponent hand row (lines 39–62), the opponent spread row (lines 64–84), `mySpreadZone` rendering (lines 102–115), and the `HandZone` block (lines 117–134) all stay verbatim (D-10).
    - The outer scroll container at line 64 (`overflow-x-hidden overflow-y-auto sm:overflow-hidden flex flex-col`) is preserved unchanged.

    Step B — Playwright spec cleanup:

    In `playwright/game.spec.ts`, perform the following per the interface section's locations. Verify line numbers against the current file before editing — file may have shifted since the research snapshot.
    - Test "both players see communal + personal zones" (around lines 115–135): remove the two assertions `await expect(p1.getByTestId('grid-zone-play')).toBeVisible();` and the corresponding p2 assertion. Keep the personal spread zone assertions for `data-testid="spread-zone-spread-${playerId}"` if present. Update the test name to reflect that it only checks personal spread zones — rename to `"both players see personal spread zones"`. Also remove the `// The communal play area has pile id "play" → data-testid="grid-zone-play"` comment block.
    - Test "select 2 cards, drag to communal zone, both players see them" (around lines 163–210): DELETE this test block entirely. The replacement canvas-drop e2e arrives in Phase 32+.
    - Test "drag card from communal spread zone to hand" (around lines 212–270): DELETE entirely.
    - Test "rendered in center row band, not bottom bar" (around lines 330–390): DELETE every line that uses `getByTestId('grid-zone-play')` or `p2Communal`. The test's surviving body should still assert that the personal spread zones render in the center vertical band. If after deletion the surviving body is trivial or asserts only on positions of zones no longer compared, delete the entire test.
    - Test "drag card within communal grid zone to a new cell" (around lines 402–460): DELETE entirely.
    - Verify with `grep -nE "(grid-zone-play|GridZone|MOVE_GRID_CARD|grid-cell|communalZone)" playwright/game.spec.ts` — expected zero matches.

    Final verification (still in Task 2):
    - Run `npm run typecheck` — exits 0.
    - Run `npm test` — exits 0.
    - Run `npm run dev` in one terminal and `npm run dev:client` in another (background). With both servers up, run `npm run test:e2e`. The pre-push hook will require this on push, but verify locally before the commit. If servers are down, document this in the SUMMARY and surface it for Plan 03's human-verify step (which will start the servers anyway).
    - Commit message: `feat(31): replace communal grid with sidebar + canvas shell (MIGRATE-02, MIGRATE-01)`

    Note on dev servers and e2e: the project CLAUDE.md says the pre-push hook gates e2e on both servers running. If e2e cannot be run from the autonomous agent's environment, push the commit and let the hook surface the failure to the operator — do not skip the hook. If the hook blocks the push, hand off to Plan 03's human-verify step where the operator can run e2e locally.
  </action>
  <verify>
    <automated>grep -cE "(grid-zone-play|GridZone|MOVE_GRID_CARD|grid-cell)" playwright/game.spec.ts src/components/BoardView.tsx src/components/BoardDragLayer.tsx 2>&amp;1; npm run typecheck 2>&amp;1 | tail -3 &amp;&amp; npm test 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { GridZone }" src/components/BoardView.tsx` returns 0
    - `grep -c "communalZone" src/components/BoardView.tsx` returns 0
    - `grep -c "border-r border-border" src/components/BoardView.tsx` returns at least 1 (sidebar divider present per D-04)
    - `grep -c "items-start" src/components/BoardView.tsx` returns at least 1 (middle band alignment per D-11)
    - `grep -c "flex-shrink-0 flex flex-col" src/components/BoardView.tsx` returns at least 1 (sidebar layout per D-01, D-12)
    - `grep -c "flex-1 min-w-0" src/components/BoardView.tsx` returns at least 1 (canvas shell per D-06, D-12)
    - `grep -c "bg-background" src/components/BoardView.tsx` returns at least 1 (canvas felt per D-05)
    - `grep -c "data-testid=\"canvas-shell\"" src/components/BoardView.tsx` returns 1
    - `grep -cE "(grid-zone-play|GridZone|MOVE_GRID_CARD|grid-cell)" playwright/game.spec.ts` returns 0
    - `npm run typecheck` exits 0
    - `npm test` exits 0
    - When `npm run dev` and `npm run dev:client` are running, `npm run test:e2e` exits 0 (this may be deferred to Plan 03's human-verify if dev servers are unavailable from the agent's shell)
  </acceptance_criteria>
  <done>
    The middle band of BoardView is a sidebar + canvas split. GridZone import is gone.
    Playwright spec has no grid references. All automated tests pass. The DOM at the
    middle band now exposes `data-testid="canvas-shell"` for Plan 03's visual verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user pointer → DndContext customCollision | Untrusted pointer events feed collision detection; reduction in customCollision branch count (no grid-cell tier) shrinks the surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Tampering | `BoardDragLayer.customCollision` | mitigate | Grep gate in acceptance criteria asserts no `grid-cell` matches remain; future re-introduction of grid surface fails the gate loudly |
| T-31-06 | Denial of Service | `BoardView` middle band layout | accept | Sidebar uses `flex-shrink-0`; even with arbitrarily wide pile contents, the layout cannot collapse — UI-SPEC.md §"Responsive Behavior" pins the 375px contract |
| T-31-07 | Information Disclosure | `data-testid="canvas-shell"` | accept | testid is a stable DOM attribute used only for test instrumentation; carries no PII or runtime data |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; no slopcheck gate required |
</threat_model>

<verification>
- `npm test` exits 0
- `npm run typecheck` exits 0
- `npm run test:e2e` exits 0 (when dev servers running; otherwise deferred to Plan 03 manual verify)
- BoardView middle band renders sidebar + canvas per UI-SPEC.md
- GridZone.tsx is absent from `src/components/`
- Playwright spec contains zero grid references
</verification>

<success_criteria>
- MIGRATE-01 client surface: COMPLETE (GridZone deleted, BoardDragLayer clean, BoardView import removed)
- MIGRATE-02 layout: COMPLETE (sidebar with stacked draw+discard piles on left, free canvas shell to the right, 1px muted right border on sidebar, top-aligned middle band)
- Automated test suite green; e2e green when servers are available
- No grid references remain in any source or test file
</success_criteria>

<output>
Create `.planning/phases/31-migration/31-02-SUMMARY.md` when done, summarizing:
- Confirmation that `src/components/GridZone.tsx` is deleted
- The exact className strings used on the sidebar and canvas divs
- The list of Playwright test blocks deleted vs. trimmed
- Result of `npm run test:e2e` (or note if dev servers were unavailable from the agent's shell)
- Any deviation from UI-SPEC.md and why
</output>
