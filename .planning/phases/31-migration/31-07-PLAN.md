---
phase: 31-migration
plan: 07
type: execute
wave: 5
depends_on:
  - 31-04
files_modified:
  - src/components/SpreadZone.tsx
  - src/components/PileZone.tsx
autonomous: true
gap_closure: true
requirements:
  - MIGRATE-02
tags:
  - migration
  - layout
  - padding
  - gap-closure

must_haves:
  truths:
    - "SpreadZone populated slot has no fixed h-[64px] or sm:h-[88px] — slot grows to card intrinsic height + py-2 (8px top + 8px bottom), making bg-secondary visible above and below the card"
    - "PileZone slot has no fixed h-[64px] or sm:h-[88px] — slot grows to card intrinsic height + py-2 (8px top + 8px bottom), making bg-secondary visible above and below the card"
    - "npm test and npm run typecheck both exit 0"
  artifacts:
    - path: "src/components/SpreadZone.tsx"
      provides: "Populated slot uses intrinsic sizing — no h-[64px] or sm:h-[88px] in populated branch className"
      contains: "min-w-[56px] sm:min-w-[80px] rounded-lg border flex items-center px-2 py-2 overflow-x-auto bg-secondary"
    - path: "src/components/PileZone.tsx"
      provides: "Pile slot uses intrinsic sizing with py-2 — no h-[64px] or sm:h-[88px], adds py-2"
      contains: "py-2"
---

<objective>
Close GAP-04: vertical card padding not visually matching horizontal padding.

Root cause: py-2 was added to SpreadZone in Plan 04 but inside fixed-height containers
(h-[64px] sm:h-[88px]), so cards overflow the padded content area and the bg-secondary
slot shows no visible breathing room above or below the card.

Fix: switch both SpreadZone and PileZone card slots to intrinsic sizing — remove the
fixed heights. With py-2 and natural card height (~59-88px), the slot grows to
card height + 16px, showing 8px of bg-secondary above and below matching the 8px
horizontal breathing room from the wider slot (px-2 on SpreadZone; inherent width
gap on PileZone).

Operator-selected approach: intrinsic sizing (remove fixed heights, let py-2 show).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: SpreadZone — remove fixed heights from populated slot</name>
  <files>src/components/SpreadZone.tsx</files>
  <read_first>
    - src/components/SpreadZone.tsx (full file — the slot className is around line 171)
  </read_first>
  <action>
    Locate the populated (non-empty) slot className in SpreadZone.tsx:
    
    Current:
    `'min-w-[56px] h-[64px] sm:min-w-[80px] sm:h-[88px] rounded-lg border flex items-center px-2 py-2 overflow-x-auto bg-secondary'`
    
    Remove `h-[64px]` and `sm:h-[88px]`. New:
    `'min-w-[56px] sm:min-w-[80px] rounded-lg border flex items-center px-2 py-2 overflow-x-auto bg-secondary'`
    
    Do NOT touch:
    - The empty+isOver branch (`h-[40px] sm:h-[56px]` drop-target indicator — keep fixed heights, there's no card to pad around)
    - The empty+not-isOver branch (`h-4` strip — unchanged)
    - Any other line in the file
    
    Commit: `fix(31-07): intrinsic sizing on SpreadZone populated slot — remove fixed heights (GAP-04)`
  </action>
  <acceptance_criteria>
    - `grep -c "h-\[64px\]" src/components/SpreadZone.tsx` returns 0
    - `grep -c "sm:h-\[88px\]" src/components/SpreadZone.tsx` returns 0
    - `grep -c "min-w-\[56px\] sm:min-w-\[80px\] rounded-lg border flex items-center px-2 py-2 overflow-x-auto bg-secondary" src/components/SpreadZone.tsx` returns 1
    - `npm run typecheck` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: PileZone — remove fixed heights, add py-2</name>
  <files>src/components/PileZone.tsx</files>
  <read_first>
    - src/components/PileZone.tsx (full file — the slot className is around line 91)
  </read_first>
  <action>
    Locate the pile slot className in PileZone.tsx:
    
    Current:
    `'w-[56px] h-[64px] sm:w-[80px] sm:h-[88px] rounded-lg border flex flex-col items-center justify-center relative bg-secondary'`
    
    Remove `h-[64px] sm:h-[88px]`, add `py-2`. New:
    `'w-[56px] sm:w-[80px] rounded-lg border flex flex-col items-center justify-center relative bg-secondary py-2'`
    
    Keep the fixed widths (`w-[56px] sm:w-[80px]`) — only heights change.
    Do NOT touch any other line.
    
    Commit: `fix(31-07): intrinsic sizing + py-2 on PileZone slot — remove fixed heights (GAP-04)`
  </action>
  <acceptance_criteria>
    - `grep -c "h-\[64px\]" src/components/PileZone.tsx` returns 0
    - `grep -c "sm:h-\[88px\]" src/components/PileZone.tsx` returns 0
    - `grep -c "py-2" src/components/PileZone.tsx` returns 1
    - `grep -c "w-\[56px\] sm:w-\[80px\]" src/components/PileZone.tsx` returns 1
    - `npm run typecheck` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
</task>

</tasks>

<output>
Create `.planning/phases/31-migration/31-07-SUMMARY.md` recording:
- Exact pre/post className strings for SpreadZone and PileZone
- npm test and typecheck results
- Any deviation
</output>
