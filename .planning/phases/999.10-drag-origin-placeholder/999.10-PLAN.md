---
phase: 999.10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/DraggableCard.tsx
  - src/components/HandZone.tsx
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
must_haves:
  truths:
    - "When a player drags a pile card, a dashed card-shaped outline appears in the pile slot where the card originated"
    - "When a player drags a hand card cross-zone (to a pile or opponent hand), a dashed outline appears in the hand slot where the card originated"
    - "When a player drags a hand card within their own hand (reorder), the origin slot is not replaced by a separate dashed outline — the dnd-kit sortable shift is the feedback"
    - "When a drag is cancelled, the placeholder persists visibly through the 250ms snap-back animation, then the card reappears in place of the placeholder"
    - "The placeholder visually matches the existing empty-pile dashed border (same border color, same border style, rounded corners, no fill)"
  artifacts:
    - path: "src/components/DraggableCard.tsx"
      provides: "Pile-card drag origin placeholder"
      contains: "isDragging"
    - path: "src/components/HandZone.tsx"
      provides: "Hand-card drag origin placeholder"
      contains: "isDragging"
  key_links:
    - from: "src/components/DraggableCard.tsx"
      to: "useDraggable().isDragging"
      via: "conditional render in JSX body"
      pattern: "isDragging \\?"
    - from: "src/components/HandZone.tsx"
      to: "useSortable().isDragging"
      via: "conditional render in SortableHandCard JSX body"
      pattern: "isDragging \\?"
    - from: "placeholder classes"
      to: "empty pile visual language (PileZone)"
      via: "shared Tailwind utility classes (border border-dashed border-border)"
      pattern: "border-dashed border-border"
---

<objective>
Replace the invisible-but-present dragged card (current: `opacity: isDragging ? 0 : 1`) with a visible dashed card-shaped outline at the drag origin slot. Applies to both pile cards (`DraggableCard`) and hand cards (`SortableHandCard` in `HandZone`).

Purpose: Give players a clear visual anchor showing where the card came from while it's in flight, so cancelled drags return to an obvious slot and successful drags feel less like the origin "disappeared".

Output: Two modified files with identical conceptual change (replace opacity with conditional content branch). Visual consistency with the existing empty-pile dashed border.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.10-drag-origin-placeholder/999.10-CONTEXT.md
@.planning/phases/999.10-drag-origin-placeholder/999.10-RESEARCH.md
@.planning/phases/999.10-drag-origin-placeholder/999.10-VALIDATION.md

<interfaces>
<!-- Existing hook return shapes and visual tokens executors will reuse. -->
<!-- These are already used in the files being modified — no new interfaces introduced. -->

From @dnd-kit/core (DraggableCard uses):
```typescript
const { attributes, listeners, setNodeRef, transform, isDragging } = useDraggable({ id, data });
// isDragging: boolean — true from onDragStart until after snap-back/drop animation completes
```

From @dnd-kit/sortable (SortableHandCard uses):
```typescript
const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id, data });
// transition: string — required in style for sortable shift animations; do NOT remove
```

From src/components/PileZone.tsx (reference for visual language — empty pile state, line 44):
```
className={cn(
  'w-[80px] h-[112px] rounded-lg border flex flex-col items-center justify-center relative bg-secondary',
  isEmpty ? 'border-dashed' : '',
  isOver ? 'border-primary' : 'border-border'
)}
```
The empty-pile visual uses: `border border-dashed border-border` with `rounded-lg` and `w-[80px] h-[112px]`. Reuse this style exactly for the pile placeholder.

From src/components/CardFace.tsx / CardBack.tsx:
Card face/back inner content is `w-[63px] h-[88px] rounded-md`. Hand placeholder matches these dimensions so the `flex gap-2` hand strip layout does not shift.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render dashed placeholder in DraggableCard when isDragging</name>
  <files>src/components/DraggableCard.tsx</files>
  <read_first>
    - src/components/DraggableCard.tsx (current state — 45 lines)
    - src/components/PileZone.tsx (read lines 38-51 to confirm the empty-pile className pattern this placeholder must match)
    - .planning/phases/999.10-drag-origin-placeholder/999.10-CONTEXT.md (D-01, D-02, D-05, D-06)
    - .planning/phases/999.10-drag-origin-placeholder/999.10-RESEARCH.md (Architecture Patterns: Target Pattern; Pitfall 2; Pitfall 3; Code Examples: DraggableCard)
  </read_first>
  <action>
    Modify `src/components/DraggableCard.tsx` per D-05:

    1. Remove `opacity: isDragging ? 0 : 1` from the `style` object (keep `transform` and `touchAction`). The content branch now controls visibility — per RESEARCH Pitfall 2, leaving `opacity` in alongside conditional content causes double-hiding and a flash when `isDragging` transitions.

    2. In the returned JSX, replace the single-expression `{card.faceUp ? <CardFace card={card} /> : <CardBack />}` with a conditional on `isDragging`:
       - When `isDragging` is true: render a `<div>` with classes `w-[80px] h-[112px] rounded-lg border border-dashed border-border` (matches PileZone empty-pile visual exactly; 80×112 matches the pile container so the placeholder fills the pile slot — per RESEARCH Pitfall 1).
       - Otherwise: render `card.faceUp ? <CardFace card={card} /> : <CardBack />` as before.

    3. Leave `ref={setNodeRef}`, `onClick={handleClick}`, `{...listeners}`, `{...attributes}` on the outer `div` unchanged (per RESEARCH Pitfall 3 — moving the ref breaks dnd-kit position tracking).

    4. Do NOT introduce a new component or new state. Do NOT change any other file. Do NOT touch the `didDragRef`/`useEffect` click-vs-drag logic.

    Rationale for exact classes (per CONTEXT Claude's Discretion + RESEARCH Design Token Reference): `border-border` matches empty-pile HSL token (already established visual language); `rounded-lg` matches the pile container's corner radius; `w-[80px] h-[112px]` fills the PileZone parent exactly so layout does not shift.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - grep -E "opacity:\s*isDragging" src/components/DraggableCard.tsx returns no matches (opacity removed from style)
    - grep -E "isDragging \?" src/components/DraggableCard.tsx returns at least one match inside JSX (conditional content branch present)
    - grep -F "w-[80px] h-[112px] rounded-lg border border-dashed border-border" src/components/DraggableCard.tsx returns 1 match (exact placeholder className present)
    - grep -F "ref={setNodeRef}" src/components/DraggableCard.tsx still matches on the outer wrapper div (ref unchanged)
    - grep -F "{...listeners} {...attributes}" src/components/DraggableCard.tsx still matches on the outer wrapper div (listeners/attributes unchanged)
    - grep -F "CSS.Translate.toString(transform)" src/components/DraggableCard.tsx still matches (transform preserved in style)
    - `npx tsc --noEmit` exits 0
    - `npm run test -- --run` exits 0 (no regressions — existing tests must still pass)
  </acceptance_criteria>
  <done>DraggableCard renders the dashed 80×112 placeholder when isDragging is true and the card face/back otherwise. Opacity-based hiding is fully removed. TypeScript compiles and existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Render dashed placeholder in SortableHandCard when isDragging</name>
  <files>src/components/HandZone.tsx</files>
  <read_first>
    - src/components/HandZone.tsx (current state — 100 lines; the `SortableHandCard` function is lines 14-32, hand reorder useDndMonitor is lines 55-76 and must NOT be modified)
    - src/components/CardFace.tsx and src/components/CardBack.tsx (to confirm inner card dimensions: `w-[63px] h-[88px] rounded-md`)
    - .planning/phases/999.10-drag-origin-placeholder/999.10-CONTEXT.md (D-02, D-03, D-05; code_context Integration Points — D-03 does NOT require a runtime check, the sortable shift naturally reads correctly alongside the placeholder)
    - .planning/phases/999.10-drag-origin-placeholder/999.10-RESEARCH.md (Architecture Patterns: D-03 Why SortableHandCard Gets Placeholder for All isDragging States; Pitfall 1 sizing; Pitfall 4 transition; Code Examples: SortableHandCard)
  </read_first>
  <action>
    Modify `src/components/HandZone.tsx` inside the `SortableHandCard` function (lines 14-32) per D-05:

    1. Remove `opacity: isDragging ? 0 : 1` from the `style` object. **Keep `transition`** in style — per RESEARCH Pitfall 4, removing `transition` breaks the shift animation of adjacent cards during hand-to-hand reorder.

    2. In the returned JSX, replace the single-expression `{card.faceUp ? <CardFace card={card} /> : <CardBack />}` with a conditional on `isDragging`:
       - When `isDragging` is true: render a `<div>` with classes `w-[63px] h-[88px] rounded-md border border-dashed border-border` (dimensions match CardFace/CardBack inner content exactly — per RESEARCH Pitfall 1, using 80×112 here would inflate the hand strip layout; `rounded-md` matches the card face/back corner radius).
       - Otherwise: render `card.faceUp ? <CardFace card={card} /> : <CardBack />` as before.

    3. Leave `ref={setNodeRef}`, `{...listeners}`, `{...attributes}` on the outer `div` unchanged (RESEARCH Pitfall 3).

    4. Do NOT add any logic to distinguish hand-to-hand reorder from cross-zone drags. Per D-03 and RESEARCH Architecture "D-03: Why SortableHandCard Gets Placeholder for All isDragging States", the placeholder applies to all `isDragging` states inside `SortableHandCard` — during same-hand reorder, the origin card becomes the dashed placeholder and adjacent cards shift around it via dnd-kit's sortable transform. No runtime zone detection is required.

    5. Do NOT modify the outer `HandZone` component, the `useDndMonitor` reorder logic (lines 55-76), or any `sendAction` call. Do NOT introduce a new component or new state.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - grep -E "opacity:\s*isDragging" src/components/HandZone.tsx returns no matches
    - grep -F "transition," src/components/HandZone.tsx still matches inside the SortableHandCard style object (transition preserved)
    - grep -E "isDragging \?" src/components/HandZone.tsx returns at least one match inside SortableHandCard JSX
    - grep -F "w-[63px] h-[88px] rounded-md border border-dashed border-border" src/components/HandZone.tsx returns 1 match
    - grep -F "ref={setNodeRef}" src/components/HandZone.tsx still matches at least twice (SortableHandCard wrapper and HandZone droppable — both unchanged)
    - grep -F "REORDER_HAND" src/components/HandZone.tsx still matches (reorder logic unchanged)
    - `npx tsc --noEmit` exits 0
    - `npm run test -- --run` exits 0 (no regressions)
  </acceptance_criteria>
  <done>SortableHandCard renders the dashed 63×88 placeholder when isDragging is true and the card face/back otherwise. Opacity-based hiding removed. `transition` preserved. Hand-reorder logic and outer HandZone unchanged. TypeScript compiles and existing tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual verification of drag origin placeholder behavior</name>
  <files>src/components/DraggableCard.tsx, src/components/HandZone.tsx</files>
  <read_first>
    - .planning/phases/999.10-drag-origin-placeholder/999.10-VALIDATION.md (Per-Task Verification Map — the four manual cases 999.10-01-01 through 999.10-01-04 and the Manual-Only Verifications section)
    - .planning/phases/999.10-drag-origin-placeholder/999.10-CONTEXT.md (D-01, D-02, D-03, D-04 — the decisions being verified)
  </read_first>
  <what-built>
    Dashed card-shaped outline now replaces the invisible `opacity: 0` card at the drag origin in both pile (DraggableCard) and hand (SortableHandCard) components. The placeholder matches the empty-pile visual language. Placeholder persists through cancel snap-back animation.
  </what-built>
  <action>
    Start the dev environment and perform the four manual verification cases below. This is a checkpoint — no code changes; you're asking the human to observe rendered behavior that cannot be automated (visual CSS state during a drag gesture).
  </action>
  <how-to-verify>
    Run the app locally and open two browser windows to the same room (so you can drag between your hand, the shared piles, and an opponent's hand area).

    ```
    npm run dev
    # In another terminal, if needed for PartyKit:
    npx partykit dev
    ```

    Then verify each of these four cases (all from VALIDATION.md):

    1. **Pile card drag (D-02 pile):**
       - Drag the top card off a pile.
       - Expected: A dashed 80×112 outline appears in the pile slot where the card was. Matches the empty-pile dashed border visually.
       - Drop the card anywhere valid.
       - Expected: Placeholder disappears when the card lands.

    2. **Hand card cross-zone drag (D-02 hand):**
       - Drag one of your hand cards to a pile (or to an opponent's hand area).
       - Expected: A dashed 63×88 outline appears in the hand slot where the card was, between the remaining hand cards. The hand strip height does not change (Pitfall 1 check).
       - Drop the card on the target.
       - Expected: Placeholder disappears; hand closes up around the gap.

    3. **Hand-to-hand reorder — NO placeholder (D-03):**
       - Drag a hand card to a different position within your own hand.
       - Expected: The dragged card is shown as a dashed outline at its *original* slot, and the other hand cards shift positions around it via dnd-kit's sortable transform. There is NOT a second/separate dashed outline elsewhere. (Per RESEARCH D-03 note: "the origin card is what shifts, not a separate placeholder" — treat this as correct behavior.)
       - Release on a new position.
       - Expected: Card snaps into the new position, placeholder is gone, order persists on the server (no error in console).

    4. **Drag cancel — placeholder persists through snap-back (D-04):**
       - Start dragging a pile card OR a hand card cross-zone.
       - Press Escape (or drag to empty space off any droppable and release).
       - Expected: The overlay card animates back to the origin over ~250ms. During that entire 250ms window, the dashed outline is visible at the origin. When the snap-back completes, the card re-appears at full opacity and the placeholder disappears in the same frame (no visible gap where the slot is empty).

    Also: Check `npm run test` is still green and `npx tsc --noEmit` passes.
  </how-to-verify>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - All 4 manual cases from VALIDATION.md Per-Task Verification Map pass (999.10-01-01 pile placeholder, 999.10-01-02 hand cross-zone placeholder, 999.10-01-03 no hand-to-hand duplicate placeholder, 999.10-01-04 cancel snap-back persistence)
    - Human resume-signal received: "approved"
    - `npx tsc --noEmit` exits 0
    - `npm run test -- --run` exits 0
  </acceptance_criteria>
  <done>All four manual verification cases observed as described. Human approved. Automated checks still green.</done>
  <resume-signal>Type "approved" if all four cases pass. If any case fails, describe which case and what you observed — I'll revise the placeholder implementation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase is a purely client-local visual change. No new trust boundaries are introduced:
- No user input is processed (drag events are already handled by dnd-kit upstream)
- No network messages are sent (D-06: placeholder is client-local, no `sendAction` calls)
- No state changes affect server or other clients
- No secrets, credentials, or PII involved

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.10-01 | Information Disclosure | DraggableCard / SortableHandCard placeholder render | accept | Placeholder is a static dashed `<div>` with no card data (no suit/rank/faceUp text). It renders strictly less information than the `CardFace`/`CardBack` it replaces. No PII or game-secret leakage is possible. |
| T-999.10-02 | Denial of Service | Conditional JSX branch in render | accept | Adding one ternary to each component's render path has negligible perf impact (same number of DOM nodes rendered per frame, same React reconciliation cost). No new state subscriptions, no new effects, no new event listeners. |
| T-999.10-03 | Tampering | Drag state (`isDragging`) | accept | `isDragging` is owned by dnd-kit hooks already trusted elsewhere in the codebase (e.g. `BoardDragLayer`). This phase consumes the same flag — no new trust is placed. |

No `mitigate`-disposition threats: this phase introduces no new attack surface.
</threat_model>

<verification>
Per `.planning/phases/999.10-drag-origin-placeholder/999.10-VALIDATION.md`:

Automated (every task commit):
- `npx tsc --noEmit` — 0 errors
- `npm run test -- --run` — all existing tests pass (this phase adds no new tests; visual behavior is manual-only)

Manual (phase gate — Task 3 checkpoint):
- Pile drag shows dashed 80×112 outline at origin (D-01, D-02)
- Hand cross-zone drag shows dashed 63×88 outline at origin; hand strip height unchanged (D-02)
- Hand-to-hand reorder: dragged card becomes placeholder at origin, others shift via sortable transform — no separate/duplicate placeholder (D-03)
- Drag cancel: placeholder visible throughout snap-back animation; disappears only when card renders at full opacity (D-04)
</verification>

<success_criteria>
- [ ] `src/components/DraggableCard.tsx` no longer sets `opacity: isDragging ? 0 : 1` in style
- [ ] `src/components/DraggableCard.tsx` renders `<div className="w-[80px] h-[112px] rounded-lg border border-dashed border-border" />` when `isDragging` is true
- [ ] `src/components/HandZone.tsx` (SortableHandCard) no longer sets `opacity: isDragging ? 0 : 1` in style
- [ ] `src/components/HandZone.tsx` (SortableHandCard) renders `<div className="w-[63px] h-[88px] rounded-md border border-dashed border-border" />` when `isDragging` is true
- [ ] `transition` is still present in SortableHandCard's style object
- [ ] No new components, no new state variables, no new hooks, no new server actions
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run test -- --run` passes (no regressions)
- [ ] Human checkpoint (Task 3) approved: all four manual verification cases pass
</success_criteria>

<output>
After completion, create `.planning/phases/999.10-drag-origin-placeholder/999.10-01-SUMMARY.md` using the standard summary template. Note that this phase has no new automated tests (visual-only change) — record manual verification results in the SUMMARY.
</output>
