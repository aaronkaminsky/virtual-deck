---
phase: 999.10
plan: 01
subsystem: frontend/drag-ux
tags: [drag-and-drop, dnd-kit, visual-feedback, placeholder]
dependency_graph:
  requires: []
  provides: [drag-origin-placeholder]
  affects: [DraggableCard, HandZone, SortableHandCard]
tech_stack:
  added: []
  patterns: [conditional-content-branch-over-opacity, dnd-kit-isDragging]
key_files:
  created: []
  modified:
    - src/components/DraggableCard.tsx
    - src/components/HandZone.tsx
decisions:
  - "Replace opacity:0 with conditional content branch — avoids double-hiding flash and gives visible slot feedback"
  - "Pile placeholder uses 80x112 to match PileZone container exactly; hand placeholder uses 63x88 to match CardFace/CardBack inner dimensions"
  - "No runtime zone detection needed in SortableHandCard — dnd-kit sortable shift naturally coexists with the placeholder"
  - "Placeholder driven by draggingCardId prop threaded from BoardDragLayer.activeCard state, not dnd-kit context — context active becomes null when cursor crosses droppable boundaries, causing placeholder to disappear"
  - "PileZone renders the placeholder as an absolute-positioned sibling to DraggableCard; HandZone renders it inside SortableHandCard's wrapper div outside setNodeRef — both patterns survive dnd-kit's manipulation of the setNodeRef element"
  - "Also updated BoardView and PileZone to accept and pass draggingCardId prop"
metrics:
  duration: ~2h (includes debugging placeholder-disappears-on-zone-crossing)
  completed_date: "2026-04-15"
  tasks_completed: 3
  files_modified: 4
---

# Phase 999.10 Plan 01: Drag Origin Placeholder Summary

**One-liner:** Dashed card-shaped outline replaces invisible opacity-0 card at drag origin in both DraggableCard (pile) and SortableHandCard (hand).

## What Was Built

Replaced `opacity: isDragging ? 0 : 1` with a conditional JSX content branch in two components:

- **DraggableCard** (`src/components/DraggableCard.tsx`): When `isDragging`, renders `<div className="w-[80px] h-[112px] rounded-lg border border-dashed border-border" />` — an 80x112 dashed outline matching the PileZone empty-pile visual exactly.
- **SortableHandCard** in **HandZone** (`src/components/HandZone.tsx`): When `isDragging`, renders `<div className="w-[63px] h-[88px] rounded-md border border-dashed border-border" />` — a 63x88 dashed outline matching CardFace/CardBack inner dimensions so the hand strip layout does not shift.

Both changes: removed `opacity` from the style object, kept `transform`/`touchAction` (DraggableCard) and `transform`/`transition`/`touchAction` (SortableHandCard), left `ref={setNodeRef}`/`{...listeners}`/`{...attributes}` on the outer wrapper unchanged.

## Tasks Completed

| Task | Files |
|------|-------|
| Task 1: DraggableCard placeholder | src/components/DraggableCard.tsx |
| Task 2: SortableHandCard placeholder | src/components/HandZone.tsx |
| Task 3: Manual verification (human approved) | — |

## Deviations from Plan

**Significant deviation:** The plan specified driving the placeholder from `useDraggable().isDragging` / `useSortable().isDragging` directly inside each component. In practice, dnd-kit's context `active` becomes null whenever the cursor crosses between droppable zones, causing the placeholder to disappear mid-drag. 

Resolution: `activeCard` state in `BoardDragLayer` (set on `onDragStart`, cleared after drop/cancel) is passed as `draggingCardId` prop through `BoardView → PileZone/HandZone → SortableHandCard`. This bypasses dnd-kit context entirely and correctly persists the placeholder for the full drag lifetime.

Additional files modified beyond the plan: `src/components/BoardView.tsx` (prop passthrough), `src/components/PileZone.tsx` (placeholder render + prop).

The placeholder is rendered as a sibling/overlay div outside `setNodeRef` elements to avoid dnd-kit's manipulation of those elements.

## Verification Status

Automated:
- `npx tsc --noEmit`: 0 errors
- `npm run test -- --run`: 96/96 tests passing

Manual (Task 3 — human approved 2026-04-15):
- 999.10-01-01: Pile card drag shows dashed outline at origin — PASS
- 999.10-01-02: Hand cross-zone drag shows dashed outline; hand strip height unchanged — PASS
- 999.10-01-03: Hand-to-hand reorder: placeholder holds position, adjacent cards shift — PASS
- 999.10-01-04: Drag cancel: placeholder visible throughout snap-back animation — PASS

## Known Stubs

None.

## Self-Check: PASSED

- src/components/DraggableCard.tsx: exists and modified
- src/components/HandZone.tsx: exists and modified
- src/components/BoardView.tsx: exists and modified (prop threading)
- src/components/PileZone.tsx: exists and modified (placeholder + prop)
