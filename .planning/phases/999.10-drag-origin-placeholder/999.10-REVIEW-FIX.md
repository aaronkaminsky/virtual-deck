---
phase: 999.10-drag-origin-placeholder
fixed_at: 2026-04-16T00:00:00Z
review_path: .planning/phases/999.10-drag-origin-placeholder/999.10-REVIEW.md
iteration: 1
findings_in_scope: 2
fixed: 2
skipped: 0
status: all_fixed
---

# Phase 999.10: Code Review Fix Report

**Fixed at:** 2026-04-16
**Source review:** .planning/phases/999.10-drag-origin-placeholder/999.10-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 2
- Fixed: 2
- Skipped: 0

## Fixed Issues

### CR-01: Non-null assertion on `dragDataRef.current` in `isPassCard` branch without null guard

**Files modified:** `src/components/BoardDragLayer.tsx`
**Commit:** 29b8db5
**Applied fix:** Added `&& dragDataRef.current` to the `isPassCard` boolean expression so it mirrors the same defensive null guard already used by `isSuccess`. The non-null assertion at line 65 (`dragDataRef.current!`) is now safe because `isPassCard` can only be `true` when `dragDataRef.current` is non-null.

### WR-01: `didDragRef` in `DraggableCard` stays `true` permanently after drag-without-click

**Files modified:** `src/components/DraggableCard.tsx`
**Commit:** 1568144
**Applied fix:** Added `prevIsDragging` ref to track the previous `isDragging` value. The `useEffect` now detects the `true → false` transition and schedules a 300 ms `setTimeout` to reset `didDragRef.current` back to `false`. This lets the synchronous click handler (if one fires immediately after drag-end) still consume the suppression, while ensuring the ref is cleared when no click follows the drag.

---

_Fixed: 2026-04-16_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
