---
phase: 25
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/BoardView.tsx
  - src/components/GridZone.tsx
autonomous: true
requirements:
  - POLISH-03
  - POLISH-04

must_haves:
  truths:
    - "Personal spread band in BoardView has no bg-card background (inherits bg-background)"
    - "Personal spread band vertical padding is py-1 (was py-2)"
    - "GridZone GridCell droppable div uses h-[64px] sm:h-[88px]"
  artifacts:
    - path: "src/components/BoardView.tsx"
      provides: "Updated personal spread band wrapper"
      contains: "flex-shrink-0 px-4 py-1"
    - path: "src/components/GridZone.tsx"
      provides: "Updated GridCell height"
      contains: "h-[64px] sm:h-[88px]"
  key_links:
    - from: "BoardView personal spread band div"
      to: "SpreadZone component"
      via: "wrapper div wrapping the personal mySpreadZone render"
      pattern: "flex-shrink-0 px-4 py-1"
---

<objective>
Two targeted polish changes: remove bg-card and reduce padding on the personal spread band in BoardView.tsx (POLISH-03), and update GridCell height in GridZone.tsx to match the compact zone height from POLISH-04.

Purpose: The personal spread band blends into the board surface rather than appearing as a distinct card-colored footer. GridZone stays proportional with PileZone and SpreadZone after their height reductions.
Output: Updated BoardView.tsx and GridZone.tsx.
</objective>

<execution_context>
@/Users/aaronkaminsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronkaminsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-play-area-layout-center-canvas/25-CONTEXT.md
@.planning/phases/25-play-area-layout-center-canvas/25-UI-SPEC.md

<interfaces>
From src/components/BoardView.tsx (read before modifying):

Personal spread band wrapper (line 91 area):
```tsx
{mySpreadZone && (
  <div className="flex-shrink-0 bg-card px-4 py-2">
    <SpreadZone
      pile={mySpreadZone}
      sendAction={sendAction}
      draggingCardId={draggingCardId}
      interactive={true}
      selectedIds={selectedIds}
      onToggleSelect={onToggleSelect}
      onSelectAll={onSelectAll}
      selectionSource={selectionSource}
    />
  </div>
)}
```
Target: change `flex-shrink-0 bg-card px-4 py-2` → `flex-shrink-0 px-4 py-1`

From src/components/GridZone.tsx (read before modifying):

GridCell droppable div className (line 71 area):
```tsx
className={cn(
  'relative w-14 h-[79px] sm:w-20 sm:h-[112px] flex items-center justify-center',
  isOver ? 'bg-primary/20' : 'bg-secondary'
)}
```
Target: replace `h-[79px]` with `h-[64px]` and `sm:h-[112px]` with `sm:h-[88px]`.
Width classes `w-14` and `sm:w-20` are unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: POLISH-03 — Remove bg-card and reduce padding on personal spread band</name>
  <files>src/components/BoardView.tsx</files>
  <read_first>
    - src/components/BoardView.tsx — read the full file before touching anything; locate the `mySpreadZone &&` conditional block (around line 90-103) containing the personal spread band wrapper div.
    - .planning/phases/25-play-area-layout-center-canvas/25-UI-SPEC.md §POLISH-03 — confirm the target classNames: remove bg-card, reduce py-2 to py-1.
  </read_first>
  <action>
    In BoardView.tsx, locate the personal spread band wrapper div inside the `{mySpreadZone && (...)}` block. Change the wrapper div className from `"flex-shrink-0 bg-card px-4 py-2"` to `"flex-shrink-0 px-4 py-1"`.

    This removes `bg-card` (so the band inherits `bg-background` from the outer board div, per D-04) and reduces vertical padding from `py-2` (8px) to `py-1` (4px), per D-04. The SpreadZone component and all its props inside the wrapper are unchanged.

    Per D-03: the personal spread band remains a separate band in the flex-col layout above the HandZone — no structural layout changes to the board flex hierarchy.
  </action>
  <verify>
    <automated>grep -n "flex-shrink-0" /Users/aaronkaminsky/code/virtual-deck/src/components/BoardView.tsx</automated>
  </verify>
  <done>
    - BoardView.tsx contains `flex-shrink-0 px-4 py-1` as the personal spread band wrapper className
    - BoardView.tsx does NOT contain `bg-card px-4 py-2` on the personal spread band wrapper
    - npm run typecheck exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: POLISH-04 — Update GridCell height in GridZone to compact dimensions</name>
  <files>src/components/GridZone.tsx</files>
  <read_first>
    - src/components/GridZone.tsx — read the full file before touching anything; locate the GridCell function's droppable div className (around line 69-73) containing the height classes.
    - .planning/phases/25-play-area-layout-center-canvas/25-UI-SPEC.md §POLISH-04 — confirm new height tokens: mobile h-[64px], desktop sm:h-[88px], and that GridZone must be audited to match.
  </read_first>
  <action>
    In GridZone.tsx, inside the GridCell function's return block, locate the droppable div (the one with `ref={setDropRef}`) and update its className:
    - Replace `h-[79px]` with `h-[64px]`
    - Replace `sm:h-[112px]` with `sm:h-[88px]`

    Width classes (`w-14` and `sm:w-20`), the `relative`, `flex items-center justify-center`, and the `isOver ? 'bg-primary/20' : 'bg-secondary'` cn() conditional are all unchanged.

    Scan the rest of GridZone.tsx for any other occurrences of `h-[79px]` or `h-[112px]` — if any additional height declarations are found (e.g., in the outer grid container or in other elements), update those proportionally to `h-[64px]` / `sm:h-[88px]` as well. The goal is that no element in GridZone.tsx retains the old 79px/112px card-height tokens.
  </action>
  <verify>
    <automated>grep -n "h-\[79px\]\|h-\[112px\]\|h-\[64px\]\|h-\[88px\]" /Users/aaronkaminsky/code/virtual-deck/src/components/GridZone.tsx</automated>
  </verify>
  <done>
    - GridZone.tsx contains `h-[64px]` and `sm:h-[88px]` in the GridCell droppable div className
    - GridZone.tsx does NOT contain `h-[79px]` or `sm:h-[112px]` anywhere (old heights fully removed)
    - npm run typecheck exits 0
    - npm test exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→BoardView | gameState flows from server via PartyKit; no user-controlled CSS injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Information Disclosure | BoardView mySpreadZone visibility | accept | mySpreadZone is only rendered when present in gameState; server-controlled which zones are visible |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
- `grep -c "flex-shrink-0 px-4 py-1" src/components/BoardView.tsx` returns 1
- `grep -c "bg-card px-4 py-2" src/components/BoardView.tsx` returns 0 (removed)
- `grep -v "^" src/components/GridZone.tsx | grep -c "h-\[79px\]\|h-\[112px\]"` returns 0 (old heights gone)
- `grep -c "h-\[64px\]" src/components/GridZone.tsx` returns 1 or more
- `grep -c "sm:h-\[88px\]" src/components/GridZone.tsx` returns 1 or more
- `npm run typecheck` exits 0
- `npm test` exits 0
</verification>

<success_criteria>
1. Personal spread band in BoardView has no bg-card — blends into bg-background board surface (POLISH-03 / D-04)
2. Personal spread band padding is py-1 (4px) instead of py-2 (8px) (POLISH-03 / D-04)
3. GridCell droppable div uses h-[64px] sm:h-[88px] — proportional with PileZone and SpreadZone compact heights (POLISH-04)
4. No element in GridZone.tsx retains old h-[79px] or sm:h-[112px] card-height values
5. npm run typecheck exits 0; npm test exits 0
</success_criteria>

<output>
Create `.planning/phases/25-play-area-layout-center-canvas/25-03-SUMMARY.md` when done.
</output>
