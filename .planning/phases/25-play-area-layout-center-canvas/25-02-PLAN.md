---
phase: 25
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PileZone.tsx
autonomous: true
requirements:
  - POLISH-02
  - POLISH-04

must_haves:
  truths:
    - "Pile controls appear above the pile card, not below it"
    - "Pile label and buttons share a single flex justify-between header row"
    - "Pile label is hidden at narrow widths (hidden sm:inline)"
    - "Pile card uses compact height h-[64px] sm:h-[88px]"
    - "Eye, Shuffle, and SelectAll buttons retain variant=ghost h-7 w-7 p-0 styling"
  artifacts:
    - path: "src/components/PileZone.tsx"
      provides: "Restructured PileZone with header row above card"
      contains: "flex justify-between items-center"
  key_links:
    - from: "PileZone outer wrapper"
      to: "header row div"
      via: "flex flex-col gap-1 containing flex justify-between items-center"
      pattern: "flex justify-between items-center"
---

<objective>
Restructure PileZone.tsx to move pile controls from a row below the pile card to a header row above it (POLISH-02), and update card zone height to compact dimensions (POLISH-04).

Purpose: Controls are logically grouped with the pile label at the top rather than buried below. Zone heights are more compact across the board.
Output: Updated PileZone.tsx with header-row layout and compact height.
</objective>

<execution_context>
@/Users/aaronkaminsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronkaminsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-play-area-layout-center-canvas/25-CONTEXT.md
@.planning/phases/25-play-area-layout-center-canvas/25-UI-SPEC.md

<interfaces>
From src/components/PileZone.tsx (read before modifying):

PileZoneProps:
```typescript
interface PileZoneProps {
  pile: ClientPile;
  sendAction: (action: ClientAction) => void;
  draggingCardId: string | null;
  shufflingPileIds?: Set<string>;
  onSelectAll?: (cardIds: string[], zone: 'hand' | 'pile', zoneId: string) => void;
}
```

Current outer wrapper (line 49): `<div className="flex flex-col items-center gap-1">`
Current label (line 50): `<span className="text-xs text-muted-foreground">{pile.name}</span>`
Current droppable div className (lines 54-58):
  `'w-[56px] h-[79px] sm:w-[80px] sm:h-[112px] rounded-lg border flex flex-col items-center justify-center relative bg-secondary'`
Current controls div below the card (line 82): `<div className="flex gap-1 mt-1">`
  - Button: Eye/EyeOff, onClick=handleToggleFace, title/aria-label variants for faceUp state
  - Button: Shuffle icon, onClick=handleShuffle, title="Shuffle pile" aria-label="Shuffle pile"
  - Button: SquareCheck icon, onClick=handleSelectAll, title="Select all cards in pile" aria-label="Select all", disabled when isEmpty or !topCard or !('id' in topCard)
  All buttons use: `variant="ghost" className="h-7 w-7 p-0"`

Width classes on droppable div are unchanged: `w-[56px] sm:w-[80px]`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: POLISH-02 — Restructure PileZone to header row above card</name>
  <files>src/components/PileZone.tsx</files>
  <read_first>
    - src/components/PileZone.tsx — read the full file before touching anything; note the current outer wrapper at line 49, the standalone label at line 50, and the controls div at lines 82-111.
    - .planning/phases/25-play-area-layout-center-canvas/25-UI-SPEC.md §POLISH-02 — confirm the exact header row structure and mobile label hiding pattern.
  </read_first>
  <action>
    Restructure the outer wrapper and header/footer layout in PileZone.tsx:

    1. Change outer wrapper from `flex flex-col items-center gap-1` to `flex flex-col gap-1` — remove `items-center` so the header row can span the full pile width rather than being centered to card width.

    2. Replace the standalone label span (line 50) and the controls div below the card (lines 82-111) with a single header row div placed ABOVE the droppable card div. The new header row is:
       `<div className="flex justify-between items-center">`
         Left child: `<span className="text-xs text-muted-foreground hidden sm:inline">{pile.name}</span>`
         Right child: `<div className="flex gap-1">` containing the three buttons (Eye/EyeOff, Shuffle, SelectAll) in the same order they appear in the current controls div, with identical props.
       `</div>`

    Per D-01: label on the left, buttons on the right, all on one row above the pile card.
    Per D-02: label uses `hidden sm:inline` to hide at mobile widths while buttons remain visible at all sizes.

    The droppable card div (setNodeRef, data-testid, className) and all its children (shuffle animation, drag placeholder, card/badge rendering) are unchanged — only the surrounding structure changes.

    Delete the old standalone label `<span>` at the old line 50 and the old controls `<div className="flex gap-1 mt-1">` at old lines 82-111. The `mt-1` was the bottom margin for the controls div; since controls move to the top header, this margin is no longer needed.
  </action>
  <verify>
    <automated>grep -n "flex justify-between items-center\|hidden sm:inline\|flex gap-1 mt-1\|items-center gap-1" /Users/aaronkaminsky/code/virtual-deck/src/components/PileZone.tsx</automated>
  </verify>
  <done>
    - PileZone.tsx contains `flex justify-between items-center` (the new header row)
    - PileZone.tsx contains `hidden sm:inline` (label hidden at mobile, per D-02)
    - PileZone.tsx does NOT contain `flex gap-1 mt-1` (old controls div removed)
    - PileZone.tsx does NOT contain `flex flex-col items-center gap-1` (old outer wrapper removed)
    - npm run typecheck exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: POLISH-04 — Compact pile card zone height</name>
  <files>src/components/PileZone.tsx</files>
  <read_first>
    - src/components/PileZone.tsx — already read in Task 1; focus on the className of the droppable div (was line 54-58, may shift after Task 1 edits).
    - .planning/phases/25-play-area-layout-center-canvas/25-UI-SPEC.md §POLISH-04 — confirm new height tokens: mobile h-[64px], desktop sm:h-[88px].
  </read_first>
  <action>
    In the droppable div's className, replace height class values:
    - Replace `h-[79px]` with `h-[64px]`
    - Replace `sm:h-[112px]` with `sm:h-[88px]`

    Width classes `w-[56px]` and `sm:w-[80px]` are unchanged. All other classes on the droppable div (rounded-lg, border, flex, flex-col, items-center, justify-center, relative, bg-secondary, and the cn() conditionals for isEmpty and isOver) are unchanged.
  </action>
  <verify>
    <automated>grep -n "h-\[79px\]\|h-\[112px\]\|h-\[64px\]\|h-\[88px\]" /Users/aaronkaminsky/code/virtual-deck/src/components/PileZone.tsx</automated>
  </verify>
  <done>
    - `h-[64px]` appears in PileZone.tsx (new mobile height)
    - `sm:h-[88px]` appears in PileZone.tsx (new desktop height)
    - `h-[79px]` and `sm:h-[112px]` do NOT appear in PileZone.tsx (old heights removed)
    - npm run typecheck exits 0
    - npm test exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→PileZone | pile prop data comes from server-authoritative state; no user-controlled class injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02 | Tampering | PileZone button handlers | accept | handleToggleFace, handleShuffle, handleSelectAll dispatch to server; server validates all actions |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
- `grep -c "flex justify-between items-center" src/components/PileZone.tsx` returns 1
- `grep -c "hidden sm:inline" src/components/PileZone.tsx` returns 1
- `grep -v "^" src/components/PileZone.tsx | grep -c "h-\[79px\]\|h-\[112px\]"` returns 0 (old heights gone)
- `grep -c "h-\[64px\]" src/components/PileZone.tsx` returns 1
- `grep -c "sm:h-\[88px\]" src/components/PileZone.tsx` returns 1
- `npm run typecheck` exits 0
- `npm test` exits 0
</verification>

<success_criteria>
1. Pile label and controls appear in a single header row above the pile card, not below (POLISH-02 / D-01)
2. Pile label uses `hidden sm:inline` — hidden at mobile, visible at sm+ widths (POLISH-02 / D-02)
3. Eye, Shuffle, SelectAll buttons retain identical variant, size, click handlers, title, aria-label, and disabled logic
4. Pile card droppable div uses h-[64px] sm:h-[88px] (POLISH-04)
5. npm run typecheck exits 0; npm test exits 0
</success_criteria>

<output>
Create `.planning/phases/25-play-area-layout-center-canvas/25-02-SUMMARY.md` when done.
</output>
