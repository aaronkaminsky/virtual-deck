---
phase: 05-resilience-polish
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified: []
autonomous: false
requirements:
  - ROOM-04

must_haves:
  truths:
    - "A player who disconnects and reconnects using the same room link gets their previous hand restored exactly"
    - "All players can see which players are currently connected vs. disconnected"
    - "A player who closes and reopens the tab (same browser, same room link) resumes with their hand intact"
  artifacts: []
  key_links: []
---

<objective>
Human verification of reconnect-to-hand, player presence indicators, and disconnection banner.

Purpose: Verify the three phase success criteria are met in a real multi-player session.
Output: Confirmation that ROOM-04 is satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-resilience-polish/05-CONTEXT.md
@.planning/phases/05-resilience-polish/05-01-SUMMARY.md
@.planning/phases/05-resilience-polish/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify reconnect-to-hand, presence, and disconnection UX</name>
  <files>none</files>
  <read_first>
    - .planning/phases/05-resilience-polish/05-CONTEXT.md
  </read_first>
  <what-built>
    Reconnect-to-hand (ROOM-04): Server identity now uses the stable ?player= token from localStorage. Closing and reopening a tab restores the player's hand. Player presence dots show connected/disconnected state for all players. A disconnection banner appears when the WebSocket drops, escalating after 10 seconds.
  </what-built>
  <action>
    Start the dev servers (`npx partykit dev` and `npx vite dev`) and present the 4 test scenarios below to the user for manual verification. No code changes — this is a verification-only checkpoint.
  </action>
  <how-to-verify>
    Start the dev server: `npx partykit dev` and `npx vite dev` (or however the project starts).

    **Test 1 — Reconnect restores hand:**
    1. Open two browser tabs to the same room URL (e.g., `http://localhost:5173/?room=test123`)
    2. Deal cards (use the Deal button)
    3. Note the cards in Player 1's hand
    4. Close Player 1's tab completely
    5. Reopen the same URL in a new tab (`http://localhost:5173/?room=test123`)
    6. EXPECTED: Player 1's hand is restored exactly as before closing

    **Test 2 — Presence dots:**
    1. With both tabs open, check the top-right area of the board
    2. EXPECTED: Two colored dots visible, both green
    3. Close one tab
    4. EXPECTED: In the remaining tab, one dot turns gray (disconnected)
    5. Hover over the dots
    6. EXPECTED: Tooltips show "Connected" or "Disconnected" (and "You" for your own dot)

    **Test 3 — Disconnection banner:**
    1. In one tab, open DevTools > Network and set throttling to "Offline"
    2. EXPECTED: After ~1 second, an amber banner appears: "Connection lost. Reconnecting..."
    3. Wait 10+ seconds
    4. EXPECTED: Banner text changes to "Connection lost — refresh to rejoin"
    5. Set network back to "Online"
    6. EXPECTED: Banner disappears automatically

    **Test 4 — Room cap with reconnect:**
    1. Open 4 tabs to the same room URL (4 different players)
    2. Close one tab
    3. Reopen the same URL in a new tab
    4. EXPECTED: The reconnecting player gets their slot back (not rejected as a 5th player)
  </how-to-verify>
  <verify>
    <automated>cd /Users/aaronkaminsky/code/virtual-deck && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Test 1: Player's hand is restored after tab close/reopen
    - Test 2: Presence dots correctly reflect connected/disconnected state
    - Test 3: Banner appears on disconnect, escalates at 10s, dismisses on reconnect
    - Test 4: Reconnecting player is not blocked by 4-player cap
  </acceptance_criteria>
  <done>Human confirms all 4 test scenarios pass and ROOM-04 is satisfied</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All three phase success criteria verified by human:
1. Reconnect restores hand
2. Presence dots show connected/disconnected
3. Tab close + reopen resumes with hand intact
</verification>

<success_criteria>
- Human confirms all 4 test scenarios pass
- ROOM-04 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-resilience-polish/05-03-SUMMARY.md`
</output>
