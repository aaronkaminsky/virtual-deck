---
phase: 21
plan: 04
type: execute
wave: 2
depends_on: [21-02, 21-03]
files_modified:
  - .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [SPREAD-02]
tags: [uat, manual-verification, requirements-traceability]
must_haves:
  truths:
    - "A HUMAN-UAT.md file exists in the phase directory with the same frontmatter shape as 19-HUMAN-UAT.md"
    - "The UAT script covers all manual-only verifications listed in 21-VALIDATION.md (SC2 drag-start selection, SC2 drag-end selection, SC2 group reorder feel, SC2 unselected-drag does NOT trigger group, SC3 spread undo, SC3 hand undo, SC1 multi-player sync)"
    - "A live human verification checkpoint is run with the actual app (npm run dev + npm run dev:client) and results are recorded"
    - "REQUIREMENTS.md traceability table marks SPREAD-02 Status as Complete"
    - "REQUIREMENTS.md SPREAD-02 line in the v1.3 Requirements section moves from [ ] unchecked to [x] checked"
  artifacts:
    - path: ".planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md"
      provides: "Manual UAT script and live-session results"
      contains: "## Tests"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table"
      contains: "| SPREAD-02 | Phase 21 | Complete |"
  key_links:
    - from: "21-HUMAN-UAT.md"
      to: "21-VALIDATION.md (Manual-Only Verifications section)"
      via: "1:1 mapping of test cases"
      pattern: "Selection preserved through intra-zone drag"
    - from: ".planning/REQUIREMENTS.md SPREAD-02 traceability row"
      to: "21-HUMAN-UAT.md status"
      via: "completion gate"
      pattern: "SPREAD-02.*Phase 21.*Complete"
---

<objective>
Drive the manual UAT pass that covers behaviors no Vitest can verify (dnd-kit DOM/pointer flows: drag-start selection, drag-end selection, group-reorder feel, multi-player sync), then close the phase by marking SPREAD-02 complete in REQUIREMENTS.md.

Purpose: SPREAD-02 SC1 (multi-player sync), SC2 selection preservation feel, and SC3 undo feel all require a live game session. Plans 02 and 03 produced automated coverage for everything that CAN be automated; this plan closes the verification gap and updates the traceability table.

Output:
- 21-HUMAN-UAT.md with completed test results (7 tests minimum)
- REQUIREMENTS.md updated: SPREAD-02 checkbox → [x], traceability row → Complete
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-phase-14-live-session-verification/21-CONTEXT.md
@.planning/phases/21-phase-14-live-session-verification/21-VALIDATION.md
@.planning/phases/19-npm-audit/19-HUMAN-UAT.md

<interfaces>
UAT frontmatter shape — read directly from `.planning/phases/19-npm-audit/19-HUMAN-UAT.md` lines 1–7. The canonical fields are: `status` (pending | in_progress | complete), `phase` (full directory name), `source` (inline array of related planning artifacts), `started` (ISO-8601 timestamp), `updated` (ISO-8601 timestamp).

Per-test block format — each numbered `### N. <Test name>` heading is followed by an `expected:` line (single sentence describing expected behavior) and a `result:` line (one of: pending, passed, failed).

Summary block format — `## Summary` heading followed by four lines: `total: N`, `passed: N`, `issues: N`, `pending: N`. (See 19-HUMAN-UAT.md lines 47–55.)

Dev stack to launch for UAT (verified from CLAUDE.md):
- Terminal 1: `npm run dev` (PartyKit local server)
- Terminal 2: `npm run dev:client` (Vite dev server)
- For SC1 multi-player: open two browser windows to the same room URL with two different player names

Current REQUIREMENTS.md state for SPREAD-02 (verified):
- v1.3 Requirements section line: `- [ ] **SPREAD-02**: Player can drag-reorder cards within a spread zone; reorder behaves correctly when multi-select state is active`
- Traceability table row: `| SPREAD-02 | Phase 21 | Pending |`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 21-HUMAN-UAT.md scaffold with 7 pending tests (mirrors 19-HUMAN-UAT.md format)</name>
  <files>.planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/19-npm-audit/19-HUMAN-UAT.md (canonical format — frontmatter at lines 1–7, ## Current Test, ## Tests with numbered ### blocks, ## Summary at lines 47–55, ## Gaps; copy structure exactly)
    - .planning/phases/21-phase-14-live-session-verification/21-VALIDATION.md "Manual-Only Verifications" section (the test list this UAT must cover)
    - .planning/phases/21-phase-14-live-session-verification/21-CONTEXT.md (D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-13 — context for what the UAT is verifying)
  </read_first>
  <action>
    Use the Write tool to create the file at the exact path: `.planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md`. Substitute the current ISO-8601 timestamp at file write time (use `new Date().toISOString()` semantics — e.g., `2026-05-11T16:45:00.000Z`).

    The file structure is exactly:

    (1) YAML frontmatter block delimited by triple-dash lines, containing exactly these 5 fields in this order:
        - status: pending
        - phase: 21-phase-14-live-session-verification
        - source: [21-CONTEXT.md, 21-RESEARCH.md, 21-VALIDATION.md]
        - started: <CURRENT_ISO_TIMESTAMP>
        - updated: <CURRENT_ISO_TIMESTAMP>

    (2) A `## Current Test` heading followed by the line `[not yet started]`.

    (3) A `## Setup` heading with three lines describing the two-terminal dev stack and the multi-window requirement for Test 7. Verbatim text:
        - "Two terminals required:"
        - "1. `npm run dev` — PartyKit local server (room state authority)"
        - "2. `npm run dev:client` — Vite dev server (open http://localhost:5173)"
        - blank line
        - "For Test 7 (multi-player sync): open the same room URL in two browser windows (e.g., Chrome + Chrome Incognito), join with two distinct player names."

    (4) A `## Tests` heading followed by exactly 7 numbered test blocks. Each block has a `### N. <name>` heading line, an `expected:` line, and a `result: pending` line. Use these exact test definitions:

    Test 1 — heading: `### 1. Selection preserved through intra-spread-zone drag-start (SPREAD-02 SC2, D-01)`
    expected: Select 2+ cards in a personal spread zone (e.g., the "play" pile after dealing some cards there). Begin dragging one of the selected cards within the same spread zone (do not drop yet). The "N selected" badge stays visible and the selected ring/lift treatment remains on all selected cards while the drag is in progress.
    result: pending

    Test 2 — heading: `### 2. Selection preserved through intra-spread-zone drag-end (SPREAD-02 SC2, D-02)`
    expected: Continuing from Test 1, complete the drop within the same spread zone (drag onto another card in the same zone). After the drop completes, the "N selected" badge is still visible and the selected cards are still highlighted. No selection clear after a successful intra-zone reorder.
    result: pending

    Test 3 — heading: `### 3. Group reorder — selected block moves together with original relative order preserved (SPREAD-02 SC2, D-03/D-04/D-06)`
    expected: With cards [A, B, C, D, E] in a spread zone, select B and D (two non-adjacent cards), then drag B onto E. After the drop, the spread zone order is [A, C, B, D, E] — selected cards B and D moved as a block to the position before E, and their original relative order (B before D) is preserved. Then with [A, B, C, D, E] select A, B, C and drag onto E — order becomes [D, A, B, C, E]. Test against both a personal spread zone and the communal "play" spread zone.
    result: pending

    Test 4 — heading: `### 4. Unselected drag does NOT trigger group reorder (SPREAD-02 SC2)`
    expected: With cards [A, B, C, D, E] in a spread zone, select B and D. Then drag UNSELECTED card C onto E. Only card C moves (single-card reorder via arrayMove). Cards B and D do NOT move and the selection is cleared (per Phase 20 D-04 invariant — dragging an unselected card while others are selected clears selection).
    result: pending

    Test 5 — heading: `### 5. Selection preserved through intra-hand drag (SPREAD-02 SC2, D-01/D-02 — hand variant)`
    expected: Select 2+ cards in your own hand. Drag one selected card within the hand to reorder. The "N selected" badge in the hand row stays visible during drag and after drop. Group reorder applies — all selected hand cards move as a block in their original relative order. Dragging an unselected hand card while others are selected clears selection (Phase 20 invariant).
    result: pending

    Test 6 — heading: `### 6. Undo restores prior order — spread zone reorder (SPREAD-02 SC3, D-07)`
    expected: Place at least 3 face-up cards in a spread zone (e.g., [A, B, C] in "play"). Reorder them to [C, A, B] via drag. Open the controls panel and click Undo (or press the undo button). The spread zone returns to [A, B, C]. Repeat with a group reorder: cards [A, B, C, D, E], select B+D, drag onto E → [A, C, B, D, E]. Click Undo → cards return to [A, B, C, D, E].
    result: pending

    Test 7 — heading: `### 7. Multi-player sync — reorder visible to other players in real time (SPREAD-02 SC1, D-13)`
    expected: Open two browser windows joined to the same room as two different players. With cards in the communal "play" spread zone, reorder them in Window 1. Window 2 reflects the new order within ~1 second (PartyKit broadcast latency). Then perform a group reorder in Window 1 with 2 selected cards — Window 2 sees both cards moved as a block. Trigger Undo in Window 1 — both windows see the prior order.
    result: pending

    (5) A `## Summary` heading followed by 4 count lines:
        - total: 7
        - passed: 0
        - issues: 0
        - pending: 7

    (6) A `## Gaps` heading followed by the line: `(none yet — populate during execution if any test fails)`

    Do NOT add any other sections. Do NOT use code fences anywhere in the file (the format is a flat markdown document — see 19-HUMAN-UAT.md for the precise template). Do NOT include emojis.

    After write, run `head -10 .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` and confirm the frontmatter parses (status: pending visible on line 2).

    Commit message:
    `docs(21-04): add HUMAN-UAT scaffold for SPREAD-02 manual verification`
  </action>
  <verify>
    <automated>test -f .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md && grep -c '^### [0-9]' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md && grep -E '^status:|^phase:|^source:' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the canonical path: `test -f .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md`
    - Frontmatter has the five required fields: `grep -cE '^(status|phase|source|started|updated):' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` returns 5
    - Status starts pending: `grep -c '^status: pending' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` returns 1
    - Phase value matches directory: `grep -c '^phase: 21-phase-14-live-session-verification' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` returns 1
    - Exactly 7 numbered tests: `grep -cE '^### [1-7]\.' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` returns 7
    - Every test has an `expected:` line and a `result:` line: `grep -c '^expected:' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` returns 7; `grep -c '^result:' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` returns 7
    - Summary section present: `grep -c '^## Summary' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` returns 1
    - Summary counts match the pending state: `grep -E '^(total|passed|issues|pending):' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md | head -4` shows `total: 7`, `passed: 0`, `issues: 0`, `pending: 7`
    - Each SC referenced at least once: `grep -cE 'SC1|SC2|SC3' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` returns 7 or more
    - D-decisions traced: `grep -cE 'D-0[1-9]|D-1[0-3]' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` returns 7 or more
  </acceptance_criteria>
  <done>
    21-HUMAN-UAT.md exists with 7 pending tests covering all behaviors enumerated in 21-VALIDATION.md "Manual-Only Verifications." Ready for live session execution.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT pass — execute 21-HUMAN-UAT.md against the running app and record results</name>
  <what-built>
    All of Phase 21's automated work is GREEN: tests/reorderUndo.test.ts (4/4) + tests/groupReorder.test.ts (5/5) + full suite. Plans 02 and 03 shipped surgical edits to party/index.ts, BoardDragLayer.tsx, SpreadZone.tsx, HandZone.tsx. The HUMAN-UAT.md scaffold from Task 1 lists exactly which behaviors need eyes.
  </what-built>
  <how-to-verify>
    1. Pull the latest code on this branch.
    2. Start the local stack in two terminals:
       - Terminal A: `npm run dev`
       - Terminal B: `npm run dev:client`
    3. Open http://localhost:5173 and create a room. Join with a name.
    4. For Test 7, open the same room URL in a second browser window with a different name.
    5. Walk through tests 1–7 in `.planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` exactly in order. For each test, update the `result:` line from `pending` to `passed` or `failed`. If `failed`, write a one-paragraph detail block under `## Gaps` describing what went wrong (e.g., "Gap 1: Selection cleared on drag-start in personal spread zone — selectedIds.size dropped to 0 immediately when grabbing a selected card").
    6. After all 7 tests are walked, update the frontmatter `status:` to either `complete` (if 7/7 passed) or `in_progress` (if any failed — gaps need plans).
    7. Update the `updated:` frontmatter timestamp and the `## Summary` counts.

    Tests must be performed in a real browser with real pointer events. dnd-kit drag-flow behaviors (selection preservation through drag-start/drag-end, the visual feel of the group block landing where you dropped it) cannot be unit-tested in node Vitest — this checkpoint is the only verification that covers them.

    Type "approved" when 7/7 pass and the file is updated. If any test fails: describe the gaps; we will plan gap closure plans before proceeding to Task 3.
  </how-to-verify>
  <resume-signal>Type "approved" once 21-HUMAN-UAT.md shows status: complete with total: 7 / passed: 7 / pending: 0. If any test fails, type "gaps: <list>" — we will halt and run /gsd-plan-phase --gaps.</resume-signal>
  <files>.planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md</files>
  <action>Pause execution. The user runs the live UAT pass per the steps in &lt;how-to-verify&gt;, walks through tests 1–7 in 21-HUMAN-UAT.md against the running app (npm run dev + npm run dev:client), updates each result line, and finalizes the frontmatter status, updated timestamp, and Summary counts.</action>
  <verify>
    <automated>grep -c "^status: complete" .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md</automated>
  </verify>
  <done>21-HUMAN-UAT.md frontmatter status is "complete"; Summary shows passed: 7, pending: 0. User has typed "approved" to resume.</done>
</task>

<task type="auto">
  <name>Task 3: Mark SPREAD-02 complete in REQUIREMENTS.md (D-12)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — v1.3 Spread Zones checklist around line 19; traceability table around line 46)
    - .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md (frontmatter status must read `complete` before this task runs)
    - .planning/phases/21-phase-14-live-session-verification/21-CONTEXT.md (D-12)
  </read_first>
  <action>
    Pre-check: confirm `.planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` frontmatter shows `status: complete`. Run `grep -c '^status: complete' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` — if 0, abort and report to the user (REQUIREMENTS.md update is conditional on UAT pass per D-12).

    Two surgical edits in .planning/REQUIREMENTS.md (use the Edit tool — exact strings):

    Edit 1 — flip the requirements checkbox. Replace the line:
        `- [ ] **SPREAD-02**: Player can drag-reorder cards within a spread zone; reorder behaves correctly when multi-select state is active`
    with:
        `- [x] **SPREAD-02**: Player can drag-reorder cards within a spread zone; reorder behaves correctly when multi-select state is active`
    (The only change is `[ ]` → `[x]`.)

    Edit 2 — flip the traceability table row. Replace the line:
        `| SPREAD-02 | Phase 21 | Pending |`
    with:
        `| SPREAD-02 | Phase 21 | Complete |`
    (The only change is `Pending` → `Complete`.)

    Do NOT modify any other line in REQUIREMENTS.md — no other SPREAD or LAYOUT requirements change. Do NOT touch the milestone goal, future requirements, or out-of-scope sections.

    Commit message:
    `docs(21-04): mark SPREAD-02 complete after UAT pass (D-12)`
  </action>
  <verify>
    <automated>grep -c '^- \[x\] \*\*SPREAD-02\*\*' .planning/REQUIREMENTS.md && grep -c '| SPREAD-02 | Phase 21 | Complete |' .planning/REQUIREMENTS.md && grep -c '| SPREAD-02 | Phase 21 | Pending |' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - SPREAD-02 checkbox is checked: `grep -c '^- \[x\] \*\*SPREAD-02\*\*' .planning/REQUIREMENTS.md` returns 1
    - SPREAD-02 traceability row marked Complete: `grep -c '| SPREAD-02 | Phase 21 | Complete |' .planning/REQUIREMENTS.md` returns 1
    - Old Pending row removed: `grep -c '| SPREAD-02 | Phase 21 | Pending |' .planning/REQUIREMENTS.md` returns 0
    - Old unchecked checkbox removed: `grep -c '^- \[ \] \*\*SPREAD-02\*\*' .planning/REQUIREMENTS.md` returns 0
    - Other v1.3 requirements unchanged: `grep -cE '^- \[(x| )\] \*\*(LAYOUT-0[1-4]|SPREAD-0[134])\*\*' .planning/REQUIREMENTS.md` returns 7 (all 7 non-SPREAD-02 requirements untouched)
    - HUMAN-UAT precondition met: `grep -c '^status: complete' .planning/phases/21-phase-14-live-session-verification/21-HUMAN-UAT.md` returns 1
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects SPREAD-02 completion (both checkbox and traceability row). All 8 v1.3 requirements are now checked. Phase 21 is closeable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan only touches planning documents (HUMAN-UAT.md, REQUIREMENTS.md). No production code, no network paths, no new authorization surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Repudiation | UAT result accuracy | mitigate | UAT results are recorded in 21-HUMAN-UAT.md with a git commit; status changes are auditable via git log. D-12 explicitly requires UAT pass before REQUIREMENTS.md is updated — Task 3 includes a pre-check that the UAT frontmatter shows `status: complete`. |
| T-21-11 | Information Disclosure | HUMAN-UAT.md content | accept | UAT document contains only test descriptions and pass/fail results; no PII, no secrets, no production data. Same risk profile as 19-HUMAN-UAT.md. |

No high-severity threats. This plan does not introduce any new code paths.
</threat_model>

<verification>
- 21-HUMAN-UAT.md exists with frontmatter and 7 tests (verified by grep counts in Task 1 acceptance criteria)
- Live UAT pass recorded — `status: complete` and 7/7 passed (Task 2 checkpoint)
- REQUIREMENTS.md SPREAD-02 row + checkbox both updated (Task 3 acceptance criteria)
- Phase 21 status across all artifacts is consistent: ROADMAP.md Phase 21 row will be flipped by /gsd-verify-work to Complete in the next workflow step
</verification>

<success_criteria>
- SPREAD-02 fully verified across automated (Plans 02+03 tests) + manual (this plan's UAT) gates
- D-10, D-11, D-12 all satisfied
- 21-HUMAN-UAT.md committed with complete results — durable evidence for milestone review
- REQUIREMENTS.md traceability matches reality
</success_criteria>

<output>
After completion, create `.planning/phases/21-phase-14-live-session-verification/21-04-SUMMARY.md`.
</output>
