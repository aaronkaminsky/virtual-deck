---
phase: 999.2
slug: put-card-back-on-draw-pile-top-bottom-or-random-position
status: verified
threats_open: 0
asvs_level: L1
created: 2026-04-12
---

# Phase 999.2 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Client → PartyKit server | WebSocket MOVE_CARD action | `insertPosition` string field (user-controlled) |
| Server internal | Game state mutation | Random index computed and consumed server-side; never broadcast |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-999.2-01 | Tampering | `party/index.ts` MOVE_CARD handler | mitigate | `else` fallback at line 237 defaults any unexpected `insertPosition` value to `dest.push()` (top). No code path can be reached via arbitrary string — only `'bottom'` and `'random'` branch, all else is safe push. | closed |
| T-999.2-02 | Information Disclosure | `party/index.ts` broadcast | mitigate | Random `idx` is computed and immediately consumed via `dest.splice(idx, 0, card)`. It is never included in any message broadcast to clients. D-05 enforced. | closed |
| T-999.2-03 | Tampering | `party/index.ts` crypto | mitigate | `crypto.getRandomValues` (CSPRNG) generates the random index. Modulo bias (`buf[0] % (dest.length + 1)`) is negligible for pile sizes ≤ 52 cards (< 0.0001% bias). Acceptable for a card game. | closed |
| T-999.2-04 | Tampering | `BoardDragLayer.tsx` `pendingMove` state | mitigate | `Dialog.Root onOpenChange` fires `sendPendingMove('top')` on any close event (Escape, click-outside, or programmatic). Card cannot enter limbo — it always lands somewhere. D-03 enforced. | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

No accepted risks.

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-04-12 | 4 | 4 | 0 | gsd-secure-phase (orchestrator) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-04-12
