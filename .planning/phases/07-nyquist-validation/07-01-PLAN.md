---
phase: 07-nyquist-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-server-foundation/01-VALIDATION.md
  - .planning/phases/03-core-board/03-VALIDATION.md
  - .planning/phases/04-game-controls/04-VALIDATION.md
  - .planning/phases/05-resilience-polish/05-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 1 VALIDATION.md frontmatter has nyquist_compliant: true and wave_0_complete: true"
    - "Phase 3 VALIDATION.md frontmatter has wave_0_complete: true (nyquist_compliant already true)"
    - "Phase 4 VALIDATION.md frontmatter has nyquist_compliant: true and wave_0_complete: true"
    - "Phase 5 VALIDATION.md exists with nyquist_compliant: true"
    - "Per-task verification maps reflect reality: tasks with existing passing test files marked green"
    - "npm test still passes 88/88 after edits (no code touched)"
  artifacts:
    - path: ".planning/phases/01-server-foundation/01-VALIDATION.md"
      provides: "Updated flags + green status for completed tasks"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/03-core-board/03-VALIDATION.md"
      provides: "wave_0_complete flipped to true"
      contains: "wave_0_complete: true"
    - path: ".planning/phases/04-game-controls/04-VALIDATION.md"
      provides: "Updated flags + green status for CARD-03/04 + CTRL-01..04"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/05-resilience-polish/05-VALIDATION.md"
      provides: "New VALIDATION.md for phase 5 covering reconnect + manual items"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/0X-*/0X-VALIDATION.md"
      to: "tests/*.test.ts"
      via: "Per-Task Verification Map rows referencing test files"
      pattern: "npm test.*tests/"
---

<objective>
Close the v1.0 Nyquist-compliance audit gap: all completed phases must have VALIDATION.md files with correct frontmatter flags and verification maps reflecting the current passing test suite (88/88). Phases 1, 3, 4 need in-place edits. Phase 5 needs a new VALIDATION.md created from the Phase 3 template.

Purpose: Validation artifacts are audit documents; stale flags undermine trust in the Nyquist compliance signal before v1.0 ship.
Output: 3 edited VALIDATION.md files + 1 new VALIDATION.md file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-nyquist-validation/07-CONTEXT.md

<!-- Phase 3 VALIDATION.md is the canonical structural template for Phase 5's new file -->
@.planning/phases/03-core-board/03-VALIDATION.md

<!-- Files being edited -->
@.planning/phases/01-server-foundation/01-VALIDATION.md
@.planning/phases/04-game-controls/04-VALIDATION.md

<!-- Phase 5 source material for new VALIDATION.md -->
@.planning/phases/05-resilience-polish/05-CONTEXT.md
@.planning/phases/05-resilience-polish/05-01-SUMMARY.md
@.planning/phases/05-resilience-polish/05-02-SUMMARY.md
@.planning/phases/05-resilience-polish/05-03-SUMMARY.md
@tests/reconnect.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Phase 1, 3, and 4 VALIDATION.md frontmatter and verification maps</name>
  <files>.planning/phases/01-server-foundation/01-VALIDATION.md, .planning/phases/03-core-board/03-VALIDATION.md, .planning/phases/04-game-controls/04-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01-server-foundation/01-VALIDATION.md (current state before editing)
    - .planning/phases/03-core-board/03-VALIDATION.md (current state before editing)
    - .planning/phases/04-game-controls/04-VALIDATION.md (current state before editing)
    - .planning/phases/07-nyquist-validation/07-CONTEXT.md (D-01 through D-04 decisions)
    - tests/ directory listing (confirm all referenced test files exist)
  </read_first>
  <action>
    First run `npm test` and confirm 88/88 passing. If not green, STOP and report — do not proceed with flag updates.

    **Phase 1** (`.planning/phases/01-server-foundation/01-VALIDATION.md`):
    1. Frontmatter: change `nyquist_compliant: false` → `nyquist_compliant: true`; change `wave_0_complete: false` → `wave_0_complete: true`.
    2. In the Test Infrastructure table, change the Config file row value from `` `vitest.config.ts` (Wave 0 gap — does not yet exist) `` to `` `vitest.config.ts` (root) ``.
    3. In the Per-Task Verification Map: the following tasks now have existing passing test files — change `File Exists` from `❌ W0` to `✅` and `Status` from `⬜ pending` to `✅ green`:
       - 1-01-01 (DECK-01, tests/deck.test.ts)
       - 1-01-02 (DECK-02, tests/shuffle.test.ts)
       - 1-01-03 (CARD-05, tests/viewFor.test.ts)
       - 1-02-01 (DECK-01, tests/deck.test.ts)
       - 1-02-02 (DECK-02, tests/shuffle.test.ts)
       - 1-02-03 (CARD-05, tests/viewFor.test.ts)
       - Leave 1-02-04 (ROOM-03, manual) as `N/A` / `⬜ pending` per D-02.
    4. In the Wave 0 Requirements section: change every `- [ ]` to `- [x]` (all six items exist: tests/deck.test.ts, tests/shuffle.test.ts, tests/viewFor.test.ts, vitest.config.ts, src/shared/types.ts, party/index.ts).
    5. In the Validation Sign-Off section: change every `- [ ]` to `- [x]`. Change `**Approval:** pending` to `**Approval:** approved 2026-04-09 (Phase 7 audit)`.

    **Phase 3** (`.planning/phases/03-core-board/03-VALIDATION.md`):
    1. Frontmatter: change `wave_0_complete: false` → `wave_0_complete: true`. Leave `nyquist_compliant: true` unchanged.
    2. In the Per-Task Verification Map: for rows 03-01-01 (TABLE-01, TABLE-02, CARD-01, CARD-02) change `File Exists` from `❌ W0` to `✅` and `Status` from `⬜ pending` to `✅ green`. For all other rows with `File Exists: ✅` change `Status` from `⬜ pending` to `✅ green`. Leave the manual row 03-03-03 as `⬜ pending` per D-02.
    3. In the Wave 0 Requirements section: change both `- [ ]` entries to `- [x]` (tests/moveCard.test.ts exists; tests/deck.test.ts has been updated).
    4. Change `**Approval:** pending` to `**Approval:** approved 2026-04-09 (Phase 7 audit)`.

    **Phase 4** (`.planning/phases/04-game-controls/04-VALIDATION.md`):
    1. Frontmatter: change `nyquist_compliant: false` → `nyquist_compliant: true`; change `wave_0_complete: false` → `wave_0_complete: true`.
    2. In the Per-Task Verification Map: change `File Exists` from `❌ W0` to `✅` and `Status` from `⬜ pending` to `✅ green` for every row (all 18 rows — CARD-03, CARD-04, CTRL-01..04). All referenced test files exist: tests/flipCard.test.ts, tests/passCard.test.ts, tests/dealCards.test.ts, tests/shufflePile.test.ts, tests/resetTable.test.ts, tests/undoMove.test.ts.
    3. In the Wave 0 Requirements section: change all six `- [ ]` entries to `- [x]`.
    4. In the Validation Sign-Off section: change every `- [ ]` to `- [x]`. Change `**Approval:** pending` to `**Approval:** approved 2026-04-09 (Phase 7 audit)`.

    After edits, run `npm test` again to confirm no code was accidentally touched (should still be 88/88).
  </action>
  <verify>
    <automated>npm test && grep -q "nyquist_compliant: true" .planning/phases/01-server-foundation/01-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/01-server-foundation/01-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/03-core-board/03-VALIDATION.md && grep -q "nyquist_compliant: true" .planning/phases/04-game-controls/04-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/04-game-controls/04-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with 88 passed
    - `.planning/phases/01-server-foundation/01-VALIDATION.md` contains `nyquist_compliant: true` and `wave_0_complete: true`
    - `.planning/phases/01-server-foundation/01-VALIDATION.md` contains `approved 2026-04-09 (Phase 7 audit)`
    - `.planning/phases/01-server-foundation/01-VALIDATION.md` contains no remaining `❌ W0` substrings
    - `.planning/phases/01-server-foundation/01-VALIDATION.md` Wave 0 Requirements section contains no `- [ ]` checkboxes
    - `.planning/phases/03-core-board/03-VALIDATION.md` contains `wave_0_complete: true`
    - `.planning/phases/03-core-board/03-VALIDATION.md` contains `nyquist_compliant: true` (unchanged)
    - `.planning/phases/03-core-board/03-VALIDATION.md` Wave 0 Requirements section contains no `- [ ]` checkboxes
    - `.planning/phases/04-game-controls/04-VALIDATION.md` contains `nyquist_compliant: true` and `wave_0_complete: true`
    - `.planning/phases/04-game-controls/04-VALIDATION.md` contains no `❌ W0` substrings
    - `.planning/phases/04-game-controls/04-VALIDATION.md` Wave 0 Requirements section contains no `- [ ]` checkboxes
    - No changes to any file outside the three VALIDATION.md files (verify via `git status`)
  </acceptance_criteria>
  <done>All three VALIDATION.md files reflect current reality; frontmatter flags correct; verification maps show green for tasks with passing tests; manual-only rows remain pending; test suite still green.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 5 VALIDATION.md from scratch</name>
  <files>.planning/phases/05-resilience-polish/05-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-core-board/03-VALIDATION.md (canonical template structure to mirror)
    - .planning/phases/05-resilience-polish/05-CONTEXT.md (phase 5 scope and decisions)
    - .planning/phases/05-resilience-polish/05-01-SUMMARY.md
    - .planning/phases/05-resilience-polish/05-02-SUMMARY.md
    - .planning/phases/05-resilience-polish/05-03-SUMMARY.md
    - tests/reconnect.test.ts (to list specific test cases in verification map)
    - .planning/phases/07-nyquist-validation/07-CONTEXT.md (D-05, D-06, D-07)
  </read_first>
  <action>
    Create `.planning/phases/05-resilience-polish/05-VALIDATION.md` following the Phase 3 VALIDATION.md structure exactly. Use this frontmatter:

    ```yaml
    ---
    phase: 5
    slug: resilience-polish
    status: draft
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-09
    ---
    ```

    Required sections in order (mirror Phase 3 VALIDATION.md headings):

    1. **Title**: `# Phase 5 — Validation Strategy` with the same blockquote subtitle as Phase 3.

    2. **Test Infrastructure** table with columns Property/Value and rows:
       - Framework: `Vitest 4.1.2`
       - Config file: `` `vitest.config.ts` (root — `tests/**/*.test.ts`) ``
       - Quick run command: `npm test`
       - Full suite command: `npm test`
       - Estimated runtime: `~5 seconds`

    3. **Sampling Rate** section with same four bullets as Phase 3 (`After every task commit`, `After every plan wave`, `Before /gsd:verify-work`, `Max feedback latency: 10 seconds`).

    4. **Per-Task Verification Map** table with columns `| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |`. Include these rows (read tests/reconnect.test.ts first to map specific `it(...)` cases to task IDs; use the it-block description as the task identifier suffix). Minimum rows:
       - `05-01-01` | 01 | 0 | ROOM-04 | unit | `npm test -- tests/reconnect.test.ts` | ✅ | ✅ green — server reconnect re-sends state
       - `05-01-02` | 01 | 0 | ROOM-04 | unit | `npm test -- tests/reconnect.test.ts` | ✅ | ✅ green — reconnect preserves hand privacy
       - `05-02-01` | 02 | 1 | PRESENCE | manual | Multi-tab visual | — | ⬜ pending — presence dots
       - `05-03-01` | 03 | 1 | DISCONNECT-UX | manual | Network disconnect simulation | — | ⬜ pending — disconnection banner

       (Adjust reconnect row count and descriptions to match actual `it(...)` cases in tests/reconnect.test.ts; every automated reconnect case should be its own row marked `✅ green`.)

       Include the status legend line: `*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*`.

    5. **Wave 0 Requirements** section as a bullet list, all checked:
       - `- [x] tests/reconnect.test.ts — covers ROOM-04 server reconnect logic (exists and passes)`

       Followed by italic note: `*No new test files needed — reconnect coverage exists from Phase 5 execution. Presence and disconnection banner verifications are manual per D-07.*`

    6. **Manual-Only Verifications** table with columns Behavior/Requirement/Why Manual/Test Instructions:
       - `Presence dots reflect connected players` | `PRESENCE` | `Requires multiple live WebSocket connections` | `Open 2+ browser tabs in same room; verify presence dot appears next to each connected player in the opponent list; close one tab; verify that player's dot disappears within ~2 seconds`
       - `Disconnection banner appears on network loss` | `DISCONNECT-UX` | `Requires browser network simulation` | `Open room; in DevTools Network tab set Offline; verify banner appears indicating disconnected state; set back to Online; verify banner clears and state resyncs`

    7. **Validation Sign-Off** checklist with six items (mirror Phase 3) all checked `- [x]`:
       - All tasks have `<automated>` verify or Wave 0 dependencies
       - Sampling continuity: no 3 consecutive tasks without automated verify
       - Wave 0 covers all MISSING references
       - No watch-mode flags
       - Feedback latency < 10s
       - `nyquist_compliant: true` set in frontmatter

       Then: `**Approval:** approved 2026-04-09 (Phase 7 audit)`

    Use `---` horizontal rules between top-level sections to match Phase 3's layout.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-resilience-polish/05-VALIDATION.md && grep -q "nyquist_compliant: true" .planning/phases/05-resilience-polish/05-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/05-resilience-polish/05-VALIDATION.md && grep -q "tests/reconnect.test.ts" .planning/phases/05-resilience-polish/05-VALIDATION.md && grep -q "Manual-Only Verifications" .planning/phases/05-resilience-polish/05-VALIDATION.md && grep -q "Per-Task Verification Map" .planning/phases/05-resilience-polish/05-VALIDATION.md && grep -q "Wave 0 Requirements" .planning/phases/05-resilience-polish/05-VALIDATION.md && npm test</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-resilience-polish/05-VALIDATION.md` exists
    - Frontmatter contains `phase: 5`, `slug: resilience-polish`, `nyquist_compliant: true`, `wave_0_complete: true`
    - Contains heading `# Phase 5 — Validation Strategy`
    - Contains section `## Test Infrastructure` with a table row for `Vitest 4.1.2`
    - Contains section `## Per-Task Verification Map` with at least one row referencing `tests/reconnect.test.ts` and at least one manual row with `⬜ pending`
    - Contains section `## Wave 0 Requirements` with `- [x] tests/reconnect.test.ts` entry
    - Contains section `## Manual-Only Verifications` with rows for presence dots and disconnection banner
    - Contains section `## Validation Sign-Off` with all six checklist items marked `- [x]`
    - Contains `**Approval:** approved 2026-04-09 (Phase 7 audit)`
    - `npm test` still exits 0 with 88 passed (confirms no code touched)
  </acceptance_criteria>
  <done>Phase 5 VALIDATION.md exists, follows Phase 3 template, has correct compliance flags, maps reconnect.test.ts cases, lists manual-only items for presence and disconnection UX.</done>
</task>

</tasks>

<verification>
- All 4 VALIDATION.md files have `nyquist_compliant: true`
- All 4 VALIDATION.md files have `wave_0_complete: true` (phases 1, 3, 4, 5)
- `npm test` green (88/88) — no code changed
- `git diff` shows changes only in the four VALIDATION.md paths
</verification>

<success_criteria>
Phase 7 ROADMAP success criteria satisfied:
1. Phase 1 VALIDATION.md has `nyquist_compliant=true` and `wave_0_complete=true` ✓
2. Phase 3 VALIDATION.md has `wave_0_complete=true` ✓
3. Phase 4 VALIDATION.md has `nyquist_compliant=true` and `wave_0_complete=true` ✓
4. Phase 5 VALIDATION.md exists with `nyquist_compliant=true` ✓
</success_criteria>

<output>
After completion, create `.planning/phases/07-nyquist-validation/07-01-SUMMARY.md` documenting: which files were edited, which flags were flipped, any per-task status changes made, and confirmation that `npm test` remained 88/88.
</output>
