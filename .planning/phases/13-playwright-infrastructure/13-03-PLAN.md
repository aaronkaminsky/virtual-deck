---
phase: 13-playwright-infrastructure
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - playwright/game.spec.ts
autonomous: true
requirements:
  - DEV-02

must_haves:
  truths:
    - "The e2e suite covers all 5 scenarios: state sync, deal cards, pass card, reset table, hand privacy"
    - "All 5 tests pass when npx playwright test is run with both dev servers running"
    - "No assertion uses waitForTimeout or .textContent() — all use retry-based locator matchers"
    - "Tests that require the playing phase (reset, pass card) deal cards first before asserting"
    - "Hand privacy test asserts P2 sees a card count (number) for opponent but cannot see P1's card IDs"
    - "Pass card test interacts via UI (drag or alternative UI trigger) — not by calling sendAction directly from the fixture"
  artifacts:
    - path: "playwright/game.spec.ts"
      provides: "5 e2e scenario tests covering DEV-02 SC1–SC5"
      exports:
        - "describe('virtual-deck e2e')"
  key_links:
    - from: "playwright/game.spec.ts"
      to: "playwright/fixtures.ts"
      via: "import { test, expect } from './fixtures'"
      pattern: "./fixtures"
    - from: "deal test"
      to: "src/components/ControlsBar.tsx"
      via: "getByRole('button', { name: /deal/i }) + popover interaction"
      pattern: "Deal"
    - from: "reset test"
      to: "src/components/ControlsBar.tsx"
      via: "getByRole('button', { name: /reset/i }) + confirmation dialog"
      pattern: "Reset"
    - from: "hand privacy test"
      to: "src/components/OpponentHand.tsx"
      via: "getByTestId('opponent-hand') — data-testid added in 13-01"
      pattern: "opponent-hand"
    - from: "draw pile assertions"
      to: "src/components/PileZone.tsx"
      via: "getByTestId('pile-draw') — data-testid added in 13-01"
      pattern: "pile-draw"
---

<objective>
Write the 5-scenario e2e test file playwright/game.spec.ts, covering all DEV-02 success criteria: state sync, deal cards, pass card, reset table, and hand privacy.

Purpose: This is the primary deliverable for DEV-02. The fixture (13-02) and infrastructure (13-01) exist solely to make these tests runnable.

Output: playwright/game.spec.ts with 5 passing e2e tests.
</objective>

<execution_context>
@/Users/aaronkaminsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronkaminsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-playwright-infrastructure/13-01-SUMMARY.md
@.planning/phases/13-playwright-infrastructure/13-02-SUMMARY.md

<interfaces>
<!-- All contracts and UI selectors the executor needs. No codebase exploration required. -->

Import pattern (D-04 — import from fixture, NOT from @playwright/test):
```typescript
import { test, expect } from './fixtures';
// test has twoPlayerRoom fixture; expect is re-exported from @playwright/test
```

Fixture type: `{ p1: Page, p2: Page, roomCode: string }`
Both pages are already past the lobby (joinGame ran before use() yields).

UI Selectors (all verified against codebase):

| Element | Selector | Notes |
|---------|----------|-------|
| Player's hand zone | `page.getByTestId('hand-zone')` | Added in 13-01; board-ready signal |
| Opponent hand area | `page.getByTestId('opponent-hand')` | Added in 13-01 |
| Draw pile | `page.getByTestId('pile-draw')` | Added in 13-01; pile.id === "draw" |
| Deal button | `page.getByRole('button', { name: /deal/i })` | ControlsBar; opens a popover |
| Deal confirm inside popover | `page.getByRole('button', { name: /deal/i })` or specific popover button | Read ControlsBar.tsx to find the inner Deal trigger text |
| Reset button | `page.getByRole('button', { name: /reset/i })` | Only visible in "playing" phase |
| Reset confirm dialog button | `page.getByRole('button', { name: 'Reset table' })` | AlertDialogAction text |
| Cards in hand | `page.getByTestId('hand-zone').getByRole('img')` or similar | Read HandZone.tsx to confirm card element markup |
| Name input (lobby) | `page.getByPlaceholder('Your name')` | Used in fixture; not needed in tests |
| Join button (lobby) | `page.getByRole('button', { name: 'Join Game' })` | Used in fixture; not needed in tests |

Game Phase State Machine:
- "lobby" → initial state; Deal button visible; no Reset button
- "playing" → after DEAL_CARDS; Reset button visible; Undo visible
- "setup" → after RESET_TABLE; Deal button visible again

Server timing:
- DEAL_CARDS has 650ms server-side delay (party/index.ts setTimeout). Retry-based matchers handle this — do NOT add waitForTimeout.

ClientGameState.opponentHandCounts: Record<string, number>
- Key: opponent playerId (string)
- Value: card count (number) — not card IDs
- P2's view of P1's hand: a number, never the actual card objects

Pass card mechanic:
- The UI drag mechanic uses dnd-kit pointer events
- Use `p1.dragAndDrop(sourceSelector, targetSelector)` as the primary approach
- sourceSelector: first card in p1's hand zone (e.g., `p1.getByTestId('hand-zone').locator('[data-draggable]').first()` — read HandZone.tsx/DraggableCard.tsx to confirm actual draggable element attribute)
- targetSelector: `p2`'s OpponentHand area — but P2's page is in a different browser context; the drag must happen on P1's page (p1) targeting p1's view of p2's opponent-hand area
- If dragAndDrop fails with dnd-kit pointer events (known risk — see RESEARCH.md open question), the fallback is: have P1 right-click a card to trigger the context menu "Pass to Player2" action (if the ControlsBar or card context menu has a pass action) — read BoardView.tsx or ControlsBar.tsx to see if a non-drag pass UI exists
- If no non-drag pass UI exists, use page.mouse events: `await page.mouse.move(x, y); await page.mouse.down(); await page.mouse.move(targetX, targetY); await page.mouse.up()` targeting the bounding boxes of source and target elements

Retry-based assertion pattern (D-08 — MANDATORY):
```typescript
// All assertions MUST use locator matchers:
await expect(locator).toBeVisible();
await expect(locator).toContainText('text');
await expect(locator).toHaveText(/pattern/);
await expect(locator).not.toBeEmpty();
await expect(locator).toHaveCount(n);

// FORBIDDEN in committed test files:
await page.waitForTimeout(ms);          // banned D-08
const text = await locator.textContent(); // banned D-08
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read ControlsBar and card component markup, then write all 5 scenarios in game.spec.ts</name>
  <read_first>
    - /Users/aaronkaminsky/code/virtual-deck/src/components/ControlsBar.tsx (find Deal popover trigger text and inner deal button text; find Reset button text; find reset confirmation button text)
    - /Users/aaronkaminsky/code/virtual-deck/src/components/HandZone.tsx (find what element wraps each card — confirm draggable element selector for pass card drag)
    - /Users/aaronkaminsky/code/virtual-deck/src/components/DraggableCard.tsx (find data-* attributes or element type used for individual draggable cards)
    - /Users/aaronkaminsky/code/virtual-deck/src/components/OpponentHand.tsx (confirm badge element rendering cardCount — needed for hand privacy assertion)
    - /Users/aaronkaminsky/code/virtual-deck/tests/broadcastMasking.test.ts (behavioral reference — what the hand-privacy scenario verifies at unit level)
  </read_first>
  <files>playwright/game.spec.ts</files>
  <action>
    Read all five files listed in read_first BEFORE writing game.spec.ts. Extract:
    1. From ControlsBar.tsx: exact text/aria-label of Deal trigger button, inner popover Deal button, Reset button, Reset confirmation button
    2. From HandZone.tsx + DraggableCard.tsx: what attribute/element identifies a draggable card in the hand (look for data-draggable, data-card-id, or similar)
    3. From OpponentHand.tsx: how cardCount is rendered (Badge, span, div?) — needed to assert hand privacy count

    Then create `playwright/game.spec.ts` at `/Users/aaronkaminsky/code/virtual-deck/playwright/game.spec.ts`.

    The file must:
    1. Import `{ test, expect }` from `'./fixtures'` (NOT from `'@playwright/test'`)
    2. Use a top-level `describe('virtual-deck e2e', () => {` block
    3. Contain exactly 5 `test(...)` blocks matching these names (required for --grep to work):
       - `'state sync: P1 action visible to P2 in real time'`
       - `'deal cards: cards distributed to both hands'`
       - `'pass card: P1 hand to P2 hand'`
       - `'reset table: all cards return to draw pile'`
       - `'hand privacy: P2 sees count not card IDs'`

    **Scenario implementations:**

    **SC1 — State sync:** Both players are on the board (fixture handles join). P1 interacts with the draw pile (clicks Deal trigger). Assert that P2's page shows the same UI change (e.g., the Deal popover or a state change is visible). A lightweight check: after P1 clicks Deal and cards are distributed, assert `p2.getByTestId('hand-zone')` is not empty — proving P1's action updated P2's state.

    **SC2 — Deal cards:** P1 clicks Deal button (opens popover), selects a card count (1 or 5 — find the input/select in the popover by reading ControlsBar.tsx), clicks the inner Deal button. Assert with retry: `await expect(p1.getByTestId('hand-zone')).not.toBeEmpty()` and `await expect(p2.getByTestId('hand-zone')).not.toBeEmpty()`. The 650ms server delay is handled by Playwright's default 5s assertion timeout.

    **SC3 — Pass card:** Deal first (same as SC2 setup). Find the first card in P1's hand (use the draggable element selector extracted from HandZone/DraggableCard). Use `p1.dragAndDrop(sourceSelector, targetSelector)` where targetSelector is P1's view of the opponent hand area (`p1.getByTestId('opponent-hand')`). Assert with retry: `await expect(p2.getByTestId('hand-zone')).not.toBeEmpty()` — P2 receives a card. If dragAndDrop does not work with dnd-kit, use the mouse event fallback:
    ```typescript
    const src = await p1.getByTestId('hand-zone').locator(cardSelector).first().boundingBox();
    const tgt = await p1.getByTestId('opponent-hand').boundingBox();
    if (src && tgt) {
      await p1.mouse.move(src.x + src.width/2, src.y + src.height/2);
      await p1.mouse.down();
      await p1.mouse.move(tgt.x + tgt.width/2, tgt.y + tgt.height/2, { steps: 10 });
      await p1.mouse.up();
    }
    ```

    **SC4 — Reset table:** Deal first (same as SC2 setup). Click Reset button on P1's page (visible in "playing" phase). Click the confirmation button ('Reset table' in AlertDialog). Assert with retry: `await expect(p1.getByTestId('pile-draw')).toContainText('52')` and `await expect(p2.getByTestId('pile-draw')).toContainText('52')` — all 52 cards back in draw pile.

    **SC5 — Hand privacy:** Deal first. Assert on P2's page: `await expect(p2.getByTestId('opponent-hand')).toBeVisible()`. Then assert that the count indicator inside opponent-hand shows a digit (use the badge/count element selector from OpponentHand.tsx reading — likely `p2.getByTestId('opponent-hand').locator('span').filter({ hasText: /^\d+$/ })` or the specific element). Assert: `await expect(countLocator).toHaveText(/\d+/)`. Do NOT attempt to read card IDs from P2's view — this scenario only checks that the count is visible and is a number.

    MANDATORY rules for every assertion:
    - Use `await expect(locator).toBeVisible()` not `locator.isVisible()`
    - Use `await expect(locator).toContainText(...)` not `await locator.textContent()`
    - Use `await expect(locator).not.toBeEmpty()` not counting children manually
    - No `await page.waitForTimeout(...)` anywhere in the file

    After writing the file, run: `npx playwright test --grep "state sync" --headed=false` (with servers already running locally, or use `npx playwright test` if servers auto-start). Fix any selector mismatches found during this run.
  </action>
  <verify>
    <automated>grep -c "test(" /Users/aaronkaminsky/code/virtual-deck/playwright/game.spec.ts | xargs -I{} test {} -eq 5 && grep "waitForTimeout" /Users/aaronkaminsky/code/virtual-deck/playwright/game.spec.ts | wc -l | xargs -I{} test {} -eq 0 && grep "textContent()" /Users/aaronkaminsky/code/virtual-deck/playwright/game.spec.ts | wc -l | xargs -I{} test {} -eq 0 && grep "from './fixtures'" /Users/aaronkaminsky/code/virtual-deck/playwright/game.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `playwright/game.spec.ts` exists at `/Users/aaronkaminsky/code/virtual-deck/playwright/game.spec.ts`
    - File imports from `'./fixtures'` (not from `'@playwright/test'`)
    - File contains exactly 5 `test(` calls (one per scenario)
    - Test names contain: "state sync", "deal", "pass", "reset", "privacy" (matching grep patterns in VALIDATION.md)
    - Zero occurrences of `waitForTimeout` in the file
    - Zero occurrences of `.textContent()` in the file
    - All assertions use `await expect(locator).` form
    - SC2 (deal) and SC4 (reset) assert on BOTH p1 and p2 pages — verifying real-time sync
    - SC5 (hand privacy) uses `getByTestId('opponent-hand')` on P2's page — not P1's page
    - `npm run typecheck` exits 0
    - `npm run test:e2e` exits 0 with all 5 tests green (run this last after servers are started, or rely on webServer auto-start)
  </acceptance_criteria>
  <done>playwright/game.spec.ts with 5 scenario tests; all use retry-based matchers; no waitForTimeout; all 5 pass when npx playwright test runs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tooling only | game.spec.ts is a test file. It runs only during local development and CI against the local dev servers. No production code paths, no credentials, no external services beyond the local PartyKit dev server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Information Disclosure | Hand privacy test | mitigate | Test explicitly asserts P2 sees only a card count (number) and not card IDs. This confirms the server-side hand masking in viewFor works correctly end-to-end. |
| T-13-06 | Tampering | Test room state | accept | Each test gets a fresh nanoid(8) room code; PartyKit in-memory state is isolated per room. No cross-test contamination. |
</threat_model>

<verification>
After game.spec.ts is written and servers can be started:

1. `grep "from './fixtures'" playwright/game.spec.ts` — confirms correct import
2. `grep -c "test(" playwright/game.spec.ts` — outputs 5
3. `grep "waitForTimeout" playwright/game.spec.ts | wc -l` — outputs 0
4. `grep "textContent()" playwright/game.spec.ts | wc -l` — outputs 0
5. `npm run typecheck` — exits 0
6. `npm run test:e2e` — all 5 tests green (this is the final gate)
7. `npm test` — vitest unit suite still passes (no regressions)
</verification>

<success_criteria>
- 5 tests in game.spec.ts, one per SC: state sync, deal, pass card, reset table, hand privacy
- All assertions are retry-based locator matchers (no waitForTimeout, no textContent)
- `npm run test:e2e` exits 0 with all 5 tests green
- `npm test` (vitest) exits 0 — no regressions from test infrastructure addition
</success_criteria>

<output>
After completion, create `.planning/phases/13-playwright-infrastructure/13-03-SUMMARY.md`
</output>
