---
phase: 26-zero-risk-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PileZone.tsx
autonomous: true
requirements:
  - POLISH-05
  - POLISH-06

must_haves:
  truths:
    - "An empty pile shows no badge"
    - "A pile with 1+ cards shows the correct count in the badge"
    - "The pile controls row sits visually tighter against the pile card (2px gap instead of 4px)"
  artifacts:
    - path: "src/components/PileZone.tsx"
      provides: "Conditional badge render and tighter controls gap"
      contains: "pile.cards.length > 0"
  key_links:
    - from: "PileZone.tsx:49"
      to: "pile card div"
      via: "flex flex-col gap-0.5 outer wrapper"
      pattern: "gap-0\\.5"
    - from: "PileZone.tsx:112"
      to: "Badge"
      via: "conditional render on pile.cards.length > 0"
      pattern: "pile\\.cards\\.length > 0"
---

<objective>
Fix two visual polish issues in PileZone with no behavior changes.

Purpose: POLISH-05 eliminates the misleading "0" badge on empty piles; POLISH-06 closes the visual gap between the controls row and the pile card so the two elements read as a unit.
Output: PileZone.tsx with a conditionally-rendered badge (count > 0 only) and a tighter outer flex-column gap.
</objective>

<execution_context>
@/Users/aaronkaminsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronkaminsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-zero-risk-visual-polish/26-CONTEXT.md
@.planning/phases/26-zero-risk-visual-polish/26-UI-SPEC.md
</context>

<interfaces>
<!-- Key code facts extracted from src/components/PileZone.tsx -->
<!-- Line 24: const isEmpty = pile.cards.length === 0; -->
<!-- Line 49: <div className="flex flex-col gap-1"> — outer wrapper -->
<!-- Line 112: <Badge className="absolute -bottom-2 -right-2">{pile.cards.length}</Badge> — always renders today -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Conditional badge and tighter pile controls gap (POLISH-05, POLISH-06)</name>
  <files>src/components/PileZone.tsx</files>
  <read_first>
    - src/components/PileZone.tsx — read the full file before editing; the two target locations are line 49 (outer div className) and line 112 (Badge render)
    - .planning/phases/26-zero-risk-visual-polish/26-CONTEXT.md — D-01 and D-02 specify the exact changes
    - .planning/phases/26-zero-risk-visual-polish/26-UI-SPEC.md — Component Inventory and Interaction Contract tables confirm expected before/after
  </read_first>
  <behavior>
    - POLISH-05: when pile.cards.length === 0, the Badge does not render at all (no zero badge, no empty badge)
    - POLISH-05: when pile.cards.length >= 1, the Badge renders with the count value (existing behavior preserved)
    - POLISH-06: the outer flex-column wrapper uses gap-0.5 (2px), not gap-1 (4px) — controls row is visually closer to the pile card
  </behavior>
  <action>
    Two edits to src/components/PileZone.tsx:

    1. Per D-01 (POLISH-05): Wrap the Badge at line 112 with a conditional render. The existing `isEmpty` constant at line 24 (`pile.cards.length === 0`) is already computed — use it. Change:
       `<Badge className="absolute -bottom-2 -right-2">{pile.cards.length}</Badge>`
       to:
       `{!isEmpty && <Badge className="absolute -bottom-2 -right-2">{pile.cards.length}</Badge>}`

    2. Per D-02 (POLISH-06): On the outer div at line 49, change the className from `flex flex-col gap-1` to `flex flex-col gap-0.5`. This is the only gap-class change in this file — the inner `flex gap-1` on the controls row (line 52) is NOT changed.
  </action>
  <verify>
    <automated>npm run typecheck && npm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/PileZone.tsx line 49 outer div className contains `gap-0.5` (not `gap-1`)
    - src/components/PileZone.tsx contains `!isEmpty &&` immediately before the Badge render
    - src/components/PileZone.tsx does NOT contain a bare `<Badge` at line 112 without a conditional guard
    - `npm run typecheck` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Empty pile shows no badge; pile with cards shows correct count; controls row sits 2px from pile card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client | pile.cards array originates from server game state; client only reads length |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
| T-26-01 | Information Disclosure | PileZone badge | accept | Badge shows count only, not card identity; no sensitive data exposed |
</threat_model>

<verification>
After execution:
1. `npm run typecheck` — exits 0
2. `npm test` — exits 0
3. Manual: load a game with an empty pile — no badge visible
4. Manual: load a game with cards in a pile — badge shows correct count
5. Manual: the controls row (eye, shuffle, select-all buttons) is visually closer to the pile card than before
</verification>

<success_criteria>
- PileZone.tsx contains `{!isEmpty && <Badge` pattern (POLISH-05)
- PileZone.tsx outer wrapper div uses `gap-0.5` (POLISH-06)
- TypeScript compilation passes
- All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/26-zero-risk-visual-polish/26-01-SUMMARY.md` when done
</output>
