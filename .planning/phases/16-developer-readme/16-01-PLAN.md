---
phase: 16-developer-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DEV-03
must_haves:
  truths:
    - "A new developer can clone the repo, install deps, and start the dev stack using only README.md"
    - "A new developer can run both the unit test suite (vitest) and e2e suite (playwright) using only README.md"
    - "A new developer can understand the client/server split, PartyKit's role, hand masking, and the message flow from README.md prose"
    - "A new developer can perform a full first-time deploy of the frontend (GitHub Pages) and server (PartyKit Cloud) using only README.md, including all pre-flight setup"
    - "README.md contains no secret values, only references to where they live (GitHub Actions secrets, partykit dashboard)"
  artifacts:
    - path: "README.md"
      provides: "Developer-facing setup, architecture, test, and deploy documentation"
      contains: "## Local Setup"
      min_lines: 80
  key_links:
    - from: "README.md"
      to: ".planning/PROJECT.md"
      via: "Markdown link to Key Decisions"
      pattern: "\\.planning/PROJECT\\.md"
    - from: "README.md"
      to: "package.json scripts"
      via: "Documents npm run dev, dev:client, test, test:e2e, build, deploy"
      pattern: "npm run (dev|test|build|deploy)"
    - from: "README.md"
      to: ".github/workflows/deploy.yml"
      via: "Documents VITE_PARTYKIT_HOST GitHub Actions secret"
      pattern: "VITE_PARTYKIT_HOST"
---

<objective>
Create README.md at the repo root that enables a developer to set up locally, understand the architecture, run all tests, and deploy — using only this single document. This is the first README.md for the project; it does not exist yet.

Purpose: Satisfy DEV-03 (developer-facing README documenting local setup, architecture overview, and deploy instructions) and complete the v1.2 milestone.

Output: `README.md` at repo root containing 5 sections: (1) project intro, (2) Local Setup, (3) Architecture, (4) Tests, (5) Deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-developer-readme/16-CONTEXT.md
@.planning/phases/16-developer-readme/16-PATTERNS.md

<interfaces>
<!-- Source-of-truth values extracted from the codebase. The executor must use these EXACT values -->
<!-- in README.md. No invention, no paraphrasing of command names or env var names. -->

From package.json (npm scripts):
```json
{
  "dev": "partykit dev",
  "dev:client": "vite",
  "build": "vite build",
  "deploy": "partykit deploy",
  "test": "vitest run",
  "test:watch": "vitest",
  "test:e2e": "playwright test",
  "test:e2e:ui": "playwright test --ui",
  "typecheck": "tsc --noEmit"
}
```

From partykit.json:
- Project name: `virtual-deck`
- Server entrypoint: `party/index.ts`

From vite.config.ts:
- `base: '/virtual-deck/'` (required for GitHub Pages path routing)

From .github/workflows/deploy.yml:
- Trigger: `push` to `main` branch, or `workflow_dispatch` (manual)
- Build env var: `VITE_PARTYKIT_HOST: virtual-deck.aaronkaminsky.partykit.dev`
- The `VITE_PARTYKIT_HOST` value is the deployed PartyKit host URL

From playwright.config.ts:
- `webServer` block runs `npm run dev:client` (port 5173) and `npx partykit dev` (port 1999)
- `reuseExistingServer: !process.env.CI` — locally Playwright reuses already-running servers, otherwise auto-starts both

Approved architecture prose (from 16-CONTEXT.md `<specifics>`, copy verbatim or near-verbatim into the Architecture section):
> Virtual Deck is split into two parts: a React/Vite frontend deployed on GitHub Pages, and a PartyKit server running on Cloudflare's edge. The PartyKit server owns all game state in memory per room. It filters outgoing messages per connection so each player only receives their own hand data (hand masking). The client drives actions (move card, deal, shuffle) via typed messages; the server applies them, then broadcasts the updated state to each connected player with masking applied. Shared TypeScript types live in `src/shared/` and are imported by both the client and the `party/index.ts` server.

Approved project intro prose (from .planning/PROJECT.md "What This Is", shortened to 1-2 sentences per D-04):
> Virtual Deck is a web-based multiplayer virtual card table for a standard 52-card deck. 2–4 players share a real-time board with private hands and free-form card manipulation — no rule enforcement, just a digital surface that works like sitting around a table with a physical deck.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md sections 1-4 (intro, Local Setup, Architecture, Tests)</name>
  <files>README.md</files>
  <read_first>
    - README.md (does not exist yet — confirm absence with `ls README.md`; if it exists, read current contents before overwriting)
    - .planning/phases/16-developer-readme/16-CONTEXT.md (locked decisions D-01 through D-06; approved architecture prose)
    - .planning/phases/16-developer-readme/16-PATTERNS.md (heading depth convention, em-dash style, code-block conventions, command table source-of-truth)
    - .planning/PROJECT.md (canonical project description and Key Decisions table — README must link to this)
    - package.json (canonical npm script names — never invent or paraphrase)
    - playwright.config.ts (e2e prereq behavior: reuseExistingServer)
    - CLAUDE.md (heading style + Dev Commands table format — README follows this convention)
  </read_first>
  <action>
Create `README.md` at the repo root with the following structure and exact content. Use H1 only for the title; all other top-level sections use H2 (per heading depth convention from PATTERNS.md). Use em-dash (—) for parenthetical clauses, never en-dash or hyphen.

**Section 1 — Title and intro (top of file):**

```markdown
# Virtual Deck

Virtual Deck is a web-based multiplayer virtual card table for a standard 52-card deck. 2–4 players share a real-time board with private hands and free-form card manipulation — no rule enforcement, just a digital surface that works like sitting around a table with a physical deck.

For project rationale and architectural decisions, see [.planning/PROJECT.md](.planning/PROJECT.md) (Key Decisions table).
```

This satisfies D-04 (1-2 sentence intro at top) and D-02 (link to PROJECT.md for decision context, no rationale duplicated in README).

**Section 2 — Local Setup (H2):**

Heading: `## Local Setup`

Content order: prerequisites → clone/install → start commands → command reference table.

Prerequisites subsection (H3 `### Prerequisites`):
- Node.js (LTS — the deploy workflow uses `lts/*`)
- npm (bundled with Node)
- A modern browser for the client (Chrome/Firefox/Safari)

Clone and install subsection (H3 `### Clone and Install`):
```bash
git clone https://github.com/<your-fork>/virtual-deck.git
cd virtual-deck
npm install
```

Start the dev stack subsection (H3 `### Start the Dev Stack`):

Explain that the local stack has two processes that must run simultaneously: the PartyKit server and the Vite frontend. Use two terminals.

```bash
# Terminal 1 — PartyKit server (port 1999)
npm run dev

# Terminal 2 — Vite frontend (port 5173)
npm run dev:client
```

Then visit `http://localhost:5173/virtual-deck/` (note the `/virtual-deck/` path — required because `vite.config.ts` sets `base: '/virtual-deck/'` for GitHub Pages routing).

Command reference subsection (H3 `### Command Reference`) — copy this table verbatim from CLAUDE.md, extended with secondary commands from package.json:

```markdown
| Command | Purpose |
|---------|---------|
| `npm run dev` | Start PartyKit server (`partykit dev`) on port 1999 |
| `npm run dev:client` | Start Vite frontend (`vite`) on port 5173 |
| `npm test` | Run Vitest unit tests once |
| `npm run test:watch` | Run Vitest in watch mode |
| `npm run test:e2e` | Run Playwright e2e tests |
| `npm run test:e2e:ui` | Run Playwright with the interactive UI runner |
| `npm run typecheck` | TypeScript check (`tsc --noEmit`) |
| `npm run build` | Production build (Vite) — output to `dist/` |
| `npm run deploy` | Deploy PartyKit server (`partykit deploy`) |
```

**Section 3 — Architecture (H2):**

Heading: `## Architecture`

Content: 2-3 paragraphs of prose (per D-01 — no diagrams, no source map). Use the approved prose verbatim or with minor stitching for flow. Cover the three explicit topics from CONTEXT.md `<specifics>`: PartyKit server role, hand masking via `viewFor`, client/server message flow. Mention shared types in `src/shared/`.

Paragraph 1 (verbatim from CONTEXT.md `<specifics>`, possibly split into two paragraphs for readability):
> Virtual Deck is split into two parts: a React/Vite frontend deployed on GitHub Pages, and a PartyKit server running on Cloudflare's edge. The PartyKit server owns all game state in memory per room. It filters outgoing messages per connection so each player only receives their own hand data (hand masking). The client drives actions (move card, deal, shuffle) via typed messages; the server applies them, then broadcasts the updated state to each connected player with masking applied. Shared TypeScript types live in `src/shared/` and are imported by both the client and the `party/index.ts` server.

Paragraph 2 (additional concrete touches — optional, only if it improves clarity without introducing rationale per D-02):
- Mention that the PartyKit server entrypoint is `party/index.ts` (per `partykit.json`)
- Mention that hand masking is implemented via a per-connection `viewFor` function rather than `room.broadcast`

Do NOT add a "why we chose PartyKit" paragraph — D-02 forbids rationale in README. Link readers to `.planning/PROJECT.md` Key Decisions if they want the why.

**Section 4 — Tests (H2):**

Heading: `## Tests`

Content covers both runners per the DEV-03 success criterion 2:

H3 subsection `### Unit Tests (Vitest)`:
```bash
npm test           # run once
npm run test:watch # watch mode
```

H3 subsection `### End-to-End Tests (Playwright)`:

First-run setup (browsers must be installed once):
```bash
npx playwright install chromium
```

Then run tests:
```bash
npm run test:e2e          # headless run
npm run test:e2e:ui       # interactive UI runner
```

Document the prerequisite behavior from playwright.config.ts: Playwright's `reuseExistingServer: !process.env.CI` means locally, if `npm run dev` and `npm run dev:client` are already running on ports 1999 and 5173, Playwright reuses them. Otherwise Playwright starts both automatically. CI always starts fresh. **No manual pre-start required for local e2e runs.**

H3 subsection `### Playwright MCP (Claude Code dev sessions)`:

Briefly note that `.mcp.json` registers `@playwright/mcp` as a project-scoped MCP server. Claude Code sessions in this repo can drive a Playwright browser directly. The MCP config is dev-only and is never added to `package.json` runtime dependencies or CI scripts.

**Save the file**, leaving Section 5 (Deploy) for Task 2. README.md must end after the Tests section for Task 1.
  </action>
  <verify>
    <automated>test -f README.md && grep -q '^# Virtual Deck$' README.md && grep -q '^## Local Setup$' README.md && grep -q '^## Architecture$' README.md && grep -q '^## Tests$' README.md && grep -q 'npm run dev' README.md && grep -q 'npm run dev:client' README.md && grep -q 'npm run test:e2e' README.md && grep -q 'viewFor' README.md && grep -q 'PartyKit' README.md && grep -q '\.planning/PROJECT\.md' README.md && grep -q 'src/shared' README.md && grep -q '/virtual-deck/' README.md && grep -q 'playwright install chromium' README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `README.md` exists at the repo root (`test -f README.md` returns 0)
    - README.md contains exactly one H1: `# Virtual Deck` (`grep -c '^# Virtual Deck$' README.md` returns 1)
    - README.md contains the H2 `## Local Setup` (`grep -q '^## Local Setup$' README.md`)
    - README.md contains the H2 `## Architecture` (`grep -q '^## Architecture$' README.md`)
    - README.md contains the H2 `## Tests` (`grep -q '^## Tests$' README.md`)
    - README.md does NOT yet contain `## Deploy` (Task 2 adds it) — `grep -c '^## Deploy' README.md` returns 0
    - README.md contains the H3 subsections `### Prerequisites`, `### Clone and Install`, `### Start the Dev Stack`, `### Command Reference` under Local Setup
    - README.md contains the H3 subsections `### Unit Tests (Vitest)`, `### End-to-End Tests (Playwright)`, `### Playwright MCP (Claude Code dev sessions)` under Tests
    - README.md references the canonical npm script names: `npm run dev`, `npm run dev:client`, `npm test`, `npm run test:watch`, `npm run test:e2e`, `npm run test:e2e:ui`, `npm run typecheck`, `npm run build` (each verifiable with grep)
    - README.md contains the link `.planning/PROJECT.md` (per D-02)
    - README.md contains the literal string `viewFor` in the Architecture section (hand masking mechanism)
    - README.md contains the literal strings `src/shared/` and `party/index.ts`
    - README.md contains `/virtual-deck/` (the Vite base path explanation)
    - README.md contains `npx playwright install chromium` (Playwright first-run prereq)
    - README.md contains `reuseExistingServer` OR equivalent prose explaining Playwright auto-starts servers locally
    - README.md uses em-dash `—` (not `--` or `-`) for parenthetical clauses (`grep -c '—' README.md` returns >= 3)
    - File is at least 80 lines (`wc -l README.md` returns >= 80)
    - No secret values are embedded in README.md — `grep -E '(sk_|pk_|password|api_key)' README.md` returns no matches; `VITE_PARTYKIT_HOST` is mentioned only as a name in Task 2, not in this task's output
  </acceptance_criteria>
  <done>
    README.md exists at the repo root with sections 1-4 (title+intro, Local Setup, Architecture, Tests). Every npm script name matches package.json exactly. Architecture section uses the approved prose covering client/server split, PartyKit role, hand masking, message flow, and shared types. Link to .planning/PROJECT.md is present per D-02. No Deploy section yet — that is Task 2's deliverable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Deploy section to README.md (Frontend + Server pre-flight + commands)</name>
  <files>README.md</files>
  <read_first>
    - README.md (Task 1's output — must read current state before appending Deploy section)
    - .planning/phases/16-developer-readme/16-CONTEXT.md (D-05 full pre-flight, D-06 separate Frontend/Server subsections)
    - .planning/phases/16-developer-readme/16-PATTERNS.md (Analog 4: deploy.yml facts; Analog 5 already covered in Task 1)
    - .github/workflows/deploy.yml (canonical source for trigger and VITE_PARTYKIT_HOST secret)
    - partykit.json (canonical project name `virtual-deck`)
    - vite.config.ts (canonical `base: '/virtual-deck/'` value)
    - package.json (canonical `npm run deploy` and `npm run build` scripts)
  </read_first>
  <action>
Append the Deploy section to the existing `README.md`. Use H2 `## Deploy` with two H3 subsections per D-06: Frontend (GitHub Pages) and Server (PartyKit Cloud).

The user explicitly chose **full pre-flight coverage** (D-05), so each subsection includes one-time setup steps in addition to the deploy commands.

**Append the following content after the Tests section:**

```markdown
## Deploy

Virtual Deck deploys to two distinct targets: the static frontend goes to GitHub Pages, and the PartyKit server runs on Cloudflare's edge via PartyKit Cloud. Each target has independent setup and deploy commands.

### Frontend (GitHub Pages)

The frontend is built by Vite and deployed via the workflow at `.github/workflows/deploy.yml`. The workflow triggers on `push` to `main` or manually via `workflow_dispatch`.

**One-time setup (repository owner):**

1. **Enable GitHub Pages.** In the repo on GitHub, go to **Settings → Pages**. Under "Build and deployment", set **Source** to `GitHub Actions`. (The workflow uses the official `actions/deploy-pages@v4` action.)

2. **Add the `VITE_PARTYKIT_HOST` GitHub Actions secret.** The build needs to know the deployed PartyKit host URL so the production frontend connects to the right server. Go to **Settings → Secrets and variables → Actions → New repository secret**:
   - **Name:** `VITE_PARTYKIT_HOST`
   - **Value:** the host of your deployed PartyKit project (for the canonical deployment this is `virtual-deck.aaronkaminsky.partykit.dev`; if you fork, use your own PartyKit host — see Server section below)

3. **Confirm `vite.config.ts` `base` matches the GitHub Pages path.** The repo ships with `base: '/virtual-deck/'` because the published URL is `https://<owner>.github.io/virtual-deck/`. If you fork to a repo with a different name, update `base` accordingly.

**Deploying:**

- **Automatic:** push to `main` — GitHub Actions runs `npm ci`, `npm run build`, and uploads `dist/` as a Pages artifact.
- **Manual:** in the GitHub UI, go to **Actions → Deploy to GitHub Pages → Run workflow** (this triggers `workflow_dispatch`).

The deployed page URL is shown as the `page_url` output of the workflow run.

### Server (PartyKit Cloud)

The PartyKit server lives at `party/index.ts` and is deployed to PartyKit Cloud (Cloudflare's edge). PartyKit's hobby tier covers 2–4 player sessions at this project's expected usage.

**One-time setup:**

1. **Authenticate with PartyKit.** From the repo root:

   ```bash
   npx partykit login
   ```

   This opens a browser flow that links your local CLI to a PartyKit account.

2. **Confirm the PartyKit project name.** `partykit.json` declares `"name": "virtual-deck"`. The first deploy will create a project under your PartyKit account at `<project-name>.<your-account>.partykit.dev`.

3. **Update the `VITE_PARTYKIT_HOST` secret (if you forked).** After your first server deploy, copy the resulting host (printed by `npm run deploy`) into the `VITE_PARTYKIT_HOST` GitHub Actions secret from the Frontend section above. The frontend build embeds this value at compile time, so the secret must be set **before** the next frontend deploy.

**Deploying:**

```bash
npm run deploy
```

This runs `partykit deploy` and pushes `party/index.ts` to PartyKit Cloud. The CLI prints the deployed host URL on success.

### First-time deploy order

For a clean first-time deploy of a fresh fork, run in this order:

1. `npx partykit login` then `npm run deploy` — produces the PartyKit host URL.
2. Add that host URL as the `VITE_PARTYKIT_HOST` GitHub Actions secret.
3. Push to `main` (or run the workflow manually) — frontend builds with the correct server host and goes live on GitHub Pages.
```

This content satisfies D-05 (pre-flight: GitHub Pages activation in Settings, `partykit login`, project creation, `VITE_PARTYKIT_HOST` secret, plus deploy commands) and D-06 (separate Frontend and Server subsections).

**Important content rules:**
- Reference `VITE_PARTYKIT_HOST` by name only — never embed any secret value or token.
- The literal string `virtual-deck.aaronkaminsky.partykit.dev` is acceptable because it is already the public host name (printed publicly in `.github/workflows/deploy.yml`); it is not a secret. Treat it as a documentation example only.
- Do not invent CLI flags. Only use commands that exist in `package.json` or are part of the public PartyKit CLI (`partykit login`, `partykit deploy` via `npm run deploy`).

**Final pass:** after appending Deploy, re-read the full file and verify:
- Sections appear in order: title/intro → Local Setup → Architecture → Tests → Deploy
- All four DEV-03 success criteria are addressed
- All locked decisions D-01 through D-06 are reflected
- No accidental rationale in Architecture (D-02)
- No leaked secrets
  </action>
  <verify>
    <automated>grep -q '^## Deploy$' README.md && grep -q '^### Frontend (GitHub Pages)$' README.md && grep -q '^### Server (PartyKit Cloud)$' README.md && grep -q 'VITE_PARTYKIT_HOST' README.md && grep -q 'partykit login' README.md && grep -q 'npm run deploy' README.md && grep -q 'workflow_dispatch' README.md && grep -q 'Settings.*Pages' README.md && grep -c '^## ' README.md | awk '$1 >= 4 {exit 0} {exit 1}' && ! grep -E '^(sk_|pk_live|password=|api_key=|token=)' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the H2 `## Deploy` (`grep -q '^## Deploy$' README.md`)
    - README.md contains the H3 `### Frontend (GitHub Pages)` (`grep -q '^### Frontend (GitHub Pages)$' README.md`)
    - README.md contains the H3 `### Server (PartyKit Cloud)` (`grep -q '^### Server (PartyKit Cloud)$' README.md`)
    - Deploy section names the GitHub Actions secret: `grep -q 'VITE_PARTYKIT_HOST' README.md`
    - Deploy section documents `partykit login` for first-time auth: `grep -q 'partykit login' README.md`
    - Deploy section documents the deploy commands: `grep -q 'npm run deploy' README.md`
    - Deploy section documents the GitHub Pages activation step under Settings: `grep -E 'Settings.*Pages' README.md` matches at least once (case-sensitive, exact phrasing acceptable: "Settings → Pages" or "Settings -> Pages" or "Settings/Pages")
    - Deploy section mentions `workflow_dispatch` for manual deploy trigger: `grep -q 'workflow_dispatch' README.md`
    - Deploy section mentions `vite.config.ts` `base` path requirement: `grep -q "base: '/virtual-deck/'" README.md` OR `grep -q 'base.*/virtual-deck/' README.md`
    - All four section headings appear in correct order. Verify with: `grep -n '^## ' README.md` shows `## Local Setup` before `## Architecture` before `## Tests` before `## Deploy`
    - No embedded secrets — `grep -E '^(sk_|pk_live|password=|api_key=|token=)' README.md` returns nothing; the only deploy-related identifier is `VITE_PARTYKIT_HOST` (a name) and `virtual-deck.aaronkaminsky.partykit.dev` (a public host)
    - File length grew from Task 1 — final `wc -l README.md` returns >= 130 lines
    - No secrets file referenced — `grep -E '\.env|secrets\.json' README.md` returns no matches (the project has no .env workflow)
    - All four DEV-03 success criteria from REQUIREMENTS.md are reflected: local setup steps (covered in Local Setup), both test runners (covered in Tests), architecture overview of PartyKit + hand masking + message flow (covered in Architecture), GitHub Pages + PartyKit Cloud deploy (covered in Deploy)
  </acceptance_criteria>
  <done>
    README.md is a complete, self-contained developer document. A developer cloning the repo cold can: install deps and start the dev stack (Local Setup), understand how client and server interact (Architecture), run unit and e2e tests (Tests), and perform a first-time deploy of both frontend and server with all pre-flight setup (Deploy). All locked decisions D-01 through D-06 are reflected, DEV-03 is fully satisfied, and no secrets are embedded in the file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public repo → docs reader | README.md is published in a public GitHub repository; anyone can read it. The trust boundary is between the maintainer authoring docs and any future reader (including malicious actors scraping for secrets). |
| docs → external services | The Deploy section names credentials/secrets stored in external systems (GitHub Actions secrets, PartyKit account). The README must reference them by name only — never embed values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | README.md Deploy section | mitigate | Reference `VITE_PARTYKIT_HOST` by name only. The acceptance criteria for Task 2 include `grep -E '^(sk_|pk_live|password=|api_key=|token=)' README.md` returning empty. The public host string `virtual-deck.aaronkaminsky.partykit.dev` is the only identifier present and is already public via `.github/workflows/deploy.yml` (committed, public) — not a secret. |
| T-16-02 | Information Disclosure | README.md Architecture section | accept | Architecture prose describes the system at a high level (client/server split, hand masking, message flow). All facts are already public via `.planning/PROJECT.md` and the source repo. No new attack surface is exposed. Low risk. |
| T-16-03 | Tampering | README.md (incorrect deploy instructions) | mitigate | Every command and value in the Deploy section is sourced from a canonical file (`package.json`, `partykit.json`, `vite.config.ts`, `.github/workflows/deploy.yml`). Acceptance criteria include grep verification that exact script names and env var names appear. A reader who follows the README cannot be misled into running an unintended command. |
| T-16-04 | Information Disclosure | README.md (encouraging users to commit secrets) | mitigate | The Deploy section explicitly directs `VITE_PARTYKIT_HOST` to GitHub Actions secrets storage and uses `npx partykit login` (interactive auth) — never instructs the user to add tokens to a file or env var that could be committed. Acceptance criteria forbid `.env` or `secrets.json` references. |
| T-16-05 | Repudiation | README.md (no audit trail of doc changes) | accept | README.md is git-tracked; commits are signed by the author. No additional mitigation needed for documentation changes. |
| T-16-06 | Denial of Service | README.md (encouraging high-cost deploys) | accept | Deploy commands are owner-only (require `partykit login` and write access to the repo). No mass-action commands. PartyKit hobby tier is free and self-throttled. Low risk. |
</threat_model>

<verification>
After both tasks complete:

```bash
# README exists and has all required sections
test -f README.md
grep -E '^# Virtual Deck$|^## Local Setup$|^## Architecture$|^## Tests$|^## Deploy$' README.md | wc -l   # expect 5

# All canonical npm scripts mentioned
for s in 'npm run dev' 'npm run dev:client' 'npm test' 'npm run test:e2e' 'npm run build' 'npm run deploy' 'npm run typecheck'; do
  grep -q "$s" README.md || echo "MISSING: $s"
done

# No secrets leaked
grep -vE '^#' README.md | grep -E '(sk_[a-zA-Z0-9]+|pk_live|password=|api_key=|token=)' && echo "FAIL: secret-shaped value present" || echo "OK: no secret-shaped values"

# DEV-03 success criteria coverage:
# 1. Local setup steps (clone, install, start Vite, start PartyKit)
grep -q 'git clone' README.md && grep -q 'npm install' README.md && grep -q 'npm run dev' README.md && grep -q 'npm run dev:client' README.md
# 2. Both test runners
grep -q 'vitest\|npm test' README.md && grep -q 'playwright\|npm run test:e2e' README.md
# 3. Architecture: PartyKit, hand masking, message flow
grep -q 'PartyKit' README.md && grep -q 'viewFor\|hand masking' README.md && grep -q 'message' README.md
# 4. Deploy: GitHub Pages + PartyKit Cloud
grep -q 'GitHub Pages' README.md && grep -q 'PartyKit Cloud\|partykit deploy\|npm run deploy' README.md
```
</verification>

<success_criteria>
- README.md exists at repo root with sections 1-5 in order: Title+intro, Local Setup, Architecture, Tests, Deploy
- All four DEV-03 success criteria from REQUIREMENTS.md are satisfied
- All locked decisions D-01 through D-06 are reflected:
  - D-01: Architecture is 2-3 paragraphs of prose (no diagrams, no source map)
  - D-02: README links to `.planning/PROJECT.md` for decision context; no rationale embedded in README
  - D-03: Tone serves both future-self and external curious developer
  - D-04: 1-2 sentence project intro at top
  - D-05: Full pre-flight in Deploy (GitHub Pages activation, partykit login, partykit project creation, VITE_PARTYKIT_HOST secret) plus actual deploy commands
  - D-06: Deploy split into Frontend and Server subsections
- Every npm script name in README matches package.json exactly (no invented commands, no paraphrasing)
- No secret values embedded in the file
- File is at least 130 lines after both tasks complete
</success_criteria>

<output>
After completion, create `.planning/phases/16-developer-readme/16-01-SUMMARY.md` per the standard SUMMARY template.
</output>
