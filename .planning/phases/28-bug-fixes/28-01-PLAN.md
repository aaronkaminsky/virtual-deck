---
phase: 28-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/DraggableCard.tsx
  - src/components/PileZone.tsx
  - src/components/BoardView.tsx
  - tests/pileSelectRing.test.ts
autonomous: true
requirements:
  - BUG-01

must_haves:
  truths:
    - "D-01: Ring appears on the card face/back itself (not on the pile border) — same placement as SpreadZone"
    - "D-02: Ring style is ring-1 ring-primary/30 ring-offset-1 ring-offset-background — no deviation from SpreadZone"
    - "D-03: selectedIds flows from BoardDragLayer → BoardView → PileZone → DraggableCard with no new intermediate state"
    - "D-04: Vitest logic test for isSelected derivation passes"
  artifacts:
    - path: "src/components/DraggableCard.tsx"
      provides: "isSelected prop + ring class application"
      contains: "isSelected"
    - path: "src/components/PileZone.tsx"
      provides: "selectedIds prop wired to DraggableCard isSelected"
      contains: "selectedIds"
    - path: "src/components/BoardView.tsx"
      provides: "selectedIds passed to PileZone"
      contains: "selectedIds={selectedIds}"
  key_links:
    - from: "SpreadZone.tsx:52"
      to: "DraggableCard ring"
      via: "ring-1 ring-primary/30 ring-offset-1 ring-offset-background rounded-md"
      pattern: "ring-1 ring-primary/30"
    - from: "BoardView.tsx:76"
      to: "PileZone selectedIds"
      via: "selectedIds prop addition"
      pattern: "selectedIds=\\{selectedIds\\}"
---

<objective>
Fix BUG-01: Clicking "Select All" on a pile zone has no visual effect — the top card is selected in state but shows no ring feedback.

Purpose: Thread `selectedIds` from `BoardDragLayer` through `BoardView` → `PileZone` → `DraggableCard`, and add the `isSelected` prop to `DraggableCard` so the same ring style used in SpreadZone and HandZone appears on the selected pile card.
Output: `DraggableCard.tsx` accepts `isSelected`, `PileZone.tsx` accepts and forwards `selectedIds`, `BoardView.tsx` passes `selectedIds` to `PileZone`, and a Vitest logic test verifies the isSelected derivation.
</objective>

<execution_context>
@/Users/aaronkaminsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronkaminsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-bug-fixes/28-CONTEXT.md
@.planning/phases/28-bug-fixes/28-UI-SPEC.md
</context>

<interfaces>
<!-- DraggableCard.tsx (current, no isSelected prop):
  interface DraggableCardProps { card, fromZone, fromId, onFlip? }
  Returns: <div ref={setNodeRef} style={style} onClick={handleClick} {...listeners} {...attributes}>
    {card.faceUp ? <CardFace card={card} /> : <CardBack />}
  </div>
-->
<!-- PileZone.tsx (current, no selectedIds prop):
  interface PileZoneProps { pile, sendAction, draggingCardId, shufflingPileIds?, onSelectAll? }
  Line 111: renders DraggableCard without isSelected
-->
<!-- BoardView.tsx line 76 (current):
  <PileZone key={pile.id} pile={pile} sendAction={sendAction} draggingCardId={draggingCardId} shufflingPileIds={shufflingPileIds} onSelectAll={onSelectAll} />
  Missing: selectedIds={selectedIds} — selectedIds is already in BoardViewProps and used by HandZone/SpreadZone
-->
<!-- SpreadZone.tsx line 52 (reference ring implementation):
  isSelected && 'ring-1 ring-primary/30 ring-offset-1 ring-offset-background rounded-md transition-transform duration-150'
-->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add isSelected prop to DraggableCard</name>
  <files>src/components/DraggableCard.tsx</files>
  <read_first>
    - src/components/DraggableCard.tsx — read the full file; the interface is at lines 8-13, the return div is at line 46
    - src/components/SpreadZone.tsx — reference implementation; SortableSpreadCard at lines 20-62 shows the exact isSelected + ring class pattern; ring is applied to the inner div wrapping the card content, not the outer positioning div
    - .planning/phases/28-bug-fixes/28-CONTEXT.md — D-01: ring appears on the card face/back (not pile border); D-02: ring style is `ring-1 ring-primary/30 ring-offset-1 ring-offset-background` — no deviation; D-03: add isSelected prop to DraggableCard
  </read_first>
  <behavior>
    - DraggableCard accepts optional `isSelected?: boolean` prop
    - When `isSelected` is true, the card wrapper has `ring-1 ring-primary/30 ring-offset-1 ring-offset-background rounded-sm` applied
    - When `isSelected` is false or undefined, no ring classes are added
    - All existing behavior (drag, click-to-flip, opacity during drag) is unchanged
    - The ring wraps the card face/back element — it appears on the card itself, not on the pile container
  </behavior>
  <action>
    Two edits to src/components/DraggableCard.tsx:

    1. Add `isSelected?: boolean` to the `DraggableCardProps` interface (after `fromId: string`).

    2. Apply the ring class to the outer return div using `cn()`. Import `cn` from `@/lib/utils`. Change the outer div to use `className={cn(isSelected && 'ring-1 ring-primary/30 ring-offset-1 ring-offset-background rounded-sm')}` alongside the existing inline style. The `cn` import pattern is: `import { cn } from '@/lib/utils';` — follow the import order in the file.
  </action>
  <verify>
    <automated>npm run typecheck && npm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/DraggableCard.tsx DraggableCardProps interface contains `isSelected?: boolean`
    - src/components/DraggableCard.tsx imports `cn` from `@/lib/utils`
    - src/components/DraggableCard.tsx return div uses `cn(isSelected && 'ring-1 ring-primary/30 ring-offset-1 ring-offset-background rounded-sm')` in its className
    - `npm run typecheck` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>DraggableCard shows a selection ring when isSelected is true; no visual change when isSelected is false or absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add selectedIds prop to PileZone and wire to DraggableCard</name>
  <files>src/components/PileZone.tsx</files>
  <read_first>
    - src/components/PileZone.tsx — read the full file; the interface is at lines 10-16, the DraggableCard render is at line 111, topCard is computed at line 25
    - src/components/DraggableCard.tsx — confirm the isSelected prop was added in Task 1 before proceeding
    - .planning/phases/28-bug-fixes/28-CONTEXT.md — D-03: `selectedIds` flows from BoardDragLayer → BoardView → PileZone → DraggableCard; top card id is `(topCard as Card).id`
  </read_first>
  <behavior>
    - PileZone accepts optional `selectedIds?: Set<string>` prop
    - The DraggableCard at line 111 receives `isSelected={selectedIds?.has((topCard as Card).id) ?? false}`
    - No other behavior changes — shuffle animation, drag ghost, flip, empty state are all unchanged
  </behavior>
  <action>
    Two edits to src/components/PileZone.tsx:

    1. Add `selectedIds?: Set<string>` to the `PileZoneProps` interface (after `onSelectAll?`). Destructure `selectedIds` in the function signature.

    2. On line 111, update the DraggableCard render to pass `isSelected={selectedIds?.has((topCard as Card).id) ?? false}`. The existing render is:
       `'id' in (topCard ?? {}) ? <DraggableCard card={topCard as Card} fromZone="pile" fromId={pile.id} onFlip={handleFlipCard} /> : ...`
       Add `isSelected={selectedIds?.has((topCard as Card).id) ?? false}` to the DraggableCard props.
  </action>
  <verify>
    <automated>npm run typecheck && npm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/PileZone.tsx PileZoneProps interface contains `selectedIds?: Set<string>`
    - src/components/PileZone.tsx DraggableCard render contains `isSelected={selectedIds?.has`
    - `npm run typecheck` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>PileZone passes the correct isSelected value to the top card's DraggableCard based on the selectedIds set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pass selectedIds to PileZone in BoardView</name>
  <files>src/components/BoardView.tsx</files>
  <read_first>
    - src/components/BoardView.tsx — read the full file; `selectedIds` is already in BoardViewProps (line 19) and passed to HandZone and SpreadZone; PileZone at line 76 currently does NOT receive selectedIds
    - src/components/PileZone.tsx — confirm selectedIds prop was added in Task 2 before proceeding
    - .planning/phases/28-bug-fixes/28-CONTEXT.md — D-03: "the same wiring path that already exists for SpreadZone"; see BoardView line 97 for the SpreadZone selectedIds={selectedIds} pattern
  </read_first>
  <behavior>
    - Each PileZone rendered by `pilePiles.map` receives `selectedIds={selectedIds}`
    - No other props on PileZone are changed
    - All other BoardView rendering is unchanged
  </behavior>
  <action>
    One edit to src/components/BoardView.tsx:

    At line 76, add `selectedIds={selectedIds}` to the PileZone render. The current render is:
    `<PileZone key={pile.id} pile={pile} sendAction={sendAction} draggingCardId={draggingCardId} shufflingPileIds={shufflingPileIds} onSelectAll={onSelectAll} />`
    Add `selectedIds={selectedIds}` after `shufflingPileIds={shufflingPileIds}`.
  </action>
  <verify>
    <automated>npm run typecheck && npm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/BoardView.tsx PileZone render contains `selectedIds={selectedIds}`
    - `npm run typecheck` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>selectedIds now flows from BoardDragLayer through BoardView into PileZone. The full prop chain is complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Vitest logic test for pile isSelected derivation</name>
  <files>tests/pileSelectRing.test.ts</files>
  <read_first>
    - tests/boardDragLayerDialog.test.ts — reference test pattern: pure logic extraction, no component rendering, vitest imports, Card type usage
    - src/components/PileZone.tsx — confirm the isSelected computation: `selectedIds?.has((topCard as Card).id) ?? false`
    - .planning/phases/28-bug-fixes/28-CONTEXT.md — D-04: render PileZone with selectedIds containing top card's id; assert ring class is present. Since there is no @testing-library/react in this project, test the isSelected derivation logic directly (not component rendering).
  </read_first>
  <behavior>
    - Test that `selectedIds.has(topCardId)` returns true when the set contains the card id (isSelected=true path)
    - Test that `selectedIds.has(topCardId)` returns false when the set does not contain the card id (isSelected=false path)
    - Test that `undefined?.has(id) ?? false` returns false (selectedIds absent path)
    - Test covers all three branches of `selectedIds?.has((topCard as Card).id) ?? false`
  </behavior>
  <action>
    Create tests/pileSelectRing.test.ts as a pure logic test (no component imports, no jsdom). Extract and test the `isSelected` derivation:

    - Import `describe`, `it`, `expect` from `vitest`
    - Import `Card` type from `@shared/types`
    - Write a helper `deriveIsSelected(selectedIds: Set<string> | undefined, cardId: string): boolean` that mirrors the PileZone computation: `return selectedIds?.has(cardId) ?? false`
    - Test cases:
      1. `deriveIsSelected(new Set(['A-s']), 'A-s')` → `true`
      2. `deriveIsSelected(new Set(['K-h']), 'A-s')` → `false`
      3. `deriveIsSelected(new Set(), 'A-s')` → `false`
      4. `deriveIsSelected(undefined, 'A-s')` → `false`
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | grep -A5 "pileSelectRing"</automated>
  </verify>
  <acceptance_criteria>
    - tests/pileSelectRing.test.ts exists
    - `npm test` exits 0 and pileSelectRing.test.ts reports all tests passing
    - File contains at least 4 test cases covering the true, false (wrong id), false (empty set), and undefined branches
  </acceptance_criteria>
  <done>Logic test verifies the isSelected derivation for the three branches used in PileZone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client | selectedIds is local client UI state derived from user clicks; it never originates from server messages |
| Client UI state | selectedIds is a Set<string> of card IDs; it contains no sensitive information (card IDs are not secret) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-SC | Tampering | npm installs | accept | No new packages installed in this plan |
| T-28-01 | Information Disclosure | Card ID in selectedIds | accept | Card IDs are visible to all players; selection state is local UI only, not broadcast |
| T-28-02 | Tampering | isSelected prop | accept | isSelected is a pure UI prop derived from client-owned selectedIds; no server action triggered by the ring display |
</threat_model>

<verification>
After execution:
1. `npm run typecheck` — exits 0
2. `npm test` — exits 0 (all tests including pileSelectRing.test.ts)
3. Manual: start game, add cards to a pile, click "Select All" on the pile — top card shows ring (`ring-1 ring-primary/30`) matching SpreadZone selected card ring
4. Manual: click a card in a spread zone to select it, then click pile Select All — pile card shows ring, spread zone card ring clears (selection is mutually exclusive by zone)
</verification>

<success_criteria>
- DraggableCard.tsx accepts isSelected prop and applies ring class (BUG-01)
- PileZone.tsx accepts selectedIds prop and passes isSelected to DraggableCard (BUG-01)
- BoardView.tsx passes selectedIds to PileZone (BUG-01)
- pileSelectRing.test.ts passes (BUG-01)
- TypeScript compilation passes
- All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/28-bug-fixes/28-01-SUMMARY.md` when done
</output>
