---
phase: 33-overlap-visibility
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - src/components/BoardDragLayer.tsx
autonomous: true
requirements:
  - OVERLAP-03
tags: [canvas, overlap, shadow, dnd-kit, drag-and-drop, react]

must_haves:
  truths:
    - "While a canvas card is being dragged, the DragOverlay ghost shows boxShadow=STACK_SHADOW + borderRadius=6 exactly when its current position (origin + delta) covers >50% of any non-dragged canvas card; the shadow disappears when the threshold is no longer met"
    - "dragDeltaRef.current is updated on every onDragMove event WITHOUT triggering a React re-render"
    - "dragCoversSomeCard setState is called only when the covering-boolean changes value (not on every pointermove)"
    - "Drag-time shadow check is skipped (early return) for non-canvas drags (hand→pile, hand→hand, pile→pile, pile→hand, etc.)"
    - "dragCoversSomeCard and activeDragOrigin are reset to false/null in both handleDragEnd and handleDragCancel, preventing stale shadow on the next drag"
    - "DragOverlay opacity:0.5 + scale(1.05) behavior from Phase 32 D-13 is preserved unchanged"
  artifacts:
    - path: "src/components/BoardDragLayer.tsx"
      provides: "onDragMove handler with ref-throttled overlap check; activeDragOrigin state; dragCoversSomeCard boolean state; conditional shadow on DragOverlay div"
      contains: "handleDragMove"
  key_links:
    - from: "src/components/BoardDragLayer.tsx"
      to: "src/lib/canvas-utils.ts"
      via: "import { coversMajority, STACK_SHADOW }"
      pattern: "from '@/lib/canvas-utils'"
    - from: "DndContext"
      to: "handleDragMove"
      via: "onDragMove={handleDragMove} prop on DndContext"
      pattern: "onDragMove=\\{handleDragMove\\}"
    - from: "handleDragMove"
      to: "dragDeltaRef.current"
      via: "ref assignment without setState"
      pattern: "dragDeltaRef\\.current\\s*="
    - from: "handleDragMove"
      to: "setDragCoversSomeCard"
      via: "guarded setState (boolean flip only)"
      pattern: "if \\(nowCovers !== dragCoversSomeCard\\)"
---

<objective>
Wire the drag-time stack shadow into BoardDragLayer: while dragging a canvas card, the DragOverlay ghost shows the same shadow indicator when its current position covers >50% of any other canvas card. Performance contract is strict — every pointermove updates a ref; React re-renders only when the boolean flips.

Purpose: Complete OVERLAP-03 by extending the at-rest shadow path (Plan 01) into the active drag. Closes the user-facing visual feedback loop while the player is deciding where to drop a stacked card.

Output:
- `src/components/BoardDragLayer.tsx` — adds activeDragOrigin state, dragCoversSomeCard state, dragDeltaRef, handleDragMove handler with non-canvas guard, reset logic in both end/cancel handlers, conditional shadow on the DragOverlay div, onDragMove prop on DndContext, and the DragMoveEvent type import
</objective>

<execution_context>
@$HOME/code/dev-setup/claude/get-shit-done/workflows/execute-plan.md
@$HOME/code/dev-setup/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-overlap-visibility/33-CONTEXT.md
@.planning/phases/33-overlap-visibility/33-RESEARCH.md
@.planning/phases/33-overlap-visibility/33-PATTERNS.md
@.planning/phases/33-overlap-visibility/33-UI-SPEC.md
@.planning/phases/33-overlap-visibility/33-VALIDATION.md
@.planning/phases/33-overlap-visibility/33-01-PLAN.md

# Source-of-truth and reference impls
@src/spikes/Spike002OverlapHitTesting.tsx
@src/components/BoardDragLayer.tsx
@src/shared/types.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From @dnd-kit/core:
- `import type { DragMoveEvent, DragStartEvent, DragEndEvent, CollisionDetection } from '@dnd-kit/core';`
- `DragMoveEvent.delta: { x: number; y: number }` — delta is always relative to dragStart position
- `DndContext` accepts `onDragMove?: (event: DragMoveEvent) => void` callback prop

From src/lib/canvas-utils.ts (created in Plan 01):
- `export function coversMajority(top: {x:number;y:number}, bottom: {x:number;y:number}): boolean`
- `export const STACK_SHADOW: string`
- `export const CARD_W: number = 63`
- `export const CARD_H: number = 88`

From src/components/BoardDragLayer.tsx (current state, MUST preserve):
- Line 3: `import { DndContext, DragOverlay, ... MeasuringStrategy } from '@dnd-kit/core';`
- Line 4: `import type { CollisionDetection, DragStartEvent, DragEndEvent } from '@dnd-kit/core';` ← extend with DragMoveEvent
- Line 84: `const [activeCard, setActiveCard] = useState<Card | null>(null);` ← keep, add new state alongside
- Line 88: `const dragDataRef = useRef<{ card: Card; fromZone: string; fromId: string } | null>(null);` ← already populated synchronously in handleDragStart; safe to read in onDragMove (RESEARCH.md A2)
- Line 184: `function handleDragStart(event: DragStartEvent) { ... setActiveCard(data.card); setDragging(true); }` ← end with new origin capture + delta ref reset
- Line 203: `function handleDragEnd(event: DragEndEvent) { ... }` ← multi-branch; ALL early-return paths must reset the new state
- Line 396: `function handleDragCancel() { ... }` ← reset new state here too
- Line 408–414: `<DndContext sensors={sensors} collisionDetection={customCollision} onDragStart={handleDragStart} onDragEnd={handleDragEnd} onDragCancel={handleDragCancel} measuring={...}>` ← add onDragMove prop
- Lines 417–427: `createPortal(<DragOverlay dropAnimation={...}> {activeCard ? (<div style={{ opacity: 0.5, transform: 'scale(1.05)' }}> <CardOverlay ... /> </div>) : null} </DragOverlay>, document.body)` ← extend the div style with conditional shadow

From src/shared/types.ts:
- `gameState.canvasCards: ClientCanvasCard[]` — already in scope (BoardDragLayer receives `gameState` prop)
- `ClientCanvasCard = CanvasCard = { card: Card; x: number; y: number; z: number }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add drag-time shadow state, refs, and DragMoveEvent type import</name>
  <files>src/components/BoardDragLayer.tsx</files>
  <read_first>
    - src/components/BoardDragLayer.tsx (lines 1–100 for imports, useState declarations, useRef declarations; lines 84–92 for the state/ref cluster to extend)
    - src/lib/canvas-utils.ts (confirm STACK_SHADOW and coversMajority exports created by Plan 01)
    - src/spikes/Spike002OverlapHitTesting.tsx (lines 86–87 for parallel state pattern; lines 100–104 for the dragOverlapsAnother check — adapt from useMemo to ref-throttled handler in Task 2)
    - .planning/phases/33-overlap-visibility/33-CONTEXT.md (D-05, D-06: ref-based pattern requirement)
    - .planning/phases/33-overlap-visibility/33-RESEARCH.md (Pattern 2 ref-throttled drag-time shadow; Pitfall 1 activeCard is Card not CanvasCard — need separate activeDragOrigin)
  </read_first>
  <behavior>
    After this task:
    - `import type` line for `@dnd-kit/core` includes `DragMoveEvent`
    - New import: `import { coversMajority, STACK_SHADOW } from '@/lib/canvas-utils';`
    - Three new declarations alongside the existing activeCard state (around line 84–92):
      - `const [activeDragOrigin, setActiveDragOrigin] = useState<{ x: number; y: number } | null>(null);`
      - `const [dragCoversSomeCard, setDragCoversSomeCard] = useState(false);`
      - `const dragDeltaRef = useRef<{ x: number; y: number }>({ x: 0, y: 0 });`
    - No other behavior changes; no handler wired yet (Task 2 does that)
    - Type-check and full test suite still pass
  </behavior>
  <action>
    Modify src/components/BoardDragLayer.tsx:
    - Extend the `import type` from @dnd-kit/core (line 4) to add `DragMoveEvent` to the named import list. The final import becomes a `import type { CollisionDetection, DragStartEvent, DragEndEvent, DragMoveEvent } from '@dnd-kit/core';`
    - Add a new import line after the existing internal imports (after the BoardView/CardOverlay imports near line 9): `import { coversMajority, STACK_SHADOW } from '@/lib/canvas-utils';`
    - Add three new declarations directly after the existing `const [activeCard, setActiveCard] = useState<Card | null>(null);` line (currently line 84). Place them BEFORE the other selection state to keep drag-state cohesive:
      - `activeDragOrigin` state typed `{ x: number; y: number } | null` initialized to `null`
      - `dragCoversSomeCard` state typed `boolean` initialized to `false`
      - `dragDeltaRef` ref typed `{ x: number; y: number }` initialized to `{ x: 0, y: 0 }`
    - Do NOT remove or rename any existing state/ref. Do NOT wire the new state to anything yet; Task 2 adds the handler and the prop hookups.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DragMoveEvent" src/components/BoardDragLayer.tsx` returns ≥ 1
    - `grep -c "from '@/lib/canvas-utils'" src/components/BoardDragLayer.tsx` returns 1
    - `grep -c "activeDragOrigin" src/components/BoardDragLayer.tsx` returns ≥ 2 (state declaration + setter)
    - `grep -c "dragCoversSomeCard" src/components/BoardDragLayer.tsx` returns ≥ 2 (state declaration + setter)
    - `grep -c "dragDeltaRef" src/components/BoardDragLayer.tsx` returns ≥ 1 (ref declaration; usage added in Task 2)
    - `grep -E "useState<\\{ x: number; y: number \\} \\| null>" src/components/BoardDragLayer.tsx` matches exactly once (activeDragOrigin)
    - `grep -E "useRef<\\{ x: number; y: number \\}>" src/components/BoardDragLayer.tsx` matches exactly once (dragDeltaRef)
    - `npm run typecheck` exits 0 (TypeScript happy with new state types even though setters are not yet called)
    - `npm test` exits 0 (no behavioral change yet — full suite still green)
  </acceptance_criteria>
  <done>
    State/ref/import scaffolding present. Type-check + full suite green. Ready for Task 2 to wire the handler.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement handleDragMove, reset logic, and DragOverlay shadow</name>
  <files>src/components/BoardDragLayer.tsx</files>
  <read_first>
    - src/components/BoardDragLayer.tsx (full file — especially handleDragStart lines 184–201; handleDragEnd lines 203–394 including all early-return branches at lines 246–247, 266–271, 304–307, 311–319; handleDragCancel lines 396–404; DndContext at lines 408–414; DragOverlay block at lines 417–427)
    - src/spikes/Spike002OverlapHitTesting.tsx (lines 110–122 handleDragStart + handleDragMove pattern; lines 193–205 DragOverlay shadow conditional)
    - src/lib/canvas-utils.ts (coversMajority signature and STACK_SHADOW constant)
    - .planning/phases/33-overlap-visibility/33-CONTEXT.md (D-05 drag-time check runs in onDragMove; D-06 ref-based pattern; specifics: dragged card excluded from check; use stored x/y for non-dragged cards and stored+delta for dragged)
    - .planning/phases/33-overlap-visibility/33-RESEARCH.md (Pitfall 1 activeCard is Card not CanvasCard — capture origin in dragStart; Pitfall 2 reset shadow on dragEnd/Cancel; Pitfall 3 non-canvas drag guard)
    - .planning/phases/33-overlap-visibility/33-PATTERNS.md (BoardDragLayer.tsx section: exact handleDragStart extension, handleDragMove function shape, handleDragEnd reset placement, DndContext prop addition, DragOverlay shadow extension)
  </read_first>
  <behavior>
    handleDragStart end-extension:
    - After existing `setActiveCard(data.card)` + `setDragging(true)`, if `data.fromZone === 'canvas'`, look up the canvas card via `gameState.canvasCards.find(cc => cc.card.id === data.card.id)`; if found, call `setActiveDragOrigin({ x: existing.x, y: existing.y })`; else call `setActiveDragOrigin(null)`
    - If `data.fromZone !== 'canvas'`, call `setActiveDragOrigin(null)`
    - Always reset `dragDeltaRef.current = { x: 0, y: 0 }`

    New handleDragMove function (added after handleDragStart):
    - Type signature: `function handleDragMove(event: DragMoveEvent)`
    - Early return when `dragDataRef.current?.fromZone !== 'canvas'` (Pitfall 3)
    - Early return when `activeDragOrigin` is null
    - Assign `dragDeltaRef.current = { x: event.delta.x, y: event.delta.y }` (NO setState here)
    - Compute `draggedPos = { x: activeDragOrigin.x + event.delta.x, y: activeDragOrigin.y + event.delta.y }`
    - Compute `nowCovers = gameState.canvasCards.some(other => other.card.id !== activeCard?.id && coversMajority(draggedPos, other))`
    - Guard: `if (nowCovers !== dragCoversSomeCard) setDragCoversSomeCard(nowCovers);` (boolean-flip-only setState)

    Reset in handleDragEnd:
    - At every place where the function returns control to dnd-kit (every existing early-return AND the final fall-through), the two pieces of new drag-session state must be cleared: `setDragCoversSomeCard(false)` and `setActiveDragOrigin(null)`. The simplest correct placement is: at the TOP of handleDragEnd, before any branch, set both. This guarantees every code path resets regardless of which branch returns.

    Reset in handleDragCancel:
    - Add `setDragCoversSomeCard(false);` and `setActiveDragOrigin(null);` to the existing handleDragCancel function (currently lines 396–404)

    DndContext wiring:
    - Add `onDragMove={handleDragMove}` prop on the DndContext element (around line 411), between onDragStart and onDragEnd. Preserve all other existing props.

    DragOverlay shadow:
    - Modify the div style at line 421 from `{ opacity: 0.5, transform: 'scale(1.05)' }` to also include `boxShadow: dragCoversSomeCard ? STACK_SHADOW : undefined` and `borderRadius: dragCoversSomeCard ? 6 : undefined`. Preserve the existing D-13 comment.

    Performance contract: a manual pointermove sweep across a covering region triggers exactly two setState calls (one when the boolean flips to true, one when it flips back to false), not 60+ per second.
  </behavior>
  <action>
    Modify src/components/BoardDragLayer.tsx in one cohesive edit:

    1. Extend handleDragStart (currently ends around line 201) with the origin-capture block per the behavior contract above. The block goes AFTER `setActiveCard(data.card)` and AFTER `setDragging(true)`, before the function closes. Use `gameState.canvasCards.find(cc => cc.card.id === data.card.id)` to recover {x,y}; default to null when not on canvas.

    2. Insert a NEW function `handleDragMove(event: DragMoveEvent)` immediately after handleDragStart. Body per the behavior contract: non-canvas guard, null-origin guard, ref update, draggedPos computation, nowCovers some-check (excluding `activeCard?.id`), guarded setState on boolean flip.

    3. Add `setDragCoversSomeCard(false);` and `setActiveDragOrigin(null);` as the FIRST two lines inside `handleDragEnd` (before the canvas-branch early-return at line 205). This guarantees reset on every code path without modifying each individual branch.

    4. Add `setDragCoversSomeCard(false);` and `setActiveDragOrigin(null);` inside `handleDragCancel` (currently lines 396–404) — place them after `dragDataRef.current = null;` and before the setTimeout.

    5. Add `onDragMove={handleDragMove}` to the DndContext element (insert between `onDragStart={handleDragStart}` line 411 and `onDragEnd={handleDragEnd}` line 412).

    6. Replace the DragOverlay child div's inline style (currently `{ opacity: 0.5, transform: 'scale(1.05)' }` on line 421) with `{ opacity: 0.5, transform: 'scale(1.05)', boxShadow: dragCoversSomeCard ? STACK_SHADOW : undefined, borderRadius: dragCoversSomeCard ? 6 : undefined }`. Do not touch the surrounding `<DragOverlay>` element or the `<CardOverlay card={activeCard} />` child.

    Preserve every other existing behavior in handleDragStart/handleDragEnd/handleDragCancel — do not refactor, rename, or remove any branch logic.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function handleDragMove" src/components/BoardDragLayer.tsx` returns 1
    - `grep -c "onDragMove={handleDragMove}" src/components/BoardDragLayer.tsx` returns 1
    - `grep -c "dragDeltaRef.current = { x: event.delta.x, y: event.delta.y }" src/components/BoardDragLayer.tsx` returns 1
    - `grep -c "if (nowCovers !== dragCoversSomeCard) setDragCoversSomeCard(nowCovers)" src/components/BoardDragLayer.tsx` returns 1
    - `grep -c "dragDataRef.current?.fromZone !== 'canvas'" src/components/BoardDragLayer.tsx` returns ≥ 1
    - `grep -c "setActiveDragOrigin" src/components/BoardDragLayer.tsx` returns ≥ 4 (declaration + dragStart capture + handleDragEnd reset + handleDragCancel reset)
    - `grep -c "setDragCoversSomeCard(false)" src/components/BoardDragLayer.tsx` returns ≥ 2 (handleDragEnd reset + handleDragCancel reset)
    - `grep -c "boxShadow: dragCoversSomeCard ? STACK_SHADOW" src/components/BoardDragLayer.tsx` returns 1
    - `grep -c "borderRadius: dragCoversSomeCard ? 6" src/components/BoardDragLayer.tsx` returns 1
    - `grep -c "opacity: 0.5, transform: 'scale(1.05)'" src/components/BoardDragLayer.tsx` returns 1 (preserved D-13)
    - `grep -c "gameState.canvasCards.find(cc => cc.card.id === data.card.id)" src/components/BoardDragLayer.tsx` returns 1 (origin capture in dragStart)
    - `npm test` exits 0 (full suite green — no regression to existing Phase 32 canvas/drag tests)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
    Drag-time shadow active on canvas drags only. Boolean-flip setState protocol enforced (per-pointermove updates land in the ref, not in React state). State resets on both end and cancel. Existing drag/drop behavior for hand/pile/spread/canvas unchanged. Visual verification of the shadow appearing/disappearing as the dragged card crosses the 50% threshold lives in Plan 03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 33 Plan 02 is pure client-side rendering. `gameState.canvasCards` is the server-authoritative input; drag delta comes from dnd-kit (browser-trusted pointer events). No new boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Denial of Service | onDragMove handler firing at pointermove rate | mitigate | Ref-throttled pattern (D-06): `dragDeltaRef.current = ...` does NOT trigger re-render; setState only fires when the covering-boolean flips. Acceptance criterion `grep -c "if (nowCovers !== dragCoversSomeCard)"` enforces the guard. |
| T-33-05 | Tampering | dragDeltaRef value leaked across drags | mitigate | `dragDeltaRef.current = { x:0, y:0 }` reset in handleDragStart (Task 1 scaffolding + Task 2 dragStart extension); activeDragOrigin null-reset in dragEnd/Cancel prevents stale state from previous drag (Pitfall 2). |
| T-33-06 | Information Disclosure | Drag-time shadow on non-canvas drags | mitigate | Non-canvas guard `if (dragDataRef.current?.fromZone !== 'canvas') return;` (Pitfall 3) prevents the handler from accessing `activeDragOrigin` (which would be null for hand/pile drags, causing a crash) and prevents erroneous shadow flicker on non-canvas drags. |
| T-33-07 | Spoofing | activeCard.id used to exclude dragged card from covering check | accept | activeCard is set from dnd-kit's `active.data.current.card` which originates from the component's own useDraggable data — same trust as existing PASS_CARD/MOVE_CARD paths in Phase 32. No new spoofing surface. |
| T-33-SC | Tampering | npm installs (supply chain) | accept | This plan installs no new packages. |

ASVS L1 assessment from RESEARCH.md: no V2–V6 categories apply.
</threat_model>

<verification>
- `npm test` — full Vitest suite green (Phase 32 canvas + all prior tests intact)
- `npm run typecheck` — zero TypeScript errors
- Drag a canvas card across a stationary canvas card in dev env: shadow appears on the DragOverlay ghost when the dragged card covers >50% of the stationary card, disappears when it doesn't (covered by Plan 03 human verify)
- React DevTools profiler shows the BoardDragLayer renders only on boolean flip, not on every pointermove (manual perf check, optional — automated guard is the grep on the `if (nowCovers !== ...)` line)
</verification>

<success_criteria>
- BoardDragLayer imports DragMoveEvent type and the canvas-utils symbols
- handleDragStart captures activeDragOrigin for canvas drags, nulls it otherwise
- handleDragMove guards on canvas zone + non-null origin, updates dragDeltaRef without setState, calls setState only on boolean flip
- handleDragEnd and handleDragCancel both reset dragCoversSomeCard and activeDragOrigin
- DndContext receives onDragMove prop
- DragOverlay div conditionally renders boxShadow + borderRadius from dragCoversSomeCard
- Existing D-13 opacity + scale preserved
- Full test suite + typecheck green
- No new packages installed
</success_criteria>

<output>
Create `.planning/phases/33-overlap-visibility/33-02-SUMMARY.md` when done. Frontmatter must include:
- `requirements-completed: [OVERLAP-03]` (combined with Plan 01 — full requirement-level completion pending Plan 03 human verify)
- `requires: 33-01 (imports coversMajority + STACK_SHADOW from src/lib/canvas-utils.ts created in Plan 01)`
- `provides:` handleDragMove handler; activeDragOrigin + dragCoversSomeCard state; dragDeltaRef ref pattern in production; conditional DragOverlay shadow
- `affects:` 33-03 (human verify of drag-time shadow); future Phase 34 (multi-card group drop may extend activeDragOrigin or the handleDragMove guard)
- `key-files`: modified src/components/BoardDragLayer.tsx
- `patterns-established:` ref-throttled boolean-flip setState in onDragMove (production application of CLAUDE.md `isDraggingRef = useRef(false)` convention); top-of-function reset for multi-branch handler cleanup
</output>
