---
phase: 30-layout-restructure-dock-spread-zones
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/BoardDragLayer.tsx
  - src/components/BoardView.tsx
autonomous: true
requirements:
  - LAYOUT-05

must_haves:
  truths:
    - "Each opponent's spread zone renders directly below that opponent's hand, in the board area, not inside the bg-card header band"
    - "The personal spread zone continues to render flush above the local player's hand (unchanged)"
    - "On tall viewports extra vertical space grows in the piles/grid row, never between an opponent's hand and that opponent's spread or between the personal spread and the local hand"
    - "Opponent spread row columns visually align with the opponent hand columns in the header (column widths match)"
    - "DndContext uses MeasuringStrategy.Always so droppable rects re-measure on every drag (no stale rects after the DOM restructure)"
    - "vitest and tsc pass after the restructure"
  artifacts:
    - path: "src/components/BoardDragLayer.tsx"
      provides: "DndContext with MeasuringStrategy.Always for droppable measurement"
      contains: "MeasuringStrategy"
    - path: "src/components/BoardView.tsx"
      provides: "Board layout with opponent spreads docked under opponent hands in board area"
      contains: "flex-shrink-0"
  key_links:
    - from: "src/components/BoardDragLayer.tsx"
      to: "@dnd-kit/core"
      via: "named import"
      pattern: "MeasuringStrategy"
    - from: "src/components/BoardView.tsx"
      to: "src/components/SpreadZone.tsx"
      via: "opponent spreads now rendered inside the board area row, not header"
      pattern: "interactive=\\{false\\}"
---

<objective>
Restructure the board layout so opponent spread zones live in a dedicated `flex-shrink-0` row at the top of the board area (directly below the header band), one column per opponent, aligned with their hand column above. Add `MeasuringStrategy.Always` to the `DndContext` preemptively so droppable rects are re-measured every drag — eliminating stale-rect drift after the DOM restructure.

Purpose: Satisfy LAYOUT-05 — opponent spreads are spatially anchored to their owner hands instead of crammed inside the header band. Extra vertical space on tall screens grows between the piles/grid row and the spread row, not between hands and spreads.

Output: Updated `BoardDragLayer.tsx` (one-line `measuring` prop + import addition) and updated `BoardView.tsx` (opponent spread rendering moved out of the header band column into a new spread row in the board area). Personal spread and HandZone remain untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-layout-restructure-dock-spread-zones/30-CONTEXT.md
@.planning/phases/30-layout-restructure-dock-spread-zones/30-PATTERNS.md
@.planning/phases/30-layout-restructure-dock-spread-zones/30-UI-SPEC.md
@src/components/BoardView.tsx
@src/components/BoardDragLayer.tsx
@src/components/SpreadZone.tsx
@src/components/ControlsBar.tsx
@src/components/OpponentHand.tsx

<interfaces>
<!-- Key contracts the executor will reuse. Extracted directly from src/components/SpreadZone.tsx. -->

From src/components/SpreadZone.tsx (props expected by both header callers and the new spread row):
```typescript
interface SpreadZoneProps {
  pile: ClientPile;
  sendAction: (action: ClientAction) => void;
  draggingCardId: string | null;
  className?: string;
  interactive?: boolean;          // false = opponent (no controls, no select); true = personal
  selectedIds?: Set<string>;
  onToggleSelect?: (id: string, zone: 'hand' | 'pile', zoneId: string) => void;
  onSelectAll?: (cardIds: string[], zone: 'hand' | 'pile', zoneId: string) => void;
  selectionSource?: { zone: 'hand' | 'pile'; zoneId: string } | null;
}
```

The new opponent spread row supplies only: `pile`, `sendAction`, `draggingCardId`, and `interactive={false}` (mirrors the current header usage at BoardView.tsx lines 57-63).

From @dnd-kit/core (named exports, already imported in BoardDragLayer.tsx):
```typescript
import {
  DndContext, DragOverlay, closestCenter, pointerWithin, getFirstCollision,
  defaultDropAnimation, useSensors, useSensor, PointerSensor, TouchSensor,
  MeasuringStrategy,
} from '@dnd-kit/core';
```

`MeasuringStrategy` resolves to an enum: `MeasuringStrategy.Always`, `MeasuringStrategy.BeforeDragging`, `MeasuringStrategy.WhileDragging`. Use `.Always` per D-07.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MeasuringStrategy.Always to DndContext in BoardDragLayer</name>
  <files>src/components/BoardDragLayer.tsx</files>
  <read_first>
    - src/components/BoardDragLayer.tsx (read full file to see current @dnd-kit/core named import list at line 3 and the DndContext JSX at lines 367-374)
    - .planning/phases/30-layout-restructure-dock-spread-zones/30-CONTEXT.md (D-07 rationale: preemptive, ~15 droppables, <1ms/frame overhead)
    - .planning/phases/30-layout-restructure-dock-spread-zones/30-UI-SPEC.md (Layout Contract — dnd-kit measurement strategy section)
  </read_first>
  <action>
    Modify `src/components/BoardDragLayer.tsx` in two places.

    1. At the existing named import from `@dnd-kit/core` (currently at line 3), add `MeasuringStrategy` to the import list. Do not introduce a separate import statement.

    2. On the `DndContext` JSX element (currently at lines 367-373), add a new prop: `measuring={{ droppable: { strategy: MeasuringStrategy.Always } }}`. Place the new prop between `onDragCancel={handleDragCancel}` and the closing `>` of the opening tag. Preserve all existing props (`sensors`, `collisionDetection`, `onDragStart`, `onDragEnd`, `onDragCancel`) and the existing children (BoardView and the createPortal DragOverlay block).

    Do not change `customCollision`, sensors config, drag handlers, the Dialog block, or any other code in this file. This is a pure config addition per D-07.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - src/components/BoardDragLayer.tsx imports `MeasuringStrategy` from `@dnd-kit/core` (verify: `rg -n "MeasuringStrategy" src/components/BoardDragLayer.tsx | grep -v '^#'` shows at least 2 lines — import + use)
    - src/components/BoardDragLayer.tsx contains the literal string `MeasuringStrategy.Always` (verify: `rg -c "MeasuringStrategy\.Always" src/components/BoardDragLayer.tsx` returns 1)
    - src/components/BoardDragLayer.tsx contains the literal string `measuring={{` on the DndContext element (verify: `rg -c "measuring=\{\{" src/components/BoardDragLayer.tsx` returns 1)
    - No new top-level imports were added besides extending the existing `@dnd-kit/core` named import (verify: `rg -c "from '@dnd-kit/core'" src/components/BoardDragLayer.tsx` returns 1)
    - `npm run typecheck` exits 0
    - `npm test -- --run` exits 0
  </acceptance_criteria>
  <done>
    DndContext is configured with MeasuringStrategy.Always; typecheck and unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure BoardView — move opponent spreads into a board-area row</name>
  <files>src/components/BoardView.tsx</files>
  <read_first>
    - src/components/BoardView.tsx (read full file — the entire layout JSX is 126 lines)
    - src/components/SpreadZone.tsx (read lines 60-90 to confirm SpreadZoneProps signature and how `data-testid={`spread-zone-${pile.id}`}` is set)
    - src/components/ControlsBar.tsx (read to understand the rendered footprint of the trigger so the spacer width matches — UI-SPEC declares `w-7` matches `size="icon-sm"`)
    - src/components/OpponentHand.tsx (read top of file to confirm prop shape passed unchanged from header map)
    - .planning/phases/30-layout-restructure-dock-spread-zones/30-CONTEXT.md (D-01 through D-06, target structure block)
    - .planning/phases/30-layout-restructure-dock-spread-zones/30-PATTERNS.md (BoardView.tsx section — before/after JSX, exact column class tokens to reuse)
    - .planning/phases/30-layout-restructure-dock-spread-zones/30-UI-SPEC.md (Layout Contract — board area flex column order, opponent spreads row structure, spacer element spec)
  </read_first>
  <action>
    Modify `src/components/BoardView.tsx` to dock opponent spread zones inside the board area, one column per opponent, aligned with the opponent hand columns in the header band.

    Three structural edits inside the returned JSX:

    1. **Header band column — remove SpreadZone**. Inside the existing `allOpponentIds.map` block in the `bg-card` header (currently `<div className="flex items-start justify-between px-4 py-2 gap-4 bg-card">` at line 39), each opponent column is a `<div key={id} className={`flex flex-col gap-1 ...`}>` wrapping `<OpponentHand .../>` and a conditional `{opponentSpread && <SpreadZone .../>}`. After the edit:
       - Remove the `{opponentSpread && <SpreadZone ... interactive={false} />}` block entirely from this column.
       - Remove `gap-1` from the column's className (no second child remains — keep `flex flex-col` plus the existing `${opponentCount === 1 ? 'flex-1 max-w-none' : 'flex-1 min-w-0'} sm:max-w-none overflow-x-hidden`).
       - You may keep the `const opponentSpread = ...` lookup inside this map only if it is still referenced; otherwise remove it. The spread row map below will compute its own `opponentSpread` lookup.

    2. **Board area — insert new opponent spreads row as first child**. The board area is the `<div className="flex-1 min-h-0 overflow-x-hidden overflow-y-auto sm:overflow-hidden flex flex-col">` (currently at line 73). Insert a new sibling as its FIRST child, before the piles+grid row (currently at line 74). The new row must be `flex-shrink-0` (D-06) and mirror the header's flex structure so columns align:
       - Outer wrapper class: `flex items-start gap-4 px-4 flex-shrink-0` (D-01, D-06; no vertical padding per D-02).
       - First child: `<div className="flex items-start gap-4 flex-1 overflow-hidden">` containing `allOpponentIds.map(...)` — for each opponent id, render a column `<div key={id} className={`flex flex-col ${opponentCount === 1 ? 'flex-1 max-w-none' : 'flex-1 min-w-0'} sm:max-w-none overflow-x-hidden`}>`. The column class token set must match the header column exactly (no `gap-1`). Inside the column, compute `const opponentSpread = spreadPiles.find(p => p.id === `spread-${id}`)` and render `{opponentSpread && <SpreadZone pile={opponentSpread} sendAction={sendAction} draggingCardId={draggingCardId} interactive={false} />}`. Per D-03, the column is always rendered even when `opponentSpread` is undefined — the column div is present, only the inner SpreadZone is conditional.
       - Second child (spacer): `<div className="w-7 self-start shrink-0 pointer-events-none" aria-hidden="true" />` per UI-SPEC. Width `w-7` (28px) matches the ControlsBar `size="icon-sm"` trigger so the flex-1 region in the spread row aligns with the flex-1 region in the header.

    3. **Do not touch** the piles+grid row, the personal spread wrapper (`{mySpreadZone && <div className="flex-shrink-0 px-4 py-1">...` per D-04), or the `HandZone` invocation. These four lines (piles row, personal spread, IIFE-wrapped HandZone) remain byte-identical except for their position relative to the new spread row inserted above them.

    Do not introduce new state, new props, or new helpers. Reuse `allOpponentIds`, `spreadPiles`, `opponentCount`, `draggingCardId`, and `sendAction` that are already in scope from the existing component body.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - The literal `interactive={false}` appears exactly once in src/components/BoardView.tsx (verify: `rg -c "interactive=\{false\}" src/components/BoardView.tsx` returns 1) — corresponds to the opponent SpreadZone in the new spread row, not the deleted header occurrence
    - The header band column no longer renders a SpreadZone — within the JSX of the `<div className="flex items-start justify-between px-4 py-2 gap-4 bg-card">` block, no `<SpreadZone` element appears (verify: open the file, the only `<SpreadZone` occurrences are inside the new opponent spread row and the personal `mySpreadZone` block)
    - src/components/BoardView.tsx contains the substring `flex items-start gap-4 px-4 flex-shrink-0` (verify: `rg -c "flex items-start gap-4 px-4 flex-shrink-0" src/components/BoardView.tsx` returns 1) — this is the new opponent spreads row outer wrapper
    - src/components/BoardView.tsx contains `aria-hidden="true"` on the spacer (verify: `rg -c "aria-hidden=\"true\"" src/components/BoardView.tsx` returns 1)
    - src/components/BoardView.tsx contains `w-7 self-start shrink-0 pointer-events-none` (verify: `rg -c "w-7 self-start shrink-0 pointer-events-none" src/components/BoardView.tsx` returns 1)
    - Personal spread wrapper `flex-shrink-0 px-4 py-1` is unchanged (verify: `rg -c "flex-shrink-0 px-4 py-1" src/components/BoardView.tsx` returns 1) — D-04
    - Piles+grid row class `flex-1 min-h-0 flex items-center px-4 gap-4` is unchanged (verify: `rg -c "flex-1 min-h-0 flex items-center px-4 gap-4" src/components/BoardView.tsx` returns 1)
    - `allOpponentIds.map` appears twice in the file — once in the header (hands only) and once in the new spread row (verify: `rg -c "allOpponentIds\.map" src/components/BoardView.tsx` returns 2)
    - `npm run typecheck` exits 0
    - `npm test -- --run` exits 0
  </acceptance_criteria>
  <done>
    BoardView renders opponent spreads in a flex-shrink-0 row at the top of the board area, aligned with opponent hand columns in the header. Personal spread + HandZone untouched. typecheck and unit tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → DndContext | Drag gestures cross from DOM events into dnd-kit; stale droppable rects could mis-route drops |
| client UI → PartyKit server | sendAction payloads (MOVE_CARD, REORDER_PILE_SPREAD) cross WebSocket; mis-routed drop = wrong server mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | DndContext droppable rects | mitigate | Add `measuring={{ droppable: { strategy: MeasuringStrategy.Always } }}` (D-07). Every drag re-measures droppable bounds, eliminating stale-rect drift caused by the DOM restructure. Validated by Task 1 acceptance criteria. |
| T-30-02 | Information Disclosure | Opponent SpreadZone in board area | accept | The opponent spread already renders face-down cards in the existing header location. Moving the DOM node does not change `interactive={false}` (no flip control) or the server-side hand-masking contract. No new information surface is created. |
| T-30-03 | Denial of Service | flex-shrink behavior on short viewports | mitigate | Opponent spread row uses `flex-shrink-0` (D-06). Spread columns cannot be squeezed off-screen and visually detached from their hand. Validated by Task 2 acceptance criterion checking `flex-shrink-0` literal. |
| T-30-04 | Repudiation | Wrong drop target after restructure | mitigate | Plan 30-02 adds a Playwright e2e drag-and-drop test that verifies a drag-from-hand-to-personal-spread lands correctly post-restructure (D-08). Console-warning scrub also blocks duplicate-ID dnd-kit regressions. |
| T-30-05 | Spoofing | n/a | accept | Phase makes no identity-related changes. |
| T-30-06 | Elevation of Privilege | n/a | accept | Phase makes no permission-related changes. SpreadZone interactivity is unchanged. |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm test -- --run` exits 0
- Visually inspect `npm run dev:client` (Vite) + `npm run dev` (PartyKit): in a 2-player room, P1 sees P2's spread zone in the board area directly below P2's hand. The opponent spread column is the same width as the opponent hand column above it (flex-1 alignment confirmed via the w-7 spacer).
- Personal spread still sits flush above HandZone with the existing `px-4 py-1` wrapper unchanged.
- On a tall viewport (e.g. 1280×1024), extra vertical space appears between the new spread row and the piles/grid row, not between an opponent's hand and spread or between the personal spread and the local hand.
</verification>

<success_criteria>
- Opponent spread zones are docked under their owner hand in the board area (not in the bg-card header band)
- Personal spread renders flush above the local player's hand (existing behavior preserved)
- DndContext uses `MeasuringStrategy.Always`
- Plan 30-02 can run against this layout without any layout-driven test rewrites
- `npm run typecheck` and `npm test -- --run` both exit 0
</success_criteria>

<output>
Create `.planning/phases/30-layout-restructure-dock-spread-zones/30-01-SUMMARY.md` when done.
</output>
