---
phase: 34-multi-card-group-drop
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - src/components/BoardDragLayer.tsx
  - src/components/CanvasZone.tsx
autonomous: true
requirements: [MULTI-02, MULTI-03, MULTI-04]
must_haves:
  truths:
    - "Dragging any selected canvas card moves all selected cards as a group, preserving relative offsets"
    - "Multi-card hand→canvas drag: passenger cards land at offsets captured from DOM at drag start, anchored to the drop point of the drag handle"
    - "Multi-card spread→canvas drag: passenger cards land at DOM-captured offsets, same anchoring as hand→canvas"
    - "Passenger ghost divs render inside CanvasZone at opacity 0.5, zIndex 998, pointer-events:none, following the live drag delta"
    - "If any card in the group would land outside `[0, canvasW - CARD_W] × [0, canvasH - CARD_H]`, no GROUP_PLACE_ON_CANVAS dispatched; all source cards snap back silently"
    - "Selection clears (selectedIds=∅, selectionSource=null, dragDelta=null) on every onDragEnd regardless of drop outcome"
  artifacts:
    - path: "src/components/BoardDragLayer.tsx"
      provides: "handleDragStart DOM offset capture, handleDragMove dragDelta state update, handleDragEnd GROUP path with bounds check"
      contains: "GROUP_PLACE_ON_CANVAS"
    - path: "src/components/CanvasZone.tsx"
      provides: "passengerGhosts useMemo + ghost div rendering during drag"
      contains: "passengerGhosts"
  key_links:
    - from: "src/components/BoardDragLayer.tsx handleDragEnd canvas branch"
      to: "party/index.ts GROUP_PLACE_ON_CANVAS case"
      via: "sendAction({ type: 'GROUP_PLACE_ON_CANVAS', fromZone, fromId, cards })"
      pattern: "GROUP_PLACE_ON_CANVAS"
    - from: "src/components/CanvasZone.tsx passenger ghost JSX"
      to: "dragDelta state in BoardDragLayer"
      via: "dragDelta prop threaded via BoardView"
      pattern: "dragDelta"
    - from: "src/components/BoardDragLayer.tsx handleDragStart offset capture"
      to: "data-card-id attributes added in Plan 02"
      via: "document.querySelector('[data-card-id=...]').getBoundingClientRect()"
      pattern: "getBoundingClientRect"
---

<objective>
Implement multi-card group drag mechanics: DOM offset capture for hand/spread sources at drag start, parallel `dragDelta` state for passenger ghost rendering, GROUP_PLACE_ON_CANVAS dispatch with all-or-nothing bounds check, and passenger ghost rendering inside CanvasZone.

Purpose: This is the visible drag behavior. The server (Plan 01) and selection state (Plan 02) are prerequisites; this plan connects them via dnd-kit event handlers and animates the group movement.

Output: Full multi-card group drop works end-to-end. MULTI-02/03/04 satisfied.
</objective>

<execution_context>
@$HOME/code/dev-setup/claude/get-shit-done/workflows/execute-plan.md
@$HOME/code/dev-setup/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-multi-card-group-drop/34-CONTEXT.md
@.planning/phases/34-multi-card-group-drop/34-RESEARCH.md
@.planning/phases/34-multi-card-group-drop/34-PATTERNS.md
@.planning/phases/34-multi-card-group-drop/34-UI-SPEC.md
@.planning/phases/34-multi-card-group-drop/34-01-SUMMARY.md
@.planning/phases/34-multi-card-group-drop/34-02-SUMMARY.md
@src/components/BoardDragLayer.tsx
@src/components/CanvasZone.tsx
@src/components/CanvasDraggableCard.tsx
@src/spikes/Spike004MultiCardDrop.tsx
@src/lib/canvas-utils.ts

<interfaces>
<!-- Plan 02 already added (in BoardDragLayer.tsx): -->
<!--   - import { SelectionSource } from '@/shared/types' -->
<!--   - handleToggleSelectCanvas(id) -->
<!--   - handleDeselectAll() -->
<!--   - groupIds: Set<string> useMemo -->
<!--   - const [dragDelta, setDragDelta] = useState<{x,y} | null>(null) -->
<!--   - All five new props threaded to BoardView → CanvasZone -->

<!-- Plan 02 already added (in CanvasZone.tsx): -->
<!--   - new props: selectedIds, groupIds, activeCardId, dragDelta, onToggleSelectCanvas, onDeselectAll -->
<!--   - onClick={onDeselectAll} on container -->
<!--   - selection count badge -->
<!--   - per-card isSelected/isPassenger/onToggleSelect props forwarded -->

<!-- Plan 02 already added (in CanvasDraggableCard.tsx): -->
<!--   - data-card-id={canvasCard.card.id} on root div -->
<!--   - opacity:0 when isPassenger -->
<!--   - ring boxShadow when isSelected -->

<!-- Plan 02 already added (in DraggableCard.tsx, SpreadZone.tsx): data-card-id attribute on per-card root div -->

Existing BoardDragLayer state (already in place):
```typescript
const [activeCard, setActiveCard] = useState<Card | null>(null);
const activeDragOriginRef = useRef<{ x: number; y: number } | null>(null);
const dragDeltaRef = useRef<{ x: number; y: number }>({ x: 0, y: 0 });
const [selectedIds, setSelectedIds] = useState<Set<string>>(new Set());
const [selectionSource, setSelectionSource] = useState<SelectionSource>(null);
const dragDataRef = useRef<{ card: Card; fromZone: string; fromId: string } | null>(null);
const canvasRef = useRef<HTMLDivElement>(null);
const [dragDelta, setDragDelta] = useState<{ x: number; y: number } | null>(null); // added in Plan 02
```

Spike 004 passenger ghost JSX pattern (src/spikes/Spike004MultiCardDrop.tsx lines 253-268):
```typescript
{passengerGhosts.map(cc => (
  <div
    key={`ghost-${cc.card.id}`}
    style={{
      position: 'absolute',
      left: cc.x + (dragDelta?.x ?? 0),
      top: cc.y + (dragDelta?.y ?? 0),
      zIndex: 998,
      opacity: 0.5,
      pointerEvents: 'none',
    }}
  >
    <CardFace card={cc.card} />
  </div>
))}
```

Existing PLACE_ON_CANVAS dispatch in handleDragEnd (canvas branch) — read it directly from BoardDragLayer.tsx for the exact existing structure including: canvasBounds calculation, getCardDimensions(), clamping, sendAction dispatch with { type: 'PLACE_ON_CANVAS', cardId, fromZone, fromId, x, y }.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client handleDragEnd → sendAction | Last client-side gate; bounds rejection here prevents out-of-bounds coordinates being sent (server still re-validates) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Tampering | Client bounds check | accept (client-only UX gate) | Per D-13: client pre-validates bounds; if any card overflows, no dispatch. Server still independently validates coordinates (T-34-02 in Plan 01) — defense in depth |
| T-34-08 | DoS | dragDelta state on pointermove | mitigate | `dragDelta` state updates fire on each `handleDragMove`; React batches and the 2-3 affected components (CanvasZone + handle) re-render. Acceptable per RESEARCH.md Pitfall 1 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: handleDragStart DOM offset capture; handleDragMove dragDelta state update; passenger ghost rendering in CanvasZone</name>
  <files>src/components/BoardDragLayer.tsx, src/components/CanvasZone.tsx</files>
  <read_first>
    - src/components/BoardDragLayer.tsx (full file — focus handleDragStart around line 188, handleDragMove around line 215, existing dragDeltaRef pattern)
    - src/components/CanvasZone.tsx (full file as of Plan 02 — props now include dragDelta, groupIds, activeCardId)
    - src/spikes/Spike004MultiCardDrop.tsx lines 118-148 (handleDragStart, handleDragMove, handleDragEnd group logic) and lines 207-210 (passengerGhosts useMemo)
    - .planning/phases/34-multi-card-group-drop/34-RESEARCH.md Pattern 4 (DOM Offset Capture) and Pitfall 1 (dragDelta state vs ref)
  </read_first>
  <action>
    In `src/components/BoardDragLayer.tsx`:

    1. Add a new ref next to the existing refs: `const passengerOffsetsRef = useRef<Record<string, { offsetX: number; offsetY: number }>>({});`. This stores per-card DOM offsets relative to the drag handle, captured at drag start for hand/spread sources (D-11).

    2. In `handleDragStart` (after the existing logic that captures `activeCard` and `activeDragOriginRef`), add a block:
       - If `selectionSource` is non-canvas AND `selectedIds.size > 0` AND `selectedIds.has(String(event.active.id))` (the dragged card is among selected): use `document.querySelector('[data-card-id="${event.active.id}"]')` to find the handle element and `.getBoundingClientRect()` for its rect. For each `cardId` in `selectedIds`, query the corresponding element and record `{ offsetX: rect.left - handleRect.left, offsetY: rect.top - handleRect.top }` into `passengerOffsetsRef.current`. If either element is missing from the DOM, leave that entry out (defensive — passenger will be ignored at dispatch time).
       - If `selectionSource?.zone === 'canvas'` and `selectedIds.size > 0` and the dragged card is selected: offsets are NOT needed (canvas cards already have stored `x/y`; uniform `delta.x/y` applies). Clear `passengerOffsetsRef.current = {}` for safety.
       - If the dragged card is NOT in `selectedIds`: this is a single-card drag; reset `passengerOffsetsRef.current = {}` and do not modify selection (preserve existing single-card behavior).

    3. Initialize `setDragDelta({ x: 0, y: 0 })` at the end of `handleDragStart` so passenger ghosts render at their resting positions immediately.

    4. In `handleDragMove`, alongside the existing `dragDeltaRef.current = { x: event.delta.x, y: event.delta.y }` update, add `setDragDelta({ x: event.delta.x, y: event.delta.y });`. Per RESEARCH.md Pitfall 1, dual ref+state pattern is the safest fix.

    5. Locate the existing post-drop state clear pattern in handleDragEnd (where the existing single-card canvas branch resets state — `setActiveCard(null); setDragging(false); setSelectedIds(new Set()); setSelectionSource(null); dragDataRef.current = null;`). Add `setDragDelta(null);` and `passengerOffsetsRef.current = {};` to EVERY successful drop branch AND to the existing onDragCancel/non-canvas fallthrough paths. (Per D-06: selection clears on every onDragEnd regardless of success.) Do not yet add the GROUP dispatch logic — Task 2 does that.

    In `src/components/CanvasZone.tsx`:

    6. Import `CardFace` (or whatever the existing canvas card face component is — check `CanvasDraggableCard.tsx` to mirror its face render path).

    7. Add a `passengerGhosts` useMemo after the existing `coveringIds` useMemo:
       - Returns `[]` if `dragDelta === null` or `groupIds.size === 0` or `activeCardId === null`.
       - Otherwise returns `canvasCards.filter(cc => cc.card.id !== activeCardId && groupIds.has(cc.card.id))`.
       - Dependencies: `[dragDelta, canvasCards, activeCardId, groupIds]`.

       Note: This only renders ghosts for canvas→canvas group drag (where the passenger is a `ClientCanvasCard` with `x/y` already known). Hand/spread→canvas passengers do NOT have canvas `x/y` — their ghost rendering during cross-zone drag is out of scope for this task; the drop still works because dispatch reads from `passengerOffsetsRef` (Task 2). The single DragOverlay handle card provides sufficient visual feedback for cross-zone group drag.

    8. Add the passenger ghost JSX block inside the container `<div>`, after the existing `canvasCards.map(...)` block. Use exactly the Spike 004 pattern:
       ```
       {passengerGhosts.map(cc => (
         <div
           key={`ghost-${cc.card.id}`}
           data-testid={`canvas-ghost-${cc.card.id}`}
           style={{
             position: 'absolute',
             left: cc.x + (dragDelta?.x ?? 0),
             top: cc.y + (dragDelta?.y ?? 0),
             zIndex: 998,
             opacity: 0.5,
             pointerEvents: 'none',
           }}
         >
           {cc.card.faceUp ? <CardFace card={cc.card} /> : <CardBack />}
         </div>
       ))}
       ```
       Match the same `faceUp` ternary already used in `CanvasDraggableCard` so face/back state is consistent.

    Do NOT yet modify handleDragEnd's GROUP dispatch logic — Task 2 handles that. This task ONLY plumbs offset capture, delta state, and passenger rendering.
  </action>
  <acceptance_criteria>
    - `grep -n "passengerOffsetsRef" src/components/BoardDragLayer.tsx` returns matches in: ref declaration, handleDragStart capture block, post-drop clear paths
    - `grep -n "setDragDelta(" src/components/BoardDragLayer.tsx` returns matches in: handleDragStart init (to {x:0,y:0}), handleDragMove update, post-drop clear (to null)
    - `grep -n "getBoundingClientRect" src/components/BoardDragLayer.tsx` returns at least 1 new match in handleDragStart (alongside existing canvas branch usage)
    - `grep -n "data-card-id" src/components/BoardDragLayer.tsx` returns at least 1 match (querySelector in handleDragStart)
    - `grep -n "passengerGhosts" src/components/CanvasZone.tsx` returns matches in: useMemo declaration + JSX map
    - `grep -n "ghost-" src/components/CanvasZone.tsx` returns at least 1 match (data-testid prefix in ghost JSX)
    - `npm run typecheck` exits 0
    - `npm test` exits 0
    - Manual: in dev mode, select 2 canvas cards → drag one → both passenger source cards show opacity 0; passenger ghost div tracks the drag at 50% opacity at offset `(card.x + delta.x, card.y + delta.y)`
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <done>
    `handleDragStart` captures DOM offsets for hand/spread group sources; `handleDragMove` updates parallel `dragDelta` state; passenger ghost divs render in `CanvasZone` for canvas→canvas group drags. Selection clears on every drag end including cancel. Build green.
  </done>
</task>

<task type="auto">
  <name>Task 2: handleDragEnd GROUP_PLACE_ON_CANVAS dispatch with all-or-nothing bounds check</name>
  <files>src/components/BoardDragLayer.tsx</files>
  <read_first>
    - src/components/BoardDragLayer.tsx (full file — focus handleDragEnd, specifically the existing canvas branch that handles PLACE_ON_CANVAS dispatch)
    - src/lib/canvas-utils.ts (CARD_W, CARD_H, getCardDimensions)
    - src/spikes/Spike004MultiCardDrop.tsx lines 174-190 (handleDragEnd group dispatch logic)
    - .planning/phases/34-multi-card-group-drop/34-CONTEXT.md D-11 (x/y computation), D-13 (bounds), D-14 (all cards checked), D-15 (silent snap-back)
    - .planning/phases/34-multi-card-group-drop/34-RESEARCH.md Code Examples bounds check section
  </read_first>
  <action>
    Modify `handleDragEnd` in `src/components/BoardDragLayer.tsx`. The existing branch structure is roughly: check `event.over?.id === 'canvas'` → branch on hand/pile/canvas source → compute canvas-relative drop coordinates → clamp → dispatch `PLACE_ON_CANVAS`.

    Insert a new GROUP path BEFORE (or as an early return inside) the existing single-card canvas branch. Trigger condition: `event.over?.id === 'canvas' && dragDataRef.current && selectedIds.size >= 2 && selectedIds.has(String(event.active.id))`.

    Inside the GROUP path:

    1. Capture context locally:
       - `activeId = String(event.active.id)`
       - `data = dragDataRef.current` (already null-checked by condition)
       - `fromZone = data.fromZone` (either `'hand'`, `'pile'`, or `'canvas'`)
       - `fromId = data.fromId`
       - `canvasBounds = canvasRef.current?.getBoundingClientRect()` — bail to single-card path if missing
       - `{ w: CARD_W, h: CARD_H } = getCardDimensions()`
       - `canvasW = canvasBounds.width`, `canvasH = canvasBounds.height`

    2. Compute the drop coordinates for the drag handle relative to the canvas. Use the SAME formula already used in the existing single-card canvas branch (read it from the current handleDragEnd code — it combines `event.activatorEvent`/`event.delta`/`activeDragOriginRef` and canvas bounds). Let `handleDropX`, `handleDropY` be the resulting canvas-relative coordinates BEFORE clamping.

    3. Build the `cards` array `Array<{ cardId: string; x: number; y: number }>`:
       - If `fromZone === 'canvas'`: iterate `selectedIds`. For each `cardId`, look up the corresponding `ClientCanvasCard` from props' `canvasCards` (already exposed; if not in scope, surface via a new prop or via `gameState.canvasCards` if available — check existing access pattern). Compute `x = cc.x + event.delta.x`, `y = cc.y + event.delta.y`. Push `{ cardId, x, y }`.
       - If `fromZone === 'hand'` or `'pile'`: iterate `selectedIds`. For each `cardId`, read `offsets = passengerOffsetsRef.current[cardId]`. If missing (e.g., the handle card itself), use `{offsetX: 0, offsetY: 0}`. Compute `x = handleDropX + offsetX`, `y = handleDropY + offsetY`. Push `{ cardId, x, y }`.

    4. Bounds check (D-13/D-14) — ALL cards must pass:
       ```
       const allInBounds = cards.every(({x,y}) =>
         x >= 0 && x <= Math.max(0, canvasW - CARD_W) &&
         y >= 0 && y <= Math.max(0, canvasH - CARD_H)
       );
       ```
       If `!allInBounds` (D-15 silent snap-back):
       - Do NOT call `sendAction`.
       - Clear selection state: `setActiveCard(null); setDragging(false); setSelectedIds(new Set()); setSelectionSource(null); setDragDelta(null); dragDataRef.current = null; passengerOffsetsRef.current = {};`
       - `return;` (do not fall through to single-card path).

    5. If all in bounds, dispatch:
       ```
       sendAction({
         type: 'GROUP_PLACE_ON_CANVAS',
         fromZone,
         fromId,
         cards,
       });
       ```
       Set `dropSuccessRef.current = true` (matching existing single-card success pattern). Then clear selection/drag state (same five setters as the snap-back branch, plus `passengerOffsetsRef.current = {}`).
       `return;` to skip the single-card path.

    6. The single-card path (existing `PLACE_ON_CANVAS` branch) is unchanged. It runs only when `selectedIds.size < 2` or the active card is not in selection — i.e., a regular one-card drag.

    Notes:
    - Per CONTEXT D-08, the action carries `fromZone`, `fromId`, and `cards`. The server (Plan 01) determines pre-drag z by looking up cards in the source zone; the client does not send pre-drag z.
    - Per RESEARCH Pitfall 5, call `getCardDimensions()` inline at dispatch time (same source as `getBoundingClientRect`) to avoid mobile/desktop mismatch.
    - The existing `dragDataRef.current` is populated in `handleDragStart` only for single-card data. For group drops, `fromZone` and `fromId` from `dragDataRef.current` are correct because the dragged handle card carries the same `fromZone`/`fromId` as its selected peers (homogeneous source per D-09).
  </action>
  <acceptance_criteria>
    - `grep -n "GROUP_PLACE_ON_CANVAS" src/components/BoardDragLayer.tsx` returns at least 1 match in handleDragEnd dispatch
    - `grep -n "allInBounds" src/components/BoardDragLayer.tsx` returns at least 1 match (bounds variable)
    - `grep -n "passengerOffsetsRef.current\[" src/components/BoardDragLayer.tsx` returns at least 1 match (reading offsets in dispatch path)
    - `grep -n "selectedIds.size >= 2" src/components/BoardDragLayer.tsx` returns at least 1 match (GROUP path trigger)
    - The GROUP path appears BEFORE the existing single-card canvas branch in source order (sequence verifiable by line numbers)
    - `npm run typecheck` exits 0
    - `npm test` exits 0 (full suite — server tests still pass; client behavior change is verified manually + by e2e)
    - Manual happy paths (in dev mode at 1280×720):
      - Select 2 canvas cards → drag → release on canvas → both land at new positions preserving offset; both z above existing; visible in 2nd browser session
      - Select 2 hand cards → drag → release on canvas → both placed on canvas with relative spacing matching pre-drag DOM
      - Drag a group so any one card would land at x = -10 → silent snap back, no card moves, no toast
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <done>
    `handleDragEnd` dispatches `GROUP_PLACE_ON_CANVAS` when 2+ cards are selected and the dragged card is in selection. Bounds check runs against all N cards; failure = silent snap-back. State clears on every dragEnd. Existing single-card path unchanged. TypeScript + unit tests clean.
  </done>
</task>

</tasks>

<verification>
- `npm run typecheck` clean
- `npm test` full Vitest suite green
- Manual verification (deferred to Plan 04 human-verify task):
  - Canvas→canvas group drop happy path
  - Hand→canvas group drop happy path
  - Spread→canvas group drop happy path
  - Bounds violation snap-back (all three sources)
  - Internal z-order preserved after group drop
  - Single undo restores all cards
  - Two-player real-time sync of group drop
</verification>

<success_criteria>
- MULTI-02: relative offsets preserved across all three source paths (canvas→canvas, hand→canvas, spread→canvas)
- MULTI-03: server-assigned z values via Plan 01 handler land all cards above existing canvas cards, internal order preserved (server tested in Plan 01)
- MULTI-04: client-side bounds check rejects all-or-nothing with silent snap-back
- Passenger ghost divs render at correct positions during canvas→canvas group drag
- Selection always clears on dragEnd
- No regression of existing single-card drag (PLACE_ON_CANVAS) behavior
</success_criteria>

<output>
Create `.planning/phases/34-multi-card-group-drop/34-03-SUMMARY.md` when done.
</output>
