---
phase: 34-multi-card-group-drop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - party/index.ts
  - tests/canvasCards.test.ts
autonomous: true
requirements: [MULTI-02, MULTI-03, MULTI-04]
must_haves:
  truths:
    - "Client can send GROUP_PLACE_ON_CANVAS with {fromZone, fromId, cards:[{cardId,x,y}]} and server processes atomically"
    - "All cards in the group land on canvasCards with z values above any existing card; internal z-order matches pre-drop z ascending"
    - "Hand source: server rejects when fromId !== senderToken (UNAUTHORIZED_MOVE)"
    - "Server rejects: empty cards array, duplicate cardIds, NaN/Infinity coordinates, any missing cardId in source — with no mutation"
    - "Single UNDO_MOVE after GROUP_PLACE_ON_CANVAS restores all N cards to their pre-drop source positions"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "GROUP_PLACE_ON_CANVAS union member in ClientAction; SelectionSource exported type"
      contains: "GROUP_PLACE_ON_CANVAS"
    - path: "party/index.ts"
      provides: "GROUP_PLACE_ON_CANVAS case handler with auth + validation + atomic z-assignment"
      contains: "case \"GROUP_PLACE_ON_CANVAS\""
    - path: "tests/canvasCards.test.ts"
      provides: "describe('GROUP_PLACE_ON_CANVAS handler') block with happy path, z-order, undo, auth, validation tests"
      contains: "GROUP_PLACE_ON_CANVAS"
  key_links:
    - from: "tests/canvasCards.test.ts"
      to: "party/index.ts GROUP_PLACE_ON_CANVAS case"
      via: "room.onMessage(JSON.stringify({type:'GROUP_PLACE_ON_CANVAS',...}))"
      pattern: "GROUP_PLACE_ON_CANVAS"
    - from: "src/shared/types.ts ClientAction union"
      to: "party/index.ts switch statement"
      via: "discriminated union case label"
      pattern: "case \"GROUP_PLACE_ON_CANVAS\""
---

<objective>
Add server-authoritative `GROUP_PLACE_ON_CANVAS` action and Wave-0 RED tests that pin all server-side multi-card group drop behavior: validation, atomicity, z-ordering, and undo. Also extract `SelectionSource` to a shared type so Plan 02 client code can import it.

Purpose: The server is the single source of truth for canvas card placement. Multi-card group drops must be atomic (all-or-nothing) and z-order-correct (every moved card above any pre-existing canvas card, with internal pre-drag z-order preserved). Tests are written RED first to lock the contract before client integration.

Output: New `ClientAction` union member, exported `SelectionSource` type, new server handler case, new test describe block.
</objective>

<execution_context>
@$HOME/code/dev-setup/claude/get-shit-done/workflows/execute-plan.md
@$HOME/code/dev-setup/claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-multi-card-group-drop/34-CONTEXT.md
@.planning/phases/34-multi-card-group-drop/34-RESEARCH.md
@.planning/phases/34-multi-card-group-drop/34-PATTERNS.md
@.planning/phases/34-multi-card-group-drop/34-VALIDATION.md
@src/shared/types.ts
@party/index.ts
@tests/canvasCards.test.ts

<interfaces>
<!-- Existing PLACE_ON_CANVAS handler is the closest analog (party/index.ts lines 574-646). -->
<!-- It already implements: V5 coordinate validation, V4 hand-source auth guard, pre-validate-before-takeSnapshot, -->
<!-- per-zone splice (hand/pile/canvas), faceUp=true, push to canvasCards with z=max+1, single broadcast at end of switch. -->

From src/shared/types.ts (existing ClientAction union):
```typescript
export type ClientAction =
  | { type: "MOVE_CARD"; cardId: string; fromZone: "hand" | "pile" | "canvas"; fromId: string; toZone: "hand" | "pile"; toId: string; insertPosition?: 'top' | 'bottom' | 'random' }
  | { type: "PLAY_CARD_SET"; cardIds: string[]; fromZone?: "hand" | "pile"; fromId: string; toZone: "pile" | "hand"; toId: string }
  | { type: "PLACE_ON_CANVAS"; cardId: string; fromZone: "hand" | "pile" | "canvas"; fromId: string; x: number; y: number }
  | { type: "UNDO_MOVE" }
  // ... others
```

Existing SelectionSource type (currently inlined in two consumers; needs extraction):
- `src/components/BoardDragLayer.tsx` line ~64: `type SelectionSource = { zone: 'hand' | 'pile'; zoneId: string; hasMaskedCards?: boolean } | null;`
- `src/components/BoardView.tsx` line 23: `selectionSource: { zone: 'hand' | 'pile'; zoneId: string } | null;`
- `src/components/HandZone.tsx` line 122: same inline form
- `src/components/SpreadZone.tsx` line 78: same inline form

From party/index.ts (existing error pattern):
```typescript
sender.send(JSON.stringify({ type: "ERROR", code: "INVALID_COORDINATES", message: "..." } satisfies ServerEvent));
break;
```

From tests/canvasCards.test.ts existing harness (lines 1-41): `makeCard`, `makeMockRoom`, `makeMockConnection`, `getErrors` helpers — reusable.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PartyKit server | Untrusted ClientAction messages cross here; server must validate every field and authorize hand-source moves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Spoofing/Tampering | GROUP_PLACE_ON_CANVAS handler (hand source) | mitigate | Reject when `fromZone === "hand" && fromId !== senderToken` with `UNAUTHORIZED_MOVE`; mirrors existing PLACE_ON_CANVAS guard |
| T-34-02 | Tampering | GROUP_PLACE_ON_CANVAS handler (coordinate fields) | mitigate | Validate `Number.isFinite(c.x) && Number.isFinite(c.y)` for every card; reject with `INVALID_COORDINATES` |
| T-34-03 | Tampering | GROUP_PLACE_ON_CANVAS handler (atomicity) | mitigate | Pre-validate ALL cardIds resolve in source BEFORE `takeSnapshot`; reject empty array (`EMPTY_CARD_SET`), duplicate IDs (`DUPLICATE_CARD_IDS`), missing IDs (`CARD_NOT_IN_SOURCE`) with no mutation |
| T-34-04 | DoS | GROUP_PLACE_ON_CANVAS handler (empty payload) | mitigate | Reject `cards.length === 0` with `EMPTY_CARD_SET` (mirrors PLAY_CARD_SET pattern at party/index.ts line 662) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend ClientAction union and export SelectionSource type</name>
  <files>src/shared/types.ts</files>
  <read_first>
    - src/shared/types.ts (current ClientAction union, lines 73-88; CanvasCard shape lines 38-46)
    - src/components/BoardDragLayer.tsx lines 60-70 (current inline SelectionSource declaration)
    - src/components/BoardView.tsx lines 20-30 (current inline form on prop)
    - src/components/HandZone.tsx lines 120-130 (current inline form on prop)
    - src/components/SpreadZone.tsx lines 75-85 (current inline form on prop)
  </read_first>
  <action>
    Append a new `GROUP_PLACE_ON_CANVAS` member to the existing `ClientAction` union in `src/shared/types.ts`. Member shape (per D-08): `type: "GROUP_PLACE_ON_CANVAS"; fromZone: "hand" | "pile" | "canvas"; fromId: string; cards: { cardId: string; x: number; y: number }[]`. Place it immediately after the existing `PLACE_ON_CANVAS` line so related actions cluster.

    Also export a new named type `SelectionSource`: a discriminated union of three shapes — `{ zone: 'hand' | 'pile'; zoneId: string; hasMaskedCards?: boolean }`, `{ zone: 'canvas'; zoneId: 'canvas' }`, and `null`. Both client-zone variants are objects; canvas variant has literal `'canvas'` for both `zone` and `zoneId` per D-02. Do not edit the consumers yet (Plan 02 imports this); just add `export type SelectionSource = ...` to `src/shared/types.ts`.

    Do NOT introduce any new top-level types beyond `SelectionSource`. Do NOT modify `CanvasCard`, `ClientCanvasCard`, `Card`, `Pile`, `GameState`, or any existing union member.
  </action>
  <acceptance_criteria>
    - `grep -n "GROUP_PLACE_ON_CANVAS" src/shared/types.ts` returns exactly 1 match in the `ClientAction` union
    - `grep -n "export type SelectionSource" src/shared/types.ts` returns exactly 1 match
    - The exported `SelectionSource` has three variants: `'hand'`/`'pile'` form, `'canvas'` form with literal `zoneId: 'canvas'`, and `null`
    - `npm run typecheck` exits 0 (no other consumers updated yet; old inline forms still compile because they are not yet imports)
    - `npm test -- --run tests/canvasCards.test.ts` still compiles (may fail at runtime on Wave-0 stubs but TypeScript compiles)
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    `ClientAction` union now includes `GROUP_PLACE_ON_CANVAS` with the exact shape from D-08. `SelectionSource` is exported with three variants (hand|pile, canvas, null). No consumer files modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-0 RED tests for GROUP_PLACE_ON_CANVAS handler</name>
  <files>tests/canvasCards.test.ts</files>
  <behavior>
    - Happy path canvas→canvas: 2 canvas cards selected, delta applied per card, both land at new (x,y) with z above maxZ; internal z-order matches pre-drag ascending z
    - Happy path hand→canvas: 2 hand cards, both removed from hand, both placed on canvas with faceUp=true, z values consecutive above maxZ
    - Happy path pile→canvas (spread source): 2 cards from a `region:'spread'` pile, both removed from pile, both placed on canvas
    - Z-ordering: pre-drag z [3, 7, 1] → after drop z = [maxZ+2, maxZ+3, maxZ+1] (sorted by pre-drag z ascending; lowest pre-drag z gets lowest new z)
    - Auth guard: `fromZone: 'hand'` with `fromId !== senderToken` → UNAUTHORIZED_MOVE, no mutation to hands or canvasCards
    - Invalid coordinates: any card with NaN or Infinity x/y → INVALID_COORDINATES, no mutation
    - Empty cards array: `cards: []` → EMPTY_CARD_SET, no mutation
    - Duplicate cardIds: cards contain same cardId twice → DUPLICATE_CARD_IDS (or chosen error code, see action), no mutation
    - Missing cardId: one of N cardIds not in source → CARD_NOT_IN_SOURCE, no mutation (atomicity invariant)
    - Single undo restores all N cards: after GROUP_PLACE_ON_CANVAS of 2 cards from hand, UNDO_MOVE returns both cards to hand and clears them from canvasCards (snapshot taken once)
  </behavior>
  <read_first>
    - tests/canvasCards.test.ts (full file — reuse harness helpers `makeCard`, `makeMockRoom`, `makeMockConnection`, `getErrors`)
    - party/index.ts lines 574-646 (PLACE_ON_CANVAS handler — current behavior for single-card reference)
    - party/index.ts lines 658-700 (PLAY_CARD_SET handler — atomicity pattern; pre-validate-all before takeSnapshot)
    - .planning/phases/34-multi-card-group-drop/34-VALIDATION.md (Wave 0 requirements list and exact bullet checklist)
    - .planning/phases/34-multi-card-group-drop/34-RESEARCH.md security section (error codes UNAUTHORIZED_MOVE, INVALID_COORDINATES, EMPTY_CARD_SET, CARD_NOT_IN_SOURCE)
  </read_first>
  <action>
    Append a new top-level `describe("GROUP_PLACE_ON_CANVAS handler", () => { ... })` block at the end of `tests/canvasCards.test.ts`. Reuse the existing `makeCard`, `makeMockRoom`, `makeMockConnection`, `getErrors` helpers (do not redefine). Add a `beforeEach` that constructs a `GameRoom`, a `player-1` sender, registers player-1 in `room.gameState.players` with `connected:true`, and initializes `room.gameState.hands["player-1"] = []`.

    Write tests in the order listed in `<behavior>`. Choose error codes per the security section of RESEARCH.md: `UNAUTHORIZED_MOVE`, `INVALID_COORDINATES`, `EMPTY_CARD_SET`, `CARD_NOT_IN_SOURCE`, plus a new `DUPLICATE_CARD_IDS` (mirroring the naming style of existing codes). The handler does not yet exist — these tests will fail RED, which is correct for Wave 0.

    Assertion guidance:
    - For "no mutation" cases: snapshot `room.gameState.canvasCards.length` and source zone length before `onMessage`, assert unchanged after.
    - For z-ordering: pre-seed `room.gameState.canvasCards = [{ card: makeCard("existing"), x:0, y:0, z:5 }]`; sort moving cards by pre-drag z ascending; assert each placed card's final z equals `maxZ + 1 + rank`.
    - For atomicity (missing cardId among N): pre-seed hand with `[A-s, K-h]`; send `cards: [{cardId:'A-s'}, {cardId:'GHOST'}]`; assert hand still contains both A-s and K-h (no removal) and canvasCards length unchanged.
    - For single-undo: after dispatching GROUP_PLACE_ON_CANVAS, count `room.gameState.undoSnapshots.length` to assert exactly one new snapshot was taken (not N).
    - For UNDO restoration: send GROUP, then send UNDO_MOVE, assert hand has all original cards and canvasCards is empty (or equals pre-group state).

    Do NOT modify the existing PLACE_ON_CANVAS describe block. Do NOT add the handler implementation yet — that is Task 3. The tests must compile (TypeScript) using the union member added in Task 1 even though they fail at runtime.
  </action>
  <acceptance_criteria>
    - `grep -c 'GROUP_PLACE_ON_CANVAS' tests/canvasCards.test.ts` >= 10 (at least one assertion per behavior bullet)
    - `grep -n 'describe("GROUP_PLACE_ON_CANVAS' tests/canvasCards.test.ts` returns exactly 1 match
    - All 10 behavior bullets in `<behavior>` are covered by at least one `it()` block in the new describe
    - `npm run typecheck` exits 0 (tests compile against new ClientAction member)
    - `npm test -- --run tests/canvasCards.test.ts` exits with a non-zero code (RED) — the new tests fail because the server case does not exist; existing PLACE_ON_CANVAS tests still pass
    - Existing test count in file unchanged + N new failing tests in the new describe block
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- --run tests/canvasCards.test.ts 2&gt;&amp;1 | grep -E 'FAIL|GROUP_PLACE_ON_CANVAS' | head -5</automated>
  </verify>
  <done>
    `tests/canvasCards.test.ts` has a new `describe("GROUP_PLACE_ON_CANVAS handler", ...)` block covering all 10 behavior bullets. TypeScript compiles. Tests fail RED with messages referencing the missing case label. PLACE_ON_CANVAS suite still passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement GROUP_PLACE_ON_CANVAS server handler (GREEN)</name>
  <files>party/index.ts</files>
  <behavior>
    - Server resolves all source cards before any mutation; if any missing, sends CARD_NOT_IN_SOURCE and returns without mutation
    - For hand source, server rejects when `fromId !== senderToken` with UNAUTHORIZED_MOVE
    - Server validates per-card finite coordinates, non-empty array, and unique cardIds before taking a snapshot
    - Server takes exactly one snapshot per group drop (single-undo invariant)
    - Server sorts resolved cards by pre-drag z ascending then pushes them onto `canvasCards` with `z = maxZ + 1 + rank`; all moved cards set `faceUp = true`
    - Single STATE_UPDATE broadcast at end of switch (via existing outer `broadcastState()` call — no new broadcast added)
  </behavior>
  <read_first>
    - party/index.ts (full file — focus lines 540-700 for PLACE_ON_CANVAS and PLAY_CARD_SET pattern reference)
    - tests/canvasCards.test.ts new GROUP_PLACE_ON_CANVAS describe (just written in Task 2)
    - .planning/phases/34-multi-card-group-drop/34-RESEARCH.md Code Examples section (GROUP_PLACE_ON_CANVAS skeleton)
    - .planning/phases/34-multi-card-group-drop/34-PATTERNS.md `party/index.ts` analog section
    - src/shared/types.ts (verify ClientAction GROUP_PLACE_ON_CANVAS shape from Task 1)
  </read_first>
  <action>
    Insert a new `case "GROUP_PLACE_ON_CANVAS":` block in the `onMessage` switch statement of `party/index.ts`, placed immediately after the existing `case "PLACE_ON_CANVAS":` (which ends near line 647). End the case with `break;`.

    Validation order (each failure: `sender.send` error and `break;` — no mutation):
    1. `cards.length === 0` → ERROR code `EMPTY_CARD_SET`, message "cards must contain at least one card"
    2. Per-card finite check: for any `c` in `cards`, if `!Number.isFinite(c.x) || !Number.isFinite(c.y)` → ERROR code `INVALID_COORDINATES`
    3. Duplicate cardIds: build `Set(cards.map(c=>c.cardId))`; if `set.size !== cards.length` → ERROR code `DUPLICATE_CARD_IDS`, message "cardIds must be unique"
    4. Hand auth: if `fromZone === "hand" && fromId !== senderToken` → ERROR code `UNAUTHORIZED_MOVE`, message "Cannot move another player's cards"
    5. Resolve each cardId in source (read-only lookup; do NOT splice yet). Source lookup by `fromZone`:
       - `'hand'` → `this.gameState.hands[fromId]`
       - `'pile'` → `this.gameState.piles.find(p => p.id === fromId)?.cards`
       - `'canvas'` → `this.gameState.canvasCards` (look up each by `cc.card.id`)
       If any cardId cannot be found, ERROR code `CARD_NOT_IN_SOURCE`, message `Card ${cardId} not found in ${fromZone}`. NO snapshot, NO splice.

    After all validation passes:
    6. Compute `maxZ = this.gameState.canvasCards.reduce((m, c) => Math.max(m, c.z), 0)` BEFORE any splice.
    7. For canvas source: record each card's pre-drag z (read from `canvasCards` entry) before any splice. For hand/pile source: pre-drag z is treated as 0 for every card (or use the array index as pre-drag rank to preserve source order — choose `0` for non-canvas sources, deterministic and matches Spike 004 because hand/pile cards have no pre-existing canvas z).
    8. `takeSnapshot(this.gameState)` — exactly once.
    9. Remove all cards from source atomically. For canvas: splice from `this.gameState.canvasCards` (re-find index each iteration because splices shift indices, OR collect indices first then splice in descending index order). For hand: splice from `this.gameState.hands[fromId]`. For pile: splice from `pile.cards`.
    10. Sort the resolved card list by ascending pre-drag z (stable; ties retain source-order).
    11. For each `(resolvedCard, rank)` in the sorted list: set `resolvedCard.card.faceUp = true`; push `{ card: resolvedCard.card, x: resolvedCard.x, y: resolvedCard.y, z: maxZ + 1 + rank }` to `this.gameState.canvasCards`.

    Use the existing `Card`, `ServerEvent` imports already at the top of `party/index.ts`. Do NOT duplicate the broadcast call — the outer switch already calls `broadcastState()` after each handler.

    Reference the existing PLACE_ON_CANVAS handler (lines 574-646) for the exact `sender.send(JSON.stringify({...} satisfies ServerEvent))` pattern and the per-zone splice idiom. Reference PLAY_CARD_SET (lines 658-700) for the empty-array rejection pattern.
  </action>
  <acceptance_criteria>
    - `grep -n 'case "GROUP_PLACE_ON_CANVAS"' party/index.ts` returns exactly 1 match, located after the existing `PLACE_ON_CANVAS` case
    - `grep -c 'EMPTY_CARD_SET\|INVALID_COORDINATES\|DUPLICATE_CARD_IDS\|UNAUTHORIZED_MOVE\|CARD_NOT_IN_SOURCE' party/index.ts` increased by exactly 5 vs prior file (one new occurrence per error code in the new case)
    - `grep -c 'takeSnapshot' party/index.ts` increased by exactly 1 vs prior file (single snapshot per group drop)
    - `npm test -- --run tests/canvasCards.test.ts` exits 0 (all GROUP_PLACE_ON_CANVAS tests flip GREEN; existing tests still pass)
    - `npm run typecheck` exits 0
    - No reference to `broadcastState` added inside the new case (outer switch handles it)
    - `npm test` (full suite) exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/canvasCards.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>
    `GROUP_PLACE_ON_CANVAS` case is implemented with all 11 steps in the action body. All Wave-0 RED tests for the GROUP describe block flip GREEN. Existing PLACE_ON_CANVAS + UNDO_MOVE + PLAY_CARD_SET tests still pass. Full Vitest suite green.
  </done>
</task>

</tasks>

<verification>
- `npm test -- --run tests/canvasCards.test.ts` — full canvasCards suite green including new GROUP describe
- `npm test` — full Vitest suite green
- `npm run typecheck` — clean
- All four error codes (EMPTY_CARD_SET, INVALID_COORDINATES, DUPLICATE_CARD_IDS, UNAUTHORIZED_MOVE, CARD_NOT_IN_SOURCE) reachable from at least one test
- Single `takeSnapshot` call in the new case (verified by grep + UNDO_MOVE-restores-all test)
- No new `broadcastState` calls introduced (handled by outer switch)
</verification>

<success_criteria>
- `src/shared/types.ts` exports `SelectionSource` and `ClientAction` includes `GROUP_PLACE_ON_CANVAS`
- `party/index.ts` has a `GROUP_PLACE_ON_CANVAS` case that validates → snapshots → atomically removes → assigns `maxZ + 1 + rank` z values → sets `faceUp = true`
- `tests/canvasCards.test.ts` has the new describe block green
- All threats T-34-01 through T-34-04 have a passing test asserting the mitigation
- MULTI-02 (relative positions): client supplies absolute x/y per card; server stores them verbatim — covered by happy-path tests
- MULTI-03 (z-order preserved + above existing): tested by Z-ordering test
- MULTI-04 (atomicity): tested by missing-cardId and validation tests (client bounds check is in Plan 03)
</success_criteria>

<output>
Create `.planning/phases/34-multi-card-group-drop/34-01-SUMMARY.md` when done.
</output>
