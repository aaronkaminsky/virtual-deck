---
id: 08-01
phase: 08
name: Fix SUMMARY and VERIFICATION frontmatter gaps
wave: 1
autonomous: true
depends_on: []
files_modified:
  - .planning/phases/01-server-foundation/01-02-SUMMARY.md
  - .planning/phases/03-core-board/03-VERIFICATION.md
requirements_addressed: []
gap_closure: false
---

# Plan 08-01: Fix All SUMMARY and VERIFICATION Frontmatter Gaps

## Objective

Fix the two remaining documentation gaps identified in the v1.0 milestone audit so that all planning artifacts accurately reflect the completed milestone state.

**Pre-checks before starting:**
- ROADMAP.md progress table already shows phases 2–5 as Complete with dates — no change needed (success criterion 1 already satisfied)
- Phase 5 SUMMARY files 05-01 and 05-02 already include ROOM-04 in requirements-completed — no change needed (success criterion 4 already satisfied)

**Remaining gaps:**
1. `01-02-SUMMARY.md` is missing `requirements-completed: [DECK-02]` in frontmatter
2. `03-VERIFICATION.md` TABLE-03 row still says `PARTIALLY SATISFIED` — superseded by quick task 260403-pya which added `playerLabel` prop to `OpponentHand` and marked TABLE-03 complete in REQUIREMENTS.md

## Tasks

<task id="1">
  <name>Add DECK-02 to 01-02-SUMMARY.md requirements-completed</name>

  <read_first>
  - .planning/phases/01-server-foundation/01-02-SUMMARY.md
  </read_first>

  <action>
  In `.planning/phases/01-server-foundation/01-02-SUMMARY.md`, add a `requirements-completed` field to the YAML frontmatter (between the `---` delimiters).

  The current frontmatter ends with:
  ```yaml
  metrics:
    duration: "~30 minutes"
    completed: "2026-04-01"
    tasks_completed: 2
    files_modified: 2
  ```

  Add the following field immediately after the `metrics` block (before the closing `---`):
  ```yaml
  requirements-completed: [DECK-02]
  ```

  Rationale: Plan 01-02 implements `shuffle()` using Fisher-Yates + `crypto.getRandomValues` (confirmed by acceptance criteria in the SUMMARY). DECK-02 tracks exactly this requirement. It was omitted from the frontmatter when the SUMMARY was originally written.
  </action>

  <acceptance_criteria>
  - `.planning/phases/01-server-foundation/01-02-SUMMARY.md` contains the line `requirements-completed: [DECK-02]` within the YAML frontmatter (between the opening and closing `---`)
  - Running `grep "requirements-completed" .planning/phases/01-server-foundation/01-02-SUMMARY.md` returns `requirements-completed: [DECK-02]`
  </acceptance_criteria>
</task>

<task id="2">
  <name>Update TABLE-03 status in 03-VERIFICATION.md from PARTIALLY SATISFIED to SATISFIED</name>

  <read_first>
  - .planning/phases/03-core-board/03-VERIFICATION.md
  - .planning/REQUIREMENTS.md (confirm TABLE-03 is [x] complete there — it is)
  </read_first>

  <action>
  In `.planning/phases/03-core-board/03-VERIFICATION.md`, update the TABLE-03 row in the requirements table and the note below it.

  **Change 1 — TABLE-03 row:** Find the line:
  ```
  | TABLE-03 | 03-02, 03-03 | Opponent hand card counts are visible to all players (face values are not) | PARTIALLY SATISFIED | Server: `opponentHandCounts` in `viewFor` provides counts without face data (SATISFIED). UI: `OpponentHand` renders `cardCount` card backs (SATISFIED). Marked Pending in REQUIREMENTS.md — needs human verification that opponent strips display correctly in a live multi-player session. |
  ```

  Replace with:
  ```
  | TABLE-03 | 03-02, 03-03 | Opponent hand card counts are visible to all players (face values are not) | SATISFIED | Server: `opponentHandCounts` in `viewFor` provides counts without face data. UI: `OpponentHand` renders `cardCount` card backs. Quick task 260403-pya added `playerLabel` prop to `OpponentHand` and marked TABLE-03 complete in REQUIREMENTS.md. Requirement fully satisfied. |
  ```

  **Change 2 — Note below the table:** Find the note:
  ```
  **Note on REQUIREMENTS.md traceability table:** TABLE-03 is marked "Pending" in REQUIREMENTS.md despite being implemented. The implementation satisfies the requirement. The REQUIREMENTS.md status reflects the pre-Phase-3 baseline and was not updated after plan execution.
  ```

  Replace with:
  ```
  **Note on REQUIREMENTS.md traceability table:** TABLE-03 is marked complete `[x]` in REQUIREMENTS.md. Quick task 260403-pya (run during Phase 3 execution) added the `playerLabel` prop to `OpponentHand` and updated REQUIREMENTS.md to reflect full satisfaction.
  ```
  </action>

  <acceptance_criteria>
  - Running `grep "TABLE-03" .planning/phases/03-core-board/03-VERIFICATION.md` shows `SATISFIED` (not `PARTIALLY SATISFIED`) in the requirements table row
  - Running `grep "PARTIALLY SATISFIED" .planning/phases/03-core-board/03-VERIFICATION.md` returns no output
  - Running `grep "260403-pya" .planning/phases/03-core-board/03-VERIFICATION.md` returns at least one match (the updated note)
  </acceptance_criteria>
</task>

## Verification Criteria

<must_haves>
- [ ] `grep "requirements-completed" .planning/phases/01-server-foundation/01-02-SUMMARY.md` returns `requirements-completed: [DECK-02]`
- [ ] `grep "PARTIALLY SATISFIED" .planning/phases/03-core-board/03-VERIFICATION.md` returns no output
- [ ] `grep "TABLE-03" .planning/phases/03-core-board/03-VERIFICATION.md` contains `SATISFIED` (not partial)
- [ ] Both files remain valid YAML frontmatter (no syntax errors)
</must_haves>

## Self-Check

After completing both tasks, verify:
1. `01-02-SUMMARY.md` has `requirements-completed: [DECK-02]` in its frontmatter block
2. `03-VERIFICATION.md` TABLE-03 row says SATISFIED
3. `03-VERIFICATION.md` note references quick task 260403-pya
