// GridZone stubbed in Phase 31 — component removed; Plan 02 will clean up the import in BoardView.
// The stub keeps the file present so existing imports in BoardView resolve until Plan 02 deletes the file.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function GridZone(_props: any): null { return null; }
